# 微信会话字段诊断

使用公开 UIA，仅扫描显式微信窗口的 session_list，跳过 chat_message_page。命令参数是从窗口枚举得到的微信 HWND 十进制值。普通权限运行，不做任何窗口或输入操作，不安装 wxauto。

以 .NET 10 SDK 构建本目录，目标 net48。输出窗口状态、版本、条目数、行长度与数字标记，无原始名称／摘要。InlineBadge 仅表示混合字段中出现该格式，不能据此保证真实未读；unknown 不等于 0。仅用于用户明确要求的诊断，不属于桌宠监听器。
