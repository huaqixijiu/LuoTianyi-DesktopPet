# 模块 D：临时文件与 Git 历史审计（2026-09-14）

本审计只读检查，不删除文件、不改写 Git 历史、不清理候选素材。

## 结论

| 检查项 | 结果 | 判断 |
|---|---:|---|
| 当前仓库追踪 `bin/`、`obj/`、`artifacts/`、`.local-tools/` | 0 | 当前忽略规则有效 |
| 当前仓库追踪日志、临时文件、缓存、崩溃转储 | 0 | 未发现需要立即处理的已追踪产物 |
| Git 历史中的 `AGENTS.md` 路径记录 | 2 次 | 属于历史开发约定文件；本轮不改写历史 |
| Git 历史提交主题含 `codex/` | 2 条合并提交 | 是分支名，不等于代码或文档中自称由工具生成；按用户决定留到干净仓库重建时处理 |
| 当前运行时资源名含 `ai-bun` | 4 个 | 已形成动画 ID 和资源引用，不能仅为命名清理而直接改名；若要改，必须单独设计兼容迁移 |
| Git LFS 文件 | 1,428 个 | 当前二进制资源已按既有规则使用 LFS |
| Git 对象库临时对象 | 1 个 `tmp_obj_*` 警告 | 仅记录，不手动删除；后续由 Git 自身维护或在仓库重建时自然消失 |

## 当前临时目录和元数据

- `.local-tools/` 是本地加工工具和中间产物目录，已被 `.gitignore` 排除；本轮不清理。
- `原素材/`、`artifacts/`、`候选素材_官方/` 的处理继续遵循既有约定和用户确认边界。
- `assets/**/*.meta.json` 是运行时资源的来源、处理参数或校验元数据，不属于 AI 工具缓存，不删除。
- 未发现已追踪的 `.codex/`、`.cursor/`、`.claude/` 元数据文件。

## AI 痕迹判断

本轮按“工具身份表述”和“项目既有业务命名”区分：

1. 历史合并提交 `4c92a79`、`f850429` 的主题包含 `codex/` 分支名。它们是历史分支命名痕迹；用户已决定不在本轮重写历史，待干净仓库重建时统一处理。
2. `ai-bun-*` 是现有动画资源 ID，已被清单、代码和发布资源使用。直接改名会影响资源引用和兼容性，因此本轮不处理。
3. 未发现提交主题中出现工具署名、自动生成声明或其他作者声明。

## 后续建议

- 模块 E 若获确认，重建干净仓库时重新组织公开提交历史，并避免使用工具名称作为分支或提交主题。
- `ai-bun-*` 是否改为中性动画 ID，需要先建立旧 ID 到新 ID 的兼容映射，再单独确认和验证。
- 不对当前 `.git/objects/tmp_obj_*` 做人工删除；若后续 Git 命令仍报告同一警告，再单独进行仓库完整性检查。
