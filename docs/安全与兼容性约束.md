# 安全与兼容性约束

### 2026-09-13：快速卡交互边界

自动出现始终ShowActivated=false、ShowInTaskbar=false，宿主归属桌宠；只有用户主动点击查看详情才激活编辑窗口。快速卡不会自行播放音乐/动画，不覆盖人物主体，不写入单独的详情数据。收起动作只影响高度，取消必须使用RuleId+At当前发生实例；未来重复及原日历事项保留。原锁屏、全屏、受保护游戏及用户隐藏抑制继续有效。


### 2026-09-13：闹钟临时置顶及有界播放

用户明确授权闹钟播放时临时置顶；沿用不抢焦点、锁屏／全屏／受保护游戏抑制。暂停、超时、关闭、隐藏或退出必须停止播放并释放本轮置顶token。最长222秒后休息10分钟，禁止无界连续播放；丢失音频或解码失败时不影响文字提醒。音乐仅来自用户提供文件，未上传用户日历和通知内容。桌面UIA与Core Audio外部等待不得放在启动UI线程。


### 2026-09-13：用户主动填写的日历数据

用户授权持久保存自己填写的行程标题、内容、闹钟和作息；这与外部 QQ／微信临时通知不同。仅保存本地 reminders.json及备份，禁止写入内容日志或上传。损坏／未知版本／超限／写失败保留原数据；有效备份恢复须保留损坏原件。日历不再提供导入／导出入口。不安装服务或计划任务，退出不能实时响铃；提醒不得抢焦点或强制覆盖全屏、锁屏及受保护游戏。


> 本文中的条目是实现阶段的硬约束。新增原神、QQ、微信或其他应用联动时必须继续遵守。

## 1. 总体原则

桌宠只能作为独立的普通权限 Windows 桌面程序运行。它不得修改、注入、调试或分析被联动应用，不得模拟成游戏插件或覆盖层。

默认要求：

- 不申请管理员权限；
- 不安装内核驱动或系统服务；
- 不修改游戏、QQ、微信或网易云音乐文件；
- 不绕过反作弊、沙箱或应用安全机制；
- 不读取其他进程内存；
- 不枚举其他进程模块；
- 不创建指向游戏进程的长期句柄；
- 不进行 DLL 注入、远程线程、代码补丁或 API Hook；
- 不挂钩 DirectX、OpenGL 或 Vulkan 渲染；
- 不抓包、不代理流量、不读取聊天数据库；
- 不截取游戏画面或聊天窗口来识别状态。

## 2. 原神检测

原神启动检测只允许使用 Windows 提供的普通权限进程枚举或生命周期通知。监听内容限制为已配置的原神进程名、进程标识和启动/结束时间；若某种通知机制在普通用户权限下不可用，必须回退到低频只读枚举，不能要求提权。

禁止：

- 打开原神进程读取内存；
- 查询游戏模块、线程、窗口内容或反作弊组件；
- 向游戏窗口发送键盘、鼠标或窗口消息；
- 把桌宠窗口附着到游戏窗口；
- 将桌宠实现为游戏内 Overlay；
- 为保持显示而尝试绕过独占全屏或反作弊限制。

桌宠是普通的透明顶层窗口。原神启动动画可以短暂申请 Windows `TopMost`，但必须设置为不激活窗口，不抢键盘焦点。检测到受保护游戏处于独占全屏时，应停止临时覆盖、延后提醒或保持在桌面层。

原神后台“拜托”彩蛋只允许在原神进程存在且前台进程不是原神时显示。前台判断只使用 `GetForegroundWindow` 对应的进程标识，不读取窗口画面或游戏内部状态。随机位置必须限制在 Windows 工作区内并保持角色完整可见；动画播放后恢复原位置和用户原有置顶设置。原神重新位于前台、独占全屏、锁屏或进程退出时，立即暂停或取消尚未显示的彩蛋。

## 3. 游戏安全模式

配置中的受保护进程位于前台时自动启用游戏安全模式：

- 暂停所有 `SendInput`、模拟快捷键和自动鼠标操作；
- 不为 QQ、微信等普通通知申请临时置顶；
- 桌宠不得抢焦点；
- 可选择变为鼠标穿透；
- 降低后台检测频率和动画帧率；
- 仅保留进程退出检测与本地状态记录；
- 退出游戏后恢复用户此前的桌宠设置。

因此，网易云快捷键在受保护游戏前台时默认不由桌宠模拟发送。若以后需要游戏内控制，应优先研究 Windows 媒体命令或让用户使用真实媒体键，而不是向游戏前台发送组合键。

## 4. QQ 与微信

允许的检测来源：

- 用户授权的 Windows 通知访问；
- Windows Shell 的任务栏窗口闪烁事件；
- 应用进程是否位于前台。

首选通知监听需要 MSIX 包身份、清单中的 `uap3:userNotificationListener` 能力以及用户在设置页
主动授权。免安装版本没有包身份时必须显示“需要 MSIX”，不得反复请求、伪造已授权状态或改用
更高权限方案。平台层允许读取通知的应用标识、应用显示名、应用 Logo、创建时间，以及结构化 Toast
绑定中的标题元素。用户于 2026-09-12 明确选择统一详细提醒：开关开启时允许读取系统主动提供的独立正文槽作为预览，不能把此授权扩大到聊天界面、历史或数据库。标题、预览和图标只能短期保存在内存中，禁止写日志、历史文件或上传；开关关闭时停止详情读取并清理。

任务栏回退只允许桌宠窗口通过 `RegisterShellHookWindow` 接收系统广播的 `HSHELL_FLASH`，再以公开
窗口 PID 查询所属进程名；它不是 `SetWindowsHookEx` 全局钩子，也不得注入或打开聊天进程。该路径只
显示“QQ 有新消息”或“微信有新消息”，不读取窗口标题、控件树、会话名称、头像或正文。

禁止：

- 读取聊天数据库、缓存文件或日志；
- 注入 QQ/微信进程；
- 使用 UI 自动化遍历联系人和消息列表；
- OCR 截取聊天窗口；
- 抓取或解密网络流量；
- 从聊天缓存、数据库、窗口、网络或通知私有 XML 中补取联系人头像；
- 在日志或磁盘中保存联系人/群名、头像或消息正文。

桌宠长期只保存配置，不保存任何通知历史。用于去重和延后展示的来源类别、时间、标题、已授权系统通知预览与应用图标只在
内存中短期保留；展示完成、取消或关闭功能后释放。系统未提供可靠标题时必须降级为仅显示应用来源。

跨电脑安装必须使用签名 MSIX。自签名开发包只允许作为明确标注的测试分发：安装器必须先验证
包、公钥哈希和签名者一致，只把公开 CER 加入 `LocalMachine\TrustedPeople`，不得分发 PFX 私钥；
UAC 只用于证书信任，桌宠仍以普通用户权限运行。面向公众的正式版必须改用 Microsoft Store
签名或 Windows 默认信任的生产代码签名证书，不能把自动信任自签名证书冒充为正式无提示安装。

## 5. 网易云音乐与系统输入

网易云音乐控制使用用户自行配置的网易云全局快捷键。不得向网易云窗口投递
`WM_APPCOMMAND`，避免消息进入 Windows Shell 后显示系统媒体浮窗。实现时必须：

- 明确显示当前配置的快捷键；
- 禁止用户配置系统保留组合键；
- 设置发送频率限制，避免按键风暴；
- 受保护游戏前台时暂停模拟输入；
- 不向未知前台窗口发送普通字符；
- 不读取网易云进程内存或私有数据。

用户在桌宠上明确点击播放且网易云进程不存在时，允许启动网易云，但必须先执行与快捷键相同的
前台安全检查。受保护游戏前台或查询失败时禁止启动。可执行文件只允许从 Windows App Paths
注册项或少量明确的网易云常见安装目录解析；不得全盘搜索、调用网易云私有协议、修改安装文件，
也不得在没有用户点击的情况下后台自动启动。程序启动后先等目标主窗口及曲目信息就绪，再发送
一次播放/暂停快捷键；如果启动请求成功但可控制窗口没有出现，允许按固定间隔最多重试
两次启动，但不能循环发送播放命令或猜测网易云私有命令。

全局快捷键实现只允许使用公开的 `SendInput` 键盘输入能力，不发送普通字符，也不枚举其它
应用窗口。前台安全检查、350 毫秒限流和目标进程白名单适用于全部媒体快捷键。发送前通过
`GetForegroundWindow` 和 `GetWindowThreadProcessId` 取得前台进程类别；查询失败
时按安全失败处理。通过 `GetAsyncKeyState` 发现用户仍按住快捷键中的任一键或
Ctrl/Alt/Shift/Windows 修饰键时不得发送。一次发送必须包含成对的按下/抬起事件，
若系统只接受部分事件，只能尝试抬起本次组合涉及的键并报告失败，不能释放其他键。

音乐状态检测只使用 Windows Core Audio。曲目信息优先来自系统媒体会话；系统媒体会话
是 Windows 能力，不是网易云私有接口。若目标网易云版本没有注册系统媒体会话，允许
通过 Windows 桌面管理器读取 `cloudmusic.exe` 的顶层主窗口标题作为只读兜底，但必须：

- 只匹配配置中的网易云目标进程，不读取其它应用窗口标题；
- 只读取顶层标题栏，不使用 UI 自动化遍历窗口正文，不截图或 OCR；
- 不读取进程内存、模块、文件、网络或网易云私有数据；
- 曲名和歌手只保存在内存中，不写日志、不保存历史、不上传；
- 标题为空、只有“网易云音乐”或格式无法可靠识别时安全降级为“暂未读取到歌曲名称”。

当前版本禁止读取或写入 Windows 系统主音量，设置窗口和桌宠滚轮都不得提供隐藏入口。
网易云独立音量只能通过 Windows Core Audio 的公开音频会话接口枚举所有活动输出设备上
属于 `cloudmusic.exe` 的会话并使用 `ISimpleAudioVolume`；不得使用键盘模拟、全局鼠标
Hook、驱动、注入、进程内存或网易云私有接口。多个匹配会话必须同步调整；目标会话不存在、
输出设备失效、前台查询失败或受保护游戏位于前台时必须失败关闭，绝不退回系统主音量，
也不读取、录制或保存音频内容。

开机自启动默认关闭，只允许在用户主动切换后操作
`HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run` 中本产品自己的单一值。
不得安装 Windows 服务、计划任务、驱动或写入其他用户/机器级启动位置；关闭选项时只删除
本产品值，不枚举或修改其它启动项。

## 6. 文件拖放与回收站

- 只把桌宠自己的 OLE `IDropTarget`/框架拖放事件作为文件来源，不安装全局 Shell Hook、鼠标 Hook 或资源管理器扩展；
- 只接受 `CF_HDROP` 中可以解析为真实文件系统项目的路径，不读取被拖文件内容；
- 删除语义必须通过 Windows Shell `IFileOperation` 请求移入回收站，并启用允许撤销/回收站标志；
- 回收站不可用、权限不足、项目来自不支持的虚拟来源或 Shell 操作失败时，必须原样保留目标；
- 禁止永久删除回退，禁止 `File.Delete`、`DeleteFile`、命令行删除和 Shift 永久删除语义；
- 拖入文件夹或一次超过 10 个项目时再次确认；
- 受保护游戏位于前台、系统锁屏或桌宠处于鼠标穿透模式时禁用文件接收；
- 日志只记录项目数量、成功/失败和错误代码，不记录完整文件名、路径或文件内容。

拖入桌宠的操作只提供快捷回收站语义：Shell 确认成功后恢复此前桌宠状态，不得把同一投放转成
小笼包或进食动画。桌面与最大化资源管理器都属于 `explorer.exe`，允许其覆盖屏幕时投放且不要求
预先激活桌宠；其它覆盖屏幕的进程仍按全屏处理。桌面吃包子开关不得关闭快捷回收站入口。

桌面“小笼包追逐”只观察当前用户与公共桌面目录的项目删除通知，并使用 Windows UI Automation
短期缓存 Explorer 桌面图标的规范化显示名和屏幕中心点。不得读取文件内容、保存完整路径、安装
Explorer 扩展或全局 Shell/鼠标 Hook。`FileSystemWatcher.Deleted` 同样会在项目被移出桌面时出现，
所以该动画代表“项目从桌面消失”，不把它记录或宣称为已经永久删除；真实删除仍只由用户拖放后
通过 Windows Shell 回收站结果确认。缓存只驻留内存，命中或关闭功能后释放。

追逐只在可确认的普通桌面环境运行。每 500 ms 复查前台窗口；全屏、受保护游戏、锁屏、边缘隐藏
或查询失败时关闭所有包子浮层、恢复原位置和持续动画。设置页提供独立即时开关，默认最多排队 6 个，
以防桌面批量整理产生窗口风暴。

Windows 桌面宿主本身属于 `explorer.exe`，其窗口可能覆盖整个显示器；这不能被误判为独占全屏。
包子追逐的前台规则只对 `explorer` 放行该尺寸特例，任何其它覆盖显示器的进程仍视为全屏并抑制，
受保护游戏即使进程名异常或窗口尺寸变化也继续优先禁止。

吃包子追逐、进食和返程期间不显示歌名、媒体控制、网易云音量、操作反馈或消息悬浮岛；异步刷新和
鼠标悬停也不得重新显示。拖放光标只允许使用桌宠窗口内的公开 WPF/Win32 光标能力，不安装全局
鼠标 Hook、不注入 Explorer，也不隐藏或篡改系统级拖放安全反馈。

## 7. 窗口行为

- 所有临时置顶均使用引用计数或请求令牌；
- 动画结束只释放自己的置顶请求；
- 永久置顶以用户最新设置为准；
- 临时置顶窗口使用不激活样式；
- 全屏应用前台时默认不显示普通通知覆盖；
- 崩溃恢复时默认撤销临时置顶，避免遗留遮挡窗口；
- 支持紧急隐藏快捷键和托盘菜单退出。

## 8. 权限、日志与发布

- 默认普通用户权限运行；
- 首次申请通知访问时说明用途；
- 日志不记录聊天内容、窗口标题或用户输入；
- 可提供“导出诊断信息”，导出前展示具体字段；
- 发布包应包含可验证的版本号和文件哈希；
- 后续正式发布可考虑代码签名，减少系统安全提示；
- 第三方联动必须可以单独关闭，并提供进程名白名单而不是扫描所有软件。

## 9. 新增应用联动检查表

新增任何游戏或通讯软件前，需要确认：

1. 是否只使用操作系统公开事件；
2. 是否完全不读取目标进程内存和文件；
3. 是否不向目标应用注入代码或挂钩；
4. 是否不在全屏状态抢焦点或强制覆盖；
5. 是否不收集消息正文等隐私数据；
6. 是否能由用户单独关闭；
7. 是否有防抖、冷却和恢复策略；
8. 目标应用失效或升级后，桌宠是否仍能安全降级。

任意一项不能满足时，该联动默认不实现。


### 2026-09-12：QQ 详细提醒的明确授权边界

用户授权 QQ 详细提醒及其独立开关。允许仅识别 QQ 自己的托盘提醒浮层，经公开 UI Automation 按已验证的结构读取昵称／群名及独立未读数；此项是上述禁止遍历聊天列表的窄范围例外，不允许读取 QQ 主界面的联系人列表、聊天正文、缓存或内存，不自动移动鼠标展开卡片。所有详情只在本轮提醒的内存中使用；开关关闭时清理详情，接口或结构异常时退回来源提醒。微信仍保留系统通知与闪烁来源回退，不扩大其读取范围。

### 2026-09-12：正文预览授权与开源研究范围

统一详细提醒开关覆盖昵称、系统通知正文预览和新增通知计数，三项一起启停；不得从聊天数据库、进程内存、私有协议或联系人／消息列表补齐缺失正文。源码研究不等于授权安装、运行第三方机器人或扩大本机读取范围；本轮仅研究公开仓库。现有 QQ 托盘窄范围例外保留。

### 2026-09-12：微信会话列表只读验证

用户明确要求验证 wxauto 会话列表字段读取，允许本轮通过公开 UIA 读取微信主窗口 session_list 的会话条目 Name，以内存中的字段结构和未读标记核对可用性。这是实验范围的窄例外；不切换聊天、不滚动加载历史、不调用 wxauto 的聊天监听动作，不把实验授权扩展为持续监听。诊断代码跳过 chat_message_page／消息列表，输出只含条目序号、字段长度、数字及窗口状态，不保存昵称或预览；UI 工具初次整窗快照范围过宽，后续改为定向诊断，原始文本不写入项目文件。

### 2026-09-12：微信详细提醒试用授权

用户在确认 QQ 无问题后要求“微信你做一下试试”，授权将已验证的 session_list 公开字段接入试用提醒。只观察会话条目的名称、摘要及数字标记，不访问聊天消息列表、历史、数据库或内存，不切换／激活／滚动微信，不为保持字段可读修改客户端设置。开关统一控制三项详情；启动、窗口变化或读取失败后重新建立基线，不补报旧未读，免打扰条目不派发提醒。只保留当前公开条目的摘要哈希／数字，原始文字仅随当前提醒短期驻留，不记录或上传。

### 2026-09-12：微信时序修复边界

本轮只缩短既有 session_list 公开字段的观察间隔，并在 Shell 新信号后请求及时读取；保持单通道、防重入及原字段范围。禁止为了首条详情匹配而随机选择旧未读会话、打开聊天、操作客户端或持久化正文。超时、已读、失去新鲜基线时丢弃迟到结果。测试只输出长度、数量、耗时和状态，不记录实际姓名／摘要。

### 2026-09-12：耳旁列表的本地交互与保留范围

用户授权多联系人待处理列表、悬停／固定展开和本地忽略／清空。允许将授权来源已提供的昵称及最新摘要保留在本进程待处理列表内，直至确认已读、用户忽略／清空或退出；关闭详情时立即清除分会话姓名与摘要，合并为来源计数。不得把保留扩展为日志／历史文件／上传。忽略和清空只操作 MessageInbox，不调用客户端删除或标记已读能力。无唯一身份的来源闪烁仅作保守回退，不声称能准确识别所有旧消息或同名会话。鼠标状态只在提醒窗口显示时用于本地 hover／pin／outside-click，不读取按键文本或使用全局注入 Hook。
