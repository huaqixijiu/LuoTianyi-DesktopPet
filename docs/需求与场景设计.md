# 洛天依桌宠：需求与场景设计

### 2026-09-13：人物右键菜单入口

新增「设置」与「退出桌宠」，位于缩放下方分隔区。设置复用完整设置窗口；退出复用原告别及退出流程，不增加确认弹窗。


### 2026-09-13：单组件快速提醒卡（最新确认）

本段覆盖旧的角落胶囊、音乐岛下方定位及30秒自动展开约定。提前提醒显示事项、目标时间、剩余分钟、备注及「知道了／稍后10分钟／本次不再提醒」；知道了才进入AcknowledgedEarly快速卡，稍后保持隐藏，取消只影响当前实例。正式到点转换Due，只显示「知道了／稍后10分钟」，知道了结束本次，未来重复保留。

快速卡绑定桌宠当前可见包围盒，默认正下方居中、视觉间距10 DIP，下方不足向上切换，按当前显示器工作区限制、不能盖住主体；拖动中跟随、完成及DPI变化重新定位。收起仅一行小铃铛、最近事项·时间、数量和箭头，不显示备注或按钮；同一外框/背景/圆角/阴影/顶部栏以180ms ease-out改变高度。展开列出已确认近期实例，次按钮关闭本次、主按钮查看详情，顶部点击仅收起，无自动30秒收起。关闭只取消所点实例，不删除日历、不关闭重复；详情使用原条目ID和发生日期跳转，不建立副本。

普通提醒优先展示，已确认快速实例保留，待普通提醒处理后恢复；不堆叠多个浮窗。收起文字静态，展开按目标时间戳重新计算剩余分钟。出现不激活、不抢焦点、不显示任务栏图标；声音、动画仍统一读取设置→通知的全局配置，快速卡自身不响铃。


### 2026-09-13：原版来啦、整曲提醒与启动修复

本段为用户最新确认，覆盖较早同主题条目。闹钟及提前提醒使用官方十周年「来啦」原动图；播放时临时置顶，但不抢焦点。提醒音乐替换为用户提供的《再给我一天的时间吧QAQ》原MP3。音乐或动画任一开启时，一轮最长222秒；未处理则停止，10分钟后再次提醒；知道了／关闭本次提醒按既有提前／到点语义停止本轮，取消本次后不重试，重复规则的下一次保留。两项均关闭只显示卡片。全局开关及音量收进设置→通知下方，移除托盘独立提醒设置入口。

日历继续以最新九图为视觉依据：月历双日期、节日圆点与侧栏、红休蓝班，周列、事项编辑、分组闹钟与图九倒计时顺序。修正字体缩放、圆角控件、日期输入、顶部品牌页签及班休底部操作；保留整组／单日／多组删除与现有音乐岛。


### 2026-09-13：确认视觉稿及提醒提前处理

- 使用浅青白色、紧凑圆角控件；月历顶部右侧新增行程、周月切换和休息日调整，左侧日期导航及今天。月格日期栏固定单行：阳历日、农历日（不带月份）、节日及班休小标记；节日过长省略并可悬停查看，不挤占行程行。周视图保留日期／农历／节日较完整呈现。底栏显示完整日期和轻量陪伴文案。
- 每周休息日直接多选任意星期；“周六日休息”“仅周日休息”仅为快捷填入。指定日期覆盖仍优先。
- 编辑只提供单个行程内容框；旧标题与正文合并显示并保留，列表用内容首行摘要。多个日期平时显示摘要，点击修改打开紧凑选择窗，直接点选／取消，清空／确定，取消不改变已选日期。
- 倒计时可悬停或点击展开；“本次不再提醒”跳过当前一次、保留日历行程和后续重复；“隐藏倒计时”只隐藏当前一次提前提示，仍到点提醒。保存到本机；操作后8秒提供撤销。
- 提醒位于现有切歌悬浮岛下方，岛不显示则位于桌宠正下方；边缘空间不足时避让。现有切歌悬浮岛外观与交互不变。其它以最新视觉稿为样式参考，生成稿日期错误不作为历法规则。


### 2026-09-13：常见国际／西方节日

补入情人节（2/14）、愚人节（4/1）、万圣夜（10/31）、万圣节（11/1）、平安夜（12/24）、圣诞节（12/25）；母亲节采用5月第二个周日、父亲节6月第三个周日的通行日期，美国感恩节11月第四个周四并标注地域。适用2026～2099，纯本地规则，无逐年数据下载。与农历节日／节气同日时并列显示，不修改班休、不创建提醒。未宣称覆盖所有国家不同庆祝日期。


### 2026-09-13：日历界面精简

移除导入、导出及日历顶部的闹钟入口；独立闹钟仍由桌宠右键进入。已开启提醒的行程文字后显示闹钟图标；作息入口收进“调整休息日”。月视图按当月实际周数铺满可用区域，默认窗口依据工作区选择合适大小，避免需手动拉伸才能看到最后一周。行程编辑的多日期选项展开时尽量在默认窗口内完整显示；小屏保留滚动兜底。

## 2026-09-13 用户确认：离线日历与闹钟

- 人物右键顶部增加日历、闹钟；功能先行，基础 WPF 界面后续打磨。周／月切换、周一开始，日期格直接显示时间和标题，多条内部滚动，不用圆点代替行程；支持多行详细内容和不提醒的行程。
- 日历开启提醒后以同一记录进入闹钟，标注“来自日历”。关闭／删除／全部日期已过的关联项退出闹钟列表，历史行程仍保留；待处理到点事项由提醒卡保留。
- 闹钟支持单次、每天、每周几、多个日期、工作日、休息日。双休／单休／自选每周作息与指定日期覆盖分层，手动覆盖优先且可恢复常规；月份支持右键及批量调整班／休。修改作息只重排未来工作／休息日提醒，不改变固定星期／日期提醒或节日日期。
- 闹钟内“指定时间／倒计时”共用提醒机制；后者 1 秒～24 小时，首次 25 分钟，记住上次启动时长。无单独倒计时页、暂停或循环，支持再来一次。
- 闹钟列表显示剩余时间；相对提醒可在桌宠旁倒数，日历提前 30 分钟安静倒计时提示（全局及每条行程均可关闭，只保留到点提醒），两项可关。到点短提示音（可静音）及合并卡片，标题、来源、完整内容、完成／5 分钟后提醒；30 秒收起，阅读保持，未处理项保留。人物动画不变，全屏／锁屏／受保护游戏／安全隐藏时延后，不抢焦点。
- 退出或休眠时不实时响铃；恢复／重启将错过的提醒按事项合并，不连续补响每一轮历史。
- 数据只存本机 reminders.json，临时文件替换、有效备份恢复；不提供导入／导出界面；不联网、不写内容日志、不引入数据库服务。
- 内置常用公历节日、农历节日及 2026～2099 二十四节气，默认只是标注、不生成闹钟；不预设官方调休，远期历法日期可随版本校正。


### 2026-09-13 用户确认：通知设置页采用紧凑行设计

按确认效果图重排通知页：总开关独立一行，QQ／微信详细提醒统一分组，使用 `候选素材_官方/微信QQ.png` 左侧推荐扁平图标的现有透明派生 PNG，微信“试用”使用小标签。权限改为状态与按钮同一行，已授权时隐藏重复按钮；实际系统授权状态优先于本地旧标记。详细规则与隐私默认折叠，常规状态一屏呈现，展开内容在右侧滚动，底部保存按钮保持可见。开关仍在保存后生效，不改变提醒采集和本地列表行为。准确说明昵称／摘要仅在本机内存用于当前提醒，不写历史／日志、不上传，替换旧的“不会显示正文”表述。其它设置页和左侧人物装饰保留。


> 状态：草案 v0.1
>
> 更新日期：2026-09-06
>
> 本文只记录已经讨论确认的规则。其余动画映射等待继续挑选后补充。

## 1. 产品目标

桌宠是一个独立运行的 Windows 透明窗口。它通过待机、音乐、通知和鼠标互动表现状态，同时提供少量系统控制功能。

设计目标：

1. 不打断用户当前工作或游戏。
2. 动画有明确触发原因，避免无意义地频繁播放。
3. 用户的永久设置始终高于一次性动画的临时设置。
4. 所有外部应用联动都采用公开、低权限的 Windows 能力。
5. 场景和动画通过配置关联，后续更换素材不需要重写检测代码。

## 2. 动画调度模型

动画分为两类：

- **持续状态**：基础待机、随机待机、音乐舞蹈、睡眠、拖拽。
- **限时反应**：时间问候持续 10 秒且可被用户输入或音乐打断；音量变化、切歌反馈、原神启动、错误和成功提示按各自时长结束。

一次性反应播放结束后，重新计算当前持续状态，而不是固定回到待机。QQ／微信提醒独立于动画状态，播放音乐时收到消息仍继续跳舞。

每条场景规则至少包含：

- 事件来源；
- 动画标识；
- 优先级；
- 循环次数或最长时长；
- 是否允许鼠标互动中断；
- 冷却时间；
- 是否申请临时置顶；
- 播放结束后的恢复策略。

### 2.1 建议优先级

从高到低：

1. 用户正在拖拽或直接操作桌宠；
2. 系统锁屏、显示器关闭和程序退出；
3. 原神启动等明确的一次性场景；
4. 其它普通一次性反馈（QQ／微信提醒不参与动画抢占）；
5. 音乐和音量操作反馈；
6. 时间问候；
7. 音乐持续舞蹈；
8. 随机待机；
9. 基础待机或睡眠。

用户拖拽始终可以中断普通反应动画，避免桌宠失去操控感。

### 2.2 通用失败反馈

普通操作失败或等待超时只显示原因气泡并恢复待机或此前持续状态，不播放“心律共鸣·呜呜晃动”。失败气泡包括：

- 普通失败：“操作失败，请再试一次”；
- 等待超时：“等太久了，再试一次吧”；
- 网易云没有确认状态：“网易云没有响应”；
- 文件未能移入回收站：“没有放进回收站，文件还在原处”。

气泡只说明结果和下一步，不显示异常堆栈、完整文件路径或其它隐私信息。失败反馈播放一次后重新计算持续状态。

## 3. 置顶规则

用户设置和临时动画必须分开保存：

```text
实际置顶 = 用户永久置顶 || 当前存在临时置顶请求
```

临时动画结束时只撤销自己的临时请求，不能直接把窗口设置为非置顶。因此，用户原本选择置顶时，原神动画结束后仍然置顶。

桌宠窗口不得抢夺键盘焦点。全屏游戏位于前台时，默认取消普通通知的临时置顶；受保护游戏进入独占全屏后，原神启动反应可以提前结束或延后到桌面显示。

## 4. 网易云音乐

### 4.1 控制

第一阶段功能：

- 上一首；
- 下一首；
- 播放；
- 暂停或继续。

控制不接入网易云私有接口。上一首、播放/暂停和下一首使用网易云设置页可配置的全局
快捷键，桌宠通过 `SendInput` 模拟同一组合键。不得再向窗口投递 `WM_APPCOMMAND`：该消息
未被目标窗口完整消费时会进入 Windows Shell 的媒体命令链，并可能弹出系统媒体浮窗。

当前默认组合与用户本机网易云设置页显示的一致：上一首 `Ctrl+Alt+Left`、
播放/暂停 `Ctrl+Alt+P`、下一首 `Ctrl+Alt+Right`。
用户需要保持网易云中的全局快捷键与桌宠配置一致；默认组合不应被当作网易云所有版本
都固定不变的私有协议。命令发出后仍用 Core Audio 和曲名变化确认结果，不把按键发送成功
误报为播放器已经响应。

音乐悬浮岛默认关闭，由桌宠右键菜单的“显示音乐悬浮岛”统一开关并持久化；关闭时歌名、播放控制与网易云音量全部隐藏，不留下小播放按钮，播放、暂停、切歌和异步刷新都不能自动重新开启。不存在暂停 10 分钟的自动收起规则。开关不影响音乐检测和人物听歌动画。

用户勾选“显示音乐悬浮岛”后，鼠标移入桌宠时，在角色动画正下方的独立预留区域显示
天依蓝玻璃质感控制岛，依次显示上一首、播放/暂停、下一首和网易云独立音量；品牌主色使用
`#66CCFF`，图标采用可随 DPI 清晰缩放的矢量路径。控制岛不覆盖角色、表情文字或身体
交互区，从下方轻微上浮淡入；鼠标移出桌宠后继续保留 5 秒再淡出，未悬停时完全隐藏且不
参与命中。播放按钮使用独立于人物动画宽限的快速状态：桌宠成功发送播放/暂停快捷键后立即切换图标，网易云外部停止时连续约 0.75 秒静音即更新图标；人物音乐动画仍使用完整 5 秒宽限。最右侧
扬声器按钮使用实心天依蓝，点击后在按钮正上方向上展开竖向滑杆；其余媒体按钮的工具提示
展示当前配置的全局快捷键。

当 `cloudmusic.exe` 尚未运行时，用户点击播放/暂停按钮不再对空气发送快捷键。桌宠先通过
Windows 注册的 App Paths 定位并启动网易云，定位失败时只检查几个明确的常见安装目录，
不扫描磁盘。等待阶段静止显示“心律共鸣·加载中”原始单帧图片，不附加摇摆、呼吸或位移，
并持续显示“正在打开网易云音乐”的文字。冷启动至少等待 2 秒，并优先等到 Windows 已能
读取网易云当前曲目；若 10 秒仍没有曲目信息则使用超时兜底。若启动请求返回成功但 3 秒后
仍没有可控制窗口，桌宠最多自动重试两次；准备好后只发送一次全局播放/暂停快捷键。Core Audio 真正检测到有声后结束
“加载中”，先播放一次“心律共鸣·OK”，再进入本首歌曲对应的音乐持续动画。整个冷启动
最多等待 30 秒。启动、控制或切歌超时只显示明确的文字提示并恢复待机/此前持续状态，
不再请求“心律共鸣·呜呜”，也不显示“动画素材暂时不可用”占位。网易云只有
存在可交互主窗口时才视为已经运行并保持现有播放/暂停流程；退出主界面后即使仍残留无窗口
的后台子进程，再次点击播放/暂停也会重新启动网易云，而不是向残留进程发送无效快捷键。

音乐悬浮岛已开启时，鼠标移入桌宠，角色动画正上方同时显示当前歌曲名称；无曲目信息时显示简短的“未在播放”；有歌手信息时以较小字号显示在
下一行。桌宠发起上一首或下一首后先显示“正在切换歌曲”，检测到曲目变化后显示新歌名
约 5 秒。用户从网易云或其它设备直接切歌时，只要 Windows 可观察到标题变化，也显示
同样的短暂提示。歌名区域独立预留 52 DIP，不覆盖角色。

受保护游戏位于前台时，安全模式禁止发送模拟键盘输入。此时按钮可以显示“游戏安全模式下已暂停快捷键”，或者要求用户使用真实键盘媒体键。

快捷键发送需要至少 350 毫秒间隔；发送前若前台进程无法安全确认，或用户正按住
相关按键及 Ctrl/Alt/Shift/Windows 修饰键，本次操作直接取消。桌宠只释放本次
自己按下的键，不校正、不接管用户原有键盘状态。

### 4.2 状态检测

- 使用 Windows Core Audio 定位网易云音乐的音频会话。
- 连续检测到有效声音后进入音乐状态。
- 普通短暂无声或音频会话在切歌时短暂重建，人物动画使用约 5.0 秒宽限；这段时间保持当前音乐动画，不闪回待机。播放按钮另用 0.75 秒连续静音门槛，外部暂停通常在 0.75～1 秒内显示播放图标；桌宠成功发送播放/暂停快捷键时先即时切换按钮，并以 2 秒确认窗等待 Core Audio，若命令没有真正生效则按可靠采样自动纠正。用户亲自在桌宠上点击暂停后，人物状态仍以首次静音立即确认，不额外等待完整宽限。
- 连续超过 5 秒无声或会话缺失后回到待机；桌宠发起上一首/下一首时另有最长 15 秒的加载保护，曲名变化可先播放一次 OK，但音乐动画保持到新声音恢复或保护超时。
- 可选使用 Windows 系统媒体会话读取播放/暂停状态和歌曲变化；这不是网易云私有接口。
- 曲目信息优先读取 Windows 系统媒体会话；当前网易云版本没有注册媒体会话时，只兜底
  读取 `cloudmusic.exe` 由 Windows 桌面管理器公开的主窗口标题，并按“歌曲 - 歌手”
  解析。不得读取进程内存、窗口正文或网易云私有文件。
- 曲名和歌手只在内存中短暂保存用于显示，不写入日志、不保存播放历史。

“快捷键已经发送”和“播放器已经改变状态”应分开处理。无法确认结果时只显示操作反馈，不显示误导性的成功提示。

### 4.3 已确认的播放反馈

- 设置页先提供“播放音乐时使用动画”总开关；关闭时保持当前桌宠外观，但媒体控制、歌名与网易云独立音量仍正常工作；
- 默认使用“智能分配”：普通或未知歌手固定播放“享受音乐”；包含“乐正绫”或 `Yuezheng Ling` 本人、`Official` 后缀或参与合唱时，持续循环“十二周年·打 call”；包含“洛天依”或 `Luo Tianyi` 本人、`Official` 后缀或参与合唱时，仍在“音乐摇摆”和“一键唱歌”两个彩蛋中随机选择。两位歌手同时出现时洛天依分流优先；
- 用户也可以固定“享受音乐”“音乐摇摆”或“一键唱歌”中的任意一项；固定后不再判断歌手，所有歌曲只播放这一种动画；
- 选中的动画作为当前播放会话的音乐持续动画循环，不再区分“开始播放动画”和“持续播放动画”；修改设置后如果正在播放音乐，立即切换到新规则解析出的动画；
- 循环候选由统一登记表提供，未来增加音乐动作时必须同步加入登记表与设置选项，不在窗口代码中散落特殊分支；
- “享受音乐”使用 0.5 倍播放速度；持续循环只使用文字已完整显示的稳定帧并往返闭环，人物在相邻动作间衔接，英文文字不随每轮循环反复入场；处理后的文件为 `assets/animations/processed/心律共鸣_享受音乐_无缝_0.5x.gif`；
- “一键唱歌”裁掉按键入场及左侧竖排文字，只保留角色主体和动态音符；稳定段使用源第 7→15→8 帧往返闭环，每帧 160 毫秒、每圈 2.56 秒，嘴型速度为旧版的 62.5%，末帧回到首帧也只跨相邻原动作，不用交叉淡化生成重影；
- 音乐状态临时覆盖 Q 版小人或完整全身显示，但不修改用户选择的显示模式；音乐停止后恢复此前选择；
- 上一首、下一首等操作等待播放器返回时只显示歌名区域的“正在切换歌曲…”文字，不播放“加载中”素材；加载期间即使声音或音频会话暂时消失，也保持当前音乐动画；“心律共鸣·加载中”的静态原图只用于网易云尚未运行时的冷启动等待；
- 加载动画只在等待超过约 400 毫秒后显示，避免正常切歌时闪烁；
- 等待成功结束后播放“心律共鸣·OK”；
- 暂停确认后立即停止音乐动画并恢复用户原本选择的 Q 版小人或完整全身模式；不播放“九周年·没睡醒呼吸”，也不保留一分钟暂停展示；
- 暂停后恢复播放时按用户选择的随机或固定规则重新解析音乐动画；暂停期间双击恢复正常的三种完整全身外观循环行为。

加载状态必须设置超时。超时后退出加载动画并进入待机或错误反馈，不能无限循环。

### 4.4 显示模式恢复约束

用户处于完整全身模式时，开始播放音乐会由当前随机或固定规则选中的音乐动画临时覆盖；这只改变当前播放内容，不改变用户选择的完整全身模式。音乐停止后恢复完整全身待机。用户原本处于 Q 版小人模式时同理恢复 Q 版小人。音乐覆盖期间即使用户切换显示模式，也只更新被记住的用户选择，待音乐停止后再显示新选择。

## 5. 音量控制与反馈

当前版本不控制或监听 Windows 系统主音量：设置窗口不显示系统主音量、滚轮开关、外部
主音量反馈开关或步长；桌宠窗口的滚轮也不写入默认输出设备。旧版配置中的系统主音量字段
只为兼容已有 `settings.json` 保留，不参与当前运行。

网易云独立音量只在悬浮控制岛提供竖向快捷滑杆，不在设置页重复出现。滑杆支持从轨道
任意位置按下后直接连续拖动，无需先抓住圆形滑块。它枚举所有活动输出设备，
用会话进程号匹配当前全部 `cloudmusic.exe` 实例，并通过 `ISimpleAudioVolume` 同步读写匹配
会话；这与 Windows 11 音量合成器中的应用音量属于同一层，不改变系统主音量、游戏、语音
或其它应用。网易云尚未播放并创建会话、会话正在重建或音频设备暂不可用时，滑杆禁用并
显示原因，用户可在播放后重新打开控制岛滑杆；任何失败都绝不能退回系统
主音量。控制岛只在悬浮时显示，不绑定滚轮或音量反馈动画。

设置窗口采用左侧分类导航：常规、音乐、通知、关于。常规页保留启动、置顶、双击换装和吃包子四项；音乐页依次显示播放动画总开关、带规则说明和预览的“智能分配”、以及三张“固定唯一动画”预览卡；通知页保留 QQ/微信提醒与 Windows 授权状态；关于页集中展示本地运行和隐私边界。左栏人物随页面切换：常规使用“心律共鸣·我推”，音乐使用“心律共鸣·真好”，通知使用“心律共鸣·天哪”，关于使用睁眼且保留原始 `Join Us!` 文字的“心律共鸣·加入我们”；四张均从官方透明 PNG 按可见边界等比裁入，不擦字、不重绘。页面专属台词保留且不附加角色署名。程序 EXE、设置窗口和系统托盘使用同一枚由晶蓝礼服全身图裁出的近景大头图标。

## 6. 待机与时间

建议默认阈值，之后允许用户调整：

- 没有播放音乐且没有其他事件：默认显示用户提供的 Q 版小人全身立绘；用户双击后切换到完整全身模式；
- 正在播放音乐且没有其他事件：默认按歌手智能分配；普通或未知歌手固定循环“享受音乐”，包含乐正绫时循环“十二周年·打 call”，包含洛天依时优先在“音乐摇摆”和裁掉按键入场后的“一键唱歌”中随机选择。关闭播放动画总开关时保持当前外观；固定三项中的任意一项时，所有歌手都只使用该动画；
- 45 秒～3 分钟：允许随机待机；
- 经典猫耳版且没有播放音乐时，连续无输入达到 2 分钟先播放“十周年生日·摸鱼一分钟”的 00:59→00:00 精确一分钟倒计时，画面开始后独立计时 60 秒进入“心律共鸣·嘿嘿”；晶蓝礼服版不使用倒计时，在无操作 1～5 分钟之间每轮随机播放一次同模型“打哈欠”，也不再进入“嘿嘿”；华裳长发版永不触发“嘿嘿”。单击等直接操作或离开对应外观后恢复当前外观；按住拖拽期间及松手后均保留经典猫耳版的“嘿嘿”，不把它误判成退出输入；
- 连续无输入达到 30 分钟且没有播放音乐：进入睡眠；每秒只读检查一次输入空闲时长；
- 系统锁屏或显示器关闭：停止高帧率渲染。

经典猫耳版长时间睡眠保留“代号洛天依·眠了呼吸”并持续低幅度呼吸，只在连续无输入 30 分钟后触发。晶蓝礼服版同样以 30 分钟为门槛，但改为 50:50 随机进入“睡觉”或“鸭子坐”：先播放进入段，再分别定格在睡觉第 220 帧闭眼卧姿或鸭子坐第 120 帧闭眼坐姿；两段长待机都必须保持人物原大小并为头侧装饰保留透明空间，睡觉横向卧姿还必须完整显示左右头发。桌面其它键鼠输入不自动解除，点击人物才播放剩余起身段并恢复待机，不能在鼠标按下时瞬间跳回默认待机。定格后立即且每 5 分钟按 60:35:5 随机循环 zzz、梦见包子、梦见乐正绫，允许连续相同；最小 Z 与最小云朵从人物头侧同一位置开始，装饰在屏幕右侧放不完整时镜像到左侧对称位置。zzz 全程保持淡蓝色，不能在飘高后变灰；点击唤醒时包子/乐正绫梦境先快速云朵消散，zzz 直接隐藏。华裳长发版不进入长睡眠。

启动时间段已经确认如下：

- 06:00（含）～12:00（不含）：播放“收藏集·早安浮动”，表达早上好；
- 12:00（含）～13:30（不含）：播放“十周年·干饭弹跳”；
- 13:30（含）～18:00（不含）：播放“十周年生日·来不及了！”原始动画，作为下午问候；保留文字、透明背景及每帧 100 毫秒的原生节奏；
- 18:00（含）～20:00（不含）：播放“十二周年·饿”，作为傍晚问候；
- 20:00（含）～次日 06:00（不含）：播放“十周年·晚安浮动”。

“眠了呼吸”与“晚安浮动”的用途已经调换：前者负责 30 分钟长睡眠，后者负责夜间启动问候。

五段时间问候在每次桌宠正式启动时触发，包括用户手动启动和 Windows 开机自启动，并持续展示 10 秒。如果桌宠一直运行，则在本地时间跨过 06:00、12:00、13:30、18:00 或 20:00 时自动展示新时段问候 10 秒；同一时段内不重复触发。任何直接用户输入可以打断当前问候。音乐持续动画始终优先：正在播放时先保留待补问候；音乐停止后再稳定等待 10 秒，仍处于该时间段才补播，等待或问候期间重新播放音乐则立即让位。Windows 解锁或电源恢复只恢复持续状态。

7 月 12 日（洛天依生日）和 12 月 12 日（用户生日）增加“十二周年·生日快乐”待机彩蛋。彩蛋只在没有音乐、拖拽或其它一次性反应的待机期间触发，每次随机等待 45～90 分钟，播放一次后重新随机，不能进入普通高频待机池。

### 6.1 用户主动退出

用户通过 Windows 系统托盘右键快捷面板点击“让天依休息（退出）”时直接进入退出流程，不再二次确认；随后，播放一次“心律共鸣·裂开晃动”，约 0.7～0.85 秒后关闭程序。退出动画期间忽略重复退出请求。桌宠人物右键仅提供第 8 节规定的常用菜单，不提供退出入口。

Windows 关机、注销、进程崩溃或系统强制结束时不等待退出动画，以免阻塞系统流程；这些路径仍优先保存能够安全保存的状态。

## 7. 原神启动

确认行为：

1. 监听原神进程由未运行变为运行；
2. 播放“心律共鸣·你不许玩”一次；动画结束后保持最后一帧，使动画与停留合计约 5 秒；
3. 播放期间申请临时置顶，但不激活窗口；
4. 播放结束释放临时置顶；
5. 如果用户原本永久置顶，结束后继续置顶；
6. 同一个游戏进程生命周期只触发一次；
7. 全屏安全模式生效时可以提前结束、跳过置顶或延后显示。

默认进程名候选为 `YuanShen.exe` 和 `GenshinImpact.exe`，通过配置维护，不依赖窗口标题。

### 原神后台低频彩蛋

除启动反应外，新增“心律共鸣·拜托”作为原神持续运行期间的低频彩蛋：

1. 仅在原神进程仍存在、但当前前台进程不是原神时计时和显示；
2. 每个 60 分钟的后台活跃窗口随机选择第 5～55 分钟触发一次，并设置 45 分钟硬冷却，避免相邻时间窗出现连续弹出；
3. 到达触发时间时若原神已经回到前台或进入全屏，暂停显示并在重新进入后台后随机延后 5～15 分钟；
4. 在桌宠当前显示器的 Windows 工作区内随机选择位置，保留 24 像素安全边距，保证角色完整可见且不覆盖任务栏；
5. 临时移动到随机位置，播放“拜托”一次；动画结束后恢复此前的位置、持续动画和用户置顶设置；
6. 弹出窗口始终不激活、不抢焦点；原神前台时不申请临时置顶；
7. 原神进程结束、电脑锁屏或桌宠进入睡眠时取消尚未触发的计时。

这项彩蛋只读取进程是否存在以及 Windows 当前前台进程标识，不读取游戏内存、窗口画面、模块、线程或网络数据，也不向游戏发送任何输入。

## 8. QQ 与微信提醒

采用文末“耳旁图标与共享会话列表”确认规则。消息到来时保留当前待机／音乐动画，默认仅更新微信／QQ 图标及本地待处理次数，正文由用户悬停或固定展开。微信在上、QQ 在下；无提醒隐藏并上移补位。三形态按实际 Alpha 轮廓分别校准，桌宠在右则提醒放左，在左则放右；拖动与展开期间固定方向，中线迟滞避免抖动。

QQ 主要使用授权的 Windows Toast；微信使用既有只读公开会话字段与任务栏闪烁回退。首次旧未读不补报、重复通知 ID 不再入列；无法读取详情时保留来源条目，不读取聊天历史、数据库或远程头像。名称与摘要只驻留本地待处理列表，不写文件或上传。

同会话合并，昵称与最新摘要各占一行，行计数合计为应用角标；最多 3 行高，更多内部滚动。数字是桌宠内待处理事件次数，不是客户端真实未读数。悬停 300 ms 打开、离开共同区域 500 ms 关闭；点击固定，再次点击或外部点击收起。跨间隙保持展开，阅读期间冻结已有行顺序与滚动位置。

收起不清空提醒；右键会话／应用可忽略／清空对应本地记录，不删除聊天也不修改客户端已读。微信公开字段确认已读时移除对应会话；QQ 不假定打开应用即读完全部会话。新事件仍可重新入列。全屏、锁屏、安全隐藏时隐藏交互窗口；待处理记录仍只保存在内存中。

## 9. 鼠标互动

桌宠正式使用同一完整全身显示槽，并在三套外观之间切换：

- **完整全身模式**：启动后使用当前保存的外观；双击按固定顺序循环三套外观，设置关闭循环后固定当前项；
  - “华裳长发版”（新完整全身待机，默认）是纯展示模式，不响应身体触摸；
  - “晶蓝礼服版”（Q 版小人全身待机新图）使用用户导出的独立多边形命中图、9 组同模型身体动作和专属打哈欠待机动作；未标注区域保持无反应；
  - “经典猫耳版”（上一版 Q 版小人全身待机）继续使用现有表情包互动，因为人物造型与表情包风格一致。

双击间隔默认不超过 300 毫秒。第一次单击需要短暂等待双击判定；确认不是双击后才分发身体区域互动。双击只切换模式，不同时播放单击动画；若当前正在播放由身体部位触发的动作，则本次双击不切换形态、不打断动作，必须等动作完整播放后才重新允许切换。临时事件动画结束后回到事件前选中的显示模式；应用重新启动时仍默认从 Q 版小人开始，不记忆上一次的全身模式。

鼠标位于当前角色画面的不透明人物像素、媒体控制悬浮岛的四个按钮或竖向音量整轨时，使用用户提供的天依服装“指向”手型；透明画布和仅展示信息的歌名区不强制替换系统指针。只有当前外观已接入可执行的身体互动、并且当前位置实际命中头发/头顶摸头区域时，才改用专属“摸摸头”手型；离开该区域立即恢复普通指向手型。两种手型均编译为带准确热点的本地 Windows `.cur`，不使用全局鼠标 Hook；普通手型资源不可用时上述可点击区域统一回退系统 Hand。

晶蓝礼服版按用户随区域文件提供的说明执行独立映射：左右眼播放“遮眼睛”，嘴巴播放“捂嘴”，脸部播放“捏脸”，左右手播放替换旧素材后的“新比心”，左右脚播放“摸腿脚”，头部短按或完成往返抚摸播放“摸摸头”，Chest 播放“摸胸”，LowerBodySensitiveArea 播放“摸裙边”，OtherBody 播放“捂肚子”。身体动作完整播放一次后回到晶蓝礼服待机。播放时始终表现为当前本体在原地做动作：运行图集从 720×720 母版直接生成 440×476 帧并以固定 220×238 DIP 显示，使桌宠 200% 档位仍有 1:1 运行采样；开始和结束都不改变透明窗口宽高或屏幕坐标，也不使用通用的缩小、闪光或弹出换图过渡；运行图集首尾各用 6 帧、约 0.25 秒从完全一致的待机画面平滑融入或退出。动作结束直接恢复待机帧，不追加第二次青色脉冲。“鸭子坐”和“睡觉”只用于 30 分钟两段式长待机，不绑定身体部位点击。

以下既有表情包映射只适用于“经典猫耳版”。它使用透明像素命中，并划分左右眼、脸部、嘴部、头发/头顶、左右手、胸部、下体敏感区域、普通身体和左右脚。“华裳长发版”不执行这些映射，“晶蓝礼服版”也绝不回退本列表：

经典猫耳版的表情包动作按内容分别调整：心软软 0.72 倍、啾啾 0.82 倍、贴贴 0.80 倍、击掌和糟了 0.68 倍、重罪约 0.80 倍、黑化约 0.60 倍。摸摸头不再重复慢放同一十帧周期，而是按 `0→9→0→9` 重排为 28 帧连续往返、每帧约 80 ms，形成约三次抚摸且总时长约 2.24 秒。全部部位反应结束时用 110 ms 淡出、完全透明时换回待机，再用 170 ms 淡入。

经典猫耳版命中坐标以当前 480×520 透明立绘重新标定：眼睛、嘴与脸整体下移到真实五官，
左右手下移到袖口/手掌，胸部和下体保持小范围，双脚贴合鞋面；所有区域仍先经过当前帧 Alpha
检测，透明背景不会响应。

身体命中区域允许由用户通过“候选素材_官方/身体区域描边编辑器.html”自行校准。编辑器直接载入
三套当前人物画布，以 0～1 归一化多边形记录轮廓；每个部位可包含多个互不相连的轮廓，并保留
眼睛、嘴、脸、双手、敏感区域、双脚、头发和普通身体的既有优先级。编辑结果先导出为 JSON
供确认，不在浏览器中直接覆盖运行时配置，避免未完成的描边立即改变正式桌宠。第二模型“晶蓝礼服版”
和第三模型“经典猫耳版”均已采用用户确认导出的 12 区域 JSON：运行时按 priority 从小到大执行首个
多边形命中，并继续先检查当前人物像素 Alpha。第二模型 OtherBody 触发“捂肚子”，第三模型
OtherBody 触发“抱抱”；第一模型不载入可执行区域互动。

- 首次点击左眼：原方向播放一次“心律共鸣·心软软”；首次点击右眼：将同一动画水平镜像后播放；眼睛是脸部内部的更小命中区域；
- 4 秒内连续第二次点击任一眼睛：改为播放一次“十二周年·哭唧唧”；点击其它身体区域或超时会重新从第一次开始；
- 短按嘴部：播放一次“心律共鸣·啾啾”；
- 短按脸部但未命中眼睛或嘴部：人物左半脸播放一次“十二周年·充电”，人物右半脸保持“十二周年·贴贴”；沿现有脸部多边形的横向中心线区分，不要求重画命中区域；
- 按住头发或头顶区域并左右移动，持续约 350 毫秒、累计约 24 DIP 且至少换向一次：判定为摸头，播放一次“天依游学记·国乐季 · 摸摸”；短按头顶也播放一次作为易用性兜底；
- 点击左手：原方向播放一次“十周年·击掌回弹”；点击右手：将同一动画水平镜像后播放；
- 点击胸部或下体敏感区域：第一次播放一次“心律共鸣·重罪”；两个敏感多边形只覆盖用户圈定的明确中心区域，把更多身体画面留给普通部位；
- 在第一次胸部/下体点击后的 4 秒内，再次点击这两个区域中的任意一个：播放一次“心律共鸣·黑化”；
- 首次点击右脚：原方向播放一次“十周年·糟了晃动”；首次点击左脚：将同一画面及同步晃动水平镜像后播放；
- 4 秒内连续第二次点击任一只脚：改为播放一次“十二周年·斯到普”；点击其它身体区域或超时会重新从第一次开始，且不进入黑化判断；
- 点击没有被眼睛、脸、嘴、头顶、手、敏感区域或脚命中的普通身体部位：固定播放真正的“十二周年·抱抱”。官方素材包把两个文件名写反，实际“抱抱”画面来自 `12_在吗.gif`；旋转舞、九周年点赞、八周年点赞、我推和真好已取消本场景触发，只保留候选；
- “天依游学记·国乐季 · 摸摸”固定用于摸头；“心律共鸣·可爱”不再承担该状态；
- “黑化”触发后进入 10 秒敏感区域冷却，期间不重复播放黑化，避免连续刷屏；
- 滚轮：当前不绑定功能，不改变系统或其它应用音量；
- 托盘左键显示／召回天依，将边缘收纳或隐藏的人物恢复到可见工作区并带到前面；已显示时不反向隐藏，不改变永久置顶设置。托盘右键打开紧凑菜单，每次打开先记录本次托盘点击的屏幕坐标，菜单内操作不再按鼠标位置重新定位；提供显示／召回、暂时隐藏、完整设置与单击退出（无需二次确认）；完整设置保留开机自启动、音乐动画和通知等低频选项。
- 桌宠人物不透明区域右键打开人物旁的常用菜单，只提供锁定位置、始终置顶、显示音乐悬浮岛、显示大小。锁定位置只阻止用户拖动，不影响摸头、身体点击和双击换装，锁定选择持久化。菜单点击外部或按 Esc 收起；透明留白不触发，动画、音乐状态和身体区域调试仍只走命令行 QA。
- 显示大小在桌宠右键中以“− 当前百分比 +”按 5% 即时调整并保存，完整设置常规页提供 50%～200% 滑杆，保存后生效。倍率统一作用于待机、音乐、互动、边缘和吃包子人物动画。歌名岛宽度设最小／最大值，小尺寸控制岛按比例缩小，大尺寸不无限拉长。
- 在 Q 版小人模式按住左键并移动超过约 6 像素：进入拖拽并自动切换为“心律共鸣·膨胀”；动画完整播放一次后固定末帧，不因继续拖动而循环；
- 在完整全身模式进入拖拽：保持原完整全身待机画面，只移动桌宠窗口；不切换“膨胀”或任何专用全身拖拽动作；
- 正在播放音乐时进入拖拽：无论用户选择的是迷你还是完整全身模式，都保持本次播放会话的当前音乐动画，不切换“膨胀”或完整全身模型；
- Q 版小人模式普通松开鼠标左键：不播放“降落”，经纯透明淡出／淡入从“膨胀”自然恢复 Q 版小人或拖拽期间更新后的持续状态；经典猫耳版拖拽时若在 1400 ms 内完成至少 32 DIP、360 DIP/秒的五段快速左右移动（四次换向、即两次完整左右往返），则切换为“十周年·旋转舞”并连贯循环，下一次点击或音乐开始时停止；
- 完整全身模式松开鼠标左键：不播放降落动画；画面已经是完整全身待机时无需再次切换，如果拖拽前存在音乐等持续状态，则平滑恢复该状态。
- 音乐播放状态松开鼠标左键：不播放降落动画，继续同一音乐动画。
- 拖拽松手后以恢复画面的实际可见边界定位，允许透明展示槽留白移出屏幕。靠左、右、上、下或角落时保持对应边贴齐；未贴边时仅在实际人物溢出时纠偏。膨胀恢复待机／音乐统一使用 110 ms 淡出、不可见时定位、170 ms 淡入；画面未变时直接保持。附件仍按上下位置换位，真正拖出屏幕的收纳门槛不变；再次拖动取消旧恢复。

命中优先级为：窗口拖拽判定 > 已在播放的部位动作锁 > 双击模式切换 > 摸头手势 > 最小身体区域。眼睛和嘴部命中优先于脸部；鼠标移动超过 6 像素后不再把松手识别成身体点击；脸部与头顶重叠时，短按按最小区域分发，按住并抚摸头顶才触发摸头。胸部和下体共用同一个 4 秒重复点击计时器，双脚和普通身体不使用该计时器。

降落弹跳、快速向下甩动判定及其程序弹簧位移全部删除。普通松手、其它完整全身模式以及音乐播放状态直接保持或平滑恢复拖拽前持续状态；模式三快速来回甩动的旋转彩蛋触发后不进入边缘收纳，直到下一次点击或音乐开始才恢复当前持续状态。

身体区域反应动画尚未结束时，双击显示模式切换被忽略，既不换形态也不打断当前动作；动画结束并解除反应后立即恢复双击切换。随后进入 800 毫秒身体互动恢复期，这段时间只忽略新的身体单击和摸头候选，防止动画尾部的误触在恢复待机后排队播放；双击切换和窗口拖拽在恢复期内仍可用。双击识别仍会取消第一次待决单击，因此恢复期不会把双击拆成身体单击。

### 9.1 屏幕边缘躲藏

- 三种模式的待机、音乐和互动动画共用稳定透明展示区域，素材保留各自原比例和尺寸，底部居中；悬浮岛大小只随用户缩放设置变化，不再随素材裁剪宽度变化。普通靠边停放按统一展示区域为宽动作预留空间，不在每次换动画后推挤窗口；用户明确继续向外拖动时，躲藏仍按当前人物 Alpha 边界判定。
- 单纯把人物画面贴到左边缘、右边缘或任务栏上方时只执行贴边摆放，不进入躲藏；继续向屏幕外拖过约 28 DIP 才视为明确的躲藏意图，并在尚未松手前切换为对应动画预演。顶部不触发。
- 边缘预演开始后继续使用进入预演前的拖拽人物边界计算越界深度，不因“完整全身”和“偷看/闪亮登场”素材尺寸不同而改变判定；退出门槛为约 14 DIP，形成 14 DIP 回差，避免鼠标停在临界位置时反复切换图片。
- 左右边缘使用“十二周年·偷看”：松手后以原速播放第 7～3 帧无文字收回段；鼠标悬停后以当前素材速度的 1.3 倍从第 3 帧完整正放到第 19 帧，逐步显示“偷看”文字并展开到素材中的完整人物。移出时不把文字倒放进墙内，而是回到第 7～3 帧收回段。
- 右侧使用原方向，左侧水平镜像，保证角色始终从对应屏幕内侧探头。
- “十二周年·偷看”原始官方 GIF 保持不变；资产编译阶段先把每帧统一缩为 240×240，再直接裁掉右侧人物外的墙体，得到 186×240 运行时帧并按 147×190 DIP 等比例显示。每一帧只保留靠人物侧约 10 像素墙面，厚度约为上一版的一半，同时保留全部“偷看”文字、头发、脸和手；不把裁后画面横向拉伸回原宽度。
- 左右隐藏完成后只暴露裁后画面靠墙侧约 4.6%（约 7 DIP）的砖墙细条，即此前可见墙宽的一半；并按该裁剪区而非完整透明窗口对齐屏幕边缘和设置鼠标触发面，不再显示人工绘制的天依蓝三点条或整块宽墙。
- 底边使用“十二周年·闪亮登场”：收回以当前素材速度的 0.7 倍从没有文字的第 5 帧开始下钻到第 3 帧，钻出仍以原速使用第 3～7 帧效果；最终只裁出贴着任务栏上沿的猫耳/头发作为触发面。
- 边缘动画在同一播放段内改变方向时沿用当前帧，不回到动画首帧；由完整含字的左右展开切回隐藏时，为满足“收回不显示文字”，允许直接进入第 7 帧无文字收回段。动画结束后固定目标帧，不闪现另一轮的开头。
- 统一展示区域接近屏幕工作区顶边约 80 DIP 时，歌名/作者与播放控制一起移到人物下方；接近底边约 80 DIP 时，两者一起移到人物上方。无论位于人物哪一侧，纵向顺序始终是歌名/作者在上、播放控制在下；离开上下边缘后恢复分置布局。布局换侧保持展示区域屏幕位置连续，不再因动画透明留白、裁剪尺寸或摆动帧发生反复换侧。
- 从展开状态开始拖拽会立即退出边缘模式并恢复普通拖拽；边缘模式不显示音乐控制岛，也不接受身体点击。

## 10. 桌面文件拖放与回收站

桌宠注册为普通 Windows OLE 文件拖放目标。为避免全局 Shell Hook、鼠标 Hook 或资源管理器扩展，不监听文件在任意目录开始拖动的瞬间；从桌面或任意资源管理器目录拖出的真实文件/文件夹进入桌宠窗口（包含人物上下为歌名和控制岛预留的透明区域）时，才开始互动。桌面外壳和最大化资源管理器都属于 `explorer.exe`，即使窗口覆盖整个屏幕也允许投放，不要求用户预先点击或激活桌宠：

1. `DragEnter`：播放一次“心律共鸣·给我”；动画走完后固定最后一帧，不循环，也不自动恢复待机；
2. 只有指针位于当前可见人物画面内并停留至少约 400 毫秒时才接受松手，防止只是路过桌宠就误投；
3. `DragLeave`：结束定格并恢复此前动画，不对文件做任何操作；
4. `Drop`：仅接受 Windows `CF_HDROP` 提供的真实本地文件系统路径；
5. 使用 Shell `IFileOperation` 配合允许撤销和回收站标志，将文件或文件夹移入 Windows 回收站；
6. 成功后结束“给我”定格并播放一次“心律共鸣·大成功”，随后恢复此前持续动画；不使用“心律共鸣·OK”，不生成包子，也不进入追逐或进食动画；
7. 如果目标磁盘、网络位置或虚拟文件不支持回收站，则保留原文件并显示失败反馈，绝不退化成永久删除；
8. 不支持按住 Shift 永久删除，也不调用 `File.Delete`、`DeleteFile` 或绕过回收站的删除方式；
9. 文件操作期间桌宠不抢键盘焦点，原神等受保护应用位于前台时禁用文件接收区。

拖放本身视为用户对本次文件移动的明确操作，并作为始终可用的快捷回收站入口。设置中的“桌面吃包子动画”只控制桌面项目消失后的追逐，不关闭快捷回收站。文件夹和一次超过 10 个项目的批量拖放需要额外确认。

### 10.1 “吃文件”历史视觉候选（已明确不用于快捷回收站）

为了避免仅靠静态图片摇摆，候选流程改为复用官方表情中的真实动作片段：

1. 奔向桌面文件时使用收藏集“来喽”的前扑姿势，角色画面做轻微上下起伏，真正的位置变化由桌宠窗口沿桌面坐标移动完成；
2. 旧方案曾考虑在文件松手到桌宠后切换为洛天依 11 周年 QQ 动态表情“干饭”；按当前决定，快捷回收站不采用这段演出，素材仅留作历史候选；
3. 文件缩略图或文件类型图标作为独立图层向嘴边移动并缩小，动画结束后才显示 Windows 回收站的真实成功或失败结果；
4. 两套官方画风切换处使用约 120～180 毫秒交叉淡化，不额外生成新的洛天依人物画面。

对应候选预演位于 `候选素材_官方/10_新添加_文件吞食组合候选/`，当前只进入动画挑选器；快捷回收站的正式反馈固定为“给我 → 大成功”。

“用户在桌面自行删除文件后桌宠跑过去吃”的模式是独立后续功能：只能观察公开的桌面目录与 Shell 图标位置，不注入资源管理器、不安装全局 Hook，也不替用户再次删除。它只在能可靠匹配删除前图标位置时播放；无法可靠匹配时不猜测文件名或位置。

## 11. 待确认事项

- 上一首和下一首是否需要不同的图标或气泡；
- 其余已勾选表情的场景映射；
- 全屏游戏时是隐藏桌宠、保持桌面层，还是显示但禁用交互。
# 文件变包子与追逐进食（已确定）

1. 只有桌面项目被系统观察到从桌面位置消失后，才在其最近一次桌面图标中心生成一个悬浮小笼包；直接拖到桌宠的快捷回收站操作明确排除在外。
2. 小笼包在被吃掉前可由用户拖动；洛天依始终追踪当前坐标，而不是只记住初始位置。
3. 洛天依循环播放当前外观自动对应的吃包子画风，到达后切换一次张嘴进食段；包子沿曲线缩小飞入口中并消失，随后闭嘴。两套画风并存：“元气追包版”为手工 Alpha V1，“甜笑寻包版”为直接保留用户透明 PNG 序列 Alpha 的 V2；两套奔跑均使用 410×410、60 个运行帧与约 62.5 FPS，先稳定人物上半身在源帧中的全局平移，再做双向光流补帧。人物换帧和桌面位移必须共用屏幕刷新时钟，窗口位置吸附到当前 DPI 的物理像素；发生 UI 卡顿时单个可见帧最多补偿 34 ms 位移，不能为了赶上逻辑坐标产生瞬移。角色位移使用 smoothstep 平滑加速，从 180 DIP/s 起步，3.5 秒有效运动时间达到 800 DIP/s；返程固定 850 DIP/s，不参与加速。速度按当前显示器工作区的逻辑对角线缩放，1080p 为 1 倍、2K 约 1.33 倍、4K 为 2 倍，4K + 200% 缩放按约 1920×1080 DIP 处理，不重复放大。包子进嘴后，V1 从第 120 帧、V2 从第 112 帧以 1.1 倍速进入闭嘴尾段，分别约 0.79/0.91 秒后继续追逐或返程。唯一包子在满速后被连续拖住 3 秒时，每轮一次播放“我很可爱，请给我小笼包”并定格；该动画包含文字，进入前必须强制使用正向画面，不能继承向左追逐的水平镜像。包子松在人物任意不透明 Alpha 上或圆形包子足迹与可见人物有重叠时，即沿约 400 毫秒短弧线进入嘴巴；仍有包子则约 350 毫秒后继续追逐。切换只改变后续追逐动画，不改变回收站安全逻辑。
4. 天依已经开始返回本轮起点时，如果又生成包子，必须立即中断返程、重新选择最近目标并继续追逐；新的包子不能因为返程状态永久留在桌面。所有包子清空后仍返回最初记录的起点。
4. 有多个包子时优先处理最近目标，全部完成后跑回触发前的位置并恢复 Q 版小人、完整全身或音乐状态。
5. 文件直接拖到桌宠时，仍先等待约 400 ms 并通过 Windows Shell 放入回收站，但无论源自桌面还是其它目录，成功后都只恢复原状态，不演出进食；失败时文件保持原处并播放失败反馈。
6. 设置页可即时关闭桌面吃包子动画，但快捷回收站入口仍可用。全屏、原神等受保护游戏前台、锁屏或无法确认前台状态时不生成/继续追逐。
7. 整轮追逐、进食和返程期间强制隐藏歌名、媒体控制、网易云音量、反馈及消息悬浮岛；鼠标移入人物也不能重新显示，整轮结束后再恢复正常悬浮规则。
7. 甜笑寻包版的包子终点以张嘴帧的真实嘴部中心为准，人物向左时同步镜像，不能再飞到眼睛或脸部空位。

### 2026-09-12 用户确认：人物侧边提醒与 QQ 详情

旧提醒立绘方案已由文末耳旁图标方案取代；不切换或翻转人物，消息仅更新耳旁图标及共享列表。
详细提醒按最新确认由一个开关统一控制昵称、系统通知正文预览和新增通知计数；关闭后仅显示来源，基础提醒不关闭。公开托盘卡提供的独立未读数与新增通知计数区分标注，数据不可靠时降级为来源提醒。
完整设置窗口始终只保留一个实例；再次从托盘打开时恢复并激活已有窗口，保留未保存的编辑。
### 音乐状态、设置保存与提示（2026-09-12）

- 不启用音乐动画时，播放/暂停只改变音乐状态与按钮；相同画面不得闪光、缩放或重播首帧。需要真正换图的动作过渡保持。
- 保存设置时整份提交，不因内部多个分组争写而误报失败；真实磁盘写入失败仍告知用户。
- 操作提示固定排在歌名岛下方，两者能同时显示。小尺寸自动换行，屏幕上下边缘调整整体附件布局；提示出现/消失不改变角色大小与桌面位置，不强制打开用户关闭的音乐悬浮岛。

### 摸鱼一分钟补充规则（2026-09-12）

经典猫耳版无输入两分钟触发倒计时后，使用独立单调时钟播放一分钟；普通鼠标移动、悬停和桌面输入不会取消或重置倒计时。确认单击天依退出倒计时；拖拽只改变位置，计时不重置，正常松手保持当前动画。倒计时完成进入“嘿嘿”，其原有点击退出、拖拽保持规则不变；音乐、外观切换及安全隐藏仍按原优先级处理。素材先使用原白底版本，不抠背景，所有选用帧逐像素保留。

### 2026-09-12 用户确认：详细提醒统一开关

昵称、正文预览、新增通知计数由同一个详细提醒开关共同控制，不能单独关闭其中某项；关闭后仅保留来源提醒。正文只取系统提供的通知预览，不扩大到聊天历史、缓存或数据库。新通知计数与真实未读数字区分标注。开源方案先按只读源码研究，外部客户端接入方式尚未选择。

经典猫耳版无输入两分钟触发倒计时后，使用独立单调时钟播放一分钟；普通鼠标移动、悬停和桌面输入不会取消或重置倒计时。确认单击天依退出倒计时；拖拽只改变位置，计时不重置，正常松手保持当前动画。倒计时完成进入“嘿嘿”，其原有点击退出、拖拽保持规则不变；音乐、外观切换及安全隐藏仍按原优先级处理。素材改为透明背景，人物衣服、鱼腹、高光、文字与数字完整保留；实色像素使用原图 RGB，对白底抗锯齿边缘的全部灰阶恢复半透明，消除深色背景上的亮点和白边，不对人物重新绘制或全局量化颜色。

### 2026-09-12 用户确认：全面清理动画毛边

检查全部运行时动画，只对确认的透明边界硬锯齿、灰白底残留进行处理；保留人物内部原色、白色衣服、文字、标点及原有贴纸描边。帧数、时长、画面尺寸和定位不变，已有柔和 Alpha 的素材不重复处理。派生过程必须可追溯到原素材和显式处理参数。

### 2026-09-12：微信会话字段提醒试用

用户已确认 QQ 提醒无问题，并要求微信也做试用。微信增加一个统一详情开关，控制昵称、公开会话摘要与本轮新增提醒次数；读取 session_list 公开条目，不打开聊天或移动鼠标。每 750 ms 单通道观察，启动、读取失效恢复或新出现条目只建基线；已观察到的非免打扰条目出现未读标记上升或摘要变化才提醒。旧未读、仅滚动露出的会话、前台消息及免打扰会话不补报。新出现且无基线的会话第一条可能只有来源提醒，不宣称覆盖所有消息。计数是捕获的变化提醒次数，不是消息条数或微信全局未读；确认已读／忽略／清空／重启清除本地提醒；关闭详情时合并为来源条目。读不到列表则回退来源提醒。


### 2026-09-12 用户确认：嘿嘿拖动全程保留

“嘿嘿”中等待机状态按住拖拽期间及正常松手后一直保持当前嘿嘿动画，不切换膨胀；快速左右拖动不触发旋转舞，越界预览期间也保持原图且不翻转文字。单击仍退出嘿嘿；松手真正越界时仍按既有规则收纳，音乐开始等更高优先级事件照常生效。该例外优先于经典猫耳版普通拖拽的膨胀规则。

### 2026-09-12：微信首条详情与迟到提醒修复

用户确认微信实际能显示昵称／摘要，但首条偶有来源卡先出现、读完消息后才提醒。微信常规观察缩短至 750 ms；任务栏新信号请求立即观察，来源回退最多等待 900 ms 让详情先到。读取成功基线在短暂失败时保留最多 3 秒，长间断仍重新建立基线。微信待显示消息最多保留 8 秒；回到微信前台、会话未读消失／减少、静音或条目不可验证后撤销关联待显示与当前详情，异步动画准备结束再检查。首次出现且没有基线的条目仍不猜测为新消息；此限制不能用显示任意旧未读来弥补。QQ 行为不变。

### 2026-09-12 用户确认：托盘轻量菜单

托盘右键采用紧凑四行菜单：召回天依、暂时隐藏、设置…、退出桌宠。去掉顶部头像和标题，菜单实体宽 200 DIP，行高 35 DIP，圆角 11 DIP；每行带系统线性图标，仅退出前分隔。默认深灰文字，悬停浅青底，退出悬停柔和红字。鼠标打开不预选首项，键盘导航保留实线焦点提示，隐藏不可用时跳过。托盘左键召回、单击退出播放原告别动画、失焦及 Escape 收起的行为不变。

### 2026-09-12 用户确认：回收站动画正向

快捷回收站“给我”以及“大成功”含有文字，播放第一帧之前必须清除身体互动／追逐和贴边预览遗留的水平镜像，动画与定格期间保持正向。其它身体互动、左右追逐与贴边收纳仍使用各自朝向规则。400 ms 停留、取消恢复、批量／文件夹确认及只进 Windows 回收站的安全规则不变。
### 2026-09-12 用户确认：耳旁图标与共享会话列表（取代提醒立绘）

消息只更新图标和数量，保留当前待机／音乐动画，不切换提醒立绘；三种待机形态共用规则，按各自实际 Alpha 轮廓耳旁高度校准。屏幕右侧桌宠在屏幕方向左耳外放图标、列表向左；左侧相反。微信在上、QQ 在下；无提醒应用隐藏并上移补空位，全无时隐藏整组。图标 24 DIP、纵距 6 DIP、无胶囊；待处理提醒总数等于各会话数量之和，超过 99 显示 99+，不宣称原客户端真实未读数。

默认折叠正文；悬停图标 300 ms 展开共享面板，切换应用替换内容配色。跨图标与面板间隙保持展开，离开共同区域 500 ms 收起；点击图标固定、再次点击或外部点击收起，收起不清除提醒。微信浅绿白底绿细边，QQ 浅蓝白底蓝细边；小圆角、轻阴影、很小同色折角。面板 192 DIP、每行 46 DIP、横向内边距约 8 DIP，昵称 13 DIP 半粗、摘要 12 DIP，单行省略；最多 3 行高，更多会话内部滚动。

同会话合并、显示名称／右侧次数及最新摘要；默认按新消息倒序。展开阅读时保持既有行次序与滚动位置，新会话追加；下次打开恢复最新排序。拖动结束才换边，中线附近用迟滞保持方向，展开期间固定方向；按当前显示器工作区避任务栏与上下边缘。

右键会话仅忽略该会话本地提醒，右键应用图标仅清空该应用本地提醒；不修改客户端已读状态、不删除聊天，已处理事件不重复入列，新事件照常进入。统一详情开关继续共管昵称／摘要／次数的详细展示；基础来源提醒仍可显示本地次数。微信只在公开字段确认已读后移除对应会话；QQ 缺少逐会话已读确认时以本地忽略／清空为准，不因打开 QQ 而假定所有会话已读。

必须验收：左右两侧各 8 种应用有无／展开组合，三形态；长昵称摘要、超过 3 行、99+、屏幕四边、拖动与展开冻结、跨间隙连续操作、固定／外部收起、忽略与重放抑制、音乐不中断。测试数据与截图仅使用合成会话文本。

### 2026-09-12 用户确认：消息图标采用提供素材

QQ／微信耳旁图标使用 `候选素材_官方/微信QQ.png` 左侧“推荐”扁平版本，分别抠去背景、光晕、文字与示例角标，保留透明边缘；不再使用代码绘制的替代图标。仍使用 24 DIP 图标槽及原有计数、定位和交互。原素材不覆盖，派生 PNG 与来源哈希保存在 assets/ui。

### 2026-09-12 用户确认：托盘菜单进一步收窄

实体宽度由 200 调整至 170 DIP（缩小 15%）；窗口包含阴影留白为 194 DIP。左右内容内边距合计 12 DIP，保持 35 DIP 行高、四个操作和原悬停／键盘交互。


### 2026-09-12 用户确认：托盘按文字自适应

取消固定 170 DIP 宽，菜单以图标、最长文字和少量边距自适应宽度；设置文案去掉省略号，显示“设置”。保留四行高度与原有交互。此项取代上条固定宽度决定。

### 2026-09-13 用户确认：通知设置页采用紧凑行设计

按确认效果图重排通知页：总开关独立一行，QQ／微信详细提醒统一分组，使用 `候选素材_官方/微信QQ.png` 左侧推荐扁平图标的现有透明派生 PNG，微信“试用”使用小标签。权限改为状态与按钮同一行，已授权时隐藏重复按钮；实际系统授权状态优先于本地旧标记。详细规则与隐私默认折叠，常规状态一屏呈现，展开内容在右侧滚动，底部保存按钮保持可见。开关仍在保存后生效，不改变提醒采集和本地列表行为。准确说明昵称／摘要仅在本机内存用于当前提醒，不写历史／日志、不上传，替换旧的“不会显示正文”表述。其它设置页和左侧人物装饰保留。


### 2026-09-13：全页面视觉与多日期批量管理补充

按最近效果图统一月历、周历、闹钟、行程编辑、多日期选择、班休与提醒的浅青白色圆角视觉。日历上方不新增闹钟区，底部提供日历／闹钟／行程管理入口。行程编辑居中展开并保留日历背景；提醒图标固定在月格行程右侧。多日期行程按同一组管理，修改应用到整组；删除明确区分仅当前日期、整组及多选组，确认前显示范围。删除关联行程同步移除对应提醒，其他组与独立闹钟保留。

### 2026-09-13 用户确认：拖动仅改变位置与平滑追包子

按下、越过拖动阈值、移动和普通松手均不唤醒、不取消当前反应、不重播动画、不重置计时或镜像。睡眠（含入睡、定格、梦境装饰、醒来尾段）、鸭子坐、摸鱼倒计时、嘿嘿、听歌、身体反应、打哈欠、生日、问候、加载等待及原神彩蛋均保持。动画自然结束、倒计时完成、音乐与安全事件仍照常生效；单击等明确操作维持其原有作用。

模式三仅普通待机拖动使用膨胀；普通待机开始的快速左右甩动仍触发旋转舞，已在跳旋转舞时可继续拖动且不退出。正常拖动中的越界预览不替换受保护动画，松手真正越界仍可收纳。追包子、进食、返程、求包子与文件拖放提示等独立交互期间继续禁止拖动桌宠。

追逐加速使用连续 smoothstep 曲线，起步180、上限800 DIP/s与3.5秒时长不变；位移和加速共用每帧最多34ms的有效时间，避免卡顿后速度先跳到下一阶段。物理像素吸附保留小数位移余量，低速位移可累计。返程850 DIP/s、显示器速度倍率及回收站安全逻辑保持既有规则。


### 2026-09-13：九张参考图与整改规格（最新确认，覆盖此前日历UI段落）
月历以图二为准，日期格显示农历／节日及事项圆点，默认打开今日详情窄侧栏；图一提供设置班休和批量日期状态。休红班蓝。周视图无小时轴；顶层日历／闹钟，移除行程管理主导航，原整组及单日删除保留。事项使用名称、日期、可不指定时间、创建闹钟提醒、提前提醒和单行短备注。新建两级提醒默认关闭，提前时长首次30分钟。闹钟单列表按正在倒计时／我的闹钟／日历提醒分组，同一新建提醒弹窗切换指定时间和倒计时。倒计时采用图九的名称、快捷选择、三组上下按钮及可输入数字、开始布局，最大24小时，可暂停继续。状态联动、全局提醒设置和胶囊依据 docs/design/planner-spec-20260913.txt；图中1小时默认和随机分类色不采纳。已有长备注和有效提醒配置保留。本文及用户最新文字优先于参考文档和生成图。

### 2026-09-13：整改补充行为边界
每条事项独立决定是否创建提醒；铃铛仅出现在已开启提醒的事项时间前。默认未开提醒的事项不进入闹钟列表；曾创建而后关闭的日历提醒按新规格保留以便重新启用，覆盖更早“关闭后不显示”的旧描述。名称/时间/开关/删除与调度同步。
提前知道了后可在一个聚合胶囊管理本次提醒，提前本次不再提醒与胶囊关闭本次均不删除日历事项或关闭未来重复规则。到点只提供知道了/稍后10分钟。关闭提前提醒不影响正式提醒。独立倒计时支持多条并存、暂停继续、取消；默认沿用上次总时长，上限24小时。
