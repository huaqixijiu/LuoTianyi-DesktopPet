# 阶段 6 QA 隔离与最终清理审计（2026-09-14）

## 审计范围

- QA 入口和命令行参数归属。
- 旧 helper、兼容代码、占位实现和未引用文件的静态扫描。
- Git 当前跟踪的构建产物、临时文件和候选素材边界。
- 本次只读审计，不删除、不移动、不重写 Git 历史。

## 结果

| 项目 | 结果 | 判断 |
|---|---|---|
| QA 源文件 | `src/LuoTianyiPet.App/Diagnostics/Qa/` 下 15 个 QA partial | 已集中在 Diagnostics；继续由 `--qa-*` / `--preview-*` 显式触发 |
| QA 命令行入口 | `App` 解析参数，`MainWindow` 启动 QA，具体场景在 Diagnostics partial | 无需迁移；参数是已有本地验证接口，不能擅自删除 |
| 占位/模拟实现 | 源码、工具、配置扫描未发现 `TODO`、`FIXME`、`NotImplemented`、placeholder 或模拟数据命中 | 没有可按本项删除的代码 |
| net48 兼容代码 | Core、Animation、Platform.Windows 下共 7 个 `Compatibility` 文件 | 仍被目标框架和项目编译使用，不能删除 |
| 构建/临时目录 | Git 跟踪统计：`bin/`、`obj/`、`artifacts/`、`.local-tools/`、`.log`、`.tmp`、`.cache`、`.dmp` 均为 0 | `.gitignore` 边界有效；本机目录仍按保护规则保留 |
| 候选素材 | Git 当前仍跟踪 1,311 个 `候选素材_官方/` 文件 | 属于历史仓库内容；按约定不自动删除，待用户最终手动处理 |
| 用户已有改动 | `待处理_AI互动视频/README.md` 删除及多个未跟踪 docs/tools 文件保持原样 | 未纳入本轮清理或提交 |

## 不删除清单

- `候选素材_官方/`：用户明确保留到最终手动删除。
- `artifacts/` 及历史 worktree：Module C 已确认存在未合并分支或未提交改动。
- `Compatibility/`：仍有 net48 兼容职责。
- `docs/validation/` 用户新增文件：按用户决定保留。
- 用户已有删除项和未跟踪文件：不代为处理。

## 阶段结论

QA 已与正式业务目录隔离；本次没有发现可以在不影响功能、历史资产或用户改动的前提下直接删除的源码文件。阶段 6 的静态清理审计完成，最终删除候选素材、artifacts、未合并 worktree 或历史 Git 内容不属于本阶段自动操作范围。

## 验证

- `git diff --check`：通过；仅有 Git 的换行转换提示。
- Release net48 编译：0 警告、0 错误。
- 测试：Animation 37、Core 644、Platform.Windows 140，共 821/821 通过。
- 实际启动：`--preview-exit --qa-window` 退出码 0。

