# 开发日志

## 2026-09-15：提醒、日历导航与显示倍率小修

- 日期和目标：将“指定时间”改为用户更容易理解的“闹钟”，修复日历导航及月／周按钮显示，清理快捷面板日历／闹钟入口的焦点蓝底，并把桌宠显示倍率上限提升到 300%，设置页拖动时实时预览。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.Editor.cs`、`PlannerTheme.cs`、`src/LuoTianyiPet.App/Presentation/Controls/PetQuickPanel.xaml`、`src/LuoTianyiPet.App/Presentation/Settings/SettingsWindow.xaml` 与代码后置、`MainWindow.TraySettingsExit.cs`、`src/LuoTianyiPet.Core/State/AppSettings.cs`、`src/LuoTianyiPet.Core/Interaction/AnimationStageSizing.cs`、对应 Core/App QA 与 `config/scene-rules.example.json`；同步更新项目约定文档。
- 完成内容：补齐 `chevron-left`；PlannerChip 绑定按钮自身背景／边框，确保选中的“月／周”文字可见；日历／闹钟快捷入口设为不可获取键盘焦点；倍率统一为 50%～300%；设置滑杆通过预览事件实时调用既有缩放路径，取消对话框时回滚原倍率；舞台尺寸校验与同一倍率常量统一。
- 实际执行的测试及结果：Release net48 全解决方案编译 0 警告/0 错误；Core 648/648、Animation 37/37、Platform.Windows 147/147，共 832/832；`--qa-planner --portable` 112/112；`--qa-quick-actions --portable` 35/35；`--qa-notification-settings --portable` 105/105；已目视检查月视图、周视图、设置页 150% 以及 300% 桌宠快照。
- 未验证内容或风险：用户尚未在自己的显示器上拖动设置滑杆并测试取消／保存；300% 下所有动画和多显示器异 DPI 的长期观感仍需用户实机复核。
- Git 提交：待用户验收。
- 下一步的单一入口：用户启动最新 Debug 构建，检查四张截图对应问题及设置页 300% 实时缩放；确认后提交并推送。

## 2026-09-15：班休指定日期列表收紧并补充状态

- 日期和目标：降低“指定日期”区域上下留白，并利用日期后的空白显示已存在的单独班休状态。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；未触碰其他并行改动。
- 完成内容：标题行改为自动高度；日期列表使用中间可滚动区域；有日期例外时显示“日期 · 休息日／工作日”，仅跟随每周规则的日期保持简洁。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；全量测试 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-planner --portable` 全部通过；已生成并目视检查 100%/125%/150% DPI 快照及单独状态列表快照。
- 未验证内容或风险：需要确认大量日期、长文本状态和小屏窗口下的滚动密度。
- Git 提交：待本轮提交，提交信息为“fix: compact planner rest selection list”。
- 下一步的单一入口：用户重启桌宠，确认指定日期列表的紧凑布局以及单独设置状态文案。

## 2026-09-15：通知页 V3 信息架构重排（条件横幅+芯片来源+条件音量行）

- 日期和目标：用户复核 V2 三卡版后认为“仍是设置列表堆叠”，按 V3 确认方向重排通知页信息架构：一次性动作与死控件条件化出现，而非继续美化。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Settings/SettingsWindow.xaml`（新增 SourceChip 芯片模板；通知页改为条件权限横幅+消息提醒卡（总开关+提醒来源芯片行）+日历与闹钟卡（条件音量行）+页尾规则文字链；闹钟 hint 进卡头 ToolTip；删除总开关说明行与常驻权限卡）、`SettingsWindow.xaml.cs`（UpdateNotificationAccessDisplay 改横幅显隐）、`src/LuoTianyiPet.App/Diagnostics/Qa/SettingsWindow.NotificationQa.cs`（横幅条件显隐、音量条件行、规则内联展开断言）。
- 实现结果：稳态（已授权+规则折叠）页面无权限 UI 且不出滚动条；未授权/不可用时顶部 accent 横幅+授权按钮，已拒绝/需安装版横幅无按钮；提醒音乐关闭时音量+试听行整体折叠；QQ/微信改可切换芯片但设置键、折叠语义与子状态保留不变；规则两段由页尾文字链原位展开。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；全量测试 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-notification-settings --portable` 104 项 PASS、0 FAIL；`--qa-quick-actions --portable` 32 项 PASS；目视 Unspecified/Allowed/rules-expanded/notification-150 快照确认横幅显隐、芯片行、条件音量行与规则内联展开均符合设计，无裁切或错位。
- 未验证内容或风险：100% DPI 实机芯片与横幅手感待用户复核；工作区 Planner 相关修改为用户并行改动，未纳入本提交。
- Git 提交：本轮代码、QA 与文档同一提交，提交信息“refactor(settings): 通知页 V3 信息架构重排（条件显示与芯片化）”。
- 下一步的单一入口：推送前按流程提供 L3 深度审查选项，确认后推送 origin/main。

## 2026-09-15：月视图显示生效班休与来源提示

- 日期和目标：解决清除日期例外后无徽标导致用户误判当天工作日／休息日的问题，并降低单独设置与每周规则的来源歧义。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、Planner QA；同步更新需求、技术架构、决策记录和项目总控。保留工作区已有的日期拖动修改。
- 完成内容：月视图始终显示实际生效的班／休；跟随每周规则使用浅色徽标，单独设置使用深色徽标；详情标题显示状态来源；班休面板增加颜色说明；“清除指定设置”改为“恢复每周安排”。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；Animation 37/37、Core 647/647、Platform.Windows 147/147，共 831/831；`--qa-planner --portable` 108 项 PASS；100/125/150% DPI 快照已生成并目视检查。
- 未验证内容或风险：需要确认浅色／深色徽标在小屏与 125%/150% DPI 下的密度和对比度。
- Git 提交：`acd7ded`，已推送到已配置的 `origin/main`。
- 下一步的单一入口：用户重启桌宠，确认月视图浅色／深色徽标、详情来源提示和“恢复每周安排”文案。

## 2026-09-15：回退基线重新实现事项与班休日期拖动

- 日期和目标：基于用户回退后的 `80c7f9c` 重新实现两处日期选择拖动，避免沿用已回退且验收不稳定的旧实现。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/DateSelectionWindow.cs`、`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；同步更新项目总控、需求、技术架构和决策记录。
- 实现结果：普通单击保留按钮 Click；Grid 统一接收真实拖动移动/松开；重新进入日期格时逐格切换，支持左→右选择、保持按住右→左取消和混合状态切换；不改数据格式及班休规则。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；全量测试通过 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-planner --portable` 通过 104/104；已生成班休混合状态快照待目视检查。
- 未验证内容或风险：尚未由用户重启桌宠进行真实鼠标验收；本轮未提交，等待确认单击、往返拖动和跨边界手感。
- Git 提交：未提交。
- 下一步的单一入口：用户启动当前源码构建版，验收两处普通单击、左→右选择和不松手右→左取消。

## 2026-09-15：日历恢复至 250b990 基线

- 日期和目标：按用户确认，将日历／班休界面恢复到截图对应的 `250b990` 版本。
- 修改模块：恢复 `src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`DateSelectionWindow.cs` 与 Planner QA 到 `250b990`；未改设置窗、音乐岛和班休底层模型。
- 完成内容：保留每周规则与日期例外分离、固定班休操作区、月历直接选区和原有拖选；撤回后续单日取消入口、混合反选、每周按钮反馈及相关新文案。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；Animation 37/37、Core 647/647、Platform.Windows 147/147，共 831/831；`--qa-planner --portable` 98 项 PASS；恢复后的班休快照已目视检查。
- 未验证内容或风险：尚未完成用户重启桌宠后的人工验收；当前恢复的是 `250b990` 的日历交互语义，不包含其之后的混合拖动反选和单日取消入口。
- Git 提交：待本轮验证后提交并推送到已配置的 `origin/main`。
- 下一步的单一入口：用户重启桌宠，确认日历外观、班休按钮文案和原有拖选行为。

## 2026-09-15：设置窗 600×664 卡片式重排完成（阶段 2 确认项）

- 日期和目标：按用户确认项实施设置窗四页重排：600×664 窗口与更低密度令牌；常规页合并行为卡；音乐页岛开关绑现有设置键；通知页总开关折叠详细提醒；关于页隐私与安全合并卡。不改设置键、Core 音乐选择逻辑、动画状态映射、原始素材和安全约束，不新增依赖。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Settings/SettingsWindow.xaml`（窗口尺寸与密度令牌、四页卡片重排、DetailedReminderSection 折叠、MusicIslandsCheckBox、三张固定卡、规则两段、隐私与安全卡）、`SettingsWindow.xaml.cs`（岛开关加载与整体提交）、`src/LuoTianyiPet.App/Diagnostics/Qa/SettingsWindow.NotificationQa.cs`（折叠/岛开关/四页 DPI 断言）。
- 实现结果：常规页=行为卡 4 行（开机自动启动/始终置顶/双击切换外观/桌面吃包子动画）+显示大小卡；音乐页=音乐行为卡 2 行（显示音乐悬浮岛绑定现有 `MediaPreferences.ShowMusicIslands`，与快捷面板同源）+智能分配卡新文案“根据当前歌曲智能选择；偶尔会遇到隐藏彩蛋”+固定动画仅享受音乐/音乐摇摆/一键唱歌三卡，无打 call 卡与歌手专属提示；通知页=消息提醒总开关关闭时 DataTrigger 折叠详细提醒（Collapsed 保留 QQ/微信子开关状态），规则压缩为两段保留来源、隐私边界与“只影响桌宠、不修改客户端状态”；关于页=品牌卡+隐私与安全合并卡两段。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；全量测试 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-notification-settings --portable` 99 项 PASS、0 FAIL（含总开关折叠/恢复、岛开关模态保存、四页 125%/150% 无横向滚动断言）；`--qa-quick-actions --portable` 32 项 PASS；目视 general/music/notification/about 四页 125%/150% 快照及 Allowed、rules-expanded，无裁切、错位或异常滚动。
- 未验证内容或风险：真实 100% DPI 下手感与观感待用户实机复核；工作区中 Planner 往返拖动修改为用户并行改动，未纳入本提交。
- Git 提交：本轮代码、QA 与文档同一提交，提交信息“refactor(settings): 设置窗 600×664 卡片式重排（阶段 2 确认项）”。
- 下一步的单一入口：推送前按流程提供 L3 深度审查选项，确认后推送 origin/main。

## 2026-09-15：恢复日期按钮普通单击

- 日期和目标：修复往返拖动改造后班休日期按钮无法单击的问题。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`；同步更新项目总控、技术架构、需求和决策记录。
- 实现结果：未发生跨日期移动时不释放鼠标捕获、不抑制 Button Click；只有真正拖动时才释放捕获并阻止松手后的重复单击。事项多日期窗口原有单击放行逻辑保持不变。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；全量测试通过 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-planner --portable` 通过 108/108。
- 未验证内容或风险：当前运行实例仍可能是之前构建的旧进程，提交后必须退出并从桌面快捷方式重启后验收。
- Git 提交：待文档检查后提交。
- 下一步的单一入口：重启桌宠，验收普通单击、左→右选择和不松手右→左取消。

## 2026-09-15：修正拖动为可往返刷选

- 日期和目标：修正上一版“同一日期一次手势只处理一次”与用户需求不符的问题，实现不松手往返拖动时重新经过日期即可再次切换。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/DateSelectionWindow.cs`、`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；同步更新项目总控、需求、技术架构和决策记录。
- 实现结果：移除永久已访问集合；连续采样停留在同一日期格时不重复切换，重新进入时再次按当前状态选中/取消；检测拖动方向反转并重新处理端点，支持左→右全选、保持按住右→左全取消。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；全量测试通过 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-planner --portable` 通过 108/108，新增两处反向拖动断言。
- 未验证内容或风险：当前运行中的桌宠仍是旧进程，提交后必须退出并通过桌面快捷方式重启后再进行真实鼠标验收。
- Git 提交：待文档检查后提交。
- 下一步的单一入口：提交后重启桌宠，验收两处日期选择的往返刷选。

## 2026-09-15：修正日期拖动的真实 WPF 事件路由

- 日期和目标：处理用户实机反馈的“路径 QA 通过但实际拖动没有连续效果”，覆盖事项多日期选择窗和班休月历两处日期拖动。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/DateSelectionWindow.cs`、`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`；同步更新项目总控、技术架构和决策记录。
- 实现结果：日期按钮继续负责左键按下；移动与左键松开改由所属日历 Grid 通过 `AddHandler` 且启用 `handledEventsToo` 接收，确保鼠标捕获后跨出原按钮仍能连续采样和结束手势。混合选中/取消规则不变。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；全量测试通过 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-planner --portable` 通过 106/106。
- 未验证内容或风险：当前用户正在运行的实例仍是提交 `803553c` 的旧进程；本次修复提交后必须关闭并通过桌面快捷方式重新启动，才能实际验收新的事件路由。
- Git 提交：待本轮文档检查后提交。
- 下一步的单一入口：提交后重启桌宠，分别验收事项多日期和班休月历的混合拖动。

## 2026-09-15：班休月历支持单手势混合选中与取消

- 日期和目标：修正班休调整中一次拖动只能全选或全取消的问题，使月历与事项多日期选择使用一致的逐日期切换体验。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；同步更新项目总控、需求、技术架构和决策记录。
- 实现结果：移除班休拖动的整段目标状态，新增 `_calendarDragVisited`；拖动经过未选日期时选中，经过已选日期时取消，同一日期在一次手势内只处理一次；班休存储和批量操作语义不变。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；全量测试通过 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-planner --portable` 通过 106/106；生成并检查班休混合选区快照 `src/LuoTianyiPet.App/bin/Release/net48/PlannerQa/639250271152884413/02-workdays-mixed.png`。
- 未验证内容或风险：真实鼠标长按跨边界、跨月拖动仍需用户实机体验；同一手势重复经过同一日期不会再次切换，这是为避免手抖反复变化的明确规则。
- Git 提交：本轮验证通过后提交，提交信息为“修复日历多日期拖动选择”。
- 下一步的单一入口：用户实际验收班休月历与事项多日期选择的混合拖动手感。

## 2026-09-15：修正多日期拖动的混合选中与取消

- 日期和目标：修正用户反馈的交互问题，使一次按住左键拖动可以在经过未选日期时选中、经过已选日期时取消，不再要求从已选日期开始才能反选。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/DateSelectionWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；同步更新项目总控、需求、技术架构和决策记录。
- 实现结果：移除整段拖动的目标状态，新增本次手势的已访问日期集合；每个日期只按进入时的当前状态切换一次，普通点击仍由按钮点击逻辑处理。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；`dotnet test LuoTianyiPet.sln --configuration Release --framework net48 --no-build --no-restore` 通过 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-planner --portable` 通过 105/105；生成并检查混合选区快照 `src/LuoTianyiPet.App/bin/Release/net48/PlannerQa/639250268110012097/14-date-picker-mixed.png`。
- 未验证内容或风险：真实鼠标长按、跨日期格边界和跨月拖动仍需用户实机体验；同一手势重复经过同一日期不会再次切换，这是为避免抖动造成结果不稳定的明确规则。
- Git 提交：未提交，保留工作区已有修改。
- 下一步的单一入口：完成本轮编译与 QA，并请用户验收一次拖动混合选择/取消。

## 2026-09-15：事项多日期入口统一与拖动选择

- 日期和目标：解决新建事项勾选多日期后同时出现上方日期和下方“已选日期”的重复入口，并按用户确认支持按住左键拖动连续多选／取消多选。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.Editor.cs`、`src/LuoTianyiPet.App/Presentation/Planner/DateSelectionWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；同步更新需求、技术架构、决策记录和项目总控。
- 完成内容：多日期模式复用上方日期区域作为摘要和选择入口，移除重复日期行；切换模式时用当前日期初始化或用最早已选日期恢复单日期；日期选择窗普通点击仍按天切换，拖动从未选日期开始连续选中，从已选日期开始连续取消；保留副本集合和确认回写，不改 `ReminderRepeat.Dates`、`ReminderItem.Dates` 或现有存档格式。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；`dotnet test LuoTianyiPet.sln --configuration Release --framework net48 --no-build --no-restore` 通过 831/831（Core 647、Animation 37、Platform.Windows 147）；`--qa-planner --portable` 105 项 PASS；生成并目视检查单日期、多日期入口和日期选择窗在 100%/125%/150% 的快照；现有桌面启动脚本 `-BuildOnly` 在 QA 进程释放后通过，0 警告/0 错误。
- 未验证内容或风险：尚未进行真实用户鼠标长按跨月拖动的手感验收；当前拖动按路径采样，起始日期状态决定整段为选中或取消，不提供同一段路径内混合选中。
- Git 提交：未提交，保留工作区已有设置窗和文档修改。
- 下一步的单一入口：用户实际体验多日期入口、跨月切换和长按拖动；若体验通过，再统一整理提交边界。

## 2026-09-15：设置窗并入浅玻璃令牌家族（阶段 1）

- 目标：设置窗是全家唯一未进浅玻璃令牌家族的窗口，且内部常规/通知两套语言分裂；本轮统一视觉，不改任何行为与设置键。
- 修改模块：SettingsWindow.xaml 合并 FloatGlassResources；本地 Primary/PrimaryDark/Text/Muted/Line 键名保留但色值对齐家族色相（#66CCFF/#176A90/#123E56/#5C7484/#33176A90）；新增 SettingsHover/Checked/CardHover/Tile/SwitchOff 五个本地派生令牌；switch 统一为单模板（通知页删除自成一套的 NotificationSwitch 及 Notification* 局部样式）；新增 SettingsSlider 自绘模板（4px 圆角轨道+ accent 已滑段+14 圆柄+hover 光环）替换 WPF 默认滑块（桌宠缩放与提醒音量共用）；通知页重排为 GroupCard 卡片语言（消息提醒/详细提醒/通知权限/规则 Expander/日历与闹钟五卡）；侧栏四个导航图标由 Segoe MDL2 字体字形换自绘线性 Path（滑杆/双音符/铃铛/信息，16px、描边 1.7）；PrimaryButton 改实底 accent ink + 独立 hover 模板；品牌/预览/关于 tile 统一 SettingsTileBrush。
- 用户可见结果：四个页面同一套卡片/开关/滑块/按钮语言；通知页不再是 Win10 设置式扁平列表；滑块从默认方柄换圆柄 accent 轨道；侧栏图标与岛/面板/Planner 线性图标同源。
- 实际验证：Release net48 编译 0 警告/0 错误；qa-quick-actions 32 项 PASS、0 FAIL；qa-notification-settings 82 项 PASS、0 FAIL；目视 NotificationSettingsQa 的 Allowed 与 rules-expanded 快照确认卡片化通知页、统一 switch、新滑块与线性导航图标。
- 未验证内容：音乐页选择卡与关于页实机观感待用户复核；阶段 2 图标资产（ico 分层重建+托盘独立简化图标，方向已定 A：强调圆角方底+白色音符）未实施。
- Git 提交：未单独提交，随阶段 2 确认项重排同一提交落库（阶段 2 复核通过即代表阶段 1 一并验收）。
- 下一步：用户实机复核设置窗四个页面；确认后提交。

## 2026-09-15：宠物右键快捷面板菜单化重排（方案 R）

- 目标：把右键面板从“列表堆叠”重排为有层级的菜单（入口/开关/会话操作三层分离），修复鼠标打开带整行焦点底与步进器不对齐，并提高复杂桌面下可读性。
- 修改模块：FloatGlassResources 新增 FloatPanelBrush/FloatControlBorderBrush/FloatDangerHoverBrush/FloatDangerPressedBrush；QuickPanelResources 的 PanelSurface 换更实背板、行高 42→40、新增 PanelIconButton/PanelStepButton/PanelDangerButton 三样式；PetQuickPanel.xaml 全量重排（头部品牌点+双图标入口、开关组、步进胶囊、footer）；PetQuickPanel.xaml.cs 去掉打开时 LockPositionCheckBox.Focus()，新增 120ms 透明度+缩放入场（QA 模式跳过以保证快照稳定）；头部入口图标经多轮复核终稿为经典线性造型（日历=圆角方框+header 线+挂耳、闹钟=14 钟面+贴角双铃弧+指针）；日历 Viewbox 21/描边 2.0，闹钟 Viewbox 23/描边 1.75（光学补偿：圆形实际尺寸略大于方形才视觉等大，描边反缩放保持同粗）。
- 用户可见结果：鼠标右键打开不再出现整行蓝色焦点底；日历/闹钟收进头部图标；显示大小为右对齐一体步进器；退出行 hover 淡红底；面板更实更紧凑（实测约 288×372）。
- 实际验证：Release net48 编译 0 警告/0 错误；qa-quick-actions 31 项 PASS、0 FAIL；qa-stable-layout 891 项 PASS、0 FAIL；目视 03-pet-menu 快照确认层级、步进器与焦点修复。
- 未验证内容：入场动效手感（120ms）需用户实机感受；托盘面板未同步更实背板（沿用轻玻璃，待复核意见）。
- Git 提交：d0f6c4b（与音乐岛复核第二轮、09-14 浅玻璃重绘同批落库；三轮改动在同一批文件交织，不拆提交）。
- 下一步：无待办；入场动效手感与托盘背板意见待用户新反馈。

## 2026-09-15：音乐悬浮岛复核第二轮（圆角裁剪、间距、图标粗细、第三行时间文字）

- 目标：按用户运行复核反馈修正封面方角、封面贴左与文本过挤、图标偏细，并按新决定加入第三行时间文字（自动检测 SMTC 时间轴）。
- 修改模块：MainWindow.xaml 封面内层 Grid 增加 RectangleGeometry 圆角裁剪（Border.ClipToBounds 只裁矩形不裁圆角）；封面列 56→60 且居中、TrackInfoBubble 左距 9→12；prev/next/volume Viewbox 19→20、StrokeThickness 1.8→2.3；播放 glyph 22→24；新增第三行 TrackTimelineText。MainWindow.xaml.cs 的 UpdateTrackTimeline 将时间文字与进度环/ToolTip 共用同一 IsValid 门同显同隐。QuickActionsQa 新增时间文字两条断言，并修复既有 flaky：无输入 QA 环境下 crystal-yawn 反应持有 BlocksDisplayModeToggle 使样式切换检查失败，改为尝试前 CompleteReaction 结束活跃反应。
- 用户可见结果：封面圆角与白内描边对齐、左侧留白与文本间距更接近效果图；图标更粗；SMTC 提供时间轴时歌手下方第三行显示“0:42 / 3:56”式文字且播放键出现进度环，未提供时两者与 ToolTip 时长一起隐藏。音量弹层收宽 58→54、垂直间隙 -7→-4，并在底部中央加 10×5 同令牌连接尾（阴影移至外层 Grid 保证整体投影连续），水平居中锚点保持音量键，消除漂浮感。
- 实际验证：Release net48 编译 0 警告/0 错误；qa-quick-actions 32 项 PASS、0 FAIL（含三档 DPI 快照与音量弹层开启断言）；qa-stable-layout 891 项 PASS、0 FAIL；目视 02-visible 快照确认三行文字、圆角封面、加粗图标与进度环。
- 未验证内容：真实其他音乐软件（非网易云）的 SMTC 时间轴现场表现；均衡器徽章/光环真实播放态视觉仍待有音频峰值环境复核。
- Git 提交：d0f6c4b（同批落库，说明见方案 R 条目）。
- 下一步：无待办；真实其他音乐软件的 SMTC 时间轴现场表现待有环境时复核。

## 2026-09-14：音乐悬浮岛与快捷面板浅玻璃视觉重绘

- 目标：按用户锁定的最终规格（效果图 + N1–N5）重绘合并音乐岛、音量弹层、宠物快捷面板与托盘面板的视觉，不改变尺寸契约、显隐与命中行为。
- 修改模块：新增 Presentation/Controls/FloatGlassResources.xaml 共享浅玻璃令牌；MainWindow.xaml 媒体按钮模板（hover 圆面 120ms 淡入、pressed 0.96、图标键透明命中扩到 40）、控制键列宽 36/44/36/36、封面宿主柔底 + 1px 白内描边 + 均衡器徽章/矢量占位、线性描边图标、播放态光环、顶部 1px 内高光与冷色阴影、音量弹层同令牌；MainWindow.xaml.cs 播放态联动 TrackEqBadge/PlayStateHalo、进度环几何改 center 22 / radius 19.5、环已完成色 #075277；QuickPanelResources 与 PetQuickPanel（288 宽、行高 42、switch 开关、线性图标、退出红）；TrayQuickPanel 同令牌；QuickActionsQa 快照支持 100/125/150 三档 DPI。
- 用户可见结果：岛、音量弹层与两个面板统一浅玻璃语言；播放中封面右下角均衡器徽章与主键光环；无封面时三柱均衡器矢量占位；本轮按 Timeline=False 不显示进度环与时间文字，保留 Timeline=True 扩展。
- 用户复核修正：封面图片原依赖 Border.ClipToBounds，该属性只裁矩形不裁圆角，导致封面方角溢出圆角框；改为内层 Grid 的 RectangleGeometry（52×52、RadiusX/Y 15）圆角裁剪。重建后 qa-quick-actions 30 项 PASS、0 FAIL，快照确认封面圆角与白色内描边对齐。
- 实际验证：Release net48 编译 0 警告/0 错误；Animation 37、Core 646、Platform.Windows 146 共 823/823 自动化测试通过；qa-quick-actions 30 项 PASS、0 FAIL（含 02/03/04 快照的 -125/-150 变体）；qa-stable-layout 891 项 PASS、0 FAIL；目视 100%/150% 快照：线性图标与进度环清晰、宠物面板实测 288×470（≤500）、托盘面板令牌生效且鼠标打开不预选。
- 未验证内容：均衡器徽章与播放光环的真实播放态视觉（QA 环境无音频峰值，显隐与播放/暂停 glyph 同一路径联动）；音量弹层空间不足时沿用 WPF Popup 既有自动翻转回退，未另写切右逻辑。
- Git 提交：d0f6c4b（同批落库，说明见方案 R 条目）。
- 下一步：无待办；均衡器徽章/光环真实播放态视觉待有音频峰值环境复核。

## 2026-09-14：合并音乐悬浮岛并接入歌曲封面

- 目标：按用户最新确认，把曲目气泡与媒体控制条合并为一个简约音乐悬浮岛，并在切歌控制区保留音量按键。
- 修改模块：Core MediaTrackSnapshot 增加可选内存 ArtworkBytes；Platform.Windows 从 Windows 系统媒体会话读取封面并按曲目身份缓存，读取上限 1 MiB；App 统一悬浮岛布局、封面解码/占位回退、整体缩放和音乐 QA。
- 用户可见结果：左侧封面、中央歌名/歌手、右侧上一首/播放暂停/下一首/音量；原快捷键、音量弹层、悬停淡出、音乐开关和无封面回退行为保留。
- 实际验证：Release net48 编译 0 警告/0 错误；Animation 37、Core 646、Platform.Windows 140，共 823/823 自动化测试通过；qa-quick-actions portable 29 项 PASS（含封面内存解码）；qa-stable-layout portable 891 项 PASS、0 FAIL；已目视检查统一岛截图；qa-track-info-live portable 启动并完成实时会话预览，未发送媒体命令。
- 未验证内容：正式发布签名包中的封面读取；不同网易云版本可能不提供封面时按占位符降级。实时网易云会话读取已在实现前确认可返回 108×108 封面字节，最终候选包仍需再做一次手工预览。
- Git 提交：d3d3e61（feat: merge music island with artwork）。
- 下一步：提交本轮代码和独立文档变更，并保留用户原有未提交文档修改不被覆盖。

## 2026-09-14：完成全部动画亮边／暗边对称审计

- 目标：回应用户对“白色颗粒、黑色颗粒以及所有边缘颗粒”的完整复核要求。
- 修改模块：`tools/animation_tools/audit_animation_edges.py`；新增 `tools/animation_tools/test_animation_edges_audit.py`。本轮未修改原始素材、运行时图集、动画 ID、帧时序或存档格式。
- 审计范围：当前 manifest 的 70 套运行时动画、3389 个逻辑帧、3387 个实际解码帧；每帧检查深色背景亮边、浅色背景暗边、两类半透明边缘及完全透明像素隐藏 RGB，并生成 8 张亮色背景和 8 张深色背景接触页。
- 实际结论：已修复的 `alarm-tenth-birthday-coming` 与 `startup-afternoon-hurry` 是本轮确认的白色误扣背景；其余高分候选经最坏帧和接触页复核，均为连续黑色线稿、白色贴纸描边、文字、衣物/高光或半透明设计边缘，未发现新的明确黑色颗粒，不做无证据的全局处理。
- 验证：动画边缘 Python 回归 9/9；全量对称审计完成并生成 `report.json`、亮边/暗边最坏帧和接触页；此前 net48 Release 编译 0 警告/0 错误、823/823 .NET 测试、420/420 WPF 动画边缘 QA 仍为通过状态。
- 未验证内容：用户个人显卡、显示器和非标准系统缩放组合；程序化逐帧审计已覆盖全部当前清单，人工检查覆盖每套动画的亮边/暗边最坏帧接触页。
- Git 提交：`6c3458d`（`test: audit animation edges symmetrically`）。
- 下一步：用户重新启动桌宠复核已修复的两个动画；后续新增动画必须先运行亮边／暗边对称审计，再决定是否增加定点清理规则。

## 2026-09-14：根据实际截图完成第二轮动画白色颗粒修正

- 目标：回应用户复核，修复上一轮仍残留在头发、裙摆和鞋边外沿的白色颗粒。
- 根因：`dark-contour-white-matte-wide` 扩大了扫描范围，但仍把完全不透明纯白像素当作需要保护的白色描边，导致真正的白底颗粒没有被去除。
- 修改模块：`tools/animation_tools/animation_edge_cleanup.py`、`tools/animation_tools/test_animation_edge_cleanup.py`、`tools/animation_tools/validate_animation_edges.py`；重新生成闹钟和下午问候运行时图集及清单哈希。原始素材未修改。
- 完成内容：仅对已确认的深色轮廓白底 profile 放开纯白不透明边缘的白底重合成；新增纯白颗粒回归测试；同步校验器对白底 profile 的允许变化。
- 验证：Python 边缘回归 7/7；选中素材全帧清单不变量校验通过；Release net48 编译 0 警告/0 错误；Animation 37、Core 646、Platform.Windows 140，共 823/823 测试通过；`--qa-animation-edges --portable` 为 420/420 PASS。
- 实际筛查：`alarm-tenth-birthday-coming` 最坏帧值 9→1，`startup-afternoon-hurry` 21→3；200% 深色/浅色截图目视复核通过。
- 未验证内容：用户个人显卡驱动和非标准系统缩放组合；应用内 100%/150%/200% 已验证。
- Git 提交：`3455866`（`fix: remove opaque animation matte pixels`）。
- 下一步：用户重新启动桌宠，重点复核“来啦”动画的头发、裙摆和鞋边；如仍有颗粒，按截图帧号继续定位。

## 2026-09-14：全量复核并修复动画透明边缘残留白边

- 目标：检查所有运行时动画的透明边缘，修复用户反馈的“来啦”动画裙摆白色点状毛刺，同时保留原画中的白色文字、衣服、描边和特效。
- 修改模块：`tools/animation_tools/animation_edge_cleanup.py`、`tools/animation_tools/validate_animation_edges.py`、`tools/animation_tools/test_animation_edge_cleanup.py`、`config/animation-sources.json`；重新生成 `assets/animations/runtime/alarm-tenth-birthday-coming.atlas.png`、`startup-afternoon-hurry.atlas.png`、`newyear-one-click-singing.atlas.png` 及对应清单哈希；原始素材未修改。
- 完成内容：新增宽范围深色轮廓去白底 profile；清除完全透明像素的隐藏 RGB；为二像素边缘和透明 RGB 清理增加 2 个回归测试；校验器只对仍可见像素执行重合成误差检查。
- 实际审查：全量 70 套动画、3389 个逻辑帧、3387 个实际解码帧；8 张接触检查页；未把合法白色文字／服装／描边误判为毛刺。
- 验证：Python 边缘回归 6/6；全帧清单不变量校验通过；Release net48 编译 0 警告/0 错误；Animation 37、Core 646、Platform.Windows 140，共 823/823 测试通过；`--qa-animation-edges --portable` 420/420 PASS。
- 未验证内容：用户个人显示器、显卡缩放和实际长时间运行中的全部 DPI 组合；当前已覆盖应用内 100%/150%/200% 与深色/浅色背景。
- Git 提交：`dc832db`（`fix: clean transparent animation edges`）。
- 下一步：用户在实际桌宠窗口复核常用动画；若仍有明确残留，提供动画 ID 和帧号后定点处理。

## 2026-09-14：Planner 五表面 UI 重绘与三档 DPI 验证

- 目标：按确认门 G1–G11 重绘 Planner 五个表面（月视图、班休面板、周视图、闹钟页、行程/倒计时编辑器），不改动提醒存档格式与既有 QA 钩子。
- 修改模块：`Presentation/Planner/PlannerTheme.cs`（令牌与共享控件样式）、`PlannerWindow.cs`（月/周/班休面板与职责切换）、`PlannerWindow.Editor.cs`（640 DIP 编辑器、多行备注、长度计数）、`PlannerWindow.Groups.cs`、`PlannerNumber.cs`、`Core/Planner/CalendarLabels.cs`（节气/节日只读拆分）、`Diagnostics/Qa/MainWindow.PlannerQa.cs`（A09 多行断言与 100/125/150% 快照）。
- 实现边界：月视图右侧在"设置班休"时切换为"调整班休"面板，周视图不显示班休入口并在切入时自动退出班休调整；节气蓝/节日红仅显示区分；编辑器备注改多行但 Notes 仍为字符串；RestWeekdays、RestOverrides、ReminderSchedule 语义与全部 QA Name 钩子、按钮 Content 字面量保持不变。
- 编译冲突修复：`PlannerTheme.cs` 的 `Geometry` 方法重命名为 `IconGeometry`（消除 CS0119）；为 `Brush`/`FontFamily`/`Cursors` 增加文件级别名（消除 `UseWindowsForms` 引入的 CS0104），解除对并行构建的阻塞。
- 验证状态：Release net48 编译 0 警告/0 错误；自动化测试 823/823（Animation 37、Platform.Windows 140、Core 646）；`--qa-planner` 76 项全部 PASS；五表面在 100%/125%/150% 三档 DPI 渲染快照经目视核验无裁切、模糊或错位（倒计时冒号与数字卡中心对齐保持）。
- 未验证内容：设置窗口、快捷面板、耳旁提醒列表、来源气泡按 G9 本轮未改；真实高 DPI 显示器下的系统级缩放表现未实机验证（快照为 RenderTargetBitmap 矢量渲染）。
- 下一步：与工作区中并行的"反应取消"改动协调提交边界；随后按 G9 排期其余表面。

## 2026-09-14：公开文档路径与工具痕迹清理完成

- 清理范围：公开文档中的作者机绝对路径、临时工作区路径、旧 GitHub 地址、工具专用称呼，以及两个带工具标记的提示词文件名；所有引用已同步到相对路径和中性名称。
- 修改文档：`AGENTS.md`（本地约定）、`docs/项目总控.md`、`docs/开发日志.md`、`docs/精细模型动画任务开场提示词.md`、`docs/authoring/精细模型动画任务开场提示词.md`、`docs/完整全身互动视频提示词.md`、`docs/authoring/完整全身互动视频提示词.md`、`docs/目录与归档指南.md`、审计/调研/验证记录等。
- 保留边界：未改代码、资源路径、运行时动画 ID、存档格式、原素材目录、Git 历史和实际构建命令含义；`ai-bun-*` 等既有兼容 ID 以及原素材真实目录名继续保留。
- 验证：全仓库文本检索未再发现作者机绝对路径、旧仓库地址或已清理的工具名称；`git diff --check` 通过。未运行编译和测试，因为本轮仅修改文档。
- 下一步：进入 UI 设计建议审查和后续界面美化准备。

## 2026-09-14：统一正式版本源与生产包身份

- 用户确认正式版本为 `0.1.0.94`，生产身份名为 `LuoTianyiPet`，正式安装包使用受信任生产代码签名证书。
- 新增 `config/version.props`，`Directory.Build.props`、便携版、完整 MSIX、旁加载和外部位置身份包构建脚本默认读取同一版本源；历史 `.56`、`.93` 产物保留，不删除或改写。
- 默认版本探针确认项目程序集版本、版本前缀和版本后缀分别为 `0.1.0.94`、`0.1.0.94`、空后缀。正式生产构建仍待用户提供或指定真实生产证书路径和密码文件。
- 下一步：生成并检查 `0.1.0.94` 正式候选包，完成清单、哈希、隐私边界和安装/卸载回归。

## 2026-09-14：完成任意目录安装器原型与隔离实机验证

- 目标：在不改业务代码、不新增 NuGet 依赖的前提下，同时满足用户选择安装目录、保留包身份通知能力和卸载前询问数据策略。
- 修改模块：新增 `packaging/external-location/` 外部位置身份清单、安装/卸载脚本和说明；新增 `tools/packaging/build_external_location_bundle.ps1`；更新 `packaging/README.md`、项目总控和本日志。
- 实现边界：使用现有 `winapp` CLI 生成 sparse identity package；把身份元数据嵌入 net48 EXE；用户安装目录通过文件夹选择器决定；卸载前选择保留或删除 `%LOCALAPPDATA%\LuoTianyiPet`，并清理包注册、HKCU 自启值、由安装器创建的桌面快捷方式和安装目录。
- 实际验证：使用显式验证版本 `0.0.0.1` 构建成功；身份包仅含清单/区块图/签名文件；清单含 `uap10:AllowExternalContent`、`uap3:userNotificationListener`、`runFullTrust`；payload 未包含原始素材或私钥；EXE 含 sparse 身份元数据。使用隔离身份 `LuoTianyiPet.ExternalUacTest`，在未受信任证书状态下实际触发 UAC 并将开发证书加入 `LocalMachine\TrustedPeople`，随后完成 `Add-AppxPackage -ExternalLocation` 注册。程序文件确实位于用户选择目录，`0.0.0.2` 到 `0.0.0.3` 覆盖升级成功，覆盖已有旧参数的桌面快捷方式在 `0.0.0.4` 后参数为空。卸载保留数据路径移除包注册、安装目录、快捷方式和本产品自启动值；用户数据保留。删除用户数据路径已在隔离 LOCALAPPDATA 沙箱中通过。构建脚本结束时会恢复仓库默认 `win-x86` 开发资产，随后解决方案 Release 构建仍通过。Windows PowerShell 5.1 脚本解析、`git diff --check` 通过。
- 实际补充：使用受信任测试证书和隔离身份 `LuoTianyiPet.NotificationTest` 安装通知测试包；用户确认页面显示“已授权”，应用日志记录 `notification.monitor_status Allowed`，说明身份包通知权限和监听启动均生效。未发送真实 QQ/微信消息，因此没有把消息正文解析记为通过。通知测试包卸载后包注册、安装目录和快捷方式均清理，用户数据保留。
- 未验证内容：真实 QQ/微信消息读取仍未验证。验证版本不属于正式发布版本；生产身份名、生产证书、开发证书卸载策略和最终统一版本号尚未定稿。PowerShell 7 的 Appx 模块不支持当前平台，后续继续使用 Windows PowerShell 5.1 验证。
- 下一步：确定正式发布决策，建立统一版本源并将外部位置入口纳入正式发布清单；真实消息读取作为发布候选包的最终验收项。

## 2026-09-14：统一三种模式与追包动画名称

- 目标：按用户确认统一项目中的三种完整全身模式名称和两套追包动画名称。
- 修改模块：README、`config/scene-rules.example.json`、身体命中图名称、动画挑选器校验脚本，以及需求、架构、动画映射、决策、开发和作者说明文档。
- 名称映射：华裳长发版 → 模式一（心率共鸣完整Q版）；晶蓝礼服版 → 模式二（心率共鸣简约Q版）；经典猫耳版 → 模式三（心率共鸣表情包版）；甜笑寻包版 → 心率简约追包版；元气追包版 → 心率表情追包版。
- 兼容性：内部稳定 ID（包括 `full-body-*`、`ai-bun-*`）、资源文件名、存档格式、动画内容和交互逻辑不变，仅更新可见名称和说明文本。
- 验证状态：JSON 解析通过；动画挑选器校验通过（101 项/54 项正式用途）；Release net48 编译通过（0 警告/0 错误）；Core、Animation、Platform.Windows 测试共 821/821 通过；`--preview-exit --qa-window` 启动退出码为 0。
- 下一步：完成验证后提交并推送；随后进入可选模块 F 素材裁剪或后续 UI/功能迭代。

## 2026-09-14：GitHub 远端创建与首次推送完成

- 目标：将干净仓库发布到用户创建的 GitHub 远端。
- 实际执行：配置 `origin` 为 `https://github.com/huaqixijiu/LuoTianyi-DesktopPet.git`，推送 `main` 和 Git LFS 对象。
- 结果：远端 `main` 创建成功，157 个 LFS 对象上传完成；本地分支已跟踪 `origin/main`。
- 未执行：未修改远端仓库设置，未自动配置协作者、Actions 权限或其他 GitHub 项目设置。
- 下一步：可选模块 F 素材裁剪，或进入后续产品/UI 迭代。

## 2026-09-14：模块 E 干净仓库本地重建完成

- 目标：在保留旧仓库回滚能力的前提下建立干净 Git 历史。
- 实际执行：将旧 `.git` 重命名为 `.git_backup_20260914/`；初始化新 `main` 仓库；加入 517 个非忽略项目文件；排除原素材、候选素材、artifacts、本地工具、构建产物和旧 Git 备份；创建唯一初始提交。
- 结果：提交 `98016a6`，提交主题为 `init: clean repository baseline`；新仓库当前只有 1 个 commit；未配置远端，未 push，旧 `.git` 未删除。
- 验证：暂存区检查无禁止路径；新仓库有 161 个 LFS 文件；运行时二进制按 `.gitattributes` 使用 LFS；工作树提交后保持干净；重建后编译 0 警告/0 错误、821/821 测试通过、实际启动退出码 0。
- 未处理事项：GitHub 新仓库创建、远端配置和 push 由用户手动执行；模块 F 素材裁剪仍未开始。
- 下一步：用户确认公开仓库地址后手动配置远端；如不做素材裁剪，则进入发布前最终检查。

## 2026-09-14：最终验收与模块 D 审计完成

- 目标：完成阶段 6 后的最终回归，并审计临时文件、AI 工具元数据和 Git 历史。
- 修改模块：新增 `docs/模块D临时文件与Git历史审计-2026-09-14.md`；更新项目总控。未删除文件，未移动素材，未改写 Git 历史。
- 实际执行：Release net48 编译；三组测试；`--preview-exit --qa-window` 实际启动；Git 追踪路径、历史提交主题、LFS、忽略规则和对象库只读检查。
- 验证结果：编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 通过；启动退出码 0；`git diff --check` 通过。
- 未处理事项：历史 `codex/` 分支名、既有 `ai-bun-*` 运行时资源 ID、Git 对象库临时对象警告，均需后续确认后处理。
- Git：待提交；保留用户已有修改和未跟踪文件。
- 下一步：模块 E，先取得用户明确确认，再决定是否重建干净仓库和整理历史。

## 2026-09-14：模块 E 重建前置审计完成

- 目标：在不改写历史、不删除文件的前提下检查干净仓库重建条件。
- 修改模块：新增 `docs/模块E重建前置审计-2026-09-14.md`；校正文档中模块 B 的过期状态描述；更新项目总控。
- 实际执行：全局检查 `候选素材_官方/` 和 `assets/source/` 引用、忽略规则、LFS 数量、工作树和用户已有修改。
- 结果：代码侧引用检查通过；当前 1,428 个 LFS 文件；新仓库可通过忽略规则排除候选素材和本地产物。
- 阻塞项：工作树包含用户已有修改和未跟踪文件；`artifacts/`、`.local-tools/` 未清理；重建前需要用户确认纳入范围和备份策略。
- Git：未执行 `git init`、未备份或删除 `.git`、未提交、未 push。
- 下一步：用户确认模块 E 的重建范围后，先生成最终纳入清单，再执行备份和重建。

## 2026-09-14：模块 B 步骤 1——项目约定文档建立

- 目标：建立公开项目约定、原素材索引、Git 忽略规则和发布边界。
- 修改模块：新增 `DEVELOPMENT.md`、`docs/项目约定.md`、`docs/原素材索引.md`；更新 README、`.gitignore`、`.gitattributes`、项目总控；将 `AGENTS.md` 改为本地忽略文件；登记精选 `docs/validation/` 文件清单。
- 实际执行：仅修改文档和 Git 规则，没有复制或删除素材，没有删除 worktree，没有改构建脚本。
- 验证：Markdown 和 Git 属性变更执行 `git diff --check`；用户已有删除项和未跟踪文件保持原状。
- 未验证内容：`原素材/` 尚未创建；步骤 2 的素材清单、哈希和路径迁移尚未执行。
- Git：待提交；本步骤未触碰用户已有文件。
- 下一步：步骤 2，核对并复制构建输入到根目录 `原素材/`。

## 2026-09-14：模块 B 步骤 2——原素材副本建立

- 目标：把当前构建输入复制到根目录 `原素材/`，不移动或删除候选素材和旧输入。
- 修改模块：新增被 `.gitignore` 排除的 `原素材/animations/` 和 `原素材/ui/` 内容；将 `.local-tools/work/ai-bun-source-frames/` 复制到 `原素材/animations/bun-source-frames/`；更新 `docs/原素材索引.md`、项目总控和本日志。
- 实际执行：复制 `assets/source/` 3,015 个文件和本地源帧 121 个文件，共 3,136 个文件，目标约 1,498.4 MiB。
- 验证：逐文件比较目标存在性、文件长度和 SHA-256，缺失及不一致均为 0；未修改候选素材、`assets/source/`、`.local-tools/` 或用户已有文件。
- 未验证内容：配置和工具仍读取 `assets/source/`；尚未切换到 `原素材/`，尚未修改打包脚本，尚未运行迁移后的构建。
- Git：仅提交文档变更；`原素材/` 被忽略，不进入 Git。
- 下一步：步骤 3，更新配置、工具路径和打包排除规则，然后进行构建验证。

## 2026-09-14：模块 B 步骤 3——构建路径、文件名和发布边界

- 目标：让构建工具读取根目录 `原素材/`，中性化已确认的文件名，并让发布脚本显式拒绝原素材和本地中间目录。
- 修改模块：更新 `config/animation-sources.json`、`config/scene-rules.example.json`、9 个素材工具、3 个打包脚本和相关技术文档；将 `process_ai_bun_video.py` 改名为 `process_bun_video.py`，将两个元数据文件改为 `bun-eat.meta.json`、`bun-eat-v2.meta.json`；运行时动画 ID 保持不变。
- 预览输出：`prepare_crystal_interactions.py` 改写到 `.local-tools/qa/crystal-interactions/`，不再写入原素材。
- 静态验证：50 条动画源和场景资源路径无缺失；Python 8 个脚本、Node 2 个验证器、PowerShell 3 个打包脚本语法通过；旧 `assets/source`、旧脚本名和旧元数据路径在源码、测试、工具和配置中无残留。
- 回归验证：net48 Release 编译 0 警告/0 错误；Core 644、Animation 37、Platform.Windows 140，共 821/821；动画挑选器 101 项、身体区域编辑器 12 区域通过。
- 未验证内容：完整 MSIX、便携包和旁加载包脚本未运行；旧 `assets/source/` 尚未删除；未处理 worktree 和验证截图归档。
- Git：待提交；用户已有删除项、未跟踪文件、候选素材和 worktree 保持原状。
- 下一步：等待确认后，执行 5 个干净已合并 worktree 的只读复核、`git worktree remove` 和对应分支清理。

## 2026-09-14：归档模块 B 审计与迁移方案

- 目标：将素材来源、发布包边界、历史 worktree、后续新素材归置和公开前文字清理方案整理成可交接文档。
- 修改模块：新增 `docs/模块B审计与迁移方案.md`；同步更新 `docs/项目总控.md` 的当前下一步。
- 实际执行：只读核对 `assets/source/` 2,961 个 PNG 的 SHA-256、打包内容、`.local-tools/`、`docs/validation/` 和 Git worktree 状态。
- 验证结果：未修改代码、配置、素材、发布脚本或用户已有文件；未执行删除、移动和实际迁移。
- 未验证内容：项目约定文档、`原素材/` 目录和路径切换尚未实施，待确认后分阶段执行。
- Git：待提交；用户已有删除项和未跟踪文件保持原状。
- 下一步：等待确认 `docs/模块B审计与迁移方案.md`，然后先建立项目约定文档和素材索引。

## 2026-09-14：模块 A 素材迁移、工具路径更新与运行验证

- 目标：让运行时、动画编译配置和维护中的素材工具脱离 `候选素材_官方/`，保留源文件并不改变成品资源、用户可见行为或清单格式。
- 修改模块：新增 `assets/source/`（3,015 个文件、约 1.43 GiB）；更新 `config/animation-sources.json`、`config/scene-rules.example.json`、9 个素材工具和 README；运行时命中图已在本阶段前一提交迁移到 `assets/body-hit-maps/`。
- 实际执行：逐项复制配置源文件及工具所需源目录；全局扫描确认 `src/`、测试、配置、工具、打包脚本无候选目录直接引用；保留 `assets/manifests/animations.json` 的 61 条历史 `sourcePath`，未修改 manifests。
- 自动化验证：7 个 Python 脚本 `py_compile` 通过；2 个 Node 脚本语法通过；动画挑选器 101 项、身体区域编辑器 12 区域通过；配置源和图集路径无缺失。
- 回归验证：net48 Release 编译 0 警告/0 错误；Core 644、Animation 37、Platform.Windows 140，共 821/821；实际 EXE 启动进程响应正常，日志确认加载 70 个动画、两套命中图、托盘、通知和媒体检测。
- 未验证内容：未运行完整 MSIX/便携打包脚本；未删除候选素材或 `artifacts/`；透明不激活工具窗口仍未取得截图级视觉证据。
- Git：`6da0ce1`（素材迁移）、`43e191e`（迁移目录路径校正）；用户已有删除项和未跟踪文件保持原状。
- 下一步：进入模块 B 中间产物专项审计，只输出现状、构成、清理建议和路径改造方案，等确认后执行。

## 2026-09-14：模块 B 中间产物专项审计

- 目标：查清 AssetCompiler、animation_tools、PNG 帧序列和 `artifacts/` 的实际输入输出，不做清理或路径改造。
- 审计结果：AssetCompiler 只校验清单；图集编译在内存处理；Blender/帧工具向调用方指定目录写帧；`prepare_crystal_interactions.py` 将预览写到 `assets/source`；项目目录（排除 artifacts/bin/obj）有 7,673 个 PNG、约 3.011 GiB；`artifacts/` 有 35,264 个文件、约 35.361 GiB。
- 未修改内容：没有修改 AssetCompiler、animation_tools、`.gitignore`、`artifacts/`、候选素材、运行时资源或用户数据。
- 报告：`docs/validation/intermediate-artifacts-audit-2026-09-14.md`，其中包含建议的 `%TEMP%` 工作目录、预览分离、分层清理、回滚和验证方案。
- 下一步：等待用户确认方案后再实施模块 B。

## 2026-09-14：重构阶段 2 低风险源码目录整理

- 目标：按目标结构归档源码文件，保持 namespace、程序集名、资源输出路径、存档路径、命令行参数和用户可见行为不变。
- 修改模块：Core 按 State/Interaction/Music/Notifications/Planner/FileTreats/Scheduling/Abstractions/Compatibility 分组；Animation 按 Catalog/Timeline/Manifest/Compatibility 分组；Platform.Windows 按 Audio/Notifications/Shell/Storage/Startup/Windowing/Compatibility 分组；App 按 Bootstrap/Presentation/Application/Animation/Diagnostics 分组。`App.xaml` 移入 Bootstrap，并在项目文件显式声明 `Bootstrap\\App.xaml`；嵌入式 `SolarTerms.tsv` 保持原位置。
- 实际执行：分批 `git mv`，每批后执行 `dotnet build LuoTianyiPet.sln -c Release` 与 `dotnet test LuoTianyiPet.sln -c Release --no-build`。
- 结果：最终 Release 编译 0 警告/0 错误；Core 644、Animation 37、Platform.Windows 140，共 821/821 通过。未修改源码内容、namespace、资源目录或存档格式。
- 启动验证：通过 Computer Use 尝试捕获窗口；桌宠进程 `Responding=True`，日志确认动画清单、命中图、托盘、通知、媒体检测完成初始化并安全退出。由于透明不激活工具窗口未暴露可捕获窗口，未取得截图级视觉证据。
- 未验证内容：模块 A 素材引用迁移、artifacts 审计、完整 MSIX/便携打包、安装卸载和人工视觉体验。
- Git：未提交；用户已有删除和未跟踪文件保持原状。
- 下一步：模块 A 全局扫描 `候选素材_官方/` 引用并形成复制迁移映射。

## 2026-09-14：重构阶段 1 文档与项目索引

- 目标：对齐 README、项目总控、技术架构和路线图，写入审计 A/B 结论，并建立当前工程结构索引。
- 修改模块：`README.md`、`docs/技术架构.md`、`docs/开发路线图.md`、`docs/项目总控.md`、`docs/项目结构索引.md`；历史开发日志和历史决策内容未覆盖改写。
- 实际执行：根据当前仓库文件清单整理四个正式项目、测试、工具、资源、打包和受保护边界；检索活动文档中的 net10、双目标、旧发布分支和过期里程碑描述。
- 结果：文档统一说明 .NET SDK 10.0.400 仅作构建工具，项目与发布仅为 net48；当前阶段切换为 Stage 2 低风险源码目录整理；新增项目结构索引。
- 验证：文档交叉检索通过；本阶段无源码改动，因此未重复执行编译和测试。
- 未验证内容：Stage 2 尚未移动源码；完整 MSIX/便携打包、安装卸载和视觉启动仍按前阶段记录未验证。
- Git：未提交；用户已有删除和未跟踪文件保持原状。
- 下一步：先建立 Stage 2 文件移动清单，选择一个低风险源码批次并验证。

## 2026-09-14：移除失效的 Net10SelfContained 打包分支

- 目标：按用户确认，使所有发布脚本与 net48-only 项目目标一致。
- 修改模块：`tools/packaging/build_msix.ps1`、`build_portable_test.ps1`、`build_sideload_bundle.ps1`；同步更新 `packaging/README.md` 和 `docs/技术架构.md`。保留 `-Framework NetFramework48`、默认输出目录、资源布局、签名及安装流程；删除 net10 自包含分支和无效的 `EnableNetFrameworkBuild` 发布参数。
- 实际执行：全局检索 `Net10SelfContained`、`EnableNetFrameworkBuild`、net10 self-contained 文本并检查脚本差异。
- 结果：当前打包脚本只接受 net48 目标，静态引用检查通过；没有删除或修改 artifacts、候选素材、Git worktree、用户数据或应用业务代码。
- 验证：三个 PowerShell 打包脚本语法通过；已移除的 `Net10SelfContained` 参数均被拒绝；隔离临时目录 net48 发布包含 EXE、动画清单、运行时动画、对象和音频资源；解决方案 Release 编译 0 警告/0 错误，net48 测试 821/821 通过。
- 未验证内容：未直接执行完整 MSIX/便携发布、安装、卸载或视觉启动验证，因为脚本会重置受保护的 `artifacts` staging 目录。
- Git：未提交；用户已有删除和未跟踪文件保持原状。
- 下一步：执行 net48 便携发布及现有打包安全测试，然后进入文档与目录结构迁移规划。

## 2026-09-14：重构阶段 0 目标框架与审计 B

- 目标：在环境恢复后完成 net48-only 基线，并静态审计持久化、隐私、外部联动、打包卸载和产物风险。
- 修改模块：`src/` 4 个正式项目、`tests/` 3 个测试项目、资产编译器和 2 个 QQ 诊断项目的目标框架统一为 `net48`；未移动源码，未改资源引用、存档路径、配置格式、快捷键或业务逻辑。
- 实际执行：`dotnet restore LuoTianyiPet.sln`、`dotnet build LuoTianyiPet.sln -c Release`、`dotnet test LuoTianyiPet.sln -c Release --no-build`；静态检查 `LocalAppPaths`、`JsonSettingsStore`、`ReminderStore`、`FileAppLogger`、自启、外部联动、网络 API、打包及清理脚本。
- 结果：net48 Release 编译 0 警告/0 错误；Core 644、Animation 37、Platform.Windows 140，共 821 项测试全部通过。所有项目文件仅剩 `net48` 目标。持久化位置为 `%LOCALAPPDATA%\\LuoTianyiPet`，便携模式为程序旁 `UserData`；提醒仍为 `reminders.json` 与备份；日志为按日事件日志。静态未发现网络客户端、上传或聊天数据库读取。
- 风险与未验证：MSIX/便携构建脚本默认 net48，但仍保留 `Net10SelfContained` 选择分支；该分支在 net48-only 项目上已无目标，移除命令参数会触及既有命令行兼容性，待用户确认。未执行打包、安装、卸载或视觉启动验证。`artifacts/` 约 35,264 文件/35.36 GiB，`候选素材_官方/` 约 4,380 文件/1.75 GiB，均未删除；多个 Git worktree 也未清理。
- Git：未提交；用户已有删除和未跟踪文件保持原状。
- 下一步：确认 `Net10SelfContained` 打包参数的兼容策略后，进入文档与目录结构迁移阶段。

## 2026-09-14：重构阶段 0 环境恢复

- 目标：开始项目结构重构前确认 .NET 10 SDK、.NET Framework 4.8 Developer Pack、MSBuild 和当前工作区状态。
- 修改模块：无业务代码修改；仅安装构建环境并更新本轮状态记录。
- 实际执行：下载并校验微软 .NET SDK 10.0.400（x64）与 .NET Framework 4.8 Developer Pack；执行静默安装；重新运行 `dotnet --list-sdks`、`dotnet --info`、.NET Framework 4.8 参考程序集目录检查和 MSBuild 检查。
- 结果：SDK 10.0.400 已安装并被 `global.json` 选中；.NET Framework 4.8 参考程序集目录存在，包含 260 个文件；dotnet 自带 MSBuild 18.9.6 可用。安装包 Windows Authenticode 签名均为 Valid。
- 未验证内容：现有项目 restore/build/test、net48-only 改造、源码目录移动、审计 B、打包和启动。
- Git：未提交；用户已有删除和未跟踪文件保持原状。
- 下一步：执行现有项目 restore、build、test 基线验证。

## 2026-09-14：统一透明头部应用图标

- 目标：将应用、设置页和任务栏图标从带身体的模型二头像改为清晰的透明头部图标。
- 修改：从 `assets/animations/runtime/user-chibi-crystal-full-body-idle.atlas.png` 裁剪 `[60, 0, 420, 260]`，自动收紧可见 Alpha 后生成 `assets/app/luotianyi-pet.png` 和多尺寸 `assets/app/luotianyi-pet.ico`；设置页继续引用 PNG，`ApplicationIcon` 与托盘继续引用 ICO；MSIX 图标生成器改为使用同一透明头像。
- 验证：PNG、ICO 及 MSIX 50/44/150 图标 Alpha 背景透明；设置页实际 QA 截图已查看，QuickActions 28/28 通过；net10/net48 编译 0 警告、0 错误；.89 安装包重新安装后状态 Ok，正式进程 Responding=True。
- 未验证：不同 Windows 缩放比例下的物理任务栏缓存刷新需用户体验确认；本轮未改变旧参考素材。
- 下一步：`docs/validation/pet-menu-2026-09-13/03-pet-menu.png`，体验设置页和任务栏图标清晰度。

## 2026-09-13：回退到 .89 并移除新版日历改动

- 目标：按用户要求放弃 .90～.93 日历 UI 更新，恢复 .89 作为默认版本。
- 修改：`main` 回到提交 `f17a82f`；移除新版月视图侧栏、周视图品牌装饰、分段控件填充及相关验证目录；保留 .89 的日历、闹钟、倒计时、提醒卡以及桌宠右键「设置／退出桌宠」。
- 验证：主目录 net10/net48 编译 0 警告、0 错误；发布版 QuickActions QA 28/28 通过；.89 MSIX 签名 Valid、安装状态 Ok，正式进程启动后 Responding=True。
- 未验证：未进行真实跨 DPI 多屏拖动和长时间运行体验；工作区保留的旧参考文档与素材未参与 .89 编译。
- Git：`main` 已强制同步远端至 `f17a82f`。
- 下一步：`docs/validation/pet-menu-2026-09-13/03-pet-menu.png`。

## 2026-09-12：音乐关闭闪光、设置误报与提示条布局修复

- 基线：已安装 .63，独立 codex/fix-music-settings-feedback 工作树从 9f20948 开始，避免覆盖主目录其他未提交修改。
- 根因：设置保存按钮触发四份并发写入，旧日志可见 0x80070020/0x80070497；音乐状态更新无条件闪光换图，即便关闭音乐动画前后是同图；反馈与歌名占同一布局槽并互相隐藏。
- 修改：分组应用与保存分离，完整设置一次提交；存储排队原子替换及失败/取消清理；同图连续状态跳过换图；提示移至歌名下方独立行，保持人物锚点、小尺寸换行、边缘自适应及独立显隐。未更换或重压动画资源，未修改外部应用控制方式。
- 实测：双目标 Release 0 警告/0 错误，1366/1366 测试（每目标 Core 516、Animation 37、Windows 130）；新 WPF QA 两目标各 276/276，包含三种模式连续音乐启动/停止、启用时真实换图、50/100/200% 三种布局与长短提示、屏幕上下边界、缩放中提示重排、真实磁盘 32 次重叠保存读回无误报。存储独立测试覆盖新文件/旧文件 64 次保存、文件锁失败、取消后恢复、临时文件清理。原舞台 net48 890/890。已查看实际 WPF 100% 中央、50% 顶边、200% 底边截图。
- 发布纠正：本轮 .64 包及发布 EXE 276/276 已通过，但并行 QQ 任务先安装了它的 .64，安装器同版本跳过，实际 EXE 哈希不一致；没有将本轮 .64 覆盖安装。已合入 bd29a21 的 QQ 统一详情并升 .65 重新验收。首次编译的过时私有字段引用及 Size 名称歧义均已修正后重测。完整安装包在 artifacts/music-settings-feedback-worktree/artifacts/sideload/release/。
- 限制：自动化通过实际应用状态方法驱动，不声称已手工操作网易云；多显示器混合 DPI 与用户实际操作最终体感待确认。
- Git：本节所在提交，推送 codex/fix-music-settings-feedback；主目录仅同步本轮差异，保留其他修改与暂存。
- 下一步：docs/项目总控.md 第 8 节。

### 本轮最终安装验收：0.1.0.65

- 合入 QQ .64 后双目标测试共 1382/1382（每目标 Core 520、Animation 37、Windows 134），两目标音乐设置 WPF 各 276/276、QQ 详情各 35/35。最终 .65 发布 EXE 两项分别 276/276 与 35/35，再安装。
- .65 签名 Valid、包状态 Ok、正式进程响应；安装 EXE、Core DLL、Windows DLL 与本轮 publish 的 SHA-256 均一致，已确认本次确实运行新修复而非同版本跳过。旧 .64 包未用于覆盖 QQ 功能；保留并合入此前通知统一开关和预览计数。
- 主目录按差异同步并通过 .NET 10 编译，保留其其他未提交/暂存修改，包括旧有关键提示优先级处理。完整安装 ZIP 在 artifacts/music-settings-feedback-worktree/artifacts/sideload/release/LuoTianyiPet-Installer-0.1.0.65-win-x64.zip。

本文件按时间追加已经实际完成和验证的工作。计划内容不写成完成。

## 2026-09-12：稳定三模式动画展示槽与音乐悬浮岛

- 目标与根因：修复模式二、模式三及公共音乐动画的岛宽不一致、靠边切换抖动。旧实现把歌曲岛宽度绑定每个素材的 DisplayWidth，先按新窗口尺寸夹紧位置、过渡后再按另一套 Alpha 边界校正，形成二次位移和位置漂移。
- 实现：Core `AnimationStageSizing` 从运行清单最大显示尺寸与用户倍率确定稳定透明槽；当前已提交资产为 244×238 DIP，因此 100% 窗口为 260×364 DIP。原动画仍按各自显示尺寸底部居中，原始及运行时素材都不修改。音乐岛使用独立 220 DIP 参考宽度，100% 歌曲岛为 226 DIP、控制岛为 178×46 DIP。删除动画过渡末尾的重复定位，普通贴边／附件换侧使用稳定槽；身体命中和明确躲藏保留真实 Alpha。工作区物理像素统一换算为 DIP。
- 验证：Release .NET 10 / net48 双目标 0 警告、0 错误；Core 469、Animation 37、Windows 126 各双份，共 1264/1264 通过。4 个缩放档、中央及四角、10 类素材往返两轮，连同全部 69 个清单项、真实播放状态过渡及左右／底部躲藏召回，共 890 项 WPF 断言通过。托盘与音乐开关既有 QA 也通过；没有发送真实音乐控制或操作文件回收。
- 视觉：WPF 直接渲染核对不同人物及听歌姿态的布局。离屏截图会偶发丢失缓存投影层，因此 QA 采用软件渲染、关闭装饰投影并在截图前明确显示附件；正式程序的投影不变。这是窗口内验证，不宣称真实鼠标端到端录制；跨显示器异 DPI 的真实拖动仍待用户体验。
- 集成：在 `1a23a7c` 上保留已安装 0.1.0.55 的托盘偏移／直接退出修复，独立构建 0.1.0.56。本项修改同时保留在用户原项目目录，不纳入其它任务暂存的素材、反馈、模式一文件投放或旋转舞修改。
- 发布：0.1.0.56 已签名并覆盖安装、从 AppsFolder 重启，包状态 Ok，正式 WindowsApps 进程响应正常，EXE 哈希与发布目录完全一致。MSIX SHA-256 为 `e4f9ab5263d58d79d8f343984613b1493e400d7f104cc36cb0ccdc67471362ed`；启动日志无 ERROR，通知授权 Allowed。证据为 `docs/validation/stable-layout-checks-2026-09-12.txt` 与 `stable-layout-music-2026-09-12.png`。
- Git：使用独立分支 `fix/stable-animation-layout-20260912` 提交并推送，避免占用其它任务正在使用的 main 索引。后续汇总功能时合入此分支，不能用尚未包含本项的旧包覆盖 0.1.0.56。
- 下一步单一入口：`docs/项目总控.md` 第 8 节，体验模式二／三在屏幕边缘反复切换音乐和待机。

## 2026-08-30：建立项目接管与上下文恢复机制

### 完成

- 检查仓库分支、提交历史和现有文档；
- 确认仓库尚无解决方案、项目源码和测试；
- 新增根目录 `AGENTS.md`，规定每轮开发的恢复、实现、验证和交接流程；
- 新增项目总控、技术架构、开发路线图、决策记录和开发日志；
- 将 WPF、.NET 10、状态模型、资产管线和安全降级确定为开发基线。

### 验证

- `git status --short --branch`：执行成功，开始工作时工作区干净；
- `git log -5 --oneline`：执行成功，当前最新提交为 `dd0bceb`；
- `dotnet --info`：失败，本机当前找不到 `dotnet` 命令；
- 检查常见安装位置：均不存在 `dotnet.exe`；本机也无法找到 `winget`；
- 文档和 JSON：已人工读取现有内容，后续提交前继续检查链接和 JSON 语法。

### 风险或未验证

- 尚未确认本机是完全未安装 .NET SDK，还是安装目录没有进入 PATH；
- 尚无代码可以编译或运行；
- 当前架构需要在 M0/M1 通过真实透明窗口和动画原型验证。

### 下一步

定位或安装 .NET 10 SDK，完成 M0 工程骨架并运行第一次 `dotnet build` 与 `dotnet test`。

### Git

- 提交消息：`docs: establish persistent project control`。

## 2026-08-30：完成 M0 开发环境与工程骨架

### 完成

- 使用微软 `dotnet-install.ps1` 将 .NET SDK 10.0.400 安装到当前用户目录；
- 将用户级 .NET 目录加入 PATH，并设置 `DOTNET_ROOT` 和 `DOTNET_ROOT_X64`；
- 创建 `LuoTianyiPet.sln`、App、Core、Animation、Platform.Windows、AssetCompiler 和三个测试项目；
- 将 Windows 项目标记为最低 Windows 10 1809，应用清单固定 `asInvoker` 普通权限；
- 新增全局构建属性、`.editorconfig` 和 Windows GitHub Actions CI；
- 实现透明无边框窗口、默认右下角定位、不进入任务栏、拖动、右键置顶和退出；
- 实现单实例守卫、JSON 设置读写、损坏配置保留、隐私最小化日志和未处理异常安全退出；
- 使用清楚标记为 M0 的程序化占位角色验证窗口，真实动画留在 M1 接入；
- 建立首个动画描述模型和资产编译工具入口。

### 验证

- `.NET SDK 10.0.400`、Windows Desktop Runtime 10.0.11：安装并可由 PowerShell 调用；
- `dotnet build LuoTianyiPet.sln --configuration Release`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：6/6 通过；
- `dotnet format whitespace --verify-no-changes`：通过；
- 单实例实测：连续启动两次，第二进程 5 秒内退出，存活进程数为 1；
- Win32 只读窗口探测：主窗口可见，标题为“洛天依桌宠”，逻辑尺寸 196×246，完整位于工作区；
- 窗口捕获视觉复检：透明外框、圆角、阴影和中文正常；首次立即捕获为黑色，等待渲染约 3 秒后复检正常。

### 风险或未验证

- 因窗口设置为 `ShowInTaskbar=false`，Computer Use 没有枚举到该窗口，拖动和右键菜单尚未通过自动化输入回放；事件处理器已编译并可由用户直接操作；
- 目前仍是 M0 占位图形，没有开始解码 GIF、动态 WebP 或官方完整全身素材；
- DPI、多显示器、锁屏、睡眠和全屏行为留在后续里程碑验证。

### 下一步

执行 M1 动画管线原型，先接入“嘿嘿”、一个动态 WebP 和官方完整全身待机，再实现双击模式切换。

### Git

- 计划提交消息：`feat: create runnable WPF project skeleton`。

## 2026-08-30：补齐音量与身体点击反馈规则

### 完成

- 将调高音量固定为原版“共鸣之声”，将调低音量固定为倒放版“共鸣之声”；静音和解除静音暂不播放动画；
- 新增可复现的 GIF 倒放工具，生成倒放运行时素材及包含来源、变换方式和 SHA-256 的元数据；
- 将操作失败和超时统一映射为“呜呜晃动”，并按失败类型配置气泡文字；
- 将完整全身模式的眼睛点击映射为“心软软”，脚部点击改为“糟了晃动”；
- 为未单独定义的普通身体部位建立五项随机动画池并禁止连续重复；因“可爱”已专用于摸头，未重复加入动画池；
- 同步更新需求文档、动画映射、示例场景配置和动画挑选器。

### 验证

- 倒放 GIF 为 10 帧、162×162、每帧 100 毫秒、无限循环，透明通道保留；
- 示例场景配置可被 PowerShell JSON 解析器读取；
- `dotnet build LuoTianyiPet.sln --configuration Release`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：6/6 通过。

### 下一步

继续 M1 最小动画管线，用真实素材替换 M0 占位角色并实现两种待机模式切换。

### Git

- 计划提交消息：`feat: define volume and body interaction feedback`。

## 2026-08-30：完成 M1 真实动画纵向原型

### 完成

- 固化“菜单退出播放裂开晃动”“恢复播放只使用享受音乐”“音乐临时覆盖但不改变用户显示模式”规则；
- 新增可复现的 GIF/WebP/PNG 至透明 PNG 图集编译脚本和 `config/animation-sources.json`；
- 生成 5 项运行时图集及带来源哈希、图集哈希、帧时长、循环、透明边界和显示尺寸的动画清单；
- 实现动画清单校验、图集路径边界检查、毫秒级帧时间线和 WPF 图集裁切播放器；
- 用真实“嘿嘿”替换 M0 占位角色，并接入完整全身待机、音乐摇摆、0.5 倍速享受音乐和裂开素材；
- 实现双击切换迷你/完整全身模式的代码路径、保持用户选择的音乐覆盖状态模型，以及 M1 音乐开始/停止验证菜单；
- 实现菜单退出时播放一次“裂开”并程序晃动，忽略重复退出请求；系统退出路径不等待动画；
- 修复窗口关闭时同步等待异步设置保存造成的 UI 线程死锁，并增加防回归测试；
- 素材清单不可用、目标动画缺失或图集损坏时显示安全占位，不让窗口崩溃。

### 验证

- 图集编译器读取 WebP 的 ANMF 毫秒时长：音乐摇摆为 20×50 ms，完整全身待机为 32×125 ms；
- C# AssetCompiler 校验 5 项运行时动画及 SHA-256 全部通过；
- `dotnet build LuoTianyiPet.sln --configuration Release`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：20/20 通过；
- 真实窗口捕获：迷你模式 196×196、完整全身模式 256×276、音乐模式 206×206，透明区域均正确显示桌面背景；
- 完整全身动画相隔 375 ms 的抽样截图有 4689 个采样像素发生变化，确认播放器持续换帧；
- 退出 QA 路径实际显示“裂开”，窗口保存设置后进程以代码 0 退出；日志同时记录 `window.state_saved` 和 `app.exited`；
- 缺失动画清单的 QA 路径实际显示安全占位，窗口保持响应且不会崩溃；
- Windows 自动化能够枚举带 QA 标志的窗口，但无法捕获 `AllowsTransparency=True` 窗口，接口返回 `0x80004002`；未使用非受控坐标注入。

### 风险或未验证

- 双击切换代码和状态测试已通过，但透明窗口的实际双击手感仍需用户直接操作确认；
- 尚未完成 M1 要求的 30 分钟内存稳定性检查；
- 100%、125%、150% DPI 和多显示器边缘仍待验收；
- `--qa-window`、`--qa-missing-assets`、`--preview-full-body`、`--preview-music` 和 `--preview-exit` 只用于本地开发验证，默认启动不会进入任务栏。

### 下一步

完成 M1 的人工双击/拖动和 30 分钟内存验收；通过后进入 M2 状态机和身体命中。

### Git

- 计划提交消息：`feat: play real runtime animations`。

## 2026-08-30：完成 M1 稳定性验收并建立 M2 核心状态机

### 完成

- 对 Release 完整全身待机窗口执行 30.04 分钟连续采样，归档 61 个原始样本和分析报告；
- 将持续状态从单一音乐布尔值扩展为待机、播放音乐、睡眠、拖拽和安全隐藏，同时保留独立的用户显示模式；
- 新增一次性反应请求、播放计划、优先级替换、同源合并、完成冷却、过期丢弃和唯一完成令牌；
- 拖拽进入时可中断普通反应，并记录拖拽前持续状态；拖拽期间发生的持续状态变化会在结束后恢复；不可打断的退出反应会拒绝拖拽；
- 只有完整全身普通待机且没有一次性反应覆盖时，播放计划才开放身体区域交互；安全隐藏状态生成不可见计划；
- 将现有音乐开始/停止验证入口接入状态机：“享受音乐”单次完成后重新求值为音乐摇摆，停止后恢复用户选择模式。

### 验证

- 30 分钟测试：工作集初始 124.9 MB、峰值 165.4 MB、结束 153.6 MB；私有内存初始 82.2 MB、峰值 132.2 MB、结束 126.3 MB；
- 去除前 5 分钟预热后，工作集趋势 -18.146 MB/小时、私有内存趋势 -4.787 MB/小时；末 20 分钟私有内存范围仅 0.074 MB；稳定段 CPU 约占单核 1.13%；判定无明显内存增长；
- `dotnet build LuoTianyiPet.sln --configuration Release`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：30/30 通过，其中新增 10 个状态机冲突、恢复与安全隐藏测试；
- `dotnet format whitespace LuoTianyiPet.sln --verify-no-changes`：通过。

### 风险或未验证

- 状态机已有拖拽语义，但窗口仍使用旧 `DragMove` 路径，尚未播放迷你膨胀和落地回弹；
- 身体区域蒙版和调试叠层尚未建立；透明窗口的单击、双击、拖拽互斥仍需下一纵向切片实机验证；
- M1 双击代码、模式状态和真实窗口分别通过验证，用户未报告手感问题；最终手感将与 M2 输入分发一起复验。

### 下一步

实现延迟单击、双击抑制、拖拽阈值和归一化身体命中调试叠层，再接入膨胀拖拽与落地回弹素材。

### Git

- 计划提交消息：`feat: add pet reaction state machine`。

## 2026-08-30：柔化音乐开始到持续播放的跨画风切换

### 完成

- 对比“享受音乐”和“音乐摇摆”素材，确认突兀来源同时包含角色占比、头身比例、服装与画风差异，未采用会产生双人重影的直接交叉淡化；
- 新增约 0.42 秒的遮罩式过渡：旧角色以底部中心为锚点缩小淡出，青白径向光圈遮住中点换帧，新角色从 84% 缩放轻微过冲到 104% 后回落；
- 将该表现封装为独立 `VisualSwapTransition` 控制器，后续其它跨画风动画可复用，主窗口只负责状态切换；
- 保留窗口底部中心锚点，180×180 到 190×190 的窗口变化在遮罩中点发生，避免角色脚下位置跳动；
- 新动画开始、音乐停止或程序退出时会统一取消未完成过渡并恢复正常透明度和缩放，不残留半透明或光圈；
- 新增 `--preview-music-transition` 只读 QA 参数，可自动执行“享受音乐 → 音乐摇摆”过程，不向桌面注入输入。

### 验证

- Release 编译成功，0 警告、0 错误；
- 自动化测试 30/30 通过；
- 真实透明窗口以约 45 ms 间隔采样 24 帧：旧角色在光圈增强时消失，新角色在光圈衰减时出现，没有两套服装同时清晰重叠，底部锚点保持稳定；
- 日志记录 `animation.music_transition_completed`，自动预览完整结束；QA 进程已关闭；
- Computer Use 能枚举窗口，但 Windows Graphics Capture 对 `AllowsTransparency=True` 窗口返回 `0x80004002`，因此改用只读屏幕采样完成视觉检查，未注入鼠标或键盘输入。

### 风险或未验证

- 当前参数按这两项音乐素材调校；未来其它跨画风动画可以复用该机制，但需要分别检查光圈大小和持续时间；
- 过渡中的青白光圈是程序动效，没有改变或重新编码原始动画素材。

### 下一步

继续 M2 输入纵向切片：延迟单击、双击抑制、拖拽阈值和身体命中调试叠层。

### Git

- 计划提交消息：`fix: smooth music animation transition`。

## 2026-08-30：完成 M2 手势互斥、身体命中调试和拖拽落地闭环

### 完成

- 新增可独立测试的 `PointerGestureRecognizer`，实现 300 ms 延迟单击、双击取消待决单击、6 DIP 拖拽阈值、拖拽结束和捕获丢失取消；
- 移除窗口按下即调用的阻塞式 `DragMove`，改为鼠标捕获期间按屏幕 DIP 差值移动窗口，并夹取在 Windows 虚拟桌面范围内；
- 新增完整全身归一化身体区域：左右眼、脸/嘴、左右手、胸部、下体、左右脚、头发和普通身体；小区域优先于大区域；
- 点击判断先读取当前动画帧的 alpha，透明画布不会误触身体；身体区域只在完整全身普通待机、没有一次性反应覆盖时开放；
- 新增默认关闭的身体命中调试叠层，所有矩形与真实命中共用同一坐标源，命中区域可高亮；
- 将“膨胀”动态 GIF 和“降落”PNG 编译为可追溯 PNG 图集，运行时动画目录从 5 项扩展为 7 项；
- 迷你模式拖拽循环“膨胀”；完整全身模式保持全身动画；松手播放一次“降落”并叠加 420 ms 弹簧位移，结束后通过状态机恢复拖拽前持续状态；
- 增加 `--preview-body-hit-debug` 与 `--preview-drag-cycle` 只读 QA 参数，用于透明窗口视觉检查，不注入鼠标或键盘。

### 验证

- AssetCompiler：7/7 项动画来源哈希、图集哈希和路径验证通过；“膨胀”10 帧，“降落”1 帧；
- `dotnet build LuoTianyiPet.sln --configuration Release`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：51/51 通过，其中 Core 35 项；
- 身体调试层真实透明窗口截图确认矩形跟随 240×260 完整全身画布，眼睛优先覆盖脸部，左右手、敏感区域和脚均位于角色对应位置；
- 迷你拖拽逐帧检查确认“嘿嘿 → 膨胀 → 降落 → 嘿嘿”；完整全身检查确认“全身待机 → 保持全身 → 降落 → 全身待机”；
- 日志分别记录 Compact 和 FullBodyInteractive 的 `interaction.drag_started` / `interaction.drag_ended`，QA 进程均已关闭。

### 风险或未验证

- 自动化已覆盖手势判定，视觉 QA 已覆盖动画闭环；透明窗口实际鼠标手感仍需用户直接操作确认；
- 身体区域目前是第一轮矩形校准，后续可根据用户点击体验调整或升级为多边形；
- 本轮只记录身体命中，不播放各区域最终反应，下一轮接入已确认动画映射；
- 跨不同 DPI 显示器拖动尚未实测。

### 下一步

批量编译并接入完整全身各身体区域反应，优先完成眼睛、脸/嘴、左右手、敏感区域两段式反馈和脚部反馈。

### Git

- 计划提交消息：`feat: add pointer gestures and drag feedback`。

## 2026-08-30：接入完整全身短按区域反应

### 完成

- 将“啾啾、心软软、可爱、击掌、重罪、黑化、糟了、旋转舞、九周年点赞、八周年点赞、我推、真好”12 项素材编译为透明 PNG 图集，运行时动画目录由 7 项扩展为 19 项；
- 新增 Core 层 `BodyInteractionResolver`，集中维护身体区域映射；眼睛、脸/嘴、双手、双脚和普通身体均返回用户已确认的动画，不把产品规则散落在窗口事件中；
- 胸部和下体使用同一个 4 秒升级计时器：首次播放“重罪”，窗口内任一区域再次点击播放“黑化”，随后共享 10 秒冷却；
- 普通身体从旋转舞、九周年点赞、八周年点赞、我推和真好中随机选择，并排除上次结果；头发短按保留为“等待摸头手势”，不误播普通反应；
- 身体反应使用状态机唯一令牌，播放期间关闭区域命中，完成后重新求值当前持续状态；从完整全身切入表情包和返回时复用青白遮罩过渡，不改变用户选择的显示模式；
- 为静态“击掌”和“糟了”增加程序回弹及左右晃动；增加 `--preview-body-reaction=<id>` 只读 QA 参数。

### 验证

- AssetCompiler：19/19 项动画来源哈希、图集哈希和路径验证通过；新增 GIF 均为 10 帧，旋转舞 8 帧，九周年点赞 67 帧，八周年点赞 32 帧；
- `dotnet build LuoTianyiPet.sln --configuration Release --no-restore`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-restore`：62/62 通过，其中 Core 46 项；新增测试覆盖固定映射、头发延迟、敏感区域跨部位升级/冷却、窗口过期及普通池不连续重复；
- 真实透明窗口逐帧检查“心软软”“击掌回弹”和“糟了晃动”：均从完整全身经遮罩进入，素材完整可见、底部锚点未跳出窗口，完成后恢复完整全身；QA 进程均已关闭。

### 风险或未验证

- 头发/头顶目前只抑制短按，约 600 毫秒往返移动的正式摸头识别尚未实现；
- 透明窗口真实鼠标点击边界、连续点击节奏和跨 DPI 体验仍需用户直接操作确认；
- 普通随机池已通过规则测试，但本轮视觉 QA 只抽查跨画风 GIF 和两项静态程序动效，未逐帧人工检查 5 项池中每一项。

### 下一步

实现头发/头顶约 600 毫秒往返摸头手势及“可爱”反应，并完成 M2 全部鼠标互动的手感验收。

### Git

- 计划提交消息：`feat: add full-body click reactions`。

## 2026-08-30：完成头顶往返摸头手势

### 完成

- 新增独立 Core `PettingGestureRecognizer`：头发/头顶按住至少 600 毫秒、累计 40 DIP 水平行程且至少两次换向后才判定摸头；
- 摸头完成后通过现有身体反应管线播放一次“可爱”，复用状态机令牌和跨画风遮罩，结束后恢复用户选择的完整全身模式；
- 单向位移达到 30 DIP 时让路给窗口拖拽；移出头部矩形或进入透明像素也会取消摸头候选，并把当前移动交回原 6 DIP 拖拽判定；
- 摸头完成会消费当前按压，松手不会补发身体单击；短按头发仍无动画，双击仍只切换显示模式。

### 验证

- `dotnet build LuoTianyiPet.sln --configuration Release --no-restore`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-restore`：66/66 通过，其中 Core 50 项；新增测试覆盖达到时长后的往返完成、时长不足不触发、单向大位移转交拖拽和取消后不补触发；
- 真实透明窗口逐帧检查“可爱”：遮罩进入后 180×180 素材完整显示，无裁切和双人重影，完成后恢复完整全身；QA 进程已关闭。

### 风险或未验证

- 核心判定和动画闭环已验证，但透明窗口真实鼠标往返速度、30 DIP 拖拽让路阈值及头部边界仍需用户直接操作确认；
- M2 在用户手感验收前不标记最终完成，也不提前切入 M3。

### 下一步

请用户实际运行 Release 桌宠，依次体验双击、各区域短按、头顶摸头、头部拖拽和落地；按反馈微调后完成 M2 验收。

### Git

- 计划提交消息：`feat: add head petting gesture`。

## 2026-08-30：音乐动画改为每次播放会话随机选择

### 完成

- 取消“享受音乐播放一次后固定切换音乐摇摆”的两阶段规则；
- 新增 Core `MusicPlaybackAnimationSelector`，每次从暂停或停止进入播放时，在“享受音乐”和“音乐摇摆”中随机选择一项；
- 将本次选择保存为持续状态的一部分，高优先级反应或拖拽结束后仍恢复同一音乐动画；暂停/停止后继续播放会重新选择；
- 将 0.5 倍速“享受音乐”运行时循环次数改为无限，与音乐摇摆一样持续播放；
- 音乐进入和停止继续复用青白遮罩过渡，但播放会话内部不再发生跨服装、跨画风切换；
- 同步更新场景配置、需求、动画映射和决策记录，移除当前规则中的旧两阶段描述。

### 验证

- AssetCompiler：19/19 项运行时动画校验通过，“享受音乐”清单为无限循环；
- 当前桌宠实例正在占用常规 Release 目录，因此使用独立 `BaseOutputPath` 执行 `dotnet build LuoTianyiPet.sln --configuration Release --no-restore`：成功，0 警告、0 错误，未中断用户正在运行的实例；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-restore`：73/73 通过，其中 Core 57 项；新增测试覆盖两个候选分支、每次播放会话重新取随机值、会话选择解析及拖拽期间改变音乐选择后的恢复。

### 风险或未验证

- 当前右键音乐入口用于状态机与视觉预演；网易云真实暂停/恢复检测要在 M3 Core Audio 纵向切片接入后再做端到端验证；
- 用户当前打开的是提交前的 Release 实例；为避免擅自关闭，未在本轮覆盖其文件或启动第二实例，更新后需由用户重新启动查看新规则；
- 随机选择允许连续两个播放会话自然抽中同一项，未额外加入“强制不重复”规则。

### 下一步

继续进行 M2 用户手感验收；音乐随机规则已经写入 M3 的权威需求和配置，后续真实网易云检测必须复用同一选择器。

### Git

- 计划提交消息：`feat: randomize music animation per playback`。

## 2026-08-30：防止身体动画尾部误触并移除全身落地动画

### 完成

- 在 Core 状态机新增可延长的身体互动恢复期；每个身体反应完成后抑制身体单击和摸头 800 毫秒，防止动画末尾按下形成的延迟单击在待机恢复后播放下一动画；
- 恢复期只影响 `BodyRegionInteractionsEnabled`，不影响双击显示模式切换和窗口拖拽；双击仍会取消第一次待决单击；
- 拖拽结束按显示模式分流：“嘿嘿”迷你模式继续播放降落弹跳与 420 毫秒程序回弹；完整全身模式不再创建降落反应；
- 完整全身拖拽前若为普通全身待机，松手保持同一动画；若拖拽前为音乐等其它持续状态，松手用现有遮罩平滑恢复；
- 同步更新可执行场景配置、需求、动画状态映射、架构和项目总控。

### 验证

- `dotnet build LuoTianyiPet.sln --configuration Release --no-restore`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-restore`：75/75 通过，其中 Core 59 项；新增测试覆盖恢复期边界、重复抑制只延长不缩短、恢复期内模式切换和拖拽仍可用；
- 真实透明窗口逐帧对比：迷你模式仍显示“膨胀 → 降落”；完整全身模式在拖拽前、拖拽中和松手后始终保持完整全身动画，没有闪现降落素材；QA 进程均已关闭。

### 风险或未验证

- 800 毫秒由返回遮罩约 420 毫秒加约 380 毫秒稳定点击间隔组成，逻辑边界已测试，最终体感仍需用户直接操作确认；
- 恢复期内双击和拖拽路径已由状态机及手势测试覆盖，但真实快速鼠标节奏仍以用户体验为准。

### 下一步

请用户体验身体动画结束后立刻单击、双击和拖拽三种情况，并比较迷你与完整全身模式松手反馈；按体感决定是否微调 800 毫秒。

### Git

- 计划提交消息：`fix: debounce body reactions after playback`。

## 2026-08-30：接入网易云 Core Audio 播放检测

### 完成

- 新增 Core `AudioSessionSnapshot`、`IAudioSessionProbe` 和 `MusicAudioActivityDetector`，将 Windows 探测与播放/停止判定分层；
- 新增 Windows `CoreAudioSessionProbe`，通过 `NAudio.Wasapi` 3.0.1 枚举默认多媒体输出设备的音频会话，只匹配 `cloudmusic.exe` 进程号并聚合会话峰值；
- 默认每 250 毫秒读取一次，峰值达到 0.001 立即进入播放，连续 1.5 秒无声才停止；会话消失立即停止，临时探测失败保留状态并安静重试；
- 真实检测接入现有状态机和每播放会话随机选择器；拖拽期间只更新恢复状态，一次性身体反应结束后恢复到最新音乐状态，不发生强制抢动画；
- 增加可持久化且可独立关闭的 `MediaPreferences`，旧配置缺少 `media` 节点时自动使用默认值；预览和 QA 运行不启动真实检测；
- 日志只记录检测启用、暂时不可用/恢复和状态转换，不记录进程号、峰值、歌曲或窗口内容；新增完整 MIT 第三方许可说明。

### 验证

- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：最终 86/86 通过（Core 66、Animation 12、Windows 8）；新增测试覆盖有声进入、短静音防抖、1.5 秒边界、重新有声刷新宽限、会话消失、临时探测失败、非法峰值、不存在的目标进程和旧配置补默认媒体设置；
- 本机本轮没有运行 `cloudmusic.exe`；平台测试已验证目标进程不存在时返回成功但无会话，不访问音频设备、不抛异常；
- 构建期间常规 Release 输出目录曾被已有桌宠实例短暂占用，未强制关闭；先用独立 `BaseOutputPath` 验证，再在该实例自行结束后完成常规 Release 全量构建：0 警告、0 错误；
- 启动新版普通窗口并保持真实检测约 26 秒：日志记录 `media.detection_started`，网易云未运行期间没有误发播放/停止状态或错误；通过标准 `WM_CLOSE` 关闭后保存设置并以代码 0 退出。

### 风险或未验证

- 尚未用正在播放的网易云完成端到端校准；0.001 阈值、250 毫秒轮询和 1.5 秒宽限是首轮安全默认值，需实测歌曲间静音、暂停/继续和播放器退出；
- 当前只完成只读检测，尚未发送上一首、下一首或播放/暂停快捷键，也未实现系统音量读写；
- 当前每次采样重新取得默认设备和会话集合，优先保证设备切换后能恢复；后续性能验证若发现影响 UI，再改为持久会话通知模型。

### 下一步

在用户实际播放网易云音乐时完成 Core Audio 端到端校准，通过后接入用户配置的网易云全局快捷键控制和等待/成功/超时反馈。

### Git

- 计划提交消息：`feat: detect cloud music audio activity`。

## 2026-08-30：音乐播放期间拖拽保持音乐动画

### 完成

- 修正拖拽状态只按用户显示模式选择外观的问题；拖拽前为音乐播放状态时，状态机直接解析为本次播放会话选中的音乐动画；
- 迷你和完整全身两种用户模式均不再在音乐拖拽时切换“膨胀”或完整全身模型；
- 音乐拖拽松手不播放降落反馈，继续同一个音乐动画；非音乐状态仍保持迷你“膨胀 → 降落”和完整全身无降落的原规则；
- 同步更新可执行场景配置、需求、动画映射、技术架构和项目总控。

### 验证

- `dotnet build LuoTianyiPet.sln --configuration Release --no-restore`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：88/88 通过（Core 68、Animation 12、Windows 8）；新增理论测试覆盖迷你和完整全身两种用户模式下，音乐动画在拖拽前、中、后均保持不变；
- 使用 `--preview-full-body --preview-music --preview-drag-cycle` 进行真实透明窗口回归：8 张连续截图在拖拽前、中、后始终显示同一“音乐摇摆”角色，没有闪回官方完整全身模型；
- 日志记录 `interaction.drag_started FullBodyInteractive`，松手记录 `Music animation continued without landing feedback.`，未创建降落反应；QA 进程已关闭。

### 风险或未验证

- 无新增系统权限或外部进程访问；本次只调整状态机解析和拖拽结束反馈。

### 下一步

继续使用真实网易云播放完成 Core Audio 播放、暂停、歌曲间静音和退出播放器校准。

### Git

- 计划提交消息：`fix: keep music animation while dragging`。

## 2026-08-30：音乐静音恢复时间缩短为 0.5 秒

### 完成

- 将 Core Audio 音乐检测的默认静音宽限从 1500 毫秒缩短为 500 毫秒；暂停后约半秒恢复用户选择的“嘿嘿”或完整全身模式；
- 用户 `settings.json` 配置版本由 1 升级为 2；加载旧配置时，仅把恰好等于旧默认值 1500 毫秒的宽限迁移为 500 毫秒，窗口位置、置顶、媒体开关、目标进程及用户自定义的其他宽限值均保留；
- 更新 499/500 毫秒边界测试和重新有声刷新计时测试；同步更新需求、架构、决策记录、示例配置和项目总控。

### 验证

- `dotnet build LuoTianyiPet.sln --configuration Release --no-restore`：成功，0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：90/90 通过（Core 68、Animation 12、Windows 10）；检测测试确认 499 毫秒仍保持音乐、500 毫秒停止，迁移测试确认旧默认 1500 → 500 且自定义 800 保持不变；
- 本机真实 `%LOCALAPPDATA%/LuoTianyiPet/settings.json` 从版本 1、1500 毫秒迁移到版本 2、500 毫秒；窗口置顶、左右位置、检测开关和目标进程设置全部保持，应用通过标准窗口关闭并以代码 0 退出。

### 风险或未验证

- 0.5 秒可能使某些歌曲本身超过半秒的完全静音段短暂恢复待机；需在真实网易云播放中继续观察，必要时再提供可调设置。

### 下一步

使用真实网易云验证暂停恢复速度及歌曲间静音表现，通过后接入全局快捷键控制。

### Git

- 计划提交消息：`tune: shorten music silence grace period`。

## 2026-08-30：音乐静音宽限调整为 1.0 秒

### 完成

- 根据真实切歌体验，将默认静音宽限从 0.5 秒调整为 1.0 秒，在暂停响应和下一首加载间隙之间取折中；
- 用户 `settings.json` 配置版本升级为 3：版本 1 的旧默认 1500 毫秒、版本 2 的旧默认 500 毫秒均迁移为 1000 毫秒，用户自定义的其他数值不变；
- 明确后续媒体快捷键阶段的设计：桌宠发起上一首/下一首时进入独立“加载中摇摆”，不再把切歌加载完全交给静音宽限猜测。

### 验证

- 用户正在运行的桌宠实例占用常规 Release 输出目录，未擅自关闭；使用独立 `BaseOutputPath` 完成整个解决方案 Release 构建：0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build`：92/92 通过（Core 68、Animation 12、Windows 12）；检测测试确认 999 毫秒仍保持音乐、1000 毫秒停止；
- 迁移测试覆盖版本 1 默认 1500 → 1000、版本 2 默认 500 → 1000，以及两个版本中用户自定义 800 均保持不变；
- 本机当前运行实例仍使用版本 2、500 毫秒配置；为避免打断用户，未关闭该实例。下次关闭并启动新版后会自动迁移到版本 3、1000 毫秒。

### 风险或未验证

- 用户直接在网易云界面切歌时，桌宠目前仍只能从音频会话判断；若实际网络加载超过 1 秒，仍可能短暂恢复待机，待媒体会话/快捷键阶段继续改进。

### 下一步

继续真实网易云端到端校准，随后接入上一首、下一首、播放/暂停全局快捷键及独立加载反馈。

### Git

- 计划提交消息：`tune: balance music silence grace period`。

## 2026-08-30：接入网易云安全全局快捷键控制

### 完成

- 新增 Core `MediaCommand`、结构化发送结果和 `IMediaCommandSender`，窗口层不直接依赖 Win32 输入细节；
- 新增 Windows `WindowsMediaCommandSender` 与 `Win32ShortcutInputBackend`，通过普通用户权限 `SendInput` 成对发送用户配置组合；首轮默认值为上一首 `Ctrl+Alt+Left`、播放/暂停 `Ctrl+Alt+P`、下一首 `Ctrl+Alt+Right`，最小发送间隔 350 毫秒；
- 发送前只用 `GetForegroundWindow`、`GetWindowThreadProcessId` 和公开进程名确认前台类别；原神受保护进程前台或查询失败时拒绝，不读取目标进程内存、模块、窗口标题或路径；
- 使用 `GetAsyncKeyState` 检查 Ctrl/Alt/Shift/Windows 和目标键；用户仍按住相关键时拒绝发送。系统只接受部分事件时尝试抬起本次组合涉及的键并报告失败，不触碰其他键；
- 右键菜单新增网易云三级入口并显示当前组合；发送成功只提示“快捷键已发送，等待播放器响应”，未确认前不播放 OK；禁用、配置无效、游戏安全模式、前台查询失败、键盘占用、限流和系统拒绝均提供具体气泡，非限流失败播放“呜呜晃动”；
- `settings.json` 在现有版本 3 中为缺失字段补安全默认值，旧运行实例以后保存的版本 3 配置仍可由属性默认值补齐；示例场景配置和权威文档已同步；
- QA/预览参数使用独立单实例标识，正式运行仍保持单实例，后续窗口验证无需结束用户正在运行的桌宠。

### 验证

- 独立 `BaseOutputPath` 完成整个解决方案 Release 构建：0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln -c Release --no-build -p:BaseOutputPath=...`：104/104 通过（Core 68、Animation 12、Windows 24）；新增 12 项 Windows 测试覆盖三键序列、进程名大小写与 `.exe` 归一化、前台查询失败关闭、Ctrl/Win/目标键占用、350 毫秒边界、无效配置、功能禁用、系统部分接受后的释放与立即重试；
- 使用 `--qa-window --qa-shortcut-menu` 启动独立透明窗口，实际截图确认网易云子菜单完整显示三组快捷键，安全模式气泡可读；QA 未点击菜单，因此没有向网易云或其他前台应用发送真实输入；
- Microsoft 官方文档确认 `SendInput` 返回实际插入事件数量并受 UIPI 限制，`GetAsyncKeyState` 最高位表示当前按下状态，前台窗口与进程号查询均使用公开 User32 API。

### 风险或未验证

- 尚未由用户实际点击确认本机网易云全局快捷键与首轮默认组合一致；当前没有设置页，只能修改本地配置；
- “快捷键已发送”之后尚未建立切歌加载、播放器响应确认、OK 和 15 秒超时闭环；本轮刻意没有把输入发送成功误报为播放器操作成功；
- 透明窗口不支持电脑控制工具的窗口边框捕获接口，本轮改用独立 QA 场景和屏幕截图完成只读视觉检查；不影响桌宠运行。

### 下一步

为上一首/下一首建立独立操作确认状态：400 毫秒后进入“加载中摇摆”，结合 Core Audio 变化确认响应，成功播放 OK，15 秒超时播放“呜呜晃动”并恢复持续状态。

### Git

- 计划提交消息：`feat: add safe cloud music shortcut controls`。

## 2026-08-30：将网易云入口改为四键悬浮控制条

### 完成

- 按用户提供的播放器按键参考，将临时网易云右键子菜单替换为角色底部白色圆角悬浮控制条；鼠标移入显示，移出隐藏，保持现有 196×196 等动画窗口尺寸不变；
- 控制条依次提供上一首、红色播放/暂停、下一首和红色爱心；播放/暂停字形只跟随 Core Audio 解析出的音乐持续状态，不在快捷键刚发送时提前改变；
- 新增 `FavoriteTrack` 命令和网易云默认 `Ctrl+Alt+L`，其余默认组合与用户本机网易云设置页一致；四个按钮工具提示展示当前配置值；
- 右键菜单继续只承担显示模式、开发验证、置顶和退出等低频操作；控制条隐藏时为 `Collapsed`，不遮挡身体区域点击或窗口拖拽；
- 修复 QA/预览实例会写回窗口位置和置顶设置的问题：QA 仍使用独立单实例标识，但关闭时跳过设置持久化。

### 本机快捷键冲突调查

- 使用 Windows `RegisterHotKey` 对 `Ctrl+Alt+P` 做只注册后立即释放的无输入探测，返回错误 1409，确认组合当前已被某个程序注册；同一时刻网易云已成功注册的上一首、下一首和喜欢歌曲也返回 1409，符合系统行为；
- Windows 公开接口只能报告组合已注册，不能返回注册它的进程，因此没有使用注入、Hook、进程内存或强制关闭程序来追踪；
- 当前正在运行 `wps.exe`，其本机安装资源多处把 `Ctrl+Alt+P` 映射为打印或打印预览，因此 WPS 是首要嫌疑；这些也可能只是 WPS 窗口内快捷键，只有用户安全保存文档并自行关闭 WPS 后再次探测，才能最终确认归因；
- Clash Verge 虽开启全局快捷键功能，但其可读配置中未发现 `Ctrl+Alt+P` 映射；网易云日志中也未找到能公开占用者的信息。

### 验证

- 整个解决方案 Release 独立输出构建成功：0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln -c Release --no-build -p:BaseOutputPath=...`：105/105 通过（Core 68、Animation 12、Windows 25）；新增测试确认喜欢歌曲生成 `Ctrl+Alt+L` 成对按下/抬起序列；
- `--qa-window --qa-media-controls` 真实透明窗口截图确认普通待机显示播放图标和四键控制条；增加 `--preview-music` 后音乐素材显示暂停图标，角色、按钮和爱心均完整可见；两个预览都没有点击按钮或发送真实快捷键；
- QA 结束前后 `settings.json` 最后写入时间完全一致，确认预览不再覆盖用户设置。

### 风险或未验证

- 当前控制条覆盖角色底部，符合“不扩大屏幕空间”的首轮方案；仍需用户体验悬停时机、按钮大小和是否遮挡角色文字后再微调；
- `Ctrl+Alt+P` 占用者尚不能在不关闭候选程序的条件下 100% 确认；WPS 是有直接配置证据的首要嫌疑，不应擅自关闭以免丢失文档；
- 四键仍只有“输入已发送”反馈，切歌加载、播放器确认、OK 和超时闭环尚待下一项。

### 下一步

为上一首/下一首建立独立操作确认状态：400 毫秒后进入“加载中摇摆”，结合 Core Audio 变化确认响应，成功播放 OK，15 秒超时播放“呜呜晃动”并恢复持续状态。

### Git

- 计划提交消息：`feat: add hover media controls`。

## 2026-08-30：将悬浮媒体条重设计为天依蓝控制岛

### 完成

- 调研 MahApps 圆形按钮与 IconPacks、MaterialDesignInXaml、WPF-UI 等公开组件方案；仅为四个按钮不引入 16–20 MB 级完整 UI 框架，改为项目内轻量组件；
- 将原白底红色控制条替换为 `#66CCFF` 天依蓝玻璃控制岛：蓝色渐变表面、品牌色主按钮、深蓝辅助按钮、边框和柔和阴影统一配色；
- 上一首、播放、暂停、下一首和喜欢歌曲全部改为本地 WPF `Path` 矢量图标，摆脱系统字体字形差异并保持 DPI 缩放清晰；
- 新增可复用 `MediaControlsVisibilityMotion`：指针进入桌宠后 170 毫秒上浮淡入，移出后保留 220 毫秒防抖并用 120 毫秒淡出；隐藏后为 `Collapsed` 且关闭命中；
- 补齐各按钮悬浮、按下、禁用和可访问名称，保留现有快捷键工具提示、Core Audio 播放/暂停状态和安全发送链路。

### 验证

- 用户旧运行实例未因构建被强制结束；整个解决方案使用独立 `BaseOutputPath` 完成 Release 构建：0 警告、0 错误；
- `dotnet test LuoTianyiPet.sln --configuration Release --no-build -p:BaseOutputPath=...`：105/105 通过（Core 68、Animation 12、Windows 25）；
- 使用独立 `--qa-window` 实例将指针移出和移入窗口，截图确认未悬浮时控制岛完全消失、悬浮后显示，测试后恢复原鼠标位置；
- 使用 `--qa-window --qa-media-controls --preview-music` 截图确认音乐素材上四键完整、中央为暂停矢量图标；全部 QA 实例均已正常关闭，未点击按钮、未发送真实快捷键、未保存设置。

### 风险或未验证

- 控制岛仍覆盖角色底部以满足“不扩大桌宠常驻空间”，但只在悬浮期间出现；最终按钮大小、遮挡程度和 220 毫秒移出宽限仍可按用户手感继续微调。

### 下一步

为上一首/下一首建立独立操作确认状态：400 毫秒后进入“加载中摇摆”，结合 Core Audio 变化确认响应，成功播放 OK，15 秒超时播放“呜呜晃动”并恢复持续状态。

### Git

- 计划提交消息：`style: redesign hover media controls`。

## 2026-08-30：控制岛下移并补充 12 周年官方动态候选

### 完成

- 将天依蓝媒体控制岛从角色内部覆盖层移到动画正下方，窗口底部预留 58 DIP；角色动画和音乐过渡层同步避让，按钮不再遮挡人物、表情文字或身体互动区域；
- 保留既有悬停淡入、移出宽限、四键顺序、矢量图标、快捷键工具提示和 Core Audio 播放状态联动；
- 从可追溯的洛天依素材归档中整理洛天依 12 周年 QQ 官方动态表情 16 个 300×300 GIF，并记录归档提交、官方发布核对页、原始文件名与 SHA-256；
- 新素材覆盖出场、打 call、思考、偷看、躺平、拒绝、生气、充电、喜欢、哭泣、拥抱、消息、贴贴、送花、生日和进食等场景，只加入候选区，不改变已确定状态；
- 动画挑选器候选数由 47 增至 63；新增蓝色“新添加”标记、“本轮新素材”标签和“只看新添加”筛选按钮。

### 验证

- 使用独立 QA 窗口截图确认 196×254 窗口内人物尺寸不变、控制岛完整位于动画下方，普通表情文字不被遮挡；QA 实例正常关闭，用户正式实例未被强制结束；
- 通过本地只读 HTTP 预览验证挑选器默认显示 63 个候选，“只看新添加”准确显示 16 个，放大预览标题和 300×300 尺寸正确；
- 16 个新增 GIF 均完成浏览器加载，挑选器控制台无错误；本地文件复核均为 300×300、169～400 帧，总计约 72.03 MiB；
- 使用独立 `BaseOutputPath` 完成 Release 全量构建：0 警告、0 错误；自动化测试 105/105 通过（Core 68、Animation 12、Windows 25）。

### 风险或未验证

- 新增素材来自社区维护的原文件归档，官方身份通过 12 周年发布页与表情名称交叉核对；归档本身不是 Vsinger 官方仓库，因此来源说明保留了这一区分；
- 16 个原始 GIF 合计约 75 MB，使用现有 Git LFS 规则管理；只有用户勾选并确定用途后，才评估转码、裁剪和运行时打包。

### 下一步

等待用户在挑选器中决定新增素材是否保留；代码开发继续为上一首/下一首建立独立操作确认状态：400 毫秒后进入“加载中摇摆”，确认后播放 OK，15 秒超时播放“呜呜晃动”。

### Git

- 计划提交消息：`feat: move media controls below pet and add official previews`。

## 2026-08-30：显示网易云当前曲名与歌手

### 完成

- 新增 Core `IMediaTrackInfoSource`、曲目信息快照和文本规范化，标题限制 80 字符、歌手限制 60 字符，并折叠控制字符和多余空白；
- 新增 Windows `SystemMediaTrackInfoSource`：优先枚举系统媒体会话并读取公开媒体属性；实测当前网易云没有注册任何系统媒体会话，因此自动回退到只读的 `cloudmusic.exe` 主窗口标题；
- 窗口标题兜底只匹配目标进程、只读取顶层标题栏，不遍历窗口正文，不读取进程内存、文件或网络；曲名与歌手只在内存中保存，日志只记录曲目发生变化而不记录文本；
- 在角色动画正上方新增天依蓝深色曲目信息气泡并预留 52 DIP；鼠标悬停时显示并刷新，检测到外部曲目变化后显示 4 秒；
- 桌宠成功发出上一首或下一首后先显示“正在切换歌曲”，以 250/500/750/1500 毫秒退避刷新曲目信息，获得新标题后立即替换；
- 保留动画下方 58 DIP 四键控制岛，曲名、人物和媒体按钮三个区域互不覆盖；无可用曲名时只在悬停期间显示明确的降级提示；
- 记录用户已解决本机 `Ctrl+Alt+P` 占用，原有键盘忙检测、受保护游戏拦截和失败关闭策略不变。

### 验证

- Microsoft 官方文档确认 `GlobalSystemMediaTransportControlsSessionManager` 从 Windows 10 1809（Build 17763）提供系统媒体会话与 `TryGetMediaPropertiesAsync`；
- 独立诊断程序枚举系统媒体会话为空，确认当前网易云版本不能依赖该接口；同一台电脑的窗口标题兜底成功读取“奔向你 (Live) - 张睿”；
- 使用 `--qa-window --qa-media-controls --qa-track-info-live` 隔离实例真实显示曲名“奔向你 (Live)”和歌手“张睿”，截图确认气泡位于角色上方、四键位于下方，没有发送任何媒体快捷键；
- Release 全量构建 0 警告、0 错误；自动化测试 121/121 通过（Core 70、Animation 12、Windows 39），新增覆盖媒体会话来源匹配、误匹配拒绝、窗口标题解析、通用标题拒绝和曲名文本规范化。

### 风险或未验证

- 当前网易云不提供系统媒体会话，若后续版本隐藏主窗口标题、改变标题格式或使用不带标题的迷你窗口，桌宠会安全降级，不能保证得到曲名；
- 本轮没有实际发送上一首、下一首或播放/暂停快捷键，避免改变用户正在播放的内容；曲目改变后的自动气泡已通过实时标题读取和代码路径验证，完整快捷键确认闭环留到下一项；
- Windows 自动化工具无法捕获无边框透明 WPF 窗口，返回 `0x80004002`；视觉验证改用隔离 QA 窗口区域的只读截图完成。

### 下一步

把“加载中摇摆”和“OK”转换为运行时动画，使用曲名变化优先、Core Audio 变化辅助的确认器完成上一首/下一首的 400 毫秒加载、成功和 15 秒超时闭环。

### Git

- 计划提交消息：`feat: show current cloud music track`。

## 2026-08-30：切歌确认闭环与十二周年互动状态

### 完成

- 将“加载中摇摆”和“OK”编译为运行时图集；上一首/下一首发送后 400 ms 仍未确认则显示加载，曲名变化优先确认，Core Audio 静音后恢复辅助确认，成功播放 OK，15 秒无响应播放“呜呜晃动”并显示失败气泡；
- 为播放器增加正向与反向时间线，同一图集可倒放；十二周年源 GIF 内重复写入的动作只取首个完整周期，并缩放为 240×240 运行帧，避免一次反应持续 13～40 秒；
- 接入十二周年“躺平”5 分钟中等待机；7 月 12 日及 12 月 12 日待机期间随机 45～90 分钟播放一次“生日快乐”；空闲时间只通过公开 `GetLastInputInfo` 获取；
- 拆分脸部和嘴部命中并缩小胸部、下体区域：嘴部“啾啾”、脸部“贴贴”、普通身体“抱抱”；原眼睛、手、脚和敏感区域升级规则不变；
- 实现左右和底部屏幕边缘躲藏：左右使用“偷看”，左侧水平镜像；底部使用“闪亮登场”；倒放隐藏、保留 12 DIP 触发条、悬停正放一次并定格、移出再次倒放；
- 新增官方“幻游世界·摸摸”和“夜航星·摸摸”两项候选及来源哈希，动画挑选器标记“新添加”；当前摸头运行时仍保留“可爱”，等待用户选择；
- 同步更新需求、动画映射、技术架构、示例配置和项目总控。

### 验证

- `dotnet test LuoTianyiPet.sln -c Release --no-restore`：132/132 通过（Core 81、Animation 12、Windows 39）；
- 使用隔离 `BaseOutputPath` 编译 WPF 应用：0 警告、0 错误；
- 独立 QA 窗口成功显示 190×190 十二周年动态素材，顶部曲名和底部控制岛未遮挡人物；透明窗口捕获接口仍不支持，改用桌面只读截图确认；
- 动画挑选器内联脚本通过 JavaScript 语法校验，两项新摸头素材路径和文件均存在；应用内浏览器连接在本轮验收时不可用，未进行自动点击筛选验收；
- 新增 Core 测试覆盖生日日期与 45 分钟门槛、左/右/下边缘判定、拆分后的脸嘴命中、缩小敏感区和普通身体抱抱映射。

### 风险或未验证

- 边缘躲藏已完成规则和位置自动化测试，仍需用户在实际多显示器和不同 DPI 环境下拖到三条边验证手感；当前按主工作区计算，跨显示器 DPI 是后续统一验收项；
- 两项摸头候选是官方静态表情，需要用户选定后再编译程序按压/回弹动效，不在本轮自动替换；
- Core Audio 辅助确认需要观察到超过现有静音宽限的停止/恢复；常规切歌仍以曲名变化为主要确认信号。

### 下一步

实现系统主音量监听与鼠标滚轮控制，复用已处理的原版/倒放“共鸣之声”，合并连续变化并显示百分比气泡。

### Git

- 计划提交消息：`feat: add twelfth anniversary pet states`。

## 2026-08-30：“享受音乐”持续动画无缝循环

### 完成

- 逐帧检查官方“享受音乐”GIF，确认第 0～3 帧是一次性英文文字入场，直接无限循环会导致文字反复消失/出现，并从末帧跨跳回初始动作；
- 新增可复现的 `make_seamless_gif.py`：从指定稳定区间生成不重复端点的往返时间线，同时记录源文件 SHA-256、源帧索引、速度倍率和输出哈希；
- 运行时改用 `心律共鸣_享受音乐_无缝_0.5x.gif`，帧序列为 `4,5,6,7,8,9,8,7,6,5`；循环边界由第 5 帧回到相邻的第 4 帧，英文文字全程保持完整；
- 重新生成 `resonance-enjoy-music` 图集与动画清单；音乐随机选择、播放会话生命周期、显示尺寸和 0.5 倍速度均未改变。

### 验证

- 生成前后逐帧联系表视觉对比：新 10 帧均保留完整文字，人物动作按相邻帧正向/反向连接；
- `dotnet test LuoTianyiPet.sln -c Release --no-restore`：132/132 通过（Core 81、Animation 12、Windows 39）；
- C# AssetCompiler 校验 27/27 项运行时动画的来源、图集和 SHA-256，通过；“享受音乐”为 10 帧、无限循环；
- 使用隔离 `BaseOutputPath` 完成 WPF Release 构建：0 警告、0 错误；
- 真实透明 QA 窗口按 200 ms 间隔截取 12 个时点，覆盖超过一个完整 2 秒周期；所有画面英文文字均保持完整，人物连续往返，应用进程无异常退出。

### 风险或未验证

- 往返闭环会让原动作在末端自然反向，而不是继续执行原 GIF 下一段；这是避免生成不存在中间动作帧时最稳定的无插值方案。

### 下一步

完成系统主音量监听与鼠标滚轮控制。

### Git

- 计划提交消息：`fix: make enjoy music loop seamless`。

## 2026-08-30：系统主音量滚轮控制与外部变化反馈

### 完成

- 新增 Core `ISystemVolumeService`、主音量快照、结构化调整结果和 `SystemVolumeChangeTracker`，将提高、降低、静音、解除静音与重复值分开处理；
- 新增 Windows `CoreAudioSystemVolumeBackend` 与 `WindowsSystemVolumeService`，只访问默认多媒体输出端点的归一化主音量和静音状态；滚轮每格默认调整 2%；
- 音量写入前通过公开前台窗口所属进程名执行受保护游戏检查；原神前台或查询失败时失败关闭，不调用 Core Audio 写入；
- 主窗口接入 `PreviewMouseWheel`，不安装全局鼠标 Hook；气泡显示 `🔊/🔇` 与整数百分比；静音/解除静音不播放人物动画；
- 调高使用原版“共鸣之声”，调低使用已有可追溯倒放素材；两项均转换为运行时图集并进入动画清单；
- 连续同方向变化使用状态机合并键保持一个动画，停止 500 ms 后恢复此前持续状态，同方向动画冷却 2 秒；方向反转会立即切换原版/倒放版；
- 外部变化同时订阅 Core Audio 通知并每 250 ms 只读采样兜底；跟踪器过滤事件和采样的重复快照，受保护游戏前台只更新基线而不显示覆盖反馈；
- QA/预览默认不连接真实系统音量，只有显式 `--qa-system-volume` 才允许端到端读写验证，避免其它动画验收被外部音量事件干扰；
- `settings.json` 升级到版本 4；版本 1～3 自动补充音量默认值并保留原窗口、媒体和安全设置。

### 验证

- Release 全量构建：0 警告、0 错误；
- 自动化测试 146/146 通过（Core 85、Animation 12、Windows 49），新增覆盖方向分类、静音无动画、不可用快照、2% 步长、上下限、禁用开关、受保护前台、前台查询失败、端点不可用、系统拒绝、通知转发和版本 3 迁移；
- AssetCompiler 校验 29/29 项运行时动画来源与图集哈希通过；原版和倒放“共鸣之声”各 10 帧；
- 真实默认输出设备从 53% 通过桌宠滚轮调到 55%，连续三格合并显示 61%；截图确认原版动画、百分比气泡及底部控制岛布局正常；随后恢复 53%；
- 使用系统音量键从 53% 外部降低到 48%，Core Audio 通知被桌宠接收并播放倒放动画；恢复到 53% 后分别切换静音和解除静音，只有通知与气泡、没有音量人物动画；最终设备为 53% 且未静音；
- 自动化安全测试确认原神或前台查询失败时不读取端点、不写音量；真实测试未启动或操作原神进程。
- `git diff --check` 通过；`dotnet format --verify-no-changes` 仍被仓库既有的 LF/CRLF 基线差异阻止，输出均为 `ENDOFLINE`，未执行全仓机械换行以避免扩大无关改动。

### 风险或未验证

- 服务当前绑定应用启动时的默认多媒体端点；运行期间切换默认声卡会返回暂不可用，尚未自动重建端点；
- 设置窗口中的系统音量滑块尚未实现；当前正式入口为桌宠窗口内滚轮和外部系统音量变化。

### 下一步

建立设置窗口的第一条纵向链路，提供系统音量滑块、滚轮开关、外部反馈开关和步长设置。

### Git

- 计划提交消息：`feat: add safe system volume feedback`。

## 2026-08-30：补充官方原生动态摸头候选

### 完成

- 按“官方发布或官方活动资产、且画面确实存在连续摸头动作”的标准，重新检查本地归档、Bilibili 官方装扮归档和 VSINGER/洛天依官方 QQ、微信动态表情归档；
- 在“天依游学记·国乐季”动态表情包中找到原生 `摸摸.gif`：162×162、10 帧、每帧 100 ms，SHA-256 为 `5DF39EC19050FBDDD9D7461D1A1DCF3D6FD330FE114EB08568D6C46205B62F75`；
- 逐帧确认手掌始终接触头顶并左右往返抚摸，洛天依同步眨眼、脸红，不是静态 PNG 的程序晃动；
- 将素材、来源、规格和哈希写入 `07_新添加_官方动态摸头候选`，并加入动画挑选器；
- 按用户反馈撤下“幻游世界·摸摸”和“夜航星·摸摸”两张静态候选，保留原始文件和来源记录，避免重复搜索；
- 挑选器调整为 64 项候选、17 项“新添加”；运行时仍使用“心律共鸣·可爱”，等待用户预览确认后再替换。

### 验证

- GIF 规格读取为 162×162、10 帧、统一 100 ms 帧延迟，透明画面与逐帧联系表核验通过；
- 挑选器内联 JavaScript 通过语法校验，数据区统计为 64 项、17 项 `newlyAdded`；新 GIF 路径存在且旧静态候选 ID 已移除；
- 应用内浏览器因 `file://` URL 安全策略拒绝自动刷新和截图，未绕过该限制；用户在现有挑选器标签页刷新即可预览。

### 风险或未验证

- 官方活动与动态表情包身份由 Vsinger_洛天依官方发布核对；GIF 文件从第三方无损归档镜像保存，因此已记录文件哈希和双重来源；
- 素材目前只进入挑选器，尚未编译为运行时图集，也没有替换当前摸头反应。

### 下一步

等待用户预览并确认该动态摸头候选；主开发线仍继续设置窗口的系统音量滑块、滚轮开关、外部反馈开关和步长设置。

### Git

- 计划提交消息：`assets: add official animated head pat candidate`。

## 2026-08-30：第二轮官方动态摸头素材扫描

### 完成

- 继续按“官方活动或品牌联动素材、且画面实际存在手掌触碰头顶的连续动作”筛选，不把静态图片、戳脸、揉脸、敲头或同人表情计入候选；
- 扫描官方表情归档中的 32 个洛天依/VSINGER 表情包，共复核 549 个 GIF，其中包括 317 个仅用编号命名、无法依赖文件名搜索的动作；
- 在美年达联动“橙子洛天依”表情包中找到 `C01.gif`：300×300、15 帧，SHA-256 为 `C61B9937483D30544638E7155A9F8EDA4A9E23944B593B75A5736BE97526017E`；
- 逐帧确认画面原文字为“摸摸头”，完整手掌从上方落到洛天依头顶并持续抚摸，人物由闭眼脸红过渡到睁眼微笑；
- 将素材以 `橙子洛天依_摸摸头.gif` 保存到官方动态摸头候选目录并加入挑选器；挑选器现为 65 项候选、18 项“新添加”；
- 排除了“不要不要”的手指推头、十三周年“被打”、2026“揉脸”以及来源明确为同人的摸头 GIF，避免用语义相近但动作或身份不符合的素材凑数；
- 运行时仍保留“心律共鸣·可爱”，等待用户比较两项动态摸头候选后决定。

### 验证

- GIF 规格读取为 300×300、15 帧，文件哈希与来源记录一致；完整逐帧联系表已检查，确认为连续摸头动作；
- 挑选器内联 JavaScript 通过语法校验，数据区统计为 65 项、18 项 `newlyAdded`；两项动态摸头文件路径均存在；
- 本轮只增加原始候选、挑选器数据和文档，不改变 WPF 运行时代码或资产清单，因此没有重新执行应用构建；
- 应用内浏览器当前打开的是 `file://` 页面，自动化仍受本地文件安全策略限制；未绕过策略，用户刷新现有标签页即可预览。

### 风险或未验证

- “橙子洛天依”官方联动身份由 VSINGER 表情归档说明与美年达联动报道交叉核对；GIF 文件来自第三方无损归档镜像，已记录直接来源和 SHA-256；
- 原 GIF 没有可依赖的显式帧时长；当前浏览器可预览，若用户确认采用，运行时编译时需按视觉效果确定帧时长。

### 下一步

等待用户比较两项动态摸头候选；主开发线仍为设置窗口的系统音量滑块、滚轮开关、外部反馈开关和步长设置。

### Git

- 计划提交消息：`assets: add second official animated head pat`。

## 2026-08-30：采用国乐季摸头并完成声音设置窗口

### 完成

- 按用户最终选择，将正式摸头反应从“心律共鸣·可爱”替换为“天依游学记·国乐季 · 摸摸”；使用独立运行时 ID `guoyue-headpat`，没有借用旧素材名称；
- 将 162×162、10 帧、每帧 100 ms 的原生 GIF 编译为透明 PNG 图集并写入运行时清单，显示尺寸为 180×180；移除不再引用的旧 `resonance-cute` 运行时图集，原始“可爱”素材仍保留在候选素材目录；
- `BodyInteractionResolver`、示例场景配置、需求和动画状态映射统一改为新摸头 ID；原有约 600 ms 往返摸头手势、遮罩过渡、恢复状态和 800 ms 防误触规则不变；
- 新增独立“声音与反馈”WPF 设置窗口，并从桌宠右键“设置…”打开；音乐的上一首、播放/暂停、下一首和喜欢歌曲仍只在角色下方悬浮控制岛显示，没有重新塞入右键菜单；
- 设置窗口提供实时系统音量滑块、桌宠滚轮控制开关、外部音量反馈开关，以及 1%、2%、3%、5%、10% 每格步长；使用天依蓝配色和标准可访问控件；
- 扩展 `ISystemVolumeService`：滑块可直接设置归一化音量，滚轮开关和步长可在运行中更新；滑块与滚轮均复用受保护游戏前台和前台查询失败的失败关闭检查；
- 保存设置后立即更新运行中服务并原子写入 `settings.json`，无需重启；关闭外部反馈时退订 Core Audio 通知并停止 250 ms 轮询，重新开启时读取当前值建立基线，不补播关闭期间的变化；
- 设置保存时一并保留当前窗口位置和永久置顶状态；边缘躲藏期间不会把屏幕外的临时位置写入用户设置。

### 验证

- Release 全量构建：0 警告、0 错误；
- 自动化测试 149/149 通过（Core 85、Animation 12、Windows 52）；新增覆盖滑块在关闭滚轮时仍可使用、受保护游戏前台拒绝滑块写入、运行中切换滚轮开关与 5% 步长，以及音量设置 JSON 往返；
- AssetCompiler 校验 29/29 项运行时动画的来源、图集和哈希；`guoyue-headpat` 为 10 帧、180×180，源 SHA-256 与候选记录一致；
- 使用 `--preview-body-reaction=guoyue-headpat --qa-window` 启动真实 WPF 反应窗口并持续运行 3 秒，进程没有提前退出，随后只终止本轮 QA 实例；
- 使用 `--qa-settings --qa-window` 成功创建“洛天依桌宠设置”真实窗口；Windows Graphics Capture 对该窗口连续两次返回 `0x80004002（不支持此接口）`，因此本轮没有可用自动截图，未尝试绕过系统限制；
- `git diff --check`、Git LFS 属性和生成资产引用在提交前继续执行。
- 对本轮 C# 文件执行 `dotnet format --verify-no-changes`；仓库既有 LF/CRLF 基线仍使其返回 `ENDOFLINE`，没有发现其它格式诊断，也没有为此机械改写整批文件。

### 风险或未验证

- 设置窗口的创建、XAML 编译、控件逻辑和无障碍名称已经验证，但 100%/125%/150% DPI 下的实际视觉布局仍需用户查看；该限制不影响音量服务和设置持久化测试；
- 系统音量服务仍绑定应用启动时的默认多媒体端点；运行中切换默认声卡时会安全降级，自动重建设备端点留给后续可靠性阶段。

### 下一步

进入 M4 的启动与恢复时间场景：接入早晨、午饭、下午和夜间四段启动问候，并以公开 Windows 会话/电源事件实现锁屏或睡眠恢复后的苏醒反应。

### Git

- 计划提交消息：`feat: add volume settings and selected head pat`。

## 2026-08-30：四时段启动问候与系统恢复苏醒

### 完成

- 新增 Core `StartupTimeSceneResolver`，使用半开时间区间稳定解析四段正式启动问候：06:00～12:00“早安浮动”、12:00～13:30“干饭弹跳”、13:30～20:00“摸鱼浮动”，20:00～次日 06:00“眠了呼吸”；
- 四段问候只在正式应用启动时播放一次，反应结束后由现有状态机恢复用户选择的“嘿嘿”或完整全身模式，不覆盖用户持久化选择；
- 将“早安”“干饭”“摸鱼”“眠了”和“苏醒”五张官方 PNG 编译为独立单帧运行时图集，保留源路径、源哈希和派生图集哈希；通过程序浮动、弹跳、呼吸和弹出运动补足静态素材的轻量动画表现；
- 新增 Windows `WindowsSystemResumeSource`，只订阅公开的 `Microsoft.Win32.SystemEvents.SessionSwitch` 与 `PowerModeChanged`，分别识别会话解锁和电源恢复；不安装全局 Hook、不读取窗口内容，也不要求管理员权限；
- 新增线程安全的 `SystemResumeEventGate`，默认将 5 秒内连续到达的解锁/电源恢复事件合并为一次；统一播放“心律共鸣·苏醒弹出”，不改变显示模式、不抢焦点、不临时修改永久置顶；
- 边缘躲藏期间忽略系统恢复反应，避免角色在用户主动藏起时突然弹出；窗口关闭时解除静态系统事件订阅并释放来源；
- 新增隔离参数 `--qa-system-resume`，可在不真的锁屏或睡眠的情况下验证完整苏醒反应链路。

### 验证

- Release 全量构建：0 警告、0 错误；
- 自动化测试 161/161 通过（Core 97、Animation 12、Windows 52）；新增覆盖四个时间边界、夜间跨午夜、5 秒重复合并、窗口外再次接收、时钟回拨和非法合并窗口；
- AssetCompiler 校验 34/34 项运行时动画的来源、图集、尺寸和 SHA-256；五个新增图集均由配置重新生成；
- 正式窗口在本机 19:14 启动时实际解析为 Afternoon 并播放 `startup-afternoon-float`，同时成功启动系统恢复监听；进程持续存活 4 秒后仅终止本轮测试实例；
- 隔离 `--qa-system-resume --qa-window` 实际触发 `PowerResumed`，状态机播放 `resonance-awake-pop`，进程持续存活 3 秒后仅终止本轮 QA 实例；
- 没有真的锁定、注销或休眠用户电脑；真实系统恢复路径留给后续人工验收。Windows Graphics Capture 对透明窗口仍受既有 `0x80004002` 限制，本轮没有可用自动截图。

### 风险或未验证

- Windows 可能在同一次物理恢复中先后发送会话解锁与电源恢复；当前 5 秒窗口会安全合并，仍需一次真实锁屏和睡眠恢复确认本机事件顺序；
- 本轮只实现操作系统恢复后的苏醒反应；15 分钟无输入触发的桌宠长待机睡眠尚未接入。

### 下一步

实现长时间无输入睡眠：连续 15 分钟无输入且没有播放音乐时循环“十周年·晚安浮动”，首次输入后播放一次“心律共鸣·苏醒弹出”，再恢复用户原显示模式；只用公开输入空闲查询，不安装全局 Hook。

### Git

- 计划提交消息：`feat: add startup and resume time scenes`。

## 2026-08-30：15 分钟长待机睡眠与输入苏醒

### 完成

- 新增 Core `IdleSceneResolver`，集中维护 5 分钟中等待机和 15 分钟睡眠边界；音乐播放、窗口拖拽和安全隐藏持续状态不会被空闲规则覆盖；
- 现有公开 `GetLastInputInfo` 轮询从 5 秒调整为每秒一次，只读取距系统最后一次输入的累计时长，不安装键盘或鼠标全局 Hook，也不记录具体输入内容；
- 连续无输入 5 分钟仍进入“十二周年·躺平”；达到 15 分钟且没有播放音乐时进入 `Sleeping`，循环“十周年·晚安浮动”；
- 将十周年“晚安”官方 PNG 编译为 `tenth-anniversary-goodnight-float` 单帧运行时图集，并由 `BodyReactionMotion` 叠加低幅度上下浮动；开始其它动画时会取消该无限程序动效；
- 睡眠后的首次输入先恢复当前空闲时长对应的 Idle/MediumIdle 状态，再播放一次“心律共鸣·苏醒弹出”，结束后恢复用户选中的“嘿嘿”或完整全身模式；
- 输入苏醒和 Windows 会话解锁/电源恢复共用既有 5 秒去重门；无论事件先后顺序如何，同一次回到电脑前只播放一次苏醒；
- 新增隔离参数 `--qa-long-idle`，自动用 15 分钟空闲值进入睡眠，2.4 秒后用 0 空闲值模拟首次输入，不保存用户设置。

### 验证

- Release 全量构建：0 警告、0 错误；
- 自动化测试 173/173 通过（Core 109、Animation 12、Windows 52）；新增覆盖 5/15 分钟精确边界、睡眠恢复到基础/中等待机、音乐/拖拽/安全隐藏不受影响、负时长拒绝，以及睡眠动画映射；
- AssetCompiler 校验 35/35 项运行时动画的来源、图集、尺寸和 SHA-256；新增“晚安浮动”为 1 帧、180×180、无限持续；
- 隔离 `--qa-long-idle --qa-window` 真实运行：日志依次记录 `animation.long_idle_sleep_started`、睡眠切换完成、`animation.long_idle_wake`、`resonance-awake-pop` 开始和恢复待机切换完成；窗口运行 8 秒未提前退出，并通过正常关闭以代码 0 结束；
- QA 同时确认输入苏醒前后的状态切换不会保存窗口位置或其它用户设置；本轮只终止自己启动的 QA 进程。

### 风险或未验证

- 为避免无意义等待，本轮没有真的静置电脑 15 分钟；时间边界由 Core 自动化测试和加速 QA 链路验证，后续长期运行验收时再观察真实触发；
- 睡眠素材是官方静态 PNG 加程序漂浮，不包含逐帧呼吸动作；当前按用户已确认的“十周年·晚安浮动”方案实现。

### 下一步

进入 M4 原神低权限联动：公开进程枚举检测启动并播放“你不许玩”三遍；原神仅在后台时，每小时随机一次“拜托”并随机桌面位置，前台或全屏时不抢焦点、不发送输入、不临时置顶。

### Git

- 计划提交消息：`feat: add long idle sleep and wake`。

## 2026-08-30：原神普通权限启动反应与后台低频彩蛋

### 完成

- 新增 Core 受保护游戏存在跟踪器、后台彩蛋调度器和安全工作区随机位置选择器；多原神进程只形成一个生命周期，已运行进程在桌宠启动时只建立基线；
- 正式应用每 2 秒只按 `YuanShen` / `GenshinImpact` 名称执行公开进程枚举并读取进程标识，立即释放进程对象；不读取内存、模块、线程、文件、画面或网络，不发送游戏输入；
- 最初验证 WMI 进程事件时先发现投影字段查询无效，修正后又在普通用户权限下得到“拒绝访问”；因此移除 `System.Management` 依赖，采用可在本机普通用户运行的低频枚举，并把原因记录为 ADR-020；
- 接入原神启动反应：安全桌面环境播放“心律共鸣·你不许玩”连续 3 遍，使用 `SetWindowPos` 的 `SWP_NOACTIVATE` 临时置顶令牌，结束后只释放自己的令牌，不改变用户永久置顶；
- 接入原神后台“拜托”彩蛋：每个 60 分钟活跃窗口随机第 5～55 分钟触发，45 分钟硬冷却；前台/全屏后重新安全时延后 5～15 分钟；
- 彩蛋在当前显示器工作区保留 24 DIP 边距随机定位，播放一轮后恢复原位置、持续状态和置顶选择；游戏停止、前台、任意全屏、查询失败、锁屏或挂起时取消当前展示并重置/延后；
- 将“你不许玩”和“拜托”两项官方 GIF 编译为统一 10 帧运行时图集，设置版本升级为 5，并为原神联动提供独立开关、进程白名单和 2000 ms 检查周期。

### 验证

- Release 全量构建：0 警告、0 错误；
- 自动化测试 188/188 通过（Core 118、Animation 12、Windows 58）；新增覆盖多进程生命周期、启动基线、非目标忽略、随机分钟、45 分钟冷却、前台后延后、退出重置、安全工作区位置、全屏边界、设置迁移，以及本机普通用户进程枚举启动/释放；
- AssetCompiler 校验 37/37 项运行时动画来源、图集、尺寸和 SHA-256；`resonance-no-playing` 与 `resonance-please` 均为 10 帧、180×180；
- `--qa-genshin-launch --qa-window` 真实 WPF 窗口日志确认“你不许玩”开始、连续三轮结束及临时置顶释放，进程以代码 0 正常退出；
- `--qa-genshin-cameo --qa-window` 日志确认“拜托”随机安全位置展示、结束恢复位置和置顶，进程以代码 0 正常退出；
- 平台测试实际启动并释放过滤进程枚举监视器；没有启动、操控或注入原神，也没有读取任何游戏内容。

### 风险或未验证

- 为避免账号与反作弊风险，本轮没有启动真实原神，也没有在游戏前台或独占全屏中运行桌宠；对应失败关闭逻辑由 Core/平台测试和隔离 QA 验证，最终兼容性留给用户后续自愿观察；
- 普通权限枚举最多有约 2 秒启动/退出检测延迟，这是避免提权、事件订阅权限差异和高频扫描的有意取舍。

### 下一步

进入 M4 Windows 通知授权原型：只判断通知来源是否为 QQ 或微信，不读取消息正文、发送者或群名；授权拒绝必须安全降级。

### Git

- 计划提交消息：`feat: add safe genshin lifecycle reactions`。

## 2026-08-30：QQ/微信隐私安全来源提醒与 MSIX 授权原型

### 完成

- 新增 `IMessageNotificationSource`、QQ/微信来源匹配器和通知协调器；只用应用标识、应用显示名及创建时间分类，3 秒内同源提醒合并，来源应用位于前台时不提醒；
- 新增 Windows `UserNotificationListener` 适配器。当前进程没有包身份时明确返回 `PackageIdentityRequired`，不会尝试请求权限；有包身份时仅由设置页显式按钮从 UI 线程调用 `RequestAccessAsync`；
- 通知事件只读取 `AppInfo.AppUserModelId`、`DisplayInfo.DisplayName` 和 `CreationTime`，代码从不访问 `UserNotification.Notification` 负载，因此不会取得发送者、群名、会话名或消息正文；
- 接入一次“代号洛天依·好奇摇摆”反应和来源气泡。QQ 显示“QQ 有新消息”，微信显示“微信 有新消息”；提醒窗口不激活，仅在安全桌面短暂置顶并在结束后恢复用户设置及此前持续动画；
- 原神/全屏前台、前台查询失败、锁屏、挂起、拖拽、睡眠和边缘隐藏期间不展示；回到安全桌面后最多补播一项，若来源应用已转到前台则清除；
- 设置页新增提醒开关、隐私说明、权限状态和显式授权按钮，设置架构升级到版本 6；免安装版直说需要 MSIX，不显示误导性的“已授权”；
- 新增最小 `Package.appxmanifest.template` 与打包说明，声明 `uap3:userNotificationListener` 和 `runFullTrust`。模板尚未包含正式图标、签名证书和可安装布局，不作为完成的安装包交付。

### 验证

- Release 全量构建：0 警告、0 错误；
- 自动化测试 204/204 通过（Core 132、Animation 12、Windows 60）；新增覆盖来源匹配、无关应用拒绝、3 秒去重、QQ/微信分别提醒、来源前台抑制、不安全状态延后/释放、设置迁移，以及免安装进程安全返回“需要包身份”；
- AssetCompiler 校验 38/38 项运行时动画来源、图集、尺寸和 SHA-256；`codename-curious-sway` 为 1 帧、180×180，左右摇摆由运行时程序动效生成；
- 隔离微信提醒 QA 日志依次记录 `codename-curious-sway` 开始、只含来源类别的提醒开始、反应完成和临时置顶释放，进程以代码 0 结束；
- 设置窗口真实启动并通过 Windows 可访问性树确认提醒卡片、隐私说明、“当前免安装版没有 MSIX 包身份”状态、禁用的授权按钮和保存按钮均存在；Windows Graphics Capture 对该窗口仍返回既有 `0x80004002`，因此没有取得自动截图；
- 本轮没有安装证书或 MSIX、没有点击系统权限，也没有读取或发送真实 QQ/微信消息。

### 风险或未验证

- `UserNotificationListener` 的真实事件必须在具备包身份、正确能力声明且用户授权的安装包内验证；当前自包含免安装 EXE 按设计不可用；
- QQ/微信不同版本可能使用不同 AUMID 或显示名。当前匹配表覆盖常见名称，真实安装验收后再按观察到的应用来源补充，不允许退回消息内容识别；
- 任务栏闪烁兼容路径尚未实现；若来源应用关闭系统通知，当前版本不会猜测有新消息。

### 下一步

生成带完整图标与测试签名的 MSIX 包，由用户明确确认安装和系统通知访问，然后发送测试消息验证真实 QQ/微信来源链路。

### Git

- 计划提交消息：`feat: add privacy-safe message notifications`。

## 2026-08-30：可重复生成的自包含签名 MSIX

### 完成

- 通过 Windows Package Manager 以当前用户安装微软官方 `WinAppCLI 0.6.1`，用于获取打包和签名工具；构建脚本设置遥测退出变量，不安装额外 Windows SDK 或 Visual Studio；
- 新增 `tools/packaging/build_msix.ps1`：安全清理仓库内 `artifacts/msix/staging`，生成 .NET 10 `win-x64` 自包含发布，写入版本化清单，创建或复用发布者匹配的开发证书，打包签名并校验关键条目；
- 新增确定性图标脚本，从正式“十二周年·抱抱”图集第 7 个无文字稳定帧生成 44×44、50×50 和 150×150 三项天依蓝 PNG；人工检查确认小尺寸仍能辨认角色绿色眼睛、灰蓝发色和轮廓；
- 修正包布局与应用运行时 `assets/` 的大小写目录冲突，将包图标固定放入 `assets/package/`；生成清单放在布局外，避免额外将模板作为应用载荷打包；
- 证书生成前读取 PFX 主题并强制与 `CN=LuoTianyiPet Development` 一致；私钥、随机密码、临时发布布局和 MSIX 全部位于 Git 忽略的 `artifacts/`，仓库只提交图标、脚本和清单；
- 成功生成 `LuoTianyiPet_0.1.0.0_x64.msix`、公钥 `LuoTianyiPet.Dev.cer` 与 `SHA256SUMS.txt`。包内含通知监听能力、应用本体、运行时素材、三项图标、块映射和签名。

### 验证

- MSIX 大小 103,161,345 字节；SHA-256 为 `0a06989cc0c543b8a6876b343826b5ff1951e720fce87bd5e8ce73b1e6e3c513`；
- 签名者为 `CN=LuoTianyiPet Development`，包中存在 `AppxSignature.p7x`；本机状态为证书链不受信任，符合尚未安装公钥证书的预期，不是未签名或哈希不匹配；
- 构建脚本解析包内 `AppxManifest.xml`，确认版本 `0.1.0.0`、发布者和 `uap3:userNotificationListener`，并检查应用 EXE、三项图标及 MSIX 基础条目；
- 通过 Windows PowerShell 查询确认 `LuoTianyiPet.Dev` 没有安装；从自包含发布目录执行退出预览，裂开反应完成后进程以代码 0 正常退出；
- 未信任证书、未安装 MSIX、未申请通知权限，也未读取或发送真实 QQ/微信消息。

### 风险或未验证

- 自签名测试证书只适合当前电脑旁加载；正式分发需要受信任生产证书或 Microsoft Store 签名；
- 当前 x64 自包含包约 103 MB，体积优化留到 M6；
- 真实包身份和通知事件仍需用户确认信任公钥、安装包并在设置页授权后验证。

### 下一步

等待用户明确确认信任开发公钥和安装 MSIX；随后由用户亲自点击通知授权，并以不含隐私内容的测试消息验证 QQ/微信提醒。

### Git

- 计划提交消息：`build: add reproducible signed msix packaging`。

## 2026-08-30：当前用户证书信任安装尝试与安全回滚

### 结果

- 用户确认继续后，先复核 MSIX SHA-256、证书主题和指纹；确认系统中没有既有证书或同名包；
- 将公钥加入 `CurrentUser\TrustedPeople` 后，`Add-AppxPackage` 仍以 `0x800B0109` 拒绝，说明当前用户范围不足以建立 MSIX 所需的受信任根链；
- 失败后立即从当前用户存储删除本轮加入的证书，复核指纹不存在，并确认 `LuoTianyiPet.Dev` 没有部分安装；
- 只读检查确认 Windows 开发者模式未开启，本机也没有已有的同发布者本机级证书；没有自动开启开发者模式、没有导入根证书、没有请求管理员权限。

### 下一步

等待用户明确批准管理员操作，将相同公钥导入 `LocalMachine\TrustedPeople`；成功后再安装包并由用户亲自在设置页授权通知访问。

### Git

- 计划提交消息：`docs: record msix trust scope requirement`。

## 2026-08-30：修复 MSIX 首次启动通知监听崩溃

### 结果

- 用户完成 `LocalMachine\TrustedPeople` 开发证书信任，`LuoTianyiPet.Dev 0.1.0.0` 成功注册；安装本身没有失败；
- 首次包身份启动在动画、音量、时间和原神模块之后抛出未处理 `COMException`，同一时刻 Windows 事件记录 `WpnUserService` 在 `wpncore.dll` 内故障，定位到未授权阶段的通知监听自动探测；
- 设置架构升级到版本 7，新增成功授权缓存标志。版本 6 旧设置迁移后始终为未授权；普通启动和设置页打开均不再提前调用通知监听器；
- 只有用户在设置页点击“授权访问”后才从 UI 线程请求权限，且仅在返回 `Allowed` 时保存授权事实并启动监听；拒绝、服务不可用和免安装包身份缺失继续安全降级；
- 最小隐私日志现在记录异常类型与 HRESULT，不记录异常消息、窗口标题、通知内容或用户输入；
- 生成并覆盖安装 `LuoTianyiPet_0.1.0.1_x64.msix`。包大小 103,162,062 字节，SHA-256 为 `07c54fcd11c3e27b02a4b6a091d0c30f18d3d4fbef45b6ba98b16e22475994d1`。

### 验证

- Release 全量构建：0 警告、0 错误；
- 自动化测试 205/205 通过（Core 132、Animation 12、Windows 61），新增版本 6 配置迁移后不携带授权标志的覆盖；
- Windows PowerShell 确认安装包版本为 `0.1.0.1`，位置为受包管理保护的 `WindowsApps`；
- 通过开始菜单应用标识真实启动安装版，进程持续运行；日志确认动画清单、系统音量、时间问候、原神检测、Core Audio 音乐检测和曲名检测均正常启动，没有新增 `dispatcher_unhandled`；
- 本轮没有代替用户点击 Windows 通知授权，也没有读取、发送或保存真实 QQ/微信消息。

### 下一步

由用户在桌宠设置页亲自点击通知授权并保存，再用测试消息验收 QQ/微信来源提醒；若 Windows 服务返回不可用，保留主体功能并记录 HRESULT 后继续排查。

### Git

- 计划提交消息：`fix: defer notification listener until consent`。

## 2026-08-30：可清理的外部便携测试包

### 完成

- 新增 `--portable` 存储模式：设置和日志统一写入可执行文件旁的 `UserData`，不再访问 `%LOCALAPPDATA%\LuoTianyiPet`；
- 新增可重复的 `tools/packaging/build_portable_test.ps1`，发布 .NET 10 x64 自包含布局，校验必需文件，并拒绝把 CER、PFX、MSIX 或预置 `UserData` 放入 ZIP；
- 包内提供“启动桌宠”“清理测试数据”和中文说明。清理脚本验证包标记与目标位于解压目录内，只停止可执行路径完全匹配的进程，然后删除 `UserData`；
- 首轮实测发现 Windows PowerShell 5.1 会错误解析无 BOM 中文脚本文本，交付前已将脚本文件名和内部文本改为 ASCII，保留中文 CMD 入口和说明；
- 生成 `LuoTianyiPet-Portable-Test-0.1.0.2-win-x64.zip`，大小 104,197,822 字节，SHA-256 为 `3f3ffee1d181498f617ce8e7ad158e1142280daf732072fe1cb8b0c7b36dbf7e`。

### 验证

- Release 自动化测试 208/208 通过（Core 132、Animation 12、Windows 64）；新增便携根目录、设置文件和日志目录始终位于应用目录下的测试；
- 从最终 ZIP 解压并以 `--portable` 真实启动，日志首项为 `app.storage_mode Portable`，动画、音量、时间、原神、音乐与曲名模块均正常启动；
- 运行最终清理脚本后，便携进程数为 0、包内 `UserData` 不存在；测试前后 `%LOCALAPPDATA%\LuoTianyiPet\settings.json` SHA-256 均为 `F4EEB770546A5C62CFC6C198DB7A93EC55179745C4FA371A41FECE5BFBCA8AD6`；
- 本机安装版随后已重新启动。便携包没有安装证书、包身份、服务、驱动、浏览器扩展或注册表项。

### 限制

- 便携版不能测试依赖 MSIX 包身份的 QQ/微信通知监听；该功能继续使用安装版验收；
- 未使用商业代码签名，其他电脑可能出现 SmartScreen“未知发布者”；随包提供 SHA-256 供发送者和测试者核对；
- Windows 自身的下载、解压、SmartScreen、安全扫描和系统事件记录不属于程序数据，清理脚本不会修改。

### 下一步

外部测试者使用便携包验收普通功能；本机安装版继续由用户在设置页显式授权后验收 QQ/微信通知来源提醒。

### Git

- 计划提交消息：`build: add clean portable test package`。

## 2026-08-30：移除喜欢歌曲并按歌手选择音乐陪伴动画

### 完成

- 从悬浮控制岛、媒体命令、默认快捷键、示例配置和设置模型中完整移除“喜欢歌曲”；控制岛固定为上一首、播放/暂停、下一首三键，避免在无法读取网易云爱心状态时误取消收藏；
- 音乐动画改为按当前曲目的歌手字段选择：歌手是“洛天依”或合唱名单中包含“洛天依 / Luo Tianyi”时，每次曲目切换或暂停后恢复播放都会在“享受音乐”和“九周年·音乐摇摆”之间随机；
- 其它歌手或暂时读不到歌手时固定使用“享受音乐”，作为通用听歌陪伴动画；不根据歌名猜测歌手，避免同名歌曲误判；
- 曲目切换时会重新分类并更新持续动画；一次性反应、拖拽或其它高优先级状态结束后仍恢复到当前歌曲应有的动画；日志只记录 `LuoTianyi` 或 `OtherOrUnknown` 分类，不记录歌曲名、歌手名或播放历史；
- 生成外部测试包 `LuoTianyiPet-Portable-Test-0.1.0.3-win-x64.zip`，本机安装包已更新为 `LuoTianyiPet.Dev 0.1.0.3`。当前仍在运行的旧便携版 `0.1.0.2` 未被中断或覆盖。

### 验证

- Release 全量构建：0 警告、0 错误；自动化测试 218/218 通过（Core 143、Animation 12、Windows 63）；
- 新增中英文歌手名、合唱分隔符、相似名称误判、未知歌手安全降级和随机池边界测试；
- 隔离 QA 窗口的自动化可访问树只包含上一首、播放/暂停、下一首三键，不再存在喜欢歌曲按钮；当前 WPF 窗口截图仍受既有 `0x80004002` 限制，未取得自动截图；
- 便携 ZIP 大小 104,198,442 字节，SHA-256 为 `cbd6f9a4cc9a83205f13a2ca13bb2c0164b05622121931869594d453223d9c72`；
- MSIX 大小 103,162,368 字节，SHA-256 为 `8ee146dc8d6e274838515be408763cd14b9ff1c65f695992da6453351560a9a0`；Windows PowerShell 确认已安装版本为 `0.1.0.3`。

### 下一步

用户关闭当前 `0.1.0.2` 便携进程后，从新版便携包或开始菜单安装版启动，使用一首洛天依独唱/合唱歌曲和一首其它歌手歌曲实机验收分类、切歌与暂停恢复行为。

## 2026-08-30：网易云独立音量、边缘墙面与托盘设置

### 完成

- 将控制岛第四格从已移除的爱心状态改为扬声器滑杆；通过公开 Core Audio 会话接口只读写 `cloudmusic.exe` 的会话音量，不影响系统主音量和其它应用，会话不存在时显示不可用；
- 音量动画合并保持时间从 500 ms 调整为 1800 ms，避免喇叭格数变化刚出现就消失；受保护游戏前台或前台查询失败时，网易云独立音量写入与既有系统音量写入都失败关闭；
- 拖拽越过左、右、工作区底边时在松手前立即切换对应边缘动画；隐藏后保留 12 DIP 天依蓝墙面按钮，并为按钮添加显式悬停唤出事件。底边与左右使用同一状态机；
- “偷看”“闪亮登场”和正式摸头动画放慢；“啾啾”“重罪”“黑化”按约 0.8 倍速度播放；完整全身显示由 240×260 缩小为 220×238；
- 摸头手势放宽为约 350 ms、24 DIP、一次换向，并加入头顶短按兜底；普通身体恢复为抱抱、旋转舞、九周年点赞、八周年点赞、我推、真好六项随机且不连续重复；
- 设置、始终置顶和开机自启动进入 Windows 系统托盘。开机自启动默认关闭，用户主动启用时只写当前用户 Run 项中的一个产品值；
- 动画挑选器把当前已接入素材排在前面，显示“已使用”标记和具体触发场景；项目配置升级为版本 8，并迁移旧的默认音量反馈时长。

### 验证

- Release 全量构建 0 警告、0 错误；自动化测试 230/230 通过（Core 151、Animation 12、Windows 67）；
- JSON 配置和动画清单均通过解析，38 项运行时动画重新编译并校验；动画挑选器内联 JavaScript 通过语法检查，当前使用映射 39 项；
- 隔离 QA 可访问树确认悬浮区含网易云独立音量滑杆、设置页含开机自启动与始终置顶、底边隐藏后仍有“唤出洛天依”墙面按钮；QA 进程均已关闭；
- 生成 `LuoTianyiPet-Portable-Test-0.1.0.4-win-x64.zip`，大小 104,208,007 字节，SHA-256 为 `72fd536afede9e8bbd72e29b1a7759b35c269fb47de9ed5cb8e8cf0034983f7c`；
- 生成签名 MSIX，大小 103,171,657 字节，SHA-256 为 `35396df355c8eb4b0a4c7ab79c71378921327f2e7175579a169f2b167f9a6cf8`；Windows PowerShell 确认本机安装版已更新为 `LuoTianyiPet.Dev 0.1.0.4`。

### 仍需实机体验

- QA 时网易云没有可用会话，因此已验证滑杆的不可用降级和平台层写入测试，但“实际拖动后网易云混音器音量同步”需在网易云正在播放时由用户体验；
- WPF 透明窗口自动截图仍受既有 `0x80004002` 限制；底边墙面按钮在可访问树中存在，但不同 DPI、多显示器的鼠标悬停与拖出手感仍需用户实测微调。

### Git

- 提交消息：`feat: refine music companion controls and desktop interactions`。

## 2026-08-30：取消悬浮音量图标及网易云独立音量

### 完成

- 按用户最新决定，从控制岛移除扬声器图标和滑杆，宽度恢复为适配上一首、播放/暂停、下一首的三键布局；
- 删除 `IApplicationVolumeService`、应用音量快照/结果模型、`CoreAudioApplicationVolumeService` 及全部对应测试，不保留隐藏入口或后台写入能力；
- 网易云音频会话继续只读用于判断是否正在播放，不再调整其会话音量；既有系统主音量滚轮控制、外部音量变化监听及“共鸣之声”正放/倒放反馈不变；
- 动画挑选器将相关场景改为“系统音量反馈”，明确控制岛不显示音量按钮；需求、架构、安全约束、路线图和决策记录已同步。

### 验证

- Release 全量构建 0 警告、0 错误；剩余自动化测试 223/223 通过（Core 147、Animation 12、Windows 64）；
- 隔离真实 WPF QA 窗口的无障碍树只显示上一首、播放/暂停、下一首三个按钮，没有音量控件；QA 进程已关闭；
- 动画挑选器内联 JavaScript 和示例场景 JSON 均通过语法解析，`git diff --check` 无错误；
- 生成 `LuoTianyiPet-Portable-Test-0.1.0.5-win-x64.zip`，大小 104,204,480 字节，SHA-256 为 `1dbd658c5b88c7927a690d1fc5cd571ec0a902697ad25ff6156c6269827fd83a`；
- 生成并安装 `LuoTianyiPet.Dev 0.1.0.5`，MSIX 大小 103,168,214 字节，SHA-256 为 `ef962e22a4cdd6d0aede7b039c7ef473228f8843cb9e4d5646636e9821b81656`。

### 下一步

启动 `0.1.0.5` 验收三键控制岛；随后继续左右/底边躲藏、摸头和普通身体随机动画的实际手感调整。

### Git

- 提交消息：`revert: remove cloud music volume control`。

## 2026-08-31：重建动画挑选器选择状态

### 完成

- 保留现有 65 项官方动画与程序动效预览，重新建立版本 3 的统一浏览器内状态；不再读取此前分散且已经混乱的三组本地存储键；
- 默认选择恢复为用户最初确认的 34 项保留清单与当前 39 项已接入动画的并集，并为 16 个概括用途和 13 个已实现联动场景填入项目当前默认值；
- 新增“恢复项目当前状态”“开始全新挑选”和最多 30 步撤销；清空及恢复前会明确确认，避免再次误操作；
- 新增当前已使用、仍待挑选、新添加和总候选数量概览，以及“只看当前已使用”筛选；
- 新增 JSON 完整备份/导入，保留原有文本复制与下载；文本结果现在同时导出当前 39 项已接入动画及其触发方式；
- 修正“可爱”“你不许玩”“享受音乐”“国乐季摸摸”和“好奇摇摆”的过时用途说明。

### 验证

- 内联 JavaScript 通过 `new Function` 语法编译；
- 数据检查确认 65 个动画、39 个当前使用映射、16 个用途、18 个联动场景均可解析；
- 65 个预览素材路径全部存在，没有缺失资源；
- Release 全量测试 223/223 通过（Core 147、Animation 12、Windows 64）；
- `git diff --check` 无空白错误。

### 下一步

用户刷新或重新打开动画挑选器，核对默认恢复后的保留清单、当前触发说明和动画预览；随后继续桌宠安装版的交互手感验收。

### Git

- 计划提交消息：`fix: rebuild animation picker state`。

## 2026-08-31：修复网易云切歌时偶发安全退出

### 定位

- 从用户正在运行的 `0.1.0.5` 便携版日志确认，切歌约 11 秒后 WPF Dispatcher 收到 `ArgumentOutOfRangeException` 并进入全局安全退出；异常前发生了曲目元数据切换和 Core Audio 播放会话停止/恢复；
- 根因是网易云切歌时会短暂销毁或重建音频会话。桌宠在 `SessionCollection.Count` 与按下标取会话之间遇到集合变化，或读取到音频端点的瞬态边界值时，旧实现只捕获 COM/设备异常，越界异常会冒到界面线程；
- 问题位于公开 Core Audio 只读探测适配器，不涉及快捷键发送、歌曲元数据、网易云进程内存或网络接口。

### 修复

- Core Audio 枚举现在将会话集合变化产生的 `ArgumentOutOfRangeException` 视为一次不可用采样，并继续下一轮检测；
- 峰值采样拒绝 `NaN`/无穷值，有限的轻微越界统一夹取到 `0～1`，避免平台瞬态数值传入严格领域模型；
- 主窗口音乐检测再增加一层故障边界：参数、无效状态、COM 和权限类适配器异常仅跳过当前 250 ms 轮询，保留原音乐/待机状态并继续重试，不再触发应用退出；
- 最小隐私日志增加异常目标类型、方法和参数名，仍不记录异常消息、歌曲名、歌手名、窗口标题、用户输入或路径。

### 验证与交付

- Release 自动化测试 232/232 通过（Core 147、Animation 12、Windows 73）；新增有限峰值夹取、非有限值拒绝和隐私安全错误日志测试，同时保留原 Core Audio 参数及缺失进程测试；
- 源码 Debug 版本执行多轮普通及快速切歌压力测试后保持运行；最终 `0.1.0.6` 绿色包在真实网易云会话中连续切换 5 首歌曲，日志记录 5 次元数据变化且 `ERROR` 数量为 0；
- 生成 `LuoTianyiPet-Portable-Test-0.1.0.6-win-x64.zip`，大小 104,204,772 字节，SHA-256 为 `8e2ecf81048584088ca4b37d2fa6be551c977eee30b8aec938f902a1eb5effbb`；
- 生成并安装 `LuoTianyiPet.Dev 0.1.0.6`，MSIX 大小 103,168,575 字节，SHA-256 为 `ccb2e7017a08c5961db2ad81309a0f75790df6b44255d409b36e2eab52183016`；本次覆盖安装沿用既有用户证书信任与设置，不新增权限。

### Git

- 计划提交消息：`fix: survive cloud music session transitions`。

## 2026-08-31：重做可逆的屏幕边缘收纳

### 用户体验问题

- 原实现只要接近边缘就自动收起，无法把桌宠正常贴边停留；底部角色以窗口而非实际图像对齐，视觉上悬空且控制岛会被任务栏遮挡；
- 收起动画在鼠标移入或移出时总从固定端点重新播放，方向切换会跳帧，播放结束还会短暂闪回开头；
- 隐藏后使用天依蓝竖线和圆点占位，与官方素材画面割裂，左右与底部也没有保留用户指定的砖墙、头发和猫耳。

### 修复

- `EdgeDockResolver` 改为解析明确的隐藏意图：允许角色实际图像贴住工作区边缘，只有向工作区外继续拖出至少 28 DIP 才进入隐藏；窗口拖拽增加 96 DIP 的受控越界范围，未达到门槛时按实际 `PetImage` 边界贴回工作区；
- `AnimationFramePlayer` 新增包含首尾帧的范围播放及单帧固定能力，边缘动画只使用第 3～7 帧；任何隐藏/展开方向变化都会从当前显示帧反向，旧完成回调以代次令牌失效，避免跳回端点或首帧闪烁；
- 左右隐藏裁剪为“十二周年·偷看”最右侧约 26.5% 的原砖墙，左侧通过镜像得到对应墙面；底部隐藏裁剪“十二周年·闪亮登场”中央 60%、底部 14% 的头发与猫耳。透明窗口余量不命中，只允许可见裁剪面接收悬停；
- 桌宠距工作区底部 80 DIP 内时切换底部布局：控制岛置于最上方，歌名/作者位于其下，角色仍以实际图像底边对齐工作区，避开任务栏；离开底部后恢复原布局，并保持角色屏幕位置连续；
- 新增 `--qa-bottom-controls` 隔离预览入口，以及左右/底部收起、当前帧反向和底部布局的视觉验收图。

### 验证与交付

- Release 全量自动化测试 244/244 通过（Core 153、Animation 18、Windows 73）；新增贴边不隐藏、越界门槛、最深边缘选择、底部邻近判定及升/降序帧范围测试；
- 真实透明 WPF 窗口验证：快速“移入—移出—再移入”保持运行且按当前帧反向；把指针移入透明余量不会误触发，只有砖墙或头发区域触发；
- 视觉证据：`edge-dock-right-hidden-0.1.0.7.png`、`edge-dock-left-hidden-0.1.0.7.png`、`edge-dock-bottom-hidden-0.1.0.7.png`、`edge-dock-right-reversed-0.1.0.7.png`、`bottom-controls-above-track-0.1.0.7.png`；
- 首次安装 `0.1.0.7` 时发现已授权的 Windows 通知 RPC 服务可能在订阅瞬间返回 `0x800706BE`；通知源现将订阅和退订阶段的权限、COM 与状态瞬态异常安全降级为暂不可用，不再冒泡到 WPF 全局错误弹窗；
- 生成 `LuoTianyiPet-Portable-Test-0.1.0.8-win-x64.zip`，大小 104,206,781 字节，SHA-256 为 `f0cc6f33ca1ea6e2cdb71a8bfa712750b59d6d90622c894ae861cce5c5603f7d`；
- 生成并安装 `LuoTianyiPet_0.1.0.8_x64.msix`，大小 103,171,036 字节，SHA-256 为 `ae452c44d236931a077500303131fc77b16191f36e780cbdcfeb64908471c1be`；实机启动日志确认通知监听状态为 `Allowed`，音乐检测、曲名检测和托盘均启动，未新增 `ERROR`。

### 下一步

在用户的实际 DPI 与多显示器环境中验收“贴边不收起”和“继续越界才收起”的拖拽门槛，并根据手感微调 28 DIP；随后继续 QQ/微信来源提醒的安装版授权验收。

### Git

- 计划提交消息：`fix: refine reversible edge docking`。

## 2026-08-31：QQ/微信提醒显示应用图标与会话标题

### 完成

- `MessageNotificationSummary` 新增仅内存存在的联系人/群标题、应用图标和可选联系人头像槽；延后提醒会保留同一份摘要，展示、取消或关闭后释放，不写设置或历史；
- `WindowsMessageNotificationSource` 通过公开 `AppInfo.DisplayInfo.GetLogo` 读取不超过 1 MiB 的应用 Logo；Toast 绑定必须至少有两个文本元素才读取第一个标题元素，后续正文元素的 `Text` 从不访问；
- 标题会清理控制字符并限制为 64 字符；单文本、空标题、权限/COM/流读取失败均安全退回仅显示 QQ/微信来源；
- 新增独立天依蓝提醒卡：左侧显示应用图标，右侧显示应用来源和联系人昵称/群名；若未来平台直接提供联系人头像，主图显示头像并以应用图标作右下角角标。当前 Windows 结构化接口不公开联系人头像，稳定使用应用 Logo；
- 设置页和安全文档明确新的隐私边界：不读取正文、不记录标题或图像，不从聊天缓存、私有 XML、窗口、远程 URL、进程内存或网络补取头像。

### 验证与交付

- Release 构建 0 警告、0 错误；自动化测试 253/253 通过（Core 155、Animation 18、Windows 80），新增延后元数据保留、标题结构失败关闭、控制字符清理、长度限制和异步图标加载乱序抑制测试；
- `--qa-message-notification=qq --qa-window` 真实透明 WPF 预览确认卡片显示“QQ · 新消息 / 测试联系人”、圆形来源图标且没有正文；视觉证据为 `docs/validation/message-notification-card-0.1.0.9.png`；
- Windows Graphics Capture 仍对透明窗口返回既有 `0x80004002`，因此使用只读窗口区域屏幕采样，不发送鼠标或键盘输入；
- 生成 `LuoTianyiPet-Portable-Test-0.1.0.10-win-x64.zip`，大小 104,212,364 字节，SHA-256 为 `5fba6382bfff0644dd2f944fffa566e49033c88082541f16625b85a604d72523`；
- 生成并安装 `LuoTianyiPet_0.1.0.10_x64.msix`，大小 103,175,395 字节，SHA-256 为 `4ad02ae5caa784a029a59a8b4fc855ca6b9f0d6f1b11861eb84b1891103eb2e1`；实机启动确认通知权限为 `Allowed`，通知、音乐、曲名和托盘模块均正常，日志无新增 `ERROR`。

### 未验证

- 未代表用户向第三方发送 QQ/微信消息；需要实际各收到一条系统通知，确认当前客户端的标题顺序。联系人头像因 Windows 当前公开接口不提供，按设计回退应用 Logo。

### 下一步

由用户让 QQ 与微信各收到一条测试消息，观察应用 Logo、昵称/群名和正文隔离是否符合预期。

### Git

- 计划提交消息：`feat: show safe chat notification metadata`。

## 2026-09-01：补全“偷看”展开并缩窄边缘墙面

### 用户体验问题

- 左右边缘悬停展开只播放到第 7 帧，删掉了官方动画中的“偷看”文字和后续完整人物；
- 隐藏后保留整块砖墙过宽，与用户圈出的靠人物侧窄条不符；
- 底部收回若从第 7 帧开始会带出“闪亮登场”文字，用户要求从无文字画面开始下钻。

### 修复

- 新增 `EdgeDockFrameRouteResolver`，把边缘动画拆成含字展开和无字收回两条可测试路线：左右展开第 3→19 帧、收回第 7→3 帧，底部展开第 3→7 帧、收回第 5→3 帧；
- 拖拽越界预演改为显示各边缘的无文字收回起点；同一无字段内快速改变方向仍从当前帧继续，从含字段切回隐藏时进入无文字收回起点；
- 左右隐藏裁剪从完整砖墙的 26.5% 图宽缩为靠人物侧 10.5% 图宽，约 20 DIP；窗口位置和透明悬停按钮都改为按裁剪后的实际变换边界计算，左右镜像后仍紧贴屏幕；
- 同步动画映射、产品行为、技术架构、默认场景规则和 ADR-030；打包脚本默认版本更新为 `0.1.0.11`。

### 验证与交付

- Release 构建成功，0 警告、0 错误；自动化测试 265/265 通过（Core 155、Animation 30、Windows 80），新增 12 个非对称路线、非法帧顺序和底部无字收回测试；
- 隔离 QA 真实透明窗口验证：右侧隐藏只留贴屏约 20 DIP 窄墙，悬停完整显示“偷看”及官方人物终帧；左侧镜像同样贴屏并可唤出；底部逐帧采样确认收回过程没有文字，最终猫耳/头发与任务栏上沿对齐；
- Windows Graphics Capture 对透明窗口仍返回 `0x80004002`，因此沿用只读屏幕区域采样；未向网易云、QQ、微信或其它应用发送输入，未保存 QA 设置；
- 生成绿色包 `LuoTianyiPet-Portable-Test-0.1.0.11-win-x64.zip`，大小 104,213,221 字节，SHA-256 为 `42f005199b9f2527ee6c292871d224db65ded3ced3ba41bb2366bad0e0bae206`；
- 生成并安装 `LuoTianyiPet_0.1.0.11_x64.msix`，大小 103,176,159 字节，SHA-256 为 `e869b128e7b89de671728f91971c118506ee84df2cef10efc639045fa96149c0`；安装包状态为 `Ok`，应用启动响应正常，通知权限为 `Allowed`，启动日志无新增 `ERROR`。

### 下一步

由用户在实际指针拖拽中验收左右/底部边缘手感；重点观察完整含字展开切换到无字收回时的视觉接受度，再决定是否需要额外的过渡帧。

### Git

- 计划提交消息：`fix: complete edge peek reveal`。

## 2026-09-01：微调边缘动画方向速度与墙面宽度

### 用户体验调整

- 用户认为底部下钻仍略快，要求在当前基础上再慢 0.2 倍；左右钻出略慢，要求加快 0.2 倍；隐藏后的侧墙还需要更窄。

### 实现

- `AnimationFrameTimeline` 和 `AnimationFramePlayer.PlayRange` 增加仅作用于本次范围播放的速率参数：逐帧时长按速率缩放且最短保持 1 ms，不修改素材图集、清单或其它场景；
- 左右钻出第 3→19 帧使用 1.2 倍速，左右收回第 7→3 帧保持 1.0 倍速；底部下钻第 5→3 帧使用 0.8 倍速，底部钻出第 3→7 帧保持 1.0 倍速；
- 左右砖墙裁剪宽度从 10.5% 降至 7.5%，190 DIP 显示尺寸下约 14 DIP；悬停按钮继续严格跟随可见裁剪面；
- 同步默认场景规则、产品行为、动画映射、技术架构和 ADR-031；打包脚本默认版本更新为 `0.1.0.12`。

### 验证与交付

- Release 构建成功，0 警告、0 错误；自动化测试 270/270 通过（Core 155、Animation 35、Windows 80），新增 0.8/1.2 倍帧时长和非法速率测试；
- 隔离真实透明窗口逐帧取样确认：底部以无文字完整人物开始，连续经过半身、头部到猫耳隐藏终帧；右侧从约 11～14 个可见像素的砖墙连续钻出人物和“偷看”文字，1.2 倍速下无跳帧；
- 生成绿色包 `LuoTianyiPet-Portable-Test-0.1.0.12-win-x64.zip`，大小 104,213,420 字节，SHA-256 为 `d36e85b20d153e96454e0613df67e7bf8d035593f0bc818c967fd339a8ae1b5f`；
- 生成并安装 `LuoTianyiPet_0.1.0.12_x64.msix`，大小 103,176,321 字节，SHA-256 为 `82056e95de31ae3c1e71aef37eee2c11f9b870088e2bb5e9a0fdf8269d467e89`；包状态为 `Ok`，正式进程重新启动且响应正常，通知权限为 `Allowed`，启动日志无新增 `ERROR`。

### 下一步

由用户体验 1.2 倍速左右钻出、0.8 倍速底部下钻和约 14 DIP 墙面；若仍需调整，继续只修改三个独立参数，不改帧路线。

### Git

- 计划提交消息：`fix: tune edge dock animation speed`。

## 2026-09-01：简化拖拽落地并修复完整全身边缘抽搐

### 用户体验问题

- “嘿嘿 → 膨胀 → 降落 → 嘿嘿”每次拖拽都跨三套不同画风素材，松手衔接生硬；用户希望取消普通降落，并让膨胀只出现一次；
- 完整全身模式拖到左右或底部临界位置时，边缘预览素材会改变人物尺寸，旧判定随即重新计算越界并退出预览，造成多张图片反复抽搐。

### 实现

- 迷你拖拽改为单次范围播放 10 帧“心律共鸣·膨胀”，完成后固定末帧；普通松手使用现有短脉冲遮罩恢复“嘿嘿”或拖拽期间更新后的持续状态，不再创建降落反应；
- 新增 Core `DownwardFlingTracker`：总下移至少 72 DIP、最近 180 ms 下移至少 22 DIP 且速度达到 650 DIP/s 时，迷你模式才播放一次“代号洛天依·降落弹跳”和弹簧回弹；音乐、完整全身和边缘收纳不触发；
- 边缘候选缓存进入预览前的拖拽人物本地边界，不再使用切换后的偷看/闪亮登场尺寸；`EdgeDockResolver` 增加 28 DIP 进入、14 DIP 退出的回差判定；
- 隔离边缘 QA 在进入门槛附近自动执行 6 次正负扰动，拖拽 QA 延长到膨胀播放完成后 600 ms，覆盖末帧保持和普通松手恢复；
- 同步默认场景规则、动画映射、需求、技术架构和 ADR-032；打包脚本默认版本更新为 `0.1.0.13`。

### 验证与交付

- Release 构建成功，0 警告、0 错误；自动化测试 278/278 通过（Core 163、Animation 35、Windows 80），新增快速/缓慢/短距离/向上甩动和三侧边缘回差测试；
- 真实透明窗口连续取样确认“膨胀”完整播放一次后末帧保持，普通松手经过短脉冲回到“嘿嘿”，全程没有降落素材；
- 完整全身右侧和底部边缘各执行 6 次门槛扰动，画面保持同一边缘预演帧，日志每次运行只记录一次 `interaction.drag_edge_preview`，随后连续收回，没有完整模型与边缘素材来回切换；
- Windows Graphics Capture 仍对透明窗口返回既有 `0x80004002`，因此使用隔离 QA 和只读窗口区域采样；没有向网易云、QQ、微信或其它应用发送输入，QA 不保存用户设置；
- 生成绿色包 `LuoTianyiPet-Portable-Test-0.1.0.13-win-x64.zip`，大小 104,215,788 字节，SHA-256 为 `699fda92ddae9c6251d221cf138568166cf073d492277fa296830d139831a6a2`；
- 生成并安装 `LuoTianyiPet_0.1.0.13_x64.msix`，大小 103,178,038 字节，SHA-256 为 `d745f290c6f1d7ae01f3f57659b9d023c1e31f327004a419b9df5142ec13a86b`；包状态为 `Ok`，正式进程响应正常，通知权限为 `Allowed`，启动日志无新增 `ERROR`。

### 下一步

由用户实际体验普通拖拽、快速向下甩动和完整全身三侧边缘门槛；若快速下甩太容易或太难触发，只调整距离/速度参数，不恢复普通降落。

### Git

- 计划提交消息：`fix: smooth drag release and stabilize edge preview`。

## 2026-09-01：裁窄“偷看”完整动画并修复拖拽时间戳异常

### 用户体验问题

- 仅在隐藏终帧裁出窄墙，无法去掉角色完整钻出时素材右侧的大半墙体；用户希望由实现侧按视觉效果决定裁剪范围，不照截图红框机械裁切；
- 安装版一次迷你拖拽中出现 `DownwardFlingTracker.Add` 的 `observedAt` 越界异常。原因是同一鼠标移动事件先记录 `now`，随后才开始拖拽并再次读取了略晚的当前时间，第一条移动采样因此可能比起点早数十微秒。

### 实现

- 动画图集编译器新增可选 `cropAfterResize`，要求四个整数坐标、严格位于统一缩放后的帧边界内，并对该动画所有帧应用同一矩形；原始官方 GIF 不移动、不覆盖；
- “十二周年·偷看”在 240×240 缩放后统一裁为 `[0, 0, 196, 240]`，保留“偷看”文字、头发、脸、手及靠人物侧约 20～22 像素墙面，运行时改为 155×190 DIP 等比例显示，避免横向拉伸；
- 隐藏终帧裁剪更新为裁后画面的 90% 起点、9.2% 宽度，实际可见墙边约 14 DIP；左右共享同一图集，左侧继续只做显示层水平镜像；
- `DownwardFlingTracker` 对中途略早的采样时间戳夹到上一采样时间；若松手时间早于最后采样则安全取消本次快速下甩判定，不再让 UI 调度器异常退出；
- 默认场景参数、需求、动画映射、技术架构和 ADR-033 已同步；打包脚本默认版本更新为 `0.1.0.15`。

### 验证与交付

- 重新运行图集编译，只有“十二周年·偷看”派生图集及对应清单发生变化；清单记录 20 帧、196×240 帧尺寸、155×190 DIP 显示尺寸及新 SHA-256；
- Release 构建成功，0 警告、0 错误；自动化测试 280/280 通过（Core 165、Animation 35、Windows 80），新增逆序移动采样和逆序松手两条回归测试；运行时素材 38/38 校验通过；
- 真实透明 WPF 右侧与左侧镜像画面确认：完整展开仍显示“偷看”文字、人物面部和手，外侧大半墙体已删除；收起后只留下约 14 DIP 墙边；拖拽循环与右侧边缘 QA 进程均保持响应，修复后的日志无新增 `ERROR`；
- 生成绿色包 `LuoTianyiPet-Portable-Test-0.1.0.15-win-x64.zip`，大小 104,051,615 字节，SHA-256 为 `21d2c21ee29b03f9daf9327099b6bb64b8b377fdda488d95447460f1330d3456`；
- 生成并安装 `LuoTianyiPet_0.1.0.15_x64.msix`，大小 103,013,125 字节，SHA-256 为 `d1a19f805f4bd60946adf3c45f3c79e7b49e132208a459ee8ae5c123a377b32f`；包状态为 `Ok`，正式进程响应正常，通知权限为 `Allowed`。

### 下一步

由用户实际体验左右“偷看”完整展开、收回及约 14 DIP 墙边是否自然；若仍需微调，只调整统一裁剪右边界和运行时等比例宽度，不做不可复现的逐帧手工裁图。

### Git

- 计划提交消息：`fix: crop edge peek animation`。

## 2026-09-01：再次微调边缘动画速度

### 用户体验调整

- 用户要求底部下钻在现有基础上再慢 0.1，左右钻出再快 0.1；相反方向和其它边缘行为不变。

### 实现

- 左右“偷看”第 3→19 帧钻出速率由 1.2 调为 1.3；左右第 7→3 帧收回仍保持 1.0；
- 底部“闪亮登场”第 5→3 帧下钻速率由 0.8 调为 0.7；底部第 3→7 帧钻出仍保持 1.0；
- 为 `AnimationFrameTimeline` 补充 0.7 和 1.3 倍速的逐帧取整测试；同步默认场景参数、动画映射、需求、技术架构、项目总控和 ADR-034；打包脚本默认版本更新为 `0.1.0.16`。

### 验证与交付

- Release 构建成功，0 警告、0 错误；自动化测试 282/282 通过（Core 165、Animation 37、Windows 80）；
- 真实透明 WPF 右侧 QA 确认 1.3 倍速仍完整走到含“偷看”文字、面部和手部的展开终点；底部 QA 按日志中的收纳开始时刻取样，确认 0.7 倍速下钻完成后仍只保留猫耳/头发触发面；两项进程均保持响应，日志无新增 `ERROR`；
- 生成绿色包 `LuoTianyiPet-Portable-Test-0.1.0.16-win-x64.zip`，大小 104,051,614 字节，SHA-256 为 `72c48777f7f13fe471653526f26fb5577bc8e95242d08301e6cc00ac7cc581a5`；
- 生成并安装 `LuoTianyiPet_0.1.0.16_x64.msix`，大小 103,013,143 字节，SHA-256 为 `77200106de922c7209de7eacd35c26b90297878962b37ca619a5101126c5f90d`；包状态为 `Ok`，正式进程响应正常，通知权限为 `Allowed`，启动日志无新增 `ERROR`。

### 下一步

由用户实际体验左右 1.3 倍速钻出和底部 0.7 倍速下钻；若继续微调，只修改两个独立速率参数，不改变已经验收的帧路线与裁剪。

### Git

- 计划提交消息：`fix: retune edge dock animation speed`。

## 2026-09-01：新增完整全身同模型动作候选

### 用户体验目标

- 用户认为完整全身模式大量切换到其它表情包时画风、服装和人物比例不协调，希望直接在现有完整全身模型上制作几个动作，并放入动画挑选器后再选择。

### 实现

- 确认仓库保留官方 PMX 模型和 32 帧完整全身待机渲染，但没有保留可复现的 MMD/Blender 骨骼渲染环境；因此本轮不伪造局部骨骼动作，也不调用生成式模型重绘角色；
- 新增确定性生成脚本 `make_full_body_action_candidates.py`，以完整全身待机的 480×520 透明帧为输入，以脚底附近为统一轴点，生成轻轻鞠躬、开心跳跃、左右探身、害羞摇摆、惊喜弹起 5 个动态 WebP；
- 所有候选首尾使用同一个源帧，保持角色、服装、画布和透明背景不变；腾空幅度经过两轮边界调整，最终全部帧的非透明边界离画布顶端至少 5 像素；
- 更新官方模型派生动画 README，记录来源、能力边界和重新生成命令；动画挑选器新增 5 张带勾选框、放大预览和“新添加”标记的候选卡片，候选总数由 65 增至 70，“新添加”由 18 增至 23；
- 同步项目总控、动画映射、完整全身调研、技术架构和 ADR-035。本轮只增加候选素材，不改变现有身体点击映射、运行时动画清单或安装版。

### 验证

- 逐个解析 5 个 WebP：帧数分别为 32、32、40、48、28，帧时长分别为 105、90、95、85、90 ms；所有文件均为 480×520 透明动画；
- 5 项首尾帧像素完全一致，保证循环或返回待机时没有首尾跳变；各项相对首帧均有显著像素变化，确认不是静态复制；全部逐帧 alpha 边界位于画布内，没有裁切头发或脚部；
- 动画挑选器 HTML 标签解析通过；5 个候选 ID 唯一、源文件均存在，且每项都包含 `newlyAdded:true`。应用内浏览器因 `file://` URL 安全策略拒绝自动刷新，未绕过限制；用户手动刷新现有标签页即可动态预览；
- .NET 运行时代码和配置未改变，因此安装版保持 `0.1.0.16`；Release 全量构建以 0 警告、0 错误通过，282 项回归测试全部通过（Core 165、Animation 37、Windows 80）。

### 下一步

由用户在动画挑选器中预览并勾选希望保留的同模型动作，同时决定它们分别用于普通身体点击、脸部互动、消息提醒还是随机待机；随后只编译选中的动作进入运行时。

### Git

- 计划提交消息：`feat: add full-body action candidates`。

## 2026-09-01：接入完整全身拖拽“双脚乱甩”精细骨骼动作

### 用户体验目标

- 用户希望完整全身模式不再依赖整图轻微变换，而是使用真正能看出肢体动作的同一官方模型；首项动作确定为拖拽时洛天依双脚乱甩，其余动作后续逐项讨论。

### 实现

- 在 Git 忽略的 `.local-tools/` 固定便携 Blender 4.5.13 LTS 与 MMD Tools 4.5.13，成功导入仓库已有的官方洛天依 V4 公式服 Q 版 PMX；保留了 166 个原生骨骼、28 个表情及原材质/贴图；
- 新增 PMX 检查、相机探针、骨骼动作生成和 PNG 序列封装脚本。动作以 32 帧、24 fps 驱动双腿、膝、脚踝、躯干、手臂、头部、发辫及眨眼/张口表情，Eevee 以 960×1040 透明帧渲染，再确定性缩为 480×520 无损动态 WebP；
- 动态 WebP 的 SHA-256 为 `a30c91e961bc665d606c214033cbb85195e028f4215313dd890f5229f9c38e66`，alpha 联合边界位于 `(61, 15, 419, 480)`，没有裁切；相邻帧和首尾均有连续变化；
- 新动作编译为 `official-v4-chibi-full-body-drag-leg-flail` 运行时图集并接入状态机：完整全身模式拖拽时循环，松手不播放降落并恢复全身待机；音乐播放期间仍由音乐动画优先；
- 动画挑选器新增“完整全身模型 · 拖拽双脚乱甩（精细骨骼）”并标记为“新添加/已使用”，总数增至 71，“新添加”增至 24；同步场景规则、动画映射、技术架构、调研、ADR-036、项目总控和打包默认版本。

### 验证与交付

- 浏览器直接加载新 WebP，确认透明背景、人物完整和动画连续推进；32 帧均为 480×520、42 ms，无静态复制或画布越界；
- 运行时素材编译器验证 39/39 项通过；Release 全量构建 0 警告、0 错误，自动化测试 282/282 通过（Core 165、Animation 37、Windows 80）；
- 真实 WPF QA 日志确认完整全身拖拽进入新骨骼动画，松手后恢复全身待机且不插入降落；正式安装版载入 39 项动画；
- 生成并安装 `LuoTianyiPet_0.1.0.17_x64.msix`，大小 107,685,646 字节，SHA-256 为 `0abc4ea13c35903b9a78059003faef3394f873d822b002fdbea33f93469f74e0`；包状态为 `Ok`，正式进程响应正常，通知权限为 `Allowed`，启动日志无新增 `ERROR`。

### 下一步

由用户实际体验全身模式拖拽的动作幅度、节奏和松手恢复。下一项建议优先讨论“挥手问候”或“伸懒腰”，二者都能复用同一 PMX 骨骼流水线，并保持完整全身模型画风统一。

### Git

- 计划提交消息：`feat: add rigged full-body drag animation`。

## 2026-09-01：增加精细模型动画专用任务交接提示词

- 新增 `docs/精细模型动画任务开场提示词.md`，集中记录项目根目录、官方 PMX、已导入 `.blend`、便携 Blender/MMD Tools、骨骼报告、现有动作脚本、输出目录和示例命令；
- 明确新任务只负责一次一个的精细动作讨论、骨骼/Morph 制作、透明 WebP 候选和浏览器视觉 QA；未经用户确认不修改桌宠正式状态映射或安装包；
- 固化 960×1040 透明渲染、480×520 无损 WebP、脚底锚点、首尾连续、alpha 边界、穿模/裁切检查和可复现 Python 关键帧脚本等质量要求；
- 将该提示词加入项目总控的权威文档索引，方便无历史上下文的新任务直接接手现有制作环境。

### Git

- 计划提交消息：`docs: add rig animation task handoff prompt`。

## 2026-09-01：收敛动画映射并延长音乐暂停反馈

### 用户体验调整

- 取消八周年点赞、十周年旋转舞、九周年点赞、我推、真好和加载中摇摆的当前触发用途，素材继续作为候选保留；
- 修正十二周年素材包中“抱抱/在吗”文件名与实际画面互换的问题；
- 音乐暂停改为“没睡醒呼吸”约一分钟，并要求用户可双击提前收起且不误切“嘿嘿/完整全身”模式；
- 夜间启动与 15 分钟睡眠素材互换，取消苏醒弹出；黑化、重罪分别调整为 0.5、0.7 倍速；原神启动“你不许玩”由三轮改为一轮并保持末帧，总展示约 5 秒。

### 实现

- Core 新增 `MusicPaused` 持续状态和正式暂停动画映射；空闲规则保留该状态，60 秒 WPF 计时结束后才恢复用户原显示模式；暂停画面可见时的双击只调用提前收起，不修改持久化显示模式；
- `MusicAudioActivityDetector` 增加用户暂停快速确认：桌宠刚发送暂停命令后的 2 秒内，首个可靠静音样本即可确认停止；通用 1 秒无声宽限仍用于外部变化和切歌加载间隙，不盲目提前翻转按钮；
- 普通身体映射收敛为唯一“十二周年·抱抱”。逐帧核对发现 `11_抱抱.gif` 实际显示“在吗”、`12_在吗.gif` 实际显示“抱抱”，因此只纠正运行时与挑选器引用，不移动或重命名原素材；
- 新增“没睡醒呼吸”单帧图集和持续呼吸/浮动程序动效；“晚安”改为 20:00～06:00 启动问候，“眠了”改为 15 分钟睡眠。正式启动问候也适用于用户主动启用的 Windows 开机自启动；系统恢复与睡眠输入恢复只还原持续状态；
- 切歌和文件处理不再播放“加载中摇摆”，只保留文字/投放反馈；“苏醒弹出”和六项取消素材均留在候选库，未删除源文件；
- “你不许玩”图集改为单轮，动画完成后保持最后一帧直到开始后约 5 秒再释放临时置顶；重罪帧时长改为 143 ms（约 0.7 倍速），黑化改为 200 ms（0.5 倍速）；
- 同步示例规则、需求、动画映射、架构、项目总控、挑选器和 ADR-037；打包脚本默认版本更新为 `0.1.0.18`。

### 验证与交付

- Release 全量构建成功，0 警告、0 错误；自动化测试 286/286 通过（Core 169、Animation 37、Windows 80）；新增覆盖暂停快速确认、暂停状态映射/模式保持、空闲规则和普通身体唯一候选；
- 运行时素材重新编译并通过 40/40 校验；清单确认没睡醒 1 帧无限持续、你不许玩 10 帧单轮、重罪每帧 143 ms、黑化每帧 200 ms；动画挑选器内联 JavaScript 和示例 JSON 均通过语法解析，`git diff --check` 无内容错误；
- 隔离原神 QA 窗口日志在反应开始约 5 秒后记录临时置顶释放，其中约 0.47 秒用于进入过渡、末帧保持后正常恢复；没有启动、读取或操作原神；
- 隔离暂停动画窗口成功启动并载入 40 项素材，暂停图集视觉核对为正确“没睡醒”角色。Windows Graphics Capture 对透明窗口仍连续返回既有 `0x80004002`，未取得自动窗口截图；
- 生成绿色包 `LuoTianyiPet-Portable-Test-0.1.0.18-win-x64.zip`，大小 108,697,336 字节，SHA-256 为 `3a10e1ddd4e9a10b274c7621a5cdf85efaa99b6f6ee5d94d5911c3e0821da33a`；
- 生成并安装 `LuoTianyiPet_0.1.0.18_x64.msix`，大小 107,679,590 字节，SHA-256 为 `f842e9e927958fadf5365cba4e9342c6c0ba9c96a68078491f28908d911f612e`；包状态为 `Ok`，新版正式进程响应正常，载入 40 项动画、通知权限为 `Allowed`，启动日志无新增 `ERROR`。

### 未验证与下一步

- 需要用户在真实网易云中执行一次播放→暂停，确认快速确认手感、暂停一分钟与双击提前收起；夜间问候、真实 15 分钟睡眠和下一次原神进程启动彩蛋也留待自然场景验收。

### Git

- 计划提交消息：`feat: refine pause and reaction animations`。

## 2026-09-01：接入“给我”文件投放与 Windows 回收站

### 用户体验目标

- 用户从资源管理器或桌面拖起真实文件并进入桌宠窗口后，播放一次“心律共鸣·给我”，动画结束定格最后一帧；
- 文件移动到人物画面并停留片刻后松手，即移入 Windows 回收站，而非永久删除。

### 实现

- 将“给我”原始官方 GIF 编译为 `resonance-give-me` 运行时图集，单轮播放完成后由帧播放器自然保持末帧；离开窗口或投放完成后才释放本次状态机反应；
- WPF 主窗口启用标准 OLE 拖放，只接受 `DataFormats.FileDrop`/`CF_HDROP` 的真实本地路径；文件进入窗口的透明预留区域即可触发提示，但只有指针位于当前可见人物画面且累计停留 400 ms 才接受松手；
- 新增 Core `IRecycleBinService` 和 Windows `WindowsRecycleBinService`。平台实现新建独立 STA 线程，调用 Shell `IFileOperation`，设置 `FOF_ALLOWUNDO`、`FOFX_RECYCLEONDELETE`，并在返回后检查源路径以区分成功、取消和部分失败；
- 成功播放“心律共鸣·OK”并提示可从回收站恢复；失败或取消保留原文件，失败播放“呜呜晃动”。文件夹和超过 10 项要求二次确认；一次最多 100 项；网络位置、磁盘根目录、无效/消失路径在进入 Shell 前拒绝；
- 受保护游戏前台、任意全屏、锁屏/挂起、边缘隐藏和桌宠自身拖拽时禁用接收。日志只记录数量和结果，不记录文件名或完整路径；没有 `File.Delete`、命令行删除或永久删除降级。

### 验证

- Debug 与 Release 全量构建成功，0 警告、0 错误；自动化测试 290/290 通过（Core 169、Animation 37、Windows 84）；新增覆盖空投放、相对路径、不存在路径和网络路径的 Shell 前拒绝；运行时素材编译器验证 41/41 项；
- 使用唯一自动生成的临时文本文件执行真实 `IFileOperation` 集成探针：结果为 `Success`，源路径消失且操作进入 Windows 回收站；探针测试随后从常规回归集移除，避免每次测试污染回收站；
- `resonance-give-me` 图集共 10 帧，透明画面和“给我”文字完整，末帧可用于等待定格；
- 尝试用 Windows Graphics Capture 捕获透明 QA 窗口，两次均返回既有 `0x80004002`，因此真实资源管理器拖入时的透明窗口命中、定格和气泡布局仍需用户用无用临时文件验收。
- 生成绿色包 `LuoTianyiPet-Portable-Test-0.1.0.19-win-x64.zip`，大小 109,073,457 字节，SHA-256 为 `dbcdd70a7b54aab6c21c927ff94edcaf607c9d0a63e91e74f5ce703ad56c4444`；
- 生成并安装 `LuoTianyiPet_0.1.0.19_x64.msix`，大小 108,057,802 字节，SHA-256 为 `7025ec0c39b706489eb8bb65870b2916a26c7328387559973de9b1bab6c8c31d`；包状态为 `Ok`，正式进程响应正常、载入 41 项动画、通知权限为 `Allowed`，启动日志无新增 `ERROR`。

### 未完成与下一步

- 尚未加入设置页总开关和首次使用说明；下一步先生成测试包供真实拖放验收，通过后再补设置入口。

### Git

- 计划提交消息：`feat: recycle files dropped onto pet`。

## 2026-09-01：播放按钮接入网易云安全冷启动与静态等待

### 用户体验

- 网易云尚未运行时点击播放/暂停，不再直接发送一个无人接收的全局快捷键；桌宠先打开网易云并进入等待；
- 等待阶段复用“心律共鸣·加载中”单帧 PNG，但删除旧 `BodyReactionMotion` 的左右摇摆，只静止显示，并持续显示“正在打开网易云音乐”文字；
- 冷启动约 2 秒后若仍未有声，至多发送一次用户配置的播放快捷键；Core Audio 检测到实际有声后直接切换到当前歌曲应有的音乐动画；30 秒仍未播放则呜呜失败并恢复原状态。

### 实现与安全

- Core 新增 `IMediaApplicationLauncher` 及结构化启动结果；Windows 新增 `WindowsMediaApplicationLauncher`，先检查目标进程，再复用前台受保护进程失败关闭规则；
- 可执行文件优先从 Windows App Paths 注册项解析，只以 Program Files、Program Files (x86) 和 LocalAppData 下明确的 Netease/CloudMusic 常见目录兜底；不扫描磁盘、不调用私有 URI、不修改或读取网易云私有数据；
- 只有用户点击播放且进程不存在时启动；网易云已运行时仍使用原播放/暂停快捷键链路。启动等待使用状态机一次性反应，完成、失败、超时和窗口关闭都会取消自己的等待任务并恢复正确持续状态；
- 音乐检测被用户关闭时，应用启动和播放快捷键仍可执行，但不会伪报已播放，文字明确说明无法确认状态。

### 验证与交付

- Debug 与 Release 构建均为 0 警告、0 错误；自动化测试 297/297 通过（Core 169、Animation 37、Windows 91）；新增覆盖已运行不重复启动、受保护前台、前台查询失败、找不到可执行文件、正常启动、进程查询异常和注册路径解析异常；
- 运行时资产校验 41/41 通过；`resonance-loading-sway` 确认为 1 帧 180×180 透明图集。隔离 QA 窗口正常响应并载入静态画面；Windows Graphics Capture 仍两次返回既有 `0x80004002`，未取得透明窗口自动截图；
- 本机只读验证 App Paths 指向存在的 `C:\Program Files\Netease\CloudMusic\cloudmusic.exe`。为避免主动改变用户音乐状态，本轮没有自动启动网易云或发送真实播放快捷键，真实冷启动留给用户主动体验；
- 生成绿色包 `LuoTianyiPet-Portable-Test-0.1.0.20-win-x64.zip`，大小 109,077,100 字节，SHA-256 为 `1999d6191138dcda103f75aa6f01435e2046a9882bca8b03e1d29d6971331019`；
- 生成并安装 `LuoTianyiPet_0.1.0.20_x64.msix`，大小 108,061,612 字节，SHA-256 为 `ae2b7b7858465869ddeaa495861d91b88461b2a04d0195395233853242b0616b`；包状态 `Ok`，正式进程响应正常、载入 41 项动画、通知权限 `Allowed`，启动日志无新增 `ERROR`。

### 下一步

由用户退出网易云后点击桌宠播放键，真实验收 2 秒启动时机、持续文字、网络较慢时的 30 秒等待以及有声后音乐动画切换。

### Git

- 计划提交消息：`feat: launch cloudmusic from play control`。

## 2026-09-01：时间问候持续 30 秒、撤销全身专用拖拽并补充普通听歌候选

### 用户体验

- 四段时间问候在用户启动、Windows 开机自启动和桌宠运行中跨过 06:00、12:00、13:30、20:00 时持续展示 30 秒，不再只走一轮短动作；
- 完整全身模式拖拽只移动窗口并保持原完整全身待机画面，不再切换“双脚乱甩”或其它专用全身拖拽动作；
- 动画挑选器彻底移除轻轻鞠躬、开心跳跃、左右探身、害羞摇摆、惊喜弹起、拖拽双脚乱甩和拖拽恐慌前后摆动 7 项；
- 新增普通歌曲听歌候选“九周年·太好听了”“收藏集·给我听”“古色花香·我不听”，统一标为新添加；拿麦克风的“一起唱”不混入听歌候选。

### 实现

- Core 新增 `TimeSceneTransitionTracker`，只在四个半开时间段发生变化时产出一次边界决定；WPF 使用独立 1 秒计时器观察本地时间，不依赖长待机计时器；
- 时间问候以 `TimeGreeting` 优先级进入状态机，轻浮动、弹跳或呼吸程序动效循环 30 秒后恢复当前持续状态。边界问候若被更高优先级事件拒绝会保留为待处理请求，安全空闲后重试；
- `PetVisualState` 将完整全身 `Dragging` 解析为完整全身待机；删除运行时配置、清单和生成图集 `official-v4-chibi-full-body-drag-leg-flail`，安装内容不再包含该动画；原始候选与可复现 PMX 流水线未删除；
- 挑选器增加 `motion-listen` 轻点头预演与三张普通歌曲候选说明。`古色花香·我不听`来自 Bilibili 官方表情包镜像；其它两项复用仓库已保存的官方表情包原图。

### 验证与交付

- Release 全量测试成功：Core 172、Animation 37、Windows 91，共 300/300 通过；新增覆盖 19:59:59→20:00 边界、同一时段不重复、首次观察只建基线及完整全身拖拽保持原画面；
- 运行时资产编译并验证 40/40 项；动画挑选器 1 段内联 JavaScript 语法解析成功，共 69 项，7 个弃用标题均不存在，3 个新听歌候选均存在；示例 JSON 可解析，`git diff --check` 通过；
- 安装版 `0.1.0.21` 状态为 `Ok`，正式进程响应正常并载入 40 项动画。日志记录 `time.startup_greeting` 于 10:26:33 开始、10:27:03 完成，实际约 30 秒；启动阶段无新增 `ERROR`；
- 绿色包 `LuoTianyiPet-Portable-Test-0.1.0.21-win-x64.zip` 为 104,422,277 字节，SHA-256 `7bd0aba2d95de17f8b93eb009bc67cab33123879f9de5ce1ed3d337ff391bc81`；
- 安装包 `LuoTianyiPet_0.1.0.21_x64.msix` 为 103,391,145 字节，SHA-256 `cbce42b16062ab2b6b1122bf87b014a65c84f9faf0f7adda832c445d0d2d0ebf`；两个包的压缩内容检查均未发现 7 个弃用动作或旧拖拽图集；
- 应用内浏览器安全策略禁止自动刷新并读取本地 `file://` 挑选器，因此未取得整页自动截图；改用离线脚本、路径和单图检查。用户浏览器中的本地标签页未被关闭。

### 下一步

- 用户在动画挑选器比较 3 个普通歌曲候选，确认哪一项用于持续陪听、歌曲开始或低频彩蛋；之后再写入正式音乐状态机。

### Git

- 计划提交消息：`feat: extend time greetings and refine drag visuals`。

## 2026-09-01：新增官方表情片段组合的“奔向并吃掉文件”候选

### 候选方案

- 从官方收藏集“来喽”提取前扑姿势，制作桌宠奔向桌面文件的位移预演；正式接入时移动整个桌宠窗口，而不是让人物只在固定窗口内滑动；
- 从洛天依 11 周年 QQ 动态表情“干饭”中截取一轮完整的拿起、送入口中和咀嚼动作，去掉原素材的长停顿和后续重复进食；
- 在进食片段上叠加独立文件图标，使它向嘴边移动并缩小；另外生成“奔向 → 吞下”的完整串联预演；
- 三项均只加入动画挑选器并标为“新添加”，尚未改变正式文件投放状态机或发布包。

### 文件与可追溯性

- 新增 `候选素材_官方/10_新添加_文件吞食组合候选/`，保存官方 11 周年源 GIF、三个预演 GIF 和来源制作说明；
- 新增可重复脚本 `tools/animation_tools/make_file_eating_candidates.py`；角色画面只做裁帧、缩放、位移与叠图，不生成或重绘新人物；
- “桌面自行删除后跑过去吃”继续作为独立后续需求记录：只能使用公开 Shell/目录观察，不注入资源管理器、不安装全局 Hook，也不执行第二次删除。

### 验证

- 三个透明动态 WebP 预演文件均成功生成：奔向 1,139,944 字节、吞下 1,433,400 字节、完整流程 2,906,918 字节；官方源 GIF 4,559,217 字节；
- 动画挑选器内联 JavaScript 语法检查通过，数据项 ID 共 90 个且无重复，三个新增候选和文件路径均存在；
- 应用内浏览器对本地 `file://` 页面仍按安全策略拒绝自动刷新，未绕过限制；用户手动刷新现有标签页即可播放预演。

### 下一步

- 用户先在动画挑选器比较三项预演；确认画风和节奏后，再把“拖到桌宠上吃掉”接入现有回收站成功链路，并单独评估桌面删除位置观察。
## 2026-09-01：接入 AI“吃包子”视频与可拖动目标追逐

- 验收根目录 `吃包子.mp4`：720×720、24 fps、121 帧、约 5.04 秒；动作覆盖静止、奔跑、停下、张嘴、保持、闭嘴，确认可用于正式交互。
- 保留原视频副本至 `候选素材_官方/11_新添加_AI生成文件吞食动画/`；新增 Blender 全帧提取和 Pillow/NumPy 转换脚本，输出透明奔跑/进食图集、包子道具、来源哈希和挑选器动态预览。
- 去除生成视频的渐变红底与角落移动水印；虎牙曾在首轮处理时修补，后按用户决定恢复原片；运行时不引入视频解码器。
- 新增桌面项目消失观察、UI Automation 图标位置缓存、可拖动包子窗、最近目标追逐、方向镜像、进食飞入、返回原位和多目标排队。
- 文件直接拖到桌宠并成功进入 Windows 回收站后改为进食演出；失败路径保持原有安全反馈，不提供永久删除回退。
- 设置页新增独立开关；追逐过程每 500 ms 执行前台安全复查，全屏、受保护游戏或查询失败立即取消并恢复。
- Release 全量构建 0 警告、0 错误；Core 新增追逐规划与桌面前台安全测试；Animation 37、Core 178、Windows 平台 91 项，共 306 项自动化测试全部通过。
- `--qa-bun-chase --qa-window` 真实透明窗口验证了奔跑、张嘴、独立包子飞入与返回原位，测试进程全程存活且日志记录 `file_treat.completed`。
- 修复首轮实测发现的桌面误判：Windows 桌面前台属于覆盖整个显示器的 `explorer.exe`，旧安全判断将其当成全屏应用并静默抑制回收事件；现在只对桌面/资源管理器放行该尺寸特例，浏览器、视频、游戏等其它全屏进程以及受保护游戏仍然失败关闭，并增加回归测试与抑制日志。

## 2026-09-01：修复吃包子动画失色、透明嘴腔与进食落点

- 抠色阶段增加角色封闭轮廓保护：背景仍从画面边缘拟合并透明化，但嘴腔、眼睛等轮廓内部的相近暖色不再被误当背景；同时回退临时轮廓膨胀，避免角色外沿残留红边。
- 重新生成奔跑、进食图集和动画挑选器预览，并同步资源哈希；抽样检查张嘴保持帧的嘴部与眼部 alpha 均为 255，画面角落 alpha 为 0。
- 包子飞入终点从人物高度 43% 的眼睛附近校准至图像宽度 60%、高度 53.5% 的张嘴中心；面向左侧时同步镜像为宽度 40%，并补充分辨率无关的双方向 Core 回归测试。
- Release 构建 0 警告、0 错误；自动化测试 308/308 通过（Core 180、Animation 37、Windows 91）。

## 2026-09-01：恢复 AI 吃包子动画的原始 sRGB 亮度

- 定位人物整体偏暗并非抠色阈值，而是 Blender 4.x 默认 AgX 在视频转 PNG 时额外执行了色调映射；旧帧的人物区域平均亮度约 150，Standard sRGB 帧约 172。
- 全部 121 帧改用 sRGB 输入、sRGB 显示设备、Standard 视图变换、None Look、零曝光和 1.0 Gamma 重新提取；不使用人工亮度或饱和度补偿。
- 重新生成透明奔跑/进食图集与挑选器预览；张嘴保持帧嘴部 alpha 仍为 255、画面角落 alpha 为 0，嘴部中心标定无需改变。
- 去除 AI 原片脚底的深色椭圆投影：仅清除画面下部与拟合背景色相一致、亮度比例为 0.55–0.95 的阴影像素，保留蓝色鞋面、黑色鞋底和脚部线稿；九个进食关键帧在蓝底合成检查中均无残留地板。
- 第二轮实测发现双脚之间仍有一个被鞋底包围的深色压缩残块；确认 66 个进食帧中它稳定落在归一化画布 `[140,228,149,240]` 的空隙，增加鞋间专用透明遮罩。10 倍像素放大检查显示左右鞋外轮廓和中央腿部黑线均保持连续。
- 刘海三角形复核表明原 MP4 不含该图形，它来自旧“去虎牙”算法把 Standard sRGB 红底误判为嘴部后画错补色位置。按用户决定移除全部牙齿修补逻辑并保留原片小虎牙；奔跑帧不再出现刘海三角形，张嘴帧的小虎牙、嘴腔 alpha 与鞋间透明区同时通过放大检查。

## 2026-09-02：用用户手工抠像序列替换 AI 吃包子旧素材

### 素材整理

- 接收用户在 After Effects 中完成的 121 帧 `720×720 RGBA` PNG 序列，整理至 `候选素材_官方/11_新添加_AI生成文件吞食动画/源素材_吃包子_手工抠像PNG/`；逐帧确认均含透明 Alpha，动作覆盖静止、奔跑、张嘴保持和闭嘴；
- 删除候选目录内旧 AI MP4、旧自动扣色预览以及仓库根目录重复 MP4；小笼包独立道具、交互代码和其它动画均未改动；
- 新挑选器预览改为 `预览_吃包子_手工抠像版.webp`，卡片明确标注手工抠像、保留嘴巴和小虎牙，并链接到新序列首帧。

### 构建与可追溯性

- `process_bun_video.py` 默认直接读取透明 PNG Alpha，只做联合裁切、Lanczos 等比缩放、底部对齐和图集打包；旧自动扣色路径保留为显式 `--input-mode auto-key` 兼容模式，不会再误伤手工修好的区域；
- 重新生成 14 帧循环奔跑与 66 帧单次进食图集；源序列聚合 SHA-256 为 `302e3c7de41b90e825ea3ff7a136c03ef4800a4155af0ffae0e368c930abc19e`，运行时图集哈希和裁切参数已同步至清单与 `bun-eat.meta.json`；
- 动画状态映射、项目总控和动画挑选器均改为以新 PNG 序列为权威来源，不再描述旧渐变背景自动扣色流程。

### 验证

- Python 脚本语法检查、121 帧数量/尺寸/Alpha 校验、源序列聚合哈希、两张图集哈希和挑选器新旧路径检查全部通过；抽查奔跑和进食关键帧的透明棋盘合成，人物、嘴巴、小虎牙与脚间区域完整；
- Release 全量测试通过 308/308（Core 180、Animation 37、Windows 91）；Release 全量构建 0 警告、0 错误；
- `--qa-bun-chase --qa-window` 实际 WPF 演出成功载入 42 项动画，进程保持响应并记录 `file_treat.completed`，没有新增错误；Windows Graphics Capture 仍以已知 `0x80004002` 拒绝该透明窗口截图，因此最终透明边缘手感仍需用户在正式桌宠中肉眼验收。

### Git

- 计划提交消息：`feat: replace bun animation with hand-keyed frames`。

## 2026-09-02：替换完整全身待机并保留原交互契约

### 素材与实现

- 将仓库根目录的 `新全身.png` 原样归档为 `候选素材_官方/04_官方模型派生动画/用户提供_新全身.png`，确认归档前后 SHA-256 均为 `b2bfc2d8cd4afe0bac3513db877b79fa7155d66f782259305c75b02979f25511` 后删除根目录重复文件；原官方模型待机素材继续保留，未覆盖；
- 新增 `prepare_full_body_idle.py`：只将与源画布四周连通的近白背景转为 Alpha，封闭在线稿内的白色裙装、鞋和浅色头发保持前景；边缘做 2 像素软化和白底去污染，再等比例装入原 480×520 画布，人物底部锚点、220×238 DIP 显示槽均不变；
- 生成透明处理图、单帧运行时图集及来源/输出哈希、背景阈值、裁切和缩放元数据；保留 `official-v4-chibi-full-body-idle` ID，因此双击模式切换、完整全身拖拽、音乐临时覆盖、边缘隐藏和一次性反应恢复不增加特殊分支；
- 依据新画面重新校准左右眼、嘴、脸、左右手、胸部、下体、左右脚、头顶和普通身体的归一化矩形；现有“心软软、贴贴、啾啾、击掌、重罪/黑化、糟了、摸摸与抱抱”语义不变；动画挑选器首项同步为新立绘并标记“新添加/已使用”。

### 验证

- 处理脚本通过 Python 语法检查；源/输出/图集 SHA-256、RGBA 模式、480×520 尺寸、单帧清单和 Alpha 边界 `[44,10,436,509]` 全部一致；黑色透明预览中人物完整，白裙、白鞋、发梢和线缆未被误删；
- AssetCompiler 校验 42/42 项运行时动画，新的完整全身待机为 1 帧、220×238 DIP；Release 全量测试通过 308/308（Core 180、Animation 37、Windows 91）；Release 全量构建 0 警告、0 错误；
- `--preview-full-body --preview-body-hit-debug --qa-window` 真实 WPF 实例持续响应；Computer Use 的 Windows Graphics Capture 仍以项目已知的 `0x80004002（不支持此接口）` 拒绝透明窗口截图，因此本轮以运行进程、静态透明图和命中框叠图完成机器侧检查，最终点击手感仍需用户在正式桌宠中确认。

### Git

- 计划提交消息：`feat: replace full-body idle artwork`。

### 下一步

- 用户双击“嘿嘿”进入新完整全身模式，确认比例、透明边缘及各身体区域触发；如有区域偏差，只调整 `BodyHitMap`，不改变对应动画语义。

## 2026-09-02：动画挑选器改为待删除清单并补齐正式用途总览

### 页面行为

- 将每张卡的“保留这个动画”改为“删掉这个动画”；勾选只加入浏览器本地的待删除清单，不删除、移动或改写任何素材；页面、提示和导出文本均明确只有收到用户后续通知才开始实际清理；
- 顶部增加“目前已经使用的动画与位置”总览，集中显示 35 张正式使用卡片及触发说明；卡片继续按已使用优先排序，并可用“只看当前已使用”筛选；
- 已使用动画也允许加入待删除清单，便于用户表达替换意愿，但卡片立即显示“删除前必须先解除代码和配置引用”的警告；新增“只看待删除”和“清空待删除清单”，清空操作不影响用途分配和联动场景；
- 本地状态升级到版本 4，字段由 `keptItems` 改为 `deletionItems`。旧版本 3 的用途分配和联动场景会迁移，但旧“保留”勾选不会反向解释成删除，以免误标整批素材；JSON 导入同样安全迁移旧版本并建立空待删除清单；
- 补齐此前被统一映射覆盖而未显示“已使用”的“心律共鸣·给我”、AI 奔跑与吃包子、网易云冷启动“加载中”三项；当前用途文本与正式状态映射保持一致。

### 验证

- 新增 `tools/animation_tools/validate_animation_picker.mjs` 离线回归检查；内联 JavaScript 语法、卡片渲染及旧状态迁移均通过，解析到 91 个动画卡片 ID、35 个正式使用映射，全部映射均指向存在的卡片且没有重复 ID；
- 离线检查确认新版必需文案“删掉这个动画”“只看待删除”“目前已经使用的动画与位置”“尚未删除任何素材”均存在，旧“保留这个动画”“只看已保留”“勾选保留的动画”均已清除；`git diff --check` 通过；
- Release 全量构建 0 警告、0 错误；自动化测试 308/308 通过（Core 180、Animation 37、Windows 91）；
- 应用内浏览器安全策略拒绝自动刷新本地 `file://` 标签页，本轮未绕过；用户在现有标签页手动刷新后即可查看新版。

### Git

- 计划提交消息：`feat: turn animation picker into deletion review`。

### 下一步

- 用户在挑选器勾选不需要的动画并导出结果；只有收到明确删除通知后才执行素材和引用清理。

## 2026-09-04：整理完整全身身体互动的视频提示词

### 目标与设计

- 核对当前完整全身模式的身体命中映射，将眼睛“心软软”、嘴部“啾啾”、脸部“贴贴”、左右手“击掌回弹”、普通身体“抱抱”、敏感区域首次“重罪”、短时间再次“黑化”、双脚“糟了晃动”和往返摸头九类反应，改写为同一完整全身模型原地连续做动作的视频生成方案；
- 新增 `docs/完整全身互动视频提示词.md`，提供九段可直接复制的中文提示词、文件命名、推荐生成参数、统一负面约束和后续拆帧接入流程；
- 所有提示词要求首帧保持待机、动作完成后准确回到同一待机姿势并再次停留，固定人物尺寸、脚底锚点和摄像机，避免现有“突然切换到另一张表情包”的割裂感；
- 背景统一为纯洋红 `#FF00FF`，禁止地板、阴影和背景变化，方便后续色键、边缘去污和透明图集生成；视频不包含鼠标，点击判定仍由桌宠程序完成；
- 先生成贴贴、击掌回弹和糟了晃动三段覆盖头部、手部大动作和全身重心的样片，验证角色一致性后再生成剩余六段，减少无效生成成本。

### 验证

- 已逐项对照 `BodyHitMap`、`BodyInteractionResolver`、自动化测试和动画挑选器的正式用途说明，九类提示词与当前触发语义一致；
- 已查看新完整全身待机透明图及七项动态表情的抽帧接触表，提示词保留原表情语义，但不复制文字、第二人物或不适合桌宠衔接的画面结构；
- 本轮只新增和更新设计文档，没有修改运行时代码或素材；Markdown 路径、编号和文件名已人工复核；Release 构建通过，0 警告、0 错误，自动化测试 308/308 通过（Core 180、Animation 37、Windows 91）。

### Git

- 计划提交消息：`docs: add full-body interaction video prompts`。

### 下一步

- 用户先按提示词生成贴贴、击掌回弹和糟了晃动三段样片，放入项目根目录 `待处理_AI互动视频`；收到后进行画面一致性和可抠像性检查。
## 2026-09-04：Q 版小人替换默认紧凑模式并重排待机时间

### 实现

- 识别并纳入用户新增的 `候选素材_官方/Q版小人全身.png`；确认源文件为 1254×1254 RGB PNG，透明棋盘格已烘焙进像素而非真实 Alpha；原文件不改名、不覆盖；
- 为 `prepare_full_body_idle.py` 增加可选 `--background-minimum` 参数，默认值仍为 248，因此此前完整全身素材可复现结果不变；新 Q 版源图使用 240 阈值，只移除与画布边缘连通的近白棋盘格，生成 `assets/animations/processed/用户提供_Q版小人全身_透明.png`、元数据与 `user-chibi-compact-idle` 运行时图集；
- `PetDisplayMode.Compact` 改为 `user-chibi-compact-idle`，双击切换另一侧的 `official-v4-chibi-full-body-idle`、身体区域、拖拽、音乐覆盖和边缘收纳逻辑保持不变；
- `mediumIdle` 门槛由 5 分钟改为 3 分钟，动画由“十二周年·躺平”改为“心律共鸣·嘿嘿”；`sleeping` 门槛由 15 分钟改为 30 分钟，继续使用实际源素材为“代号洛天依·眠了”的低幅呼吸；
- 从运行时配置、清单和安装内容移除“躺平”派生图集，官方原始 GIF 仍留在候选素材；同步更新示例配置、动画挑选器的当前用途与候选说明，以及需求、映射、架构和总控文档。

### 验证

- 新透明图已肉眼检查：棋盘格背景去除，白色头发、衣服、鞋子和黑色外轮廓完整；透明画布为 480×520，Alpha 边界 `[58,11,421,509]`，显示槽为 180×195 DIP；
- AssetCompiler 校验 40 项运行时动画，新 `user-chibi-compact-idle` 为 1 帧 180×195 DIP，“twelfth-anniversary-lie-flat”已不在运行时清单；
- Release 构建通过，0 警告、0 错误；自动化测试 309/309 通过（Core 181、Animation 37、Windows 91）；
- `--qa-window` 实例实际载入 40 项动画并持续响应，系统真实空闲达到约 180.5 秒时记录 `animation.medium_idle_started`；`--qa-long-idle` 以精确 1800000 ms 触发 `animation.long_idle_sleep_started`，随后恢复 Idle，验证 3 分钟与 30 分钟边界已进入 WPF 主链路；
- 修改后的透明处理脚本以默认参数重新处理上一张完整全身源图，输出 SHA-256 仍为 `e5f1e27bdca7b9c68ef1b102772514be69e4444544dae29ad3bd2871c9c60048`，证明新增阈值参数没有改变旧素材的默认可复现结果；
- 本轮 Computer Use 环境没有暴露原生 WPF 应用表面，无法自动截图透明窗口；未绕过限制。机器侧已完成透明图肉眼检查、实际进程响应、清单加载和状态日志验证，最终显示尺寸与双击手感留给用户直接观察。

### Git

- 计划提交消息：`feat: replace compact idle with user chibi`。

### 下一步

- 用户在正式桌宠中确认 Q 版小人的尺寸、双击切换和两级待机节奏；通过后继续完整全身 AI 动作样片处理。

## 2026-09-04：修复网易云退出后播放按钮无法再次启动

### 实现

- 根据用户复现检查本机进程，确认网易云主界面退出后仍残留 3 个 `cloudmusic.exe`，其主窗口句柄均为 0；旧实现只按进程名判断，因而误走“已经运行→发送快捷键”分支；
- `WindowsMediaApplicationLauncher` 改为只在至少一个目标进程拥有可交互主窗口时返回已运行；仅存在无窗口的常驻、GPU 或渲染子进程时重新通过 App Paths 启动应用，并复用既有静态加载画面、2 秒快捷键延迟和 30 秒有声等待；
- 仍保留前台安全检查、明确安装路径解析和失败关闭，不枚举模块、不读取网易云内存，也不强行结束任何残留进程。

### 验证

- 新增平台回归测试，覆盖“三个同名后台进程全部无主窗口时重新启动”和“任一实例有主窗口时视为可控制”两种边界；
- 本机只读检查确认当前 3 个残留 `cloudmusic.exe` 的 `MainWindowHandle` 均为 0，能够进入新判定的重新启动分支；
- Release 全量构建 0 警告、0 错误，完整自动化测试 311/311 通过（Core 181、Animation 37、Windows 93）；真实重新拉起会继续发送播放快捷键并改变用户音乐状态，因此不由自动验证擅自触发，留给用户主动点击验收。

### Git

- 计划提交消息：`fix: relaunch cloud music after exit`。

### 下一步

- 用户退出网易云主界面后再次点击播放/暂停，确认应用重新打开并在等待后开始播放。

## 2026-09-05：替换默认 Q 版立绘并增加区域专属鼠标手型

### 实现

- 将用户提供的 `exec-88ec6204-fbcf-48e5-ac73-a75e8d06dcbf.png` 原样归档为 `候选素材_官方/Q版小人全身_新默认.png`，通过既有边缘连通近白背景流程替换 `user-chibi-compact-idle` 的派生 PNG 和图集；稳定动画 ID、画布、显示大小、双击切换和完整全身模式不变；
- 将另外两张透明 PNG 原样归档到 `候选素材_官方/12_用户提供_鼠标指针`；新增 `prepare_cursor_asset.py`，确定性生成 64×64 预览、32 位 `.cur` 与哈希/热点元数据；
- 新增 Core `PetCursorResolver`：透明像素使用系统默认光标，人物不透明像素使用普通指向手型，只有完整全身身体互动启用且命中头发/头顶时使用摸头手型；运行资源缺失时回退系统 Hand；
- WPF 在普通 MouseMove 中即时更新 `PetImage.Cursor`，不要求按下鼠标，不修改媒体控制岛光标，不注册全局鼠标 Hook；窗口关闭时释放两份自定义光标资源；动画挑选器的默认待机卡同步指向新图。

### 验证

- 新 Q 版透明派生图肉眼检查通过：480×520 画布，Alpha 边界 `[33,11,447,509]`，白发、白裙、发梢和轮廓保留；输出 SHA-256 为 `7172316173330020fabdd7ea0b626bc4c043e6fcbf7a51d744d0fa781c5199cc`；
- 两份 `.cur` 均由 WPF `System.Windows.Input.Cursor` 实际加载成功；普通指向热点为 `[13,7]`，摸头热点为 `[3,24]`；
- 新增 4 项 Core 光标解析测试；AssetCompiler 校验 42 项运行时动画，动画挑选器离线校验 92 张卡片及 35 项正式用途通过；Release 全量构建 0 警告、0 错误，完整自动化测试 315/315 通过（Core 185、Animation 37、Windows 93）；
- 正式桌宠进程已启动并持续响应，载入 42 项动画，日志没有 `cursor.asset_unavailable`，确认输出目录内两套 CUR 被应用成功找到；透明 WPF 窗口的最终指针视觉与热点手感由用户直接悬停确认。

### Git

- 计划提交消息：`feat: add themed pet cursors`。

### 下一步

- 用户实际悬停普通人物像素和完整全身头顶，确认两套手型大小、热点和切换范围；如需调整，只改 CUR 缩放与热点参数，不改变身体命中语义。

## 2026-09-05：移除系统主音量设置并停用全局音量运行路径

### 实现

- 设置窗口删除系统主音量滑块、桌宠滚轮控制、外部系统音量反馈和步长控件；标题说明改为只管理桌面行为与消息提醒；
- 正式运行不再创建或注入 `ISystemVolumeService`，主窗口删除滚轮音量事件、端点通知、轮询、防抖动画和关闭清理路径，因此旧配置即使仍启用也不会读取或修改系统主音量；
- 核对项目历史原型与微软公开 Core Audio 接口，确认可按 `cloudmusic.exe` PID 枚举一个或多个音频会话，并通过 `ISimpleAudioVolume` 只调网易云在音量合成器中的独立音量；本轮不恢复控制岛音量图标，也不擅自决定新的控制入口；
- 同步更新需求、动画映射、安全约束、技术架构、路线图、总控、README 和示例规则；旧系统音量模型、平台实现与 JSON 字段暂时保留用于兼容已有设置文件。

### 验证

- Release 全量构建通过，0 警告、0 错误；
- 设置窗口 XAML 不再包含“系统音量”、滚轮步长或外部音量反馈控件，正式窗口也不再注册 `PreviewMouseWheel`；
- Computer Use 环境本轮没有暴露原生 WPF 应用表面，无法自动取得设置窗口截图；隔离 `--qa-settings --qa-window` 进程已成功启动并保持响应，最终 DPI 视觉仍需用户直接查看；
- 完整自动化测试 315/315 通过（Core 185、Animation 37、Windows 93）。
- 动画挑选器离线校验 92 张卡片、34 项正式用途通过；“共鸣之声”已改为未分配的网易云独立音量反馈候选。

### Git

- 计划提交消息：`refactor: remove system master volume controls`。

### 下一步

- 用户确认是否把网易云独立音量绑定到“鼠标位于桌宠上时滚轮调整”，或仅放入设置页；确认后恢复应用会话音量服务并补充多会话、会话重建和设备切换测试。

## 2026-09-05：增加角色/吃包子外观选择与托盘尺寸滑杆

### 实现

- 新增版本 10 `AppearancePreferences`，持久化完整全身外观、吃包子画风和 50%～200% 显示倍率；版本 1～9 自动迁移，未知样式回退当前默认，越界倍率安全夹取；
- 设置页以运行时图集真实帧显示三套完整全身外观：“模式一（心率共鸣完整Q版）”“模式二（心率共鸣简约Q版）”“模式三（心率共鸣表情包版）”。新图与经典图使用独立 220×238 DIP 运行时 ID，状态机把所选动画保存在 `PetVisualState`，身体互动按外观切换归一化命中图；
- 用户新增的 121 帧粉色背景序列保留在候选目录，通过逐帧背景拟合、封闭区域保护和角落标记移除生成 `ai-bun-v2-*` 图集；设置页以“心率表情追包版”V1 和“心率简约追包版”V2 二选一，回收站、包子追踪和安全取消路径完全复用；
- 托盘菜单用内嵌 TrackBar 增加“显示大小”滑杆。拖动实时乘到所有动画的清单显示尺寸，鼠标/键盘确认时保存；媒体控制岛保持固定尺寸，身体命中继续使用归一化坐标；
- 动画编译器支持从处理元数据注册多套预构建图集，重新编译普通素材时不会再丢失 V1/V2 吃包子动画；动画挑选器增加经典全身和 V2 吃包子卡片，并标明设置名称与触发位置。

### 验证

- 新版 V2 逐帧图集肉眼检查：粉色背景与右下角标记已移除，人物头发、脸和张嘴区域保留；经典 Q 图透明派生画布为 480×520，Alpha 边界 `[58,11,421,509]`；
- `compile_animation_atlases.py` 从头重建清单成功，隔离 `--qa-settings --qa-window` 实例载入 46 项动画并保持响应；当前 Computer Use 仍未暴露原生 WPF 表面，无法自动截图设置窗口，最终卡片间距和托盘滑杆手感留给用户直接观察；
- Release 全量构建 0 警告、0 错误；完整自动化测试 321/321 通过（Core 190、Animation 37、Windows 94）；动画挑选器离线校验 94 张卡片、34 项正式用途通过。

### Git

- 计划提交消息：`feat: add selectable pet appearances`。

### 下一步

- 用户在设置页依次切换三套完整全身和两套吃包子画风，并从托盘试用 50%～200% 尺寸；根据实际屏幕观感再微调各外观命中区域和默认倍率。

## 2026-09-05：修复 Win+D 隐藏和 Win+Tab 暴露桌宠窗口

### 实现

- 新增可复用的 `DesktopToolWindowBehavior`：窗口句柄创建后设置 `WS_EX_TOOLWINDOW` 并清除 `WS_EX_APPWINDOW`，正式桌宠与临时包子目标均不进入任务栏或任务切换器；
- 正式桌宠拦截 `WM_SYSCOMMAND/SC_MINIMIZE`，并在 WPF 意外进入 `Minimized` 时用调度器恢复，避免 Win+D“显示桌面”把桌宠一起收起；
- `--qa-window` 等显式任务栏 QA 模式保持原有普通窗口行为，便于自动截图和调试。

### 验证

- Release 构建通过，0 警告、0 错误；完整自动化测试 321/321 通过（Core 190、Animation 37、Windows 94）；
- 对正式进程枚举 HWND，验证扩展样式 `0x00080080`：`ToolWindow=True`、`AppWindow=False`；
- 向主窗口发送与系统最小化相同的 `WM_SYSCOMMAND/SC_MINIMIZE`，500 ms 后仍为 `Visible=True`、`Minimized=False`。

### Git

- 计划提交消息：`fix: keep desktop pet out of task switchers`。

### 下一步

- 用户实测 Win+D、Alt+Tab 和 Win+Tab；如多显示器或第三方桌面管理器仍发送非标准隐藏消息，再按实际消息轨迹补兼容分支。

## 2026-09-05：设置页启用 Win11 风格网易云独立音量

### 实现

- 恢复独立的 `IApplicationVolumeService` 领域接口与 Windows Core Audio 适配器；每次读取和写入均枚举所有活动 Render 输出设备，以 `cloudmusic.exe` PID 匹配全部应用音频会话，并通过 `ISimpleAudioVolume` 同步调节；
- 设置窗口增加 Win11 风格圆角轨道、天依蓝进度和圆形滑块，显示当前百分比、匹配会话数量、不可用原因及“重新检测”；入口只在设置页，不恢复桌宠控制岛音量图标，也不绑定鼠标滚轮；
- 网易云没有创建会话时禁用滑杆；音频服务异常、系统拒绝、前台检测失败或受保护游戏前台时均失败关闭，绝不退回系统主音量，也不读取音频数据；
- 增加 `--qa-application-volume`，仅在显式隔离 QA 中连接真实网易云会话；普通 QA 不接触真实音量。

### 验证

- Release 全量构建通过，0 警告、0 错误；完整自动化测试 331/331 通过（Core 196、Animation 37、Windows 98）；
- 真实网易云播放会话测试：通过设置滑杆将 `cloudmusic.exe` 会话从 100% 调到 99%，再恢复 100%；同一测试前后系统主音量均为 100%，确认没有改变端点主音量；
- UI Automation 确认设置页存在独立音量标题、Win11 应用音量说明、百分比、状态、重新检测按钮和安全边界说明，并成功通过滑杆的 RangeValue 模式完成上述真实写入；
- `git diff --check` 与示例场景 JSON 解析通过；最终 Release 全量构建为 0 警告、0 错误。

### Git

- 计划提交消息：`feat: control cloudmusic session volume`。

### 下一步

- 用户在设置页实际拖动网易云独立音量，并在蓝牙耳机、切换默认输出设备或网易云重启后点“重新检测”，确认多设备手感与状态提示。

## 2026-09-05：拆分全身外观触摸策略并增加音乐动画选择

### 实现

- 三套完整全身外观不再共用身体表情包：模式一（心率共鸣完整Q版）关闭身体触摸；模式二（心率共鸣简约Q版）标记为同模型连贯动作待接入，当前不会回退旧表情包；模式三（心率共鸣表情包版）继续启用既有眼睛、嘴、脸、头顶、手、敏感区域、脚和普通身体映射；
- `PetVisualState` 与状态机增加当前全身外观是否具备可执行互动的能力位；切换外观会同步更新能力位、命中图和光标语义，音乐、拖拽与双击模式切换保持原状；
- 设置页增加音乐播放动画下拉框：默认每次随机，也可固定循环“心律共鸣·享受音乐”或“九周年·音乐摇摆”；正在播放时保存选择会立即切换，开始、恢复和切歌按新偏好重新解析；
- 循环候选集中在 `MusicAnimationOptions.FixedOptions` 登记，设置界面从登记表动态生成固定选项，后续新增动作不需要复制窗口分支；设置架构升级到版本 11，版本 10 自动迁移为随机。

### 验证

- Release 构建通过，0 警告、0 错误；完整自动化测试 339/339 通过（Core 203、Animation 37、Windows 99）；
- 新增测试覆盖三套外观互动策略、关闭/开启身体命中、随机和两种固定音乐选择、非法选择回退，以及版本 10 配置迁移；
- 隔离设置窗口的 UI Automation 可访问树确认三项策略分别显示“展示模式·不响应触摸”“连贯互动·AI 动作待接入”“表情包互动·当前可用”；音乐下拉框确认包含随机及两个固定循环选项，保存路径记录 `settings.media_applied`；
- 示例场景 JSON 解析和 `git diff --check` 通过；最终 Release 全量构建为 0 警告、0 错误，完整自动化测试仍为 339/339。

### Git

- 计划提交消息：`feat: customize pet interaction and music loops`。

## 2026-09-05：限定“嘿嘿”的外观作用域

### 实现

- 将 3 分钟“心律共鸣·嘿嘿”限定为完整全身模式中的“模式三（心率共鸣表情包版）（Q 版小人全身待机）”；模式一（心率共鸣完整Q版）、模式二（心率共鸣简约Q版）和紧凑模式在 3～30 分钟继续显示自身待机；
- `IdleSceneResolver` 新增可测试的中等待机资格参数，30 分钟“眠了呼吸”不受影响；
- 双击离开模式三（心率共鸣表情包版）或在设置中改选其他外观时立即清理现有 `MediumIdle`，避免“嘿嘿”短暂残留。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 343/343 通过（Core 207、Animation 37、Windows 99）；
- 新增测试覆盖禁用中等待机后的 3～29 分钟状态、30 分钟统一睡眠，以及已处于 `MediumIdle` 时的即时恢复；示例场景 JSON 解析与 `git diff --check` 通过。

### Git

- 计划提交消息：`fix: scope hehe idle to classic chibi`。

## 2026-09-05：校准经典猫耳命中区域并加入竖向网易云音量

### 实现

- 按当前经典猫耳透明立绘重新标定眼睛、嘴、脸、双手、胸部、下体、双脚、头发和普通身体区域，保留小区域优先与 Alpha 像素过滤；
- 悬浮控制岛由三键扩展为四键，最右侧天依蓝扬声器按钮向上展开竖向滑杆；打开时读取真实网易云会话值，拖动只写入 `cloudmusic.exe` 会话；
- 音量弹层打开期间暂停控制岛自动隐藏；会话不存在、受保护游戏前台或音频服务不可用时显示安全失败提示，不写系统主音量。

### 验证

- Release 构建通过，0 警告、0 错误；自动化测试 355/355 通过（Core 219、Animation 37、Windows 99）；
- `PrintWindow` 实际渲染确认命中叠层贴合当前人物，四键控制岛不遮挡角色；UI Automation 成功找到并调用“网易云音乐独立音量”，弹层实际渲染为天依蓝竖向滑杆且读取本机网易云为 100%。

## 2026-09-05：外观改为双击三项循环并联动吃包子版本

### 实现

- 移除设置页三套人物与两套吃包子动画的预览选择卡；新增“允许双击循环三种外观”和“启用吃包子功能”两个开关；
- 正式启动直接进入完整全身显示，双击按华裳长发、晶蓝礼服、经典猫耳顺序循环并持久化；关闭循环后固定当前项；
- 前两套人物自动使用心率简约追包版，经典猫耳自动使用心率表情追包版；吃包子总开关同步拦截桌面删除追包及拖到桌宠回收。

### 验证

- Release 构建通过，0 警告、0 错误；自动化测试 363/363 通过（Core 226、Animation 37、Windows 100）；
- `--qa-settings --qa-window` 隔离启动后设置窗口进程持续响应，日志确认动画目录和设置窗口均成功加载；示例场景 JSON 与 `git diff --check` 通过。

## 2026-09-05：边缘收纳改为人物三分之一越界

### 实现

- 使用动画清单 `alphaBounds` 换算当前缩放、布局和变换后的真实人物边界，普通贴边和松手回正不再受透明画布留白影响；
- 左右/底部只有实际人物至少三分之一越界才显示钻入预演，回退门槛为四分之一；拖拽越界空间随显示倍率扩展；
- 边缘 QA 入口同步改为按当前人物宽高计算门槛，不再写死 28 DIP。

### 验证

- Release 构建通过，0 警告、0 错误；自动化测试 370/370 通过（Core 233、Animation 37、Windows 100）；
- 新增 200 与 400 DIP 人物尺寸的三分之一边界、左右/底部方向及四分之一回差测试；右侧真实 WPF QA 成功记录预演与收纳开始，进程持续响应。

## 2026-09-05：膨胀拖拽限定为第三种外观

### 实现

- 将“心律共鸣·膨胀”从旧显示模式规则改为外观能力：仅第三种“模式三（心率共鸣表情包版）”拖拽时播放，模式一（心率共鸣完整Q版）与模式二（心率共鸣简约Q版）保持各自立绘；
- 每次拖拽只允许启动一次膨胀，播放结束停在末帧；若中途进入又退出贴边预览，则直接恢复末帧，不从头重播；
- 音乐播放拖拽继续保持本次音乐动画，达到三分之一越界后的边缘预览继续覆盖普通拖拽画面。

### 验证

- Release 构建通过，0 警告、0 错误；自动化测试 374/374 通过（Core 237、Animation 37、Windows 100）；
- Core 新增三套外观与非法值的膨胀能力测试；示例场景 JSON、动画挑选器和状态映射同步到外观规则，挑选器校验确认 94 项素材、34 项已使用。

## 2026-09-05：收纳门槛改为四分之一并消除模式三贴边留缝

### 实现

- 左右与底部收纳的进入门槛由可见人物三分之一降为四分之一，取消候选的回差由四分之一降为六分之一；贴住边缘仍不会直接收起；
- 模式三（心率共鸣表情包版）拖拽虽然显示“心律共鸣·膨胀”，边缘判定与普通松手夹回改为投影经典猫耳本体的 Alpha 可见边界，避免两套画布尺寸干扰位置；
- 动画尺寸切换时按清单 Alpha 边界计算允许越界的透明左、右和底部留白，模式三恢复后脚底及侧边可与工作区准确贴合；
- 边缘 QA 同步改为使用目标外观的桌面可见边界，不再按当前膨胀图的局部尺寸模拟越界。

### 验证

- Release 构建通过，0 警告、0 错误；自动化测试 374/374 通过（Core 237、Animation 37、Windows 100）；
- 200% 模式三（心率共鸣表情包版）的底边和右边实窗 QA 均记录到膨胀单次拖拽、四分之一越界预演和边缘收纳，进程持续响应且无新增错误；
- 示例场景 JSON、`git diff --check` 与动画挑选器离线校验通过。

## 2026-09-05：统一模式三（心率共鸣表情包版）与膨胀的显示尺寸

### 实现

- 将 `resonance-expand` 从 180×180 DIP 调整为与模式三（心率共鸣表情包版）一致的 220×238 DIP，拖拽开始、结束时主窗口不再缩小或放大；
- 图集编译器新增可复现的 `padAfterResize` 透明补边，162×162 原帧放入 162×175 画布，保持人物原始比例而不纵向拉伸；
- 模式三播放膨胀时继续借用模式三（心率共鸣表情包版） Alpha 边界约束窗口，贴边状态不会因膨胀画面更宽而被推回屏幕内。

### 验证

- 重新生成运行时膨胀图集和哈希清单，确认帧画布 162×175、显示尺寸 220×238、Alpha 边界 0,9,162,171；
- Release 构建通过，0 警告、0 错误；自动化测试 374/374 通过（Core 237、Animation 37、Windows 100）；
- 在当前 200% 显示倍率下连续采集模式三拖拽前、膨胀播放中及恢复后的 22 个实窗矩形，全部保持 456×602 像素；启动素材尚未加载时的默认占位尺寸不计入动画切换。

## 2026-09-05：新版吃包子改为直接保留用户透明通道

### 修复

- 确认 `新版/7e845b0a115897207a7fb4cbb5859654/` 的 121 张 PNG 已带有用户处理好的 Alpha；透明区域虽然仍保存粉棕色 RGB，但不应参与画面合成；
- 撤销 V2 的自动颜色抠像，改为以 `transparent` 模式直接保留每帧原始 Alpha 与可见颜色，仅执行联合裁切、等比缩放和图集打包；
- 重新生成 15 帧奔跑、66 帧进食图集及动画挑选器预览，衣服、双脚与头发边缘不再混入粉棕色背景。

### 验证

- 121/121 源帧均确认含有效透明通道；重建元数据记录 `inputMode: transparent`，源序列哈希保持 `fb1e9bc49c0f0157e1b8e0ffd74ec61c8c43bf7e8f962ce7121d971deecab038`；
- 运行时图集逐帧拼版检查通过，奔跑和进食人物颜色与源透明 PNG 一致，透明背景无棕色块残留。
- Release 构建通过，0 警告、0 错误；自动化测试 374/374 通过（Core 237、Animation 37、Windows 100），动画挑选器离线校验通过。

## 2026-09-05：新增身体区域描边编辑器

### 实现

- 新增“候选素材_官方/身体区域描边编辑器.html”单文件离线工具，预载模式一（心率共鸣完整Q版）、模式二（心率共鸣简约Q版）和模式三（心率共鸣表情包版）三套透明运行时人物图；
- 将现有 12 类矩形命中区域作为每套外观的初始轮廓；用户可选择部位后重画或添加多块多边形，闭合后拖动顶点微调，并即时查看鼠标当前命中的最高优先级区域；
- 草稿按外观保存在浏览器本地；支持导入、复制、下载版本化 JSON，也可导出人物底图与彩色区域合成的 PNG，方便用户直接指示划分结果；
- 新增 validate_body_region_editor.mjs，校验内联脚本语法、DOM 元素引用、12 类区域和三张预设图片。

### 验证

- 执行 node tools/animation_tools/validate_body_region_editor.mjs 通过：24 个元素 ID、23 个脚本引用、12 类区域、3 套预设，脚本语法正常；
- 内置浏览器因安全策略禁止自动导航到 file 地址，未绕过限制；编辑器的实际点击、描点和浏览器下载流程留给用户打开本地文件后验收；
- Release 构建通过，0 警告、0 错误；自动化测试 375/375 通过（Core 238、Animation 37、Windows 100）；
- 本轮只增加离线编辑工具与文档，不改变正式桌宠 BodyHitMap 或运行时行为。

## 2026-09-05：修复模式三左右边缘预演抽搐

### 修复

- 模式三拖拽期间从“膨胀”切换到左右“偷看”或底部预演时保持拖拽开始时的外窗边界，仅更新人物图像，消除素材尺寸变化反向影响下一次边缘判定；
- 分数回差改为锁定当前边缘候选：只要当前方向仍达到六分之一释放线，就不允许屏幕角落的另一个方向抢占；明确退回后才重新选择；
- 新增角落双方向同时达到四分之一时的候选稳定性测试。

### 验证

- Release 构建通过，0 警告、0 错误；自动化测试 375/375 通过（Core 238、Animation 37、Windows 100）；
- `--preview-full-body --qa-edge-dock=right/left --qa-window` 在模式三真实 WPF 窗口中分别跨越门槛往返采样，日志各只记录一次对应方向预演；拖拽阶段外窗保持 236×364 像素，松手进入正式收纳后才调整为偷看窗口。

## 2026-09-05：两套吃包子动画提高清晰度并补帧

### 实现

- 两套 720×720 手工透明源序列不再先压到 256×256，运行时帧统一提升到 384×384；默认 205 DIP 更清晰，200% 显示时也只需轻微上采样；
- 构建工具新增双向光流 2 倍补帧：奔跑从 14/15 帧提升到 28/30 帧，进食从 66 帧提升到 132 帧，帧时长由 42 ms 改为 21 ms，总时长不变；
- 中间帧使用预乘 Alpha 合成，透明边缘不混入源图透明像素中的粉棕色 RGB；放弃会产生人物双重轮廓的普通交叉叠帧；
- 动画挑选器预览由隔帧 84 ms 改为保留全部源帧 42 ms，预览观感由约 12 FPS 恢复为原始约 24 FPS；新增锁定版本的离线生成依赖清单。

### 验证

- 逐帧拼版确认 V1/V2 的原始帧与光流中间帧轮廓连续，嘴巴、衣服、双脚和透明边缘无新增色块；
- 重建两套运行图集、预览、元数据及总动画清单，元数据记录 384×384、`flow2x` 和 21 ms；
- Release 构建通过，0 警告、0 错误；自动化测试 375/375 通过（Core 238、Animation 37、Windows 100），动画挑选器离线校验通过；
- `--qa-bun-chase --qa-window` 实际 WPF 演出成功完成新版追包、进食和返回流程，测试进程持续响应并记录 `file_treat.completed`。

## 2026-09-05：清理模式二（心率共鸣简约Q版）头发间隙的假透明棋盘

### 修复

- 重新核对 `候选素材_官方/Q版小人全身_新默认.png`，确认源文件整张 Alpha 均为 255，用户指出的两侧灰白棋盘属于烘焙背景；
- `prepare_full_body_idle.py` 新增可重复指定的 `--interior-background-box`，只在限定源像素区域内补充清除近白、低色差背景，不直接擦除矩形；
- 第二版登记左右两侧头发间隙区域并重建 `用户提供_Q版小人全身_透明.png`、兼容图集、晶蓝礼服正式图集及动画总清单；候选原图保持原样。

### 验证

- 将新派生图分别合成到深蓝与洋红背景，确认两侧灰白棋盘已由背景色穿透，蓝黄装饰线、发丝和人物其它区域仍在；
- `--preview-full-body --qa-window` 以用户当前晶蓝礼服设置成功载入 46 项动画，真实 WPF 窗口持续响应；
- Release 构建通过，0 警告、0 错误；自动化测试 375/375 通过（Core 238、Animation 37、Windows 100），动画挑选器离线校验通过。

## 2026-09-06：取消“没睡醒呼吸”暂停反馈

### 实现

- 音乐暂停确认后直接恢复用户当前选择的待机外观，移除 `MusicPaused` 持续状态、一分钟计时器、暂停专用呼吸动效和双击提前收起分支；
- 从运行时动画源配置、生成清单和安装资产中移除“九周年·没睡醒呼吸”，官方原图继续保留在候选素材库；动画挑选器标记为“已取消使用”，并从正式已使用统计和暂停角色默认分配中撤下；
- 保留暂停命令的 2 秒快速确认、普通静音 1 秒宽限和 30 分钟“代号洛天依·眠了呼吸”长待机。

### 验证

- Release 构建通过，0 警告、0 错误；自动化测试 373/373 通过（Core 236、Animation 37、Windows 100）；动画挑选器离线校验通过并确认正式已使用项为 33；
- 重建动画清单后确认运行时共 45 项且不含“没睡醒”，候选官方原图仍存在，“眠了呼吸”图集仍在清单；
- `--preview-music-transition --qa-window` 真实 WPF 进程成功载入 45 项动画并进入音乐动画，进程持续响应。后续桌面窗口控制检查被用户按 Escape 主动停止，因此未继续自动点击“模拟停止音乐”；暂停恢复路径由 Release 编译、Core 状态测试及代码检查覆盖。

## 2026-09-06：接入第三模型用户描边区域

### 实现

- 归档用户导出的“模式三（心率共鸣表情包版）”区域 JSON、标注 PNG 和说明；应用构建时把 JSON 复制为 `assets/body-hit-maps/full-body-classic.json`，不改写用户源文件；
- Core 新增归一化多边形、边界点判定、多个轮廓组合及 schemaVersion 1 JSON 严格解析；第三模型按用户给定的 12 个区域与优先级加载，文件异常时安全回退旧矩形；
- WPF 身体点击、摸头移动、专属摸头光标和调试描边统一使用多边形命中，调试层直接绘制真实轮廓而不是外接矩形；增加隔离 QA 外观参数，验证时可指定第三模型而不修改用户设置。

### 验证

- Release 构建通过，0 警告、0 错误；自动化测试 389/389 通过（Core 252、Animation 37、Windows 100）；
- 自动化逐项验证用户 12 个区域的代表点、三个旧矩形内但用户多边形外的排除点，以及“多边形不等于外接矩形”；脸部的两个分离轮廓按同一区域载入；
- Release 输出已包含 `assets/body-hit-maps/full-body-classic.json`；使用 `--preview-body-hit-debug --qa-full-body-style=full-body-classic-cat-ears --qa-window` 启动真实 WPF 调试层，日志确认载入 12 区域 Polygon 命中图且进程持续响应；
- 已核对用户导出的标注 PNG：五官、双手、敏感区域、双脚、头发及普通身体轮廓与正式 JSON 一致。实际逐部位点击手感仍由用户在正常桌宠中继续验收。

## 2026-09-06：接入第二模型区域与七组连续动作

### 实现

- 原样归档用户导出的模式二（心率共鸣简约Q版）区域 JSON、标注 PNG 和说明，构建时复制为 `assets/body-hit-maps/full-body-crystal.json`；第一模型明确保持无区域互动；
- 接入 7 组各 145 帧透明 PNG：眼睛遮眼、嘴巴捂嘴、脸部捏脸、双手比心、双脚摸腿脚、头部摸摸头、OtherBody 捂肚子；胸部和下体没有对应素材，保持无反应且不回退经典猫耳表情包；
- 新增可复现资产准备脚本：保留原生约 24 FPS，以预乘 Alpha 缩放到 256×256，生成 7 个运行图集、7 个挑选器 WebP 和逐序列来源哈希；509 MB 本地母版帧不直接进入 Git；
- 动画挑选器增加七张“已使用”卡片并写明触发部位，晶蓝礼服待机卡改为已接入状态。

### 验证

- Core 新增独立映射测试、第二模型 12 区域代表点及三个多边形外排除点；第一模型能力位仍为关闭；
- AssetCompiler 校验 52 项运行时动画，新增 7 项均为 145 帧、256×256 图集、238×238 DIP 显示和单次播放；动画挑选器离线校验通过，共 101 张卡片、42 项正式用途；
- Release 全量构建 0 警告、0 错误；自动化测试 417/417 通过（Core 280、Animation 37、Windows 100）；
- 7 项动作分别在隔离晶蓝礼服 WPF 实例中成功载入并保持响应；完整跑完“摸摸头”后记录结束回调并恢复待机。日志确认第二、第三模型各自载入 12 区域 Polygon 命中图，未记录新增 ERROR。

## 2026-09-06：吃包子 60 帧补帧、缓起步与嘴部校准

### 实现

- 将离线光流从“只生成中点”扩展为一次求解、多个时间比例重采样；两套奔跑各输出 60 帧，两套进食各输出 173 帧，帧时长为 16 ms（约 62.5 FPS）；
- 奔跑位移由立即 360 DIP/s 改为从 72 DIP/s 平滑加速到 270 DIP/s，返程也使用独立缓启动；增加 Core 速度推进函数，位移距离仍以实际经过时间计算；
- 根据新版张嘴帧重新测量心率简约追包版的包子终点：由错误的画面 50%/30% 改为 62.5%/45.2%，并补上向左时 37.5% 横向镜像。

### 验证

- 两份元数据均记录 `flow60`、60 帧奔跑、173 帧进食、16 ms 帧时长；重建四张 384×384 透明图集及总清单；
- 增加新版嘴部双向镜像和缓起步速度回归测试；运行图集拼版检查显示两套奔跑姿态连续，张嘴段保持透明背景。
- Release 全量构建 0 警告、0 错误；自动化测试 420/420 通过（Core 283、Animation 37、Windows 100）；AssetCompiler 校验 52 项运行时动画，挑选器 101 张卡片/42 项正式用途通过。
- “心率简约追包版”与“心率表情追包版”分别用隔离 `--qa-bun-chase` WPF 进程跑完追逐、进食和返程；两次均记录 `file_treat.completed`、全程 `Responding=True`，未记录新增 ERROR。单次完整加载峰值私有内存约 298–301 MB。

### 未验证与下一步

- 由用户在真实桌面观察缓起步和包子入嘴位置；若仍有少量偏差，只微调分数坐标，不再重建角色 Alpha。

## 2026-09-06：部位动作播放期间禁止双击换装

### 实现

- 为一次性反应请求增加可选的形态切换锁，只在身体区域点击、短按/往返摸头以及身体动作 QA 入口启动时启用；
- 双击循环三套外观前查询当前活动反应，身体动作没有播放完时忽略本次双击，不改变设置、不打断动画、不排队延迟切换；
- 锁与当前反应令牌共用生命周期，完成、过期、替换或取消会自动清除；动作后的 800 毫秒身体互动恢复期不继续锁定双击。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 421/421 通过（Core 284、Animation 37、Windows 100）；
- 新增状态机回归测试，确认带锁的身体反应播放中返回阻止、完成后立即恢复允许；
- 在隔离晶蓝礼服 `crystal-headpat` 真实 WPF 预览中，动作中发送双击只记录 `display.style_cycle_blocked`，动画完整结束后再次双击记录 `display.style_cycled` 并切换到模式三（心率共鸣表情包版）；窗口全程响应，动作画面无中断。

### 下一步

- 由用户在正常桌宠中分别试用第二、第三模型的不同部位动作，确认“动作中忽略、动作后可切换”的实际手感。

## 2026-09-06：左右隐藏墙宽度减半

### 实现

- 左右完全钻入屏幕后，可见砖墙裁剪宽度由裁后画面的 9.2% 调整为 4.6%，起点同步从 90% 移到 92.3%，默认显示约由 14 DIP 降为 7 DIP，并避开人物发梢；
- 保持裁剪起点、完整偷看动画、左右镜像、悬停钻出和底部猫耳逻辑不变；窗口及悬停命中面继续按缩窄后的实际裁剪边界贴屏。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 421/421 通过（Core 284、Animation 37、Windows 100）；
- 分别运行隔离的左、右边缘收纳真实 WPF 窗口，在无文字收回段结束且悬停钻出前抓取屏幕交集；两侧均只显示砖墙细条，没有夹带人物发梢或留下透明缝，进程保持响应；
- 单独检查隐藏第 3 帧的内、中、外三种 9 像素裁剪，最终选择居中的 92.3%～96.9% 墙面区域，避开内侧发梢和外侧圆角。

### 下一步

- 由用户确认约 7 DIP 的墙面是否兼顾纤细外观与屏幕边缘悬停可发现性。

## 2026-09-06：上下边缘悬浮岛统一换位并允许人物贴顶

### 实现

- 将原来的底部二态逻辑扩展为 `Split`、`AbovePet`、`BelowPet` 三态附件布局：中间位置保持歌名在人物上、播放控制在人物下；靠近顶部时人物在上，两座附件在下；靠近底部时两座附件在上，人物在下；
- 两种堆叠布局均固定为歌名/作者在上、播放控制在下，修正原底部布局把播放控制放在歌名上方的问题；
- 新增顶部 80 DIP 判定，并在布局切换时补偿窗口位置，避免拖拽过程中人物跳动；顶部移动下限改按当前动画 Alpha 可见上边界计算，使透明附件区可以越出工作区而人物实际像素仍能贴齐屏幕顶边；
- 增加 `--qa-top-controls` 隔离预览入口，与已有底部预览分别覆盖两种边缘排布。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 424/424 通过（Core 287、Animation 37、Windows 100）；
- 新增顶部距离边界测试，确认 0、80、81 DIP 分别进入、保持和退出顶部附件布局；
- 分别运行顶部与底部隔离真实 WPF 窗口并截图：顶部人物可贴近工作区顶边，歌名和控制岛按顺序位于人物下方；底部歌名和控制岛按同一顺序位于人物上方，人物底边贴近任务栏，两个进程均保持响应。

### Git 提交

- 本轮代码、测试与文档以“统一上下边缘悬浮岛布局并允许人物贴顶”提交并推送。

### 下一步

- 由用户在真实多显示器桌面拖动验收 80 DIP 换位时机；若需要调整，只修改统一边缘距离，不再分叉上下顺序。

## 2026-09-06：第二模型部位动画改为本体原地播放

### 实现

- 为模式二（心率共鸣简约Q版）七项部位动作增加显式原地动画集合；开始播放时直接从当前本体进入动作图集，不再执行通用的 145 ms 缩小、闪光覆盖和 280 ms 弹出过程；
- 动作完成后同步恢复晶蓝礼服待机，不再播放第二次遮罩换图；原有反应令牌、完整播放、双击锁和 800 ms 身体互动恢复期保持不变；
- 七项动作的显示尺寸由 238×238 调整为 244×244 DIP，并按用户显示倍率向下校正 7 DIP。该组合使动作人物的水平中心和脚底与 220×238 DIP 待机本体对齐，窗口虽为容纳动作发丝调整透明画布，但人物本身不再跳位；
- 离线准备脚本、派生元数据和运行时动画清单同步记录新的显示尺寸，后续重新编译素材不会退回旧参数。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 434/434 通过（Core 297、Animation 37、Windows 100）；
- 新增十组原地策略测试，覆盖七个晶蓝礼服动作以及空值、空字符串和普通表情包的失败关闭；
- 使用 `crystal-headpat` 与晶蓝礼服外观运行隔离真实 WPF：`PrintWindow` 分别取得动作中和完成后的透明窗口画面，人物水平居中、脚底保持一致，动作约 6.1 秒完整结束后直接恢复待机；日志只记录 `animation.in_place_reaction_started` 与 `animation.body_reaction_in_place_completed`，没有通用脉冲切换，进程保持响应。

### Git 提交

- 本轮代码、清单、测试与文档以“让第二模型部位动画在本体原地播放”提交并推送。

### 下一步

- 由用户依次点击第二模型七个部位，重点确认其余六项在当前显示倍率下的首尾观感；若某一动作仍有局部偏移，只为该素材增加独立锚点，不恢复通用换图过渡。

## 2026-09-06：消除第二模型动作源首帧造成的瞬时抽搐

### 实现

- 逐帧检查确认七套源 PNG 的第 0 帧已经处于动作姿势，“摸摸头”第一帧已有完整外部手掌；此前取消通用闪光只能消除 WPF 换图效果，无法消除待机轮廓到动作首帧的瞬时差异；
- 资产准备脚本把当前晶蓝礼服待机图按 220×238 与动作 244×244 的真实显示关系重新排入 256×256 动作画布，使切换前后的人物中心、头顶和脚底一致；
- 每套图集的第 0 帧与第 144 帧替换为该精确待机参考，相邻各 6 帧在预乘 Alpha 空间平滑混入/退出原动作。仍保留 145 帧与 42 ms/帧，不截掉动作中段，也不覆盖用户源 PNG；
- 重新生成七套运行时 PNG 图集、七套动画挑选器 WebP、派生元数据及清单 SHA-256。

### 验证

- 抽取“摸摸头”运行图集第 0、1、3、6、138、141、144 帧制作接触表：首帧和末帧均为清晰完整待机，进入与退出轮廓按 6 帧连续变化，没有单帧尺寸跳变或透明边缘暗边；
- 运行清单重新编译成功，七套图集哈希与派生元数据一致；Release 构建及 434 项自动化回归在最终图集生成后再次执行。

### Git 提交

- 本轮脚本、七套派生素材、清单与文档以“平滑第二模型动作首尾帧”提交并推送。

### 下一步

- 由用户在正式桌宠中再次点击第二模型部位，确认 0.25 秒融入是否自然；如仍希望更柔和，只调整统一的 6 帧过渡长度，不再改人物定位或恢复闪光换图。

## 2026-09-06：固定第二模型动作窗口，消除开始时横向瞬移

### 实现

- 确认残余横向瞬移并非动作首帧内容，而是待机 220×238 DIP 与动作 244×244 DIP 两种显示槽造成的透明窗口重设尺寸；靠近边缘或非整数 DPI 时，重新居中与工作区钳位会暴露为左右位移；
- 将七套 720×720 动作源等比装入 240×260 运行画布，清单显示尺寸统一为与晶蓝礼服待机完全相同的 220×238 DIP；动作开始、过程中和恢复时均保持 236×364 的完整 WPF 窗口尺寸；
- 移除主窗口原先用于方形动作槽的 7 DIP 纵向补偿；图集内的精确待机首尾帧和 6 帧预乘 Alpha 融入/退出保持不变；
- 重新生成七套 PNG 图集、动画挑选器 WebP、元数据与清单哈希，源 PNG 不覆盖。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 434/434 通过（Core 297、Animation 37、Windows 100）；
- 清单检查确认七项晶蓝礼服动作全部为 240×260 帧、220×238 DIP 显示；首尾接触表确认动作仍从精确待机连续融入和退出；
- `crystal-headpat` 真实 WPF 的动作中和恢复后原生矩形均为 `1672,644,1908,1008`，位置与 236×364 窗口尺寸逐项一致；`PrintWindow` 画面确认人物水平中心和脚底不变，进程保持响应。

### Git 提交

- 本轮脚本、七套派生素材、运行清单、应用代码与文档以“固定第二模型动作显示槽”提交并推送。

### 下一步

- 由用户在正式桌宠中再次触发第二模型动作；若仍感到动作内部有左右摆动，应区分为源动画自身运动并针对单个动作做稳像，不再调整窗口或全局锚点。

## 2026-09-06：重做设置页并统一悬浮附件离开时序

### 实现

- 设置窗口改为紧凑的天依蓝 Win11 卡片布局：蓝色头部、统一圆角和阴影、MDL2 本地图标、右置开关、分层说明与一致的按钮样式；设置模型和保存语义不变；
- 从设置窗口删除重复的“网易云音乐独立音量”整张卡片和对应读取/写入代码，独立音量只保留在桌宠控制岛最右侧入口；系统主音量仍完全不参与；
- 竖向音量滑杆接管预览鼠标事件：从轨道任意位置按下即可设置数值、捕获鼠标并继续连续拖动，松开后结束捕获，不再要求先抓住圆形滑块；
- 新增 `VerticalRangeMapper`，把顶部为 100%、底部为 0% 的轨道坐标换算和越界夹取从窗口事件中抽离，并用参数化测试覆盖顶部、中点、底部及两端越界；
- 媒体控制岛离开宽限由 220 毫秒改为 5 秒；歌名/作者岛不再在离开时立即隐藏，而是同样重新开始 5 秒倒计时，重新移入会取消倒计时。曲目变化的自动展示也统一为 5 秒。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 439/439 通过（Core 302、Animation 37、Windows 100）；
- `--qa-settings --qa-window --portable` 隔离实例保持响应，`PrintWindow` 实际截图确认设置页头部、三张卡片、MDL2 图标、右置开关、下拉框和滚动区域均正常，网易云独立音量卡片已消失；
- `git diff --check` 通过；真实网易云音频会话写入和 125%/150% DPI 下的拖动手感留给用户在正式桌宠中验收，任何失败仍不会降级修改系统主音量。

### Git 提交

- 本轮代码、测试与文档以“重做设置界面并优化悬浮控件”提交并推送。

### 下一步

- 由用户试用设置页和竖向音量整轨拖动，并确认离开歌名岛与控制岛后 5 秒的停留节奏；如需调整，只修改统一宽限常量，不再分叉两座附件。

## 2026-09-06：把偷看整段动画的墙体直接减半

### 实现

- 逐帧抽取当前运行图集确认：上一版 196×240 派生帧中，动画过程仍保留约 20 像素墙体，只有完全隐藏时由 WPF 裁剪为约 9 像素，因此“收起好看、运动时仍厚”的反馈成立；
- 将 `twelfth-anniversary-peek` 的编译期统一裁剪从 `[0,0,196,240]` 收窄为 `[0,0,186,240]`，每帧只保留靠人物侧约 10 像素墙体；运行显示同步从 155×190 调为 147×190 DIP，保持纵向比例，不压扁人物；
- 重新生成 20 帧 PNG 图集与动画清单，官方 `04_偷看.gif` 保持只读不覆盖；人物、手、发梢及“偷看”文字完整保留；
- 隐藏终态裁剪同步校准为左起约 95.2%、宽约 4.8%，继续显示约 7 DIP 细墙；左侧仍使用同一图集水平镜像；示例场景配置一并修正此前未同步的隐藏比例。

### 验证

- AssetCompiler 校验 52/52 项运行时动画来源、图集、尺寸和 SHA-256 通过；新偷看动画为 20 帧、186×240 帧、147×190 DIP；
- Release 全量构建 0 警告、0 错误；自动化测试 439/439 通过（Core 302、Animation 37、Windows 100）；
- 抽查第 3、7、19 帧透明图确认墙宽连续一致；`--preview-body-reaction=twelfth-anniversary-peek --qa-window --portable` 真实 WPF 中间帧与展开帧截图确认墙体约减半，人物没有横向拉伸，进程保持响应。

### Git 提交

- 本轮配置、派生图集、清单、运行裁剪与文档以“缩窄偷看动画整段墙体”提交并推送。

### 下一步

- 由用户在左右两侧各触发一次完整钻入/钻出，确认约 10 像素运动墙和约 7 DIP 收起墙的组合；若还需更窄，只继续调整统一裁剪右边界，不再单独缩放人物。

## 2026-09-06：修复音量轨道只能拖圆点

### 实现

- 复查确认原整轨拖动仍把事件挂在 WPF `Slider` 本体，模板中的圆点和轨道按钮会提前处理鼠标事件，导致点击轨道能跳转一次但按住后的移动不能持续到达窗口逻辑；
- 在原 30×116 DIP 滑杆区域增加透明整轨输入面，视觉 `Slider` 关闭鼠标命中；圆点、天依蓝已填充段和灰色未填充段现在走同一套按下、捕获、移动、松开流程；
- 新增明确的拖动生命周期；鼠标提前松开或音量弹层关闭时也会释放捕获，避免下次打开残留拖动状态；原顶部 100%、底部 0% 坐标映射和网易云独立音量写入逻辑不变。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 439/439 通过（Core 302、Animation 37、Windows 100）；
- XAML 编译确认透明输入面与视觉滑块保持相同尺寸，没有改变音量弹层布局；`VerticalRangeMapper` 既有参数化测试继续覆盖顶部、中点、底部和越界夹取；
- 当前 Computer Use 会话没有开放原生 Windows 应用目标，因此无法替用户操作正式透明窗口做三处实际拖动；本轮把该项明确保留为用户实机验收，不以推测冒充通过。

### Git 提交

- 本轮代码与文档将以“修复音量轨道按住拖动”提交并推送。

### 下一步

- 由用户在正式桌宠中分别从圆点、蓝色轨道和灰色轨道按住并上下拖动，确认三处都连续跟手；若仍有差异，直接记录起点区域与移动方向，不再调整数值映射。

## 2026-09-07：媒体悬浮岛统一使用天依手指光标

### 实现

- 移除竖向音量整轨输入面的系统 `SizeNS` 上下箭头；
- 复用已经由用户图片生成并随应用安装的 `pet-pointer.cur`，在窗口初始化后显式赋给上一首、播放/暂停、下一首、网易云独立音量按钮和音量整轨输入面；
- 人物透明区域与歌名信息岛继续保持系统默认指针，头发/头顶区域继续使用独立的摸摸头手型；普通光标资源加载失败时仍安全回退系统 Hand。

### 验证

- Release 全量构建通过，0 警告、0 错误；自动化测试 439/439 通过（Core 302、Animation 37、Windows 100）；
- XAML 编译确认音量整轨不再声明 `SizeNS`，五个交互控件都在 CUR 资源载入后获得同一个普通天依指向光标实例；音量拖动捕获和坐标映射代码未改动；
- 自定义鼠标指针不会进入普通窗口截图，最终热点与五处悬停视觉由用户在已重启的正式桌宠中直接验收。

### Git 提交

- 本轮代码与文档将以“统一悬浮岛天依手型光标”提交并推送。

### 下一步

- 用户依次悬停四个控制按钮和音量条，确认普通天依手型显示一致；再从蓝色或灰色轨道按住拖动，确认换光标没有影响整轨连续拖动。

## 2026-09-07：设置页改为顶部 Tab 并重做应用图标

### 实现

- 将单列滚动卡片重构为“常规 / 音乐 / 通知 / 关于”四个顶部 Tab；原启动、置顶、双击换装、吃包子、音乐动画、消息提醒和通知授权控件名称及保存逻辑全部保留；
- 常规页底部加入“今天也，一起加油吧~”，没有角色署名；人物直接抽取“心律共鸣·加入我们”原 GIF 第 0 帧，该帧尚未出现源素材的英文文字，不需要擦除或重绘；
- 从清理好透明背景的晶蓝礼服运行帧按 `[60,0,420,360]` 裁出双耳、脸和肩部，配浅天依蓝圆角底、白色分离边，输出 512 PNG 与 16～256 像素多尺寸 ICO；
- 新 ICO 写入 `ApplicationIcon`，设置页标题、页首头像、关于页和系统托盘从同一套人物图标读取；新增确定性生成脚本和源/输出哈希元数据。

### 验证

- Release 全量构建通过，0 警告、0 错误；新的 BAML 和三个嵌入资源成功生成；
- `--qa-settings --qa-window --portable` 隔离实例持续响应，确认设置窗口、图片 Pack URI 和标题栏 ICO 可正常载入，没有 XAML 解析或资源异常；
- EXE 提取到 32×32 关联图标；ICO 验证包含 16、20、24、32、40、48、64、128、256 九种尺寸；PNG 原图检查确认耳朵、眼睛、脸和肩部未被裁断；
- 当前 Computer Use 会话没有开放原生 Windows 应用目标，无法生成可信的实际设置窗口截图，因此四页视觉、Tab 点击和 125%/150% DPI 间距明确留给用户在已重启的正式实例中验收。

### Git 提交

- 本轮设置页、派生素材、生成脚本与文档将以“重做顶部分类设置页与应用图标”提交并推送。

### 下一步

- 用户查看默认常规页、依次点开音乐/通知/关于，并观察托盘图标；若需微调，只调整 Tab 密度、常规页装饰比例或大头裁剪框，不改变设置数据模型。

## 2026-09-08：找回三项用户指定的官方表情候选

### 实现

- 检查用户放在 `新素材/` 的三张表情商店参考截图，分别识别为“洛天依十周年生日·摸鱼”“洛天依元旦祝福·一键唱歌”和“洛天依虎年贺新春·晚安”；参考截图保持原位，不移动、不覆盖；
- 通过公开的 VSINGER/洛天依表情索引与原始表情库逐项核对编号：摸鱼对应 `L05.GIF`，一键唱歌对应 `F12.gif`，虎年晚安有明确文件名 `晚安.png`；
- 将三个原文件无转码归档到 `候选素材_官方/12_新添加_用户指定官方表情候选/`，记录来源、规格和 SHA-256；摸鱼为 240×240、70 帧、约 66 秒，一键唱歌为 240×240、16 帧、约 1.7 秒，晚安为 300×300 静态 PNG；
- 动画挑选器新增三张优先候选卡，统一标记“新添加/用户指定/候选”，并明确尚未接入触发逻辑；现有待机、音乐、时间问候和睡眠状态均未改变。

### 验证

- 三个本地文件与下载源的 SHA-256 一致，未发生裁剪、改色、补帧或重新编码；
- Pillow 逐帧读取成功，GIF 帧数、尺寸和总时长与挑选器描述一致；
- 动画挑选器离线 JavaScript 验证通过，共 104 张卡片、42 项正式用途，ID 无重复，三项文件路径全部存在，旧选择状态迁移不受影响。
- Release 全量构建通过，0 警告、0 错误；本轮没有修改桌宠运行代码，构建后正式实例已按原路径重新启动。

### Git 提交

- 本轮三项候选素材、来源记录、挑选器与状态文档将以“添加三项用户指定官方表情候选”提交并推送。

### 下一步

- 等待用户说明三项候选的实际触发场景、播放次数和是否需要停留末帧；确认后只编译选中的素材进入运行时。

## 2026-09-08：接入摸鱼倒计时与按歌手分流的三套音乐动画

### 实现

- 将“洛天依十周年生日·摸鱼”原 GIF 的第 1～69 帧整理为精确 60000 ms：跳过开头额外 5 秒的 01:00 停留，按 00:59→00:00 播放；模式三（心率共鸣表情包版）连续无输入 2 分钟进入倒计时，3 分钟整切到“嘿嘿”，输入恢复或离开该外观时立即撤销；
- 将“洛天依元旦祝福·一键唱歌”删除第 0～6 帧手指按键入场，只把第 7～15 帧稳定歌唱与音符段编译为 9 帧、1000 ms 无限循环；官方原始素材保持不变，确定性脚本和元数据记录源/输出哈希、帧号与时长；
- 音乐选择器改为歌手感知：普通或未知歌手固定“享受音乐”，包含洛天依本人或合唱时自动随机“音乐摇摆/一键唱歌”；设置页提供自动、固定三项、完全关闭，以及单独关闭一键唱歌彩蛋。选择完全关闭仍保留音乐检测、控制与 `MusicPlaying` 状态，人物视觉回退当前外观；
- 配置架构升级到版本 13，旧 `random` 自动迁移为按歌手自动模式；动画挑选器同步标注两项新用途，目前 104 张卡片、44 项正式使用。

### 验证

- Release 全量构建成功，0 警告、0 错误；自动化测试 460/460 通过（Core 322、Animation 37、Windows 101）；
- AssetCompiler 校验 54/54 项运行时动画；摸鱼图集 69 帧、总时长 60000 ms，一键唱歌图集 9 帧、总周期 1000 ms；
- 动画挑选器离线校验通过：104 张卡片、44 项正式用途；场景 JSON 可反序列化；普通歌曲、洛天依合唱、唱歌开关、固定选择、完全关闭、2/3/30 分钟边界及旧版本迁移均有自动化测试覆盖。

### Git 提交

- 本轮代码、派生素材、运行图集、设置迁移、测试与文档将以“接入摸鱼倒计时与歌手音乐动画”提交并推送。

### 下一步

- 用户实际播放一首普通歌和一首洛天依独唱/合唱，并观察模式三（心率共鸣表情包版）无操作 2～3 分钟；若特定歌曲的网易云歌手字段不规范，再补充安全的公开文本匹配样例。

## 2026-09-08：托盘右键菜单升级为天依快捷面板

### 实现

- 用 WPF 无边框圆角面板替换 WinForms `ContextMenuStrip`：宽度固定为 324 DIP，页首展示统一头像、陪伴状态和可变化的天依台词；
- 将“完整设置、始终置顶、开机自启动、显示大小”重排为清晰分组；缩放支持 50%～200% 吸附滑杆、±5% 按钮和恢复 100%，沿用原有实时预览与提交逻辑；
- 面板根据鼠标所在显示器、工作区和 DPI 自动停靠在托盘指针上方并限制在可见区域；点击外部或按 Esc 收起；
- “让天依休息”使用面板内联二次确认，5 秒无操作自动恢复，避免误触和旧式系统菜单观感；新增纯函数定位策略及边界测试。

### 验证

- Core、Animation、Windows 三组自动化测试共 467 项通过；快捷面板独立 WPF 验收壳编译并实际渲染常态、退出确认两种画面，控件无裁切、文字无重叠；
- 隔离渲染发现并修复滑杆初始化早于台词计时器导致的启动期空引用；滑杆进一步启用 5% 档位吸附，连续拖动同一句台词不再重复淡入；
- 主应用在独立输出目录完成最终 Release 构建，0 警告、0 错误；设置页文件曾在复验期间发生外部并发替换，文件恢复稳定后已重新执行并通过完整构建。

### 下一步

- 用户在正式实例中右击托盘图标，试用置顶、自启动和缩放；若希望更紧凑，可只微调面板高度与底部留白，不改变功能结构。

## 2026-09-08：重做左侧导航设置页并调整时间问候与追包速度

### 实现

- 按用户提供的设置界面确认稿，把 660×700 顶部 Tab 窗口重构为 570×620 的左侧导航控制台；左栏常驻应用头像、四项整行导航、页面台词和“加入我们”无文字角色帧，右侧四页独立滚动，底部保存栏固定；
- 四页台词分别接入“今天也，一起加油吧～”“要一起听歌吗？”“有新消息的话，我会告诉你的。”“谢谢你让我留在桌面上～”，切页用 160 ms 淡入且快速切页替换旧动画；
- 音乐页删除传统下拉框，改为自动按歌手、三项固定动作和不使用动画共五张选择卡；保留“一键唱歌”独立开关。常规页继续提供开机启动、置顶、三外观循环和吃包子总开关；通知与关于页补齐权限状态、隐私、本地运行和文件安全说明；
- 四段启动/跨时段问候由 Core 统一定义为 15 秒，示例配置和权威场景文档同步更新；
- 追包和返程按每段累计奔跑时间从 72 DIP/s 线性加速，10 秒时达到原巡航速度的三倍并封顶：追包 810 DIP/s、返程 870 DIP/s。新增纯函数速度曲线与 0/5/10/30 秒自动化覆盖。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试共 468/468 通过（Core 330、Animation 37、Windows 101）；`git diff --check` 仅报告仓库既有的 LF/CRLF 转换提示，无空白错误；
- `--qa-settings --qa-window --portable` 隔离实例成功载入；使用真实 WPF `PrintWindow` 分别检查常规、音乐、通知、关于四页，570×620 窗口无横向截断，角色和台词持续显示，音乐五张选择卡完整，右侧窄滚动条没有传统箭头；
- 100% 系统缩放实窗已验证；125% 与 150% 缩放、真实远距离追包十秒加速手感留给用户在正式实例确认。

### Git 提交

- 本轮代码、测试、配置和文档将以“重做设置控制台并调整时间与追包节奏”提交并推送。

### 下一步

- 用户查看正式设置窗口并触发一次远距离追包；如三倍封顶过猛，只调整十秒速度曲线，不改变动画帧率和安全流程。

## 2026-09-08：移除膨胀动画标题文字

### 实现

- 新增确定性派生脚本，从官方“心律共鸣·膨胀”10 帧 GIF 的顶部区域逐帧清除橙色“膨胀”标题，人物、头饰、腮红和黄色星星保持原样；官方源文件不移动、不覆盖；
- 修正橙色阈值误伤嘴巴的问题：派生脚本显式保护嘴部区域，恢复原始嘴形与颜色，仅删除标题文字；
- 输出 162×162 无损 APNG 和元数据，记录源/输出哈希、处理区域、HSV 阈值、帧时长与逐帧清理像素数；动画配置改用无文字派生源，重新生成 162×175 补边运行图集；
- 拖拽触发范围、220×238 DIP 显示大小、10 帧每帧 100 ms、单次播放后停末帧及贴边预览逻辑均保持不变。

### 验证

- 逐帧合成检查确认 10 帧均无可读标题残留，人物轮廓与四周黄色星星完整；AssetCompiler 验证 54/54 项运行时动画；
- Release 构建成功，0 警告、0 错误；自动化测试 468/468 通过（Core 330、Animation 37、Windows 101）。

### Git 提交

- 本轮派生脚本、无文字资产、运行图集、配置和文档将以“移除拖拽膨胀动画文字”提交并推送。

### 下一步

- 用户实际拖动模式三（心率共鸣表情包版），确认无文字画面与原有膨胀节奏符合预期。

## 2026-09-09：统一第二模型待机与动作亮度

### 实现

- 确认七套 720×720 动作 PNG 的人物可见区域整体比正式晶蓝礼服待机图暗约 8%～10%，问题来自源动作色调，不是 WPF 透明窗口或运行时重复预乘 Alpha；
- `prepare_crystal_interactions.py` 为每套动作从归一化第 0 帧构建 256 项亮度 CDF 映射，以真实待机图为目标，只校准 YCbCr 的 Y 通道，保留 Cb、Cr、原始 Alpha、动作内容和透明边缘；
- 校准后继续使用既有 240×260 画布、220×238 DIP 显示、145 帧、42 ms/帧与首尾各 6 帧预乘 Alpha 融合；重新生成七套运行图集、挑选器预览和派生元数据，候选母版未覆盖。

### 验证

- 从七套正式运行图集各抽取第 72 帧合成检查，白发、天依蓝、绿色眼睛和肤色均接近待机图且无整体漂白；`crystal-headpat` 的 0、2、4、6、7、72、138、140、142、144 帧检查确认亮度平滑进入和退出；
- 图集首末帧继续与精确待机参考一致；AssetCompiler 验证 54/54 项运行时动画。

### 下一步

- 用户在正式桌宠依次触发第二模型动作，确认显示器上的主观亮度；若个别动作仍有轻微素材差异，只调整该动作的离线 LUT，不增加运行时滤镜。

## 2026-09-10：音乐设置改为紧凑预览选择

### 实现

- 将音乐页五张双列大文字卡改为一排四张预览卡：自动、享受音乐、音乐摇摆、一键唱歌；“不使用动画”改为下方 40 DIP 窄选项，减少重复说明和纵向占用；
- 每张预览卡常显名称、选中勾与键盘焦点，详细用途保留在工具提示和辅助功能说明；原设置 ID、保存逻辑、歌手自动分流和一键唱歌彩蛋开关均不改变；
- `prepare_settings_ui_assets.py` 从三套正式运行图集抽取固定代表帧，按 Alpha 可见范围生成 128×128 透明缩略图，并合成自动模式三画面预览；元数据记录源图集哈希、帧号和输出哈希；
- WPF 项目把 `assets/ui/*.png` 统一嵌入程序集资源，后续增加音乐动画时可以沿同一预览资源流程扩展。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组测试 468/468 通过；AssetCompiler 验证 54/54 项运行时动画；
- `--qa-settings --qa-window --portable` 隔离实例成功打开设置窗口并保持响应，日志确认动画清单与设置窗口均正常加载。

### 下一步

- 用户查看正式音乐页的缩略图辨识度；后续新增听歌动画时同步增加一张代表帧预览卡，不恢复大段说明卡。

## 2026-09-10：补齐 QQ/微信提醒的跨电脑安装链

### 实现

- 确认截图中的不可用原因是当前进程从 Release 普通 EXE 启动，没有 Windows package identity；现有 `UserNotificationListener` 平台实现和清单能力不需要改用更高权限或读取聊天软件私有数据；
- `build_msix.ps1` 升级为开发/生产双签名模式：开发模式继续生成隔离自签名证书，生产模式要求仓库外 PFX 和密码文件，从证书 Subject 写入清单 Publisher，拒绝空密码与无私钥证书，且不发布 PFX、密码或开发 CER；默认版本升到 `0.1.0.22`；
- 新增 `build_sideload_bundle.ps1` 与测试安装器：输出 MSIX、公开 CER、SHA-256、说明和启动脚本；安装前校验哈希、签名者、证书有效期、文件数和版本，拒绝降级。首次目标电脑只把公开 CER 信任阶段提升为 UAC，随后当前登录用户安装 MSIX 并用 AppsFolder 启动；
- 设置页无包身份提示改为明确区分便携/普通 EXE 与正式 MSIX 安装版，不再称为“后续测试包”。包装文档同时记录 Microsoft Store、受信任 CA PFX 和后续可接的 Azure Artifact Signing 远程签名路径。

### 验证

- Windows PowerShell 5.1 对三份打包/安装脚本的 AST 解析通过；实际构建 `LuoTianyiPet-Installer-0.1.0.22-win-x64.zip`，确认只含公开 CER、不含 PFX/密钥，MSIX 清单包含 `uap3:userNotificationListener`、`runFullTrust` 和版本 `0.1.0.22`；
- 使用同一安装脚本把本机 `LuoTianyiPet.Dev` 从 `0.1.0.21` 升级到 `0.1.0.22`：包状态为 `Ok`，运行进程来自 `C:\Program Files\WindowsApps\LuoTianyiPet.Dev_0.1.0.22_x64__8x1jc9fpx3qdy` 且保持响应；没有在本轮代替用户点击 Windows 通知隐私授权；
- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组测试 468/468 通过；AssetCompiler 验证 54/54 项运行时动画。

### Git 提交

- 本轮安装链、提示文案与文档以“补齐通知功能跨电脑安装链”提交并推送。

### 下一步

- 用户在安装版设置的“通知”页点击“授权访问”，再用一条 QQ 和一条微信系统通知验收来源识别；准备公开发布时接入 Partner Center 或受信任生产签名凭据，开发 PFX 继续只留本机。

## 2026-09-10：设置左栏改用四张页面专属表情

### 实现

- 常规、音乐、通知、关于页左栏分别接入官方“我推”“真好”“天哪”“加入我们”静态 PNG；关于图使用双眼睁开且带原始 `Join Us!` 的画面，替换此前无文字第 0 帧；
- `prepare_settings_ui_assets.py` 新增统一派生：按各图可见 Alpha 边界裁切，等比缩放并居中装入 144×124 透明画布，不擦字、不重绘；元数据记录四项源哈希、裁剪框、输出尺寸与输出哈希；
- 设置导航切页同时更新台词、图片与辅助功能名称，160 ms 淡入保持一致；系统关闭客户端动画时直接替换为最终 0.95 不透明度。

### 验证

- 四张最终 PNG 逐张检查：人物和原有文字均完整，“加入我们”的 `Join Us!` 与睁眼状态清晰；脚本校验 4/4 张均为 144×124；
- `--qa-settings --qa-window --portable` 真实 WPF 常规页经 `PrintWindow` 截图为 570×620，左栏“我推”的粉色文字与两个人物完整显示，未遮挡页面台词、导航或底部保存栏；Windows 控制技能服务未暴露本地 WPF 接口，因此其它三页以最终 PNG、Pack URI 映射和 Release 资源编译交叉验证；
- Release 构建成功，0 警告、0 错误；自动化测试 468/468 通过；AssetCompiler 验证 54/54 项运行时动画；`0.1.0.23` MSIX 构建并安装成功，包状态 `Ok`，WindowsApps 正式进程保持响应。

### Git 提交

- 本轮页面表情、派生脚本和文档以“设置页加入页面专属表情裁图”提交并推送。

### 下一步

- 用户在正式 `0.1.0.23` 设置窗口依次切换四页确认主观占比；若某张需要更大，只调整该图的统一画布内缩放，不改原始 PNG。

## 2026-09-10：分别延长模式三身体点击动画

### 实现

- 为模式三（心率共鸣表情包版）九类身体反应建立逐动作播放倍率：心软软 0.72、啾啾 0.82、贴贴 0.80、摸摸 0.75、击掌 0.68、重罪 0.80、黑化 0.90、糟了 0.68、抱抱 0.80；原始约 0.9～2.0 秒延长为约 1.3～2.2 秒；
- `AnimationFramePlayer` 的完整动画播放入口支持时间轴倍率，按原帧时长缩放，不复制帧、不重新编码素材、不增加安装体积；未知动画保持 1.0 倍，第二模型、音乐、吃包子和边缘动画不受影响；
- 单帧“击掌”“糟了”的 WPF 回弹/晃动使用相同时间倍率，避免延长表情后程序动作提前结束；双击锁定继续覆盖完整的新时长。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组测试 478/478 通过（340、37、101）；新增测试覆盖九项倍率及未知动画原速回退；
- AssetCompiler 验证 54/54 项运行时动画；隔离实窗分别触发单帧“击掌”和动态“心软软”，两项均在动作阶段保持显示，窗口 196×306 原生边界不变且持续响应；
- 已生成并安装 `0.1.0.24` MSIX，包状态 `Ok`，正式进程从 WindowsApps 启动并正常响应。MSIX SHA-256 为 `2BF2F0B082CD062D64CAC7E43442EA4AA47820D539E21F3C571E55EFDECC897D`。

### Git 提交

- 本轮模式三节奏、播放时间轴、程序动效同步、测试与文档将以“细化模式三互动动画节奏”提交并推送。

### 下一步

- 用户逐项点击模式三（心率共鸣表情包版）身体区域确认主观节奏；后续只对有反馈的单项倍率做小幅调整。

## 2026-09-10：优化吃包子追逐节奏并修复返程漏追

### 实现

- 追包初速度由 72 提高到 180 DIP/s，加速至三倍巡航上限的时间由 10 秒缩短为 4 秒；追包上限仍为 810 DIP/s，返程上限仍为 870 DIP/s；
- 新增按逻辑桌面对角线计算的速度倍率：以 1920×1080 为基准，2K 为 1.333 倍、4K 为 2 倍并封顶；物理工作区先按当前 WPF DPI 转回逻辑 DIP，因此 4K/200% 与 1080p 使用相同倍率。四种代表环境穿越 16:9 对角线均约 4.28 秒，1080p 一圈约 8.96 秒；
- 包子完成 680 毫秒飞入口腔后，进食动画直接续播第 128～172 帧闭嘴段，约 0.8 秒完成闭嘴并继续下一目标，删除此前约 1.65 秒的张嘴等待；
- 返程期间若出现新包子，立即取消返程并重新选择最近目标继续追逐；包子窗口拖动坐标从物理像素转换为 WPF DIP，修复高 DPI/200% 缩放时指针与包子位置偏移；
- “我很可爱，请给我小笼包”彩蛋的触发排除条件、单次定格、拖拽投喂、入嘴轨迹和剩余包子处理方案仅写入桌面确认文档，未在用户确认前接入运行时。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组测试 492/492 通过（354、37、101）；新增覆盖四档显示环境的速度缩放、对角线时间估算和返程中断条件；
- 隔离真实 WPF 场景依次截取追逐、张嘴、闭嘴和返程画面，四阶段均正常显示且进程持续响应；配置 JSON 解析通过；AssetCompiler 验证 54/54 项运行时动画；
- 已生成并安装 `0.1.0.25` MSIX，包状态 `Ok`，正式 WindowsApps 进程正常响应且本次启动日志无新增 `ERROR`；MSIX 为 227,751,043 字节，SHA-256 为 `F91C5C19E71076AA11663C0E966F7421BE7EB02FD33D451FCBC15EA16F16C073`，跨电脑测试安装 ZIP 为 226,679,255 字节，SHA-256 为 `F6D0178DB00651A68A3DF5AB9E0C1B581647002816805CFBFC35FF0AEAA6FD89`。

### Git 提交

- 本轮代码、测试、配置和文档将以“优化吃包子追逐与返程衔接”提交并推送。

### 下一步

- 用户体验新版追逐速度并阅读桌面确认文档；可直接回复“按推荐方案做”，或逐项修改四个彩蛋交互选择。

## 2026-09-10：按左右部位细分模式三（心率共鸣表情包版）互动

### 实现

- 模式三（心率共鸣表情包版）首次点击的方向改为右眼、左手、右脚保持原画面，左眼、右手、左脚在运行时水平镜像；镜像状态随身体反应令牌恢复，开始拖拽、切回待机或被其它反应替换时复位，不复制图集；左脚“糟了”的程序晃动方向也同步反转；
- 继续使用用户已标注的单个 Face 多边形，在内部按归一化横坐标 0.5 分侧：人物左半脸播放“十二周年·充电”，右半脸保留“十二周年·贴贴”，眼睛和嘴巴仍按更高优先级命中；
- 眼睛与脚部各新增独立 4 秒连续点击窗口；第二次可跨左右侧，眼睛改播“十二周年·哭唧唧”，脚部改播“十二周年·斯到普”；点击其它身体部位或超时会打断计数，第二次播放后下一次重新作为第一次；
- 从原始十二周年重复 GIF 确定性编译首个完整周期：“充电”15 帧、“哭唧唧”14 帧、“斯到普”13 帧，均为 240×240 图集、190×190 DIP、0.80 倍速单次反应；原始 GIF 不移动、不覆盖；
- 动画挑选器同步更新三张新用途，以及心软软、击掌、糟了、贴贴的分侧说明；当前保持 104 张候选卡，正式用途由 44 增至 47 项。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组测试 509/509 通过（371、37、101）；新增覆盖六个左右方向、脸部中心线、眼睛/脚部跨侧第二次、4 秒边界、超时以及其它部位/摸头打断；
- AssetCompiler 验证 57/57 项运行时动画；动画来源与场景规则 JSON 均可解析，动画挑选器内联 JavaScript 语法检查通过；
- 真实 WPF 隔离窗口分别播放“充电”“哭唧唧”“斯到普”，三项均为 206×316 窗口、透明显示正常且进程保持响应；另以原向/镜像两次真实窗口播放“心软软”，确认人物方向确实左右互换；
- 已生成并安装 `0.1.0.27` MSIX，包状态 `Ok`，正式 WindowsApps 进程正常响应且载入 57 项动画，本次启动日志无新增 `ERROR`；MSIX 为 231,264,182 字节，SHA-256 为 `20DF7C4EA1BAAD01357FE3A1502F610CFFBE0A7E450D4EE08AD72E16FAB0D51D`，跨电脑测试安装 ZIP 为 230,049,508 字节，SHA-256 为 `908BED3953708846599694A53C4639120B5AA1005A3668CDCAD121C1BA3D8203`。

### Git 提交

- 本轮代码、三项运行图集、挑选器、测试和文档将以“细分模式三（心率共鸣表情包版）左右互动”提交并推送。

### 下一步

- 用户在正式安装版依次点击左右眼、左右脸、左右手和左右脚，确认方向；随后在 4 秒内连续点击眼睛两次、脚部两次，确认“哭唧唧”和“斯到普”的触发手感。

## 2026-09-10：修复“摸鱼一分钟”人物内部误透明

### 实现

- 定位到旧派生脚本把 GIF 调色板第 0 项直接指定为透明色，导致不透明白底与人物内部白色衣服、皮肤高光等在量化后共用颜色项，身体区域随背景一起被扣除；首帧对照发现 5,489 个透明像素位于外部背景连通分量之外；
- `prepare_music_idle_animations.py` 改为逐帧从画布四边遍历近白色外部连通分量，只清除该分量中的白底；没有与画布边缘连通的角色像素全部强制保护，原 GIF、69 帧倒计时和 60,000 ms 总时长不变；
- 新增生成后自检：重新解码输出 GIF，逐帧确认画布角点透明，且受保护的人物内部区域不存在新增透明像素；重新生成处理后 GIF、运行图集和带哈希清单，“一键唱歌”派生文件保持原字节不变。

### 验证

- 派生脚本完整运行通过，69/69 帧的左上角均透明，保护区误透明像素总数为 0；在天依蓝背景上并排合成旧图集与新 GIF，确认旧图中的衣服、身体穿洞已全部恢复；
- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组测试 509/509 通过（371、37、101）；AssetCompiler 验证 57/57 项运行时动画；
- 真实 WPF 隔离窗口播放 `tenth-birthday-fishing-countdown`，窗口为 196×306、人物完整、外部背景透明且进程保持响应；
- 已生成并安装 `0.1.0.28` MSIX，包状态 `Ok`，正式 WindowsApps 进程保持响应且载入 57 项动画，本次启动日志无新增 `ERROR`；MSIX 为 231,262,273 字节，SHA-256 为 `29C711EBBFFF81471C33F5CAF12A1BC6233E296AE74AECF998777CFFC12B6B09`，跨电脑测试安装 ZIP 为 230,048,876 字节，SHA-256 为 `42A12C6E52AA9564E764DA87A2DC1AFEAAF73F6FA33641B50DE6871598C4736A`。

### Git 提交

- 本轮脚本、派生 GIF、运行图集、清单与文档将以“修复摸鱼倒计时透明区域”提交并推送。

### 下一步

- 用户在正式安装版等待模式三（心率共鸣表情包版）进入“摸鱼一分钟”，在非白色桌面背景上确认人物衣服、四肢和高光保持完整且外部白底已透明。

## 2026-09-10：修复托盘快捷面板异常并移除桌宠测试右键菜单

### 实现

- 根据用户提供的完整 JIT 堆栈定位到 `TrayQuickPanel.AnimateOpen`：旧实现把 `TranslateTransform` 写到 WPF 顶层 `Window.RenderTransform`，触发框架的 `Window.CoerceRenderTransform` 保护异常；
- 快捷面板内部根 Border 新增固定 `TranslateTransform`，开场时仍保留 8 DIP、140 ms 的上移淡入，但位移只作用于合法内容元素；重复打开前先移除旧动画时钟并复位起点；
- 从主桌宠 XAML 整体移除 ContextMenu，不再显示“动画与状态验证”、模拟开始/停止音乐、身体区域调试或右键退出；开发验证继续使用现有 `--preview-*` / `--qa-*` 参数，用户设置、置顶、开机自启动、显示大小和退出统一保留在系统托盘快捷面板；
- 身体区域调试层的启用条件改为只读取 `--preview-body-hit-debug`，删除依赖已移除菜单项的运行时代码和两个仅供菜单点击的音乐模拟处理器。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 509/509 通过（371、37、101）；
- `--qa-tray --qa-window` 真实 WPF 隔离运行自动打开 324×456 快捷面板，进程保持响应，日志在 `tray.ready` 与 `app.started` 后没有新增 `Window.CoerceRenderTransform` 或其它未处理异常；PrintWindow 截图确认头像、开关、显示大小和二次确认退出区域均正常；
- 主窗口 XAML 已无 ContextMenu，Release XAML 编译成功；命令行身体区域调试入口仍保留。
- 已生成并安装 `0.1.0.29` MSIX，包状态 `Ok`，正式 WindowsApps 进程保持响应；MSIX 为 231,261,979 字节，SHA-256 为 `7DF696346F96D548106B1D913DA3FC980DE0AD6B82FF451FE3C8B06EEF61F95C`，跨电脑测试安装 ZIP 为 230,048,582 字节，SHA-256 为 `CACAB47AFE3F41D4C1DAD2A58B2B39AA063A957FB9ACFC46371C8AE589189FC5`。

### Git 提交

- 本轮托盘面板、主窗口菜单、文档与安装包将以“修复托盘面板并移除测试菜单”提交并推送。

### 下一步

- 用户在正式安装版从系统托盘右键打开快捷面板，确认无异常；并在桌宠人物本体上右键确认不再出现测试菜单。

## 2026-09-10：分离快捷回收站与桌面吃包子并修复失焦拖放

### 实现

- 直接拖入桌宠的文件回收成功后不再调用 `QueueBunTreat`，而是结束“给我”末帧定格并恢复此前持续动画；投放进行中及成功后 10 秒内抑制同一次桌面消失通知，保证从桌面拖入也不会绕道触发包子；
- 文件拖放改用已验证的 `DesktopFileTreatSafety` 前台策略，对覆盖整个显示器的 `explorer.exe` 桌面外壳和最大化目录窗口放行，因此不再要求先点击桌宠；其它全屏应用、受保护游戏和前台探测失败继续拒绝。路径本身仍只来自 `CF_HDROP`，不限制源目录；
- 快捷回收站不再受“桌面吃包子动画”设置开关控制；设置文案明确该开关只控制桌面项目消失后的追包，快捷回收站始终保留；
- 追包开始时立即关闭歌名、媒体控制、网易云音量、反馈和消息悬浮层；鼠标进入、异步曲目信息、音量按钮及消息显示入口均在追包状态下失败关闭；
- 外部文件进入桌宠拖放目标后持续设置窗口、人物和 WPF 全局覆盖光标为用户的天依指向手型，离开、拒绝、投放完成或窗口关闭时统一释放。Explorer 仍可能按 Windows OLE 规范叠加移动效果角标，不使用全局 Hook 或注入去强行覆盖。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 512/512 通过（374、37、101）；新增覆盖 `explorer`、`explorer.exe` 和完整路径形式的覆盖屏幕前台放行；
- 使用系统临时目录自动创建一份测试文本，经正式 `WindowsRecycleBinService` 从非桌面目录成功移入 Windows 回收站：状态 `Success`、请求/回收 1/1、原路径不存在；没有使用永久删除；
- `--qa-media-controls --qa-track-info --qa-bun-chase` 真实 WPF 组合运行确认追逐人物可见而歌名、媒体控制、音量和反馈岛均隐藏；设置页真实窗口截图确认“桌面吃包子动画”新文案完整可见；
- 已生成并安装 `0.1.0.30` MSIX，包状态 `Ok`、签名 `Valid`，WindowsApps 进程正常响应且启动日志无新增 `ERROR`。MSIX 为 231,262,103 字节，SHA-256 为 `A2E94196100A6605601E15ADFF11053578ED516327AFE9BA6D2C98814BC4B853`；跨电脑测试安装 ZIP 为 230,190,118 字节，SHA-256 为 `4DCCA8320847A6930AE6F404EC8F2A3B527A417D490A5A3FB39DCFE9C5BA795B`。

### Git 提交

- 本轮代码、测试、配置、验证截图和文档将以“分离快捷回收站与桌面吃包子”提交并推送。

### 下一步

- 用户在正式安装版先点击桌面或打开任意资源管理器目录，再直接拖一个可恢复的测试文件到桌宠，确认无需预先点击桌宠即可回收且不会播放吃包子；随后触发桌面包子并把鼠标移上人物，确认整轮没有悬浮岛。

## 2026-09-10：修复“一键唱歌”循环边界瞬跳

### 实现

- 定位到旧派生把源第 7～15 帧直接首尾相接，循环边界实际为第 15→7 帧，人物嘴型、头部位置和两侧音符会同时突变；
- 保留第 0～6 帧按键入场的既有裁除规则，把稳定歌唱段改为源帧 `7,8,9,10,11,12,13,14,15,14,13,12,11,10,9,8` 往返闭环；每帧统一 100 毫秒，总周期 1.6 秒；
- 派生脚本增加闭环断言，逐项检查相邻输出帧以及末帧→首帧的源序号差都必须为 1；只复用官方原帧，不用容易产生双影的交叉淡化或生成式补帧；
- 重新生成处理后 GIF、16 帧运行图集、动画清单与可追溯元数据，官方源 GIF 保持原样。

### 验证

- 派生脚本和图集编译器连续重跑后，GIF、元数据、图集与清单四项 SHA-256 均保持一致；独立断言确认 16 帧均为 100 毫秒、总周期 1600 毫秒、完整循环只跨相邻源帧；
- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组测试 512/512 通过（374、37、101）；AssetCompiler 验证 57/57 项运行时动画，其中 `newyear-one-click-singing` 为 16 帧；
- 真实 WPF 隔离窗口持续播放 `newyear-one-click-singing`，透明背景和人物显示正常且进程保持响应；另保存旧第 15→7 帧与新第 8→7 帧闭环边界对照图；
- 已生成并安装 `0.1.0.31` MSIX，包状态 `Ok`、签名 `Valid`，正式 WindowsApps 进程正常响应并载入 57 项动画，启动日志无新增 `ERROR`。MSIX 为 231,520,386 字节，SHA-256 为 `AF4F807B8873F06135D88D4D743C8003C6A9ACEE552615DB2CEF70DA85810C56`；跨电脑测试安装 ZIP 为 230,450,883 字节，SHA-256 为 `33F1B40A10F13C4EFE09B52AA04BC137C00951B1BC36F5D2085A34FC0C3D48B6`。

### Git 提交

- 本轮派生脚本、GIF、运行图集、清单、验证截图与文档将以“修复一键唱歌循环衔接”提交并推送。

### 下一步

- 用户在正式安装版播放洛天依歌曲并触发“一键唱歌”，连续观察至少三轮，确认嘴型、头部和音符在每个循环边界都平顺衔接。

## 2026-09-10：修复网易云自动播放、切歌衔接与成功反馈

### 实现

- Windows 媒体命令发送器新增定向投递：先把标准 `WM_APPCOMMAND` 上一首、播放/暂停、下一首发给 `cloudmusic.exe` 的可交互主窗口，失败才回退用户配置快捷键；返回结果和隐私日志会区分 `TargetedWindowsMessage` 与 `KeyboardShortcut`；
- 冷启动从固定 2 秒后发键改为至少等待 2 秒且优先等待歌曲标题出现，10 秒仍无信息才兜底，准备完成后仅发送一次播放/暂停。Core Audio 确认有声时先结束“加载中”，播放一次 OK，再无缝恢复当前歌曲对应的持续动画；
- 普通无声及目标音频会话短暂消失统一使用 5 秒宽限，设置架构升级到 14 并只迁移旧默认 1000 毫秒；桌宠主动切歌后另保持当前音乐动画最多 15 秒，曲名变化可先确认 OK，加载完成前不回到待机；用户主动暂停仍在首个静音采样即时确认；
- 补齐未登记的 `resonance-cry-shake` 运行时资产并叠加晃动，网易云失败不再落入“动画素材暂时不可用”占位；补齐 `resonance-big-success`，快捷回收站成功后播放“大成功”，不再使用 OK，也不进入吃包子；
- 动画挑选器同步把 OK 限定为媒体响应确认，把“大成功”登记为文件回收成功反馈；场景配置、需求、映射、架构和安全文档同步更新。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 518/518 通过（374、37、107）；新增覆盖三种定向媒体命令、快捷键回退、版本 13 的默认宽限迁移、5 秒静音边界和音频会话重建边界；
- AssetCompiler 验证 59/59 项运行时动画；`resonance-big-success` 为 10 帧，`resonance-cry-shake` 为单帧加程序晃动。两项真实 WPF `PrintWindow` 验证均显示透明人物而非缺素材圆框，证据保存于 `docs/validation/file-recycle-big-success-v0.1.0.32.png` 与 `docs/validation/cloudmusic-failure-cry-v0.1.0.32.png`；
- 真实关闭网易云后由桌宠重新拉起：约 6 秒后读取到曲目，定向播放/暂停命令仅发送一次，约 6 秒后 Core Audio 确认有声，日志依次记录 `media.application_launch_play_result`、`media.playback_started`、`resonance-ok`，没有切回待机；随后通过桌宠“下一首”按钮切到新曲，投递方式为 `TargetedWindowsMessage`，约 0.35 秒由标题变化确认并播放 OK，观察窗口内没有 `media.playback_paused`；
- 已生成并安装 `0.1.0.32` MSIX，签名 `Valid`、包状态 `Ok`，WindowsApps 正式进程正常响应并载入 59 项动画，启动日志无新增 `ERROR`。MSIX 为 231,977,261 字节，SHA-256 为 `7B1DC6AA00E830ED678535D5DE41DAF8F2D5BE4D6A033E21A7E7BE9865D5FFAC`；跨电脑测试安装 ZIP 为 230,905,455 字节，SHA-256 为 `68B07A415C1043301D707A3B28B3808088E07312DDEF734DCE30F3F130665664`。

### Git 提交

- 本轮代码、两项运行资产、测试、配置、验证截图与文档将以“修复网易云自动控制与切歌衔接”提交并推送。

### 下一步

- 用户在 `0.1.0.32` 安装版点击播放/暂停冷启动网易云，连续切换几首网络加载速度不同的歌曲，并暂停一次，确认加载期间保持音乐动画、成功只出现一次 OK、暂停仍能即时恢复当前外观；再拖一份可恢复的测试文件确认成功反馈为“大成功”。

## 2026-09-10：交换模式三（心率共鸣表情包版）左右眼与左右手方向

### 实现

- 模式三（心率共鸣表情包版）首次眼睛反应改为左眼播放“心软软”原方向、右眼运行时水平镜像；眼睛 4 秒内第二次点击“哭唧唧”的不镜像规则保持不变；
- 手部反应改为右手播放“击掌回弹”原方向、左手运行时水平镜像；程序回弹的左右方向继续跟随镜像标志；
- 同步更新场景配置、需求、动画映射、架构说明、动画挑选器标签和回归测试；左右脚、左右脸及第二次点击逻辑没有改动。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 518/518 通过（374、37、107），四个左右眼/手方向断言已按新映射更新；
- 动画挑选器校验通过：104 张候选卡、48 项正式用途、无默认删除项；场景 JSON 可解析，`git diff --check` 通过；
- 真实 WPF 分别播放左眼原向、右眼镜像、右手原向、左手镜像四个预览，四次 `PrintWindow` 均成功，手部人物与红心位置明确左右互换；验证截图保存于 `docs/validation/classic-*-v0.1.0.33.png`；
- 已生成并安装 `0.1.0.33` MSIX，签名 `Valid`、包状态 `Ok`。MSIX 为 231,977,252 字节，SHA-256 为 `0A44A5ED6AA2E2A73A700A3F80DCADDCA337F7542B72C090D8812313326CA843`；跨电脑测试安装 ZIP 为 230,905,510 字节，SHA-256 为 `E177C345517EA3FFE9FDD43694B12E3FF42CAF23E9E5D2F438B65D6B5E737655`。

### Git 提交

- 本轮代码、测试、配置、验证截图和文档将以“交换模式三（心率共鸣表情包版）左右互动方向”提交并推送。

### 下一步

- 用户在 `0.1.0.33` 安装版进入模式三（心率共鸣表情包版），分别点击左右眼和左右手，确认新方向符合角色画面；连续第二次点击眼睛仍应正常进入“哭唧唧”。

## 2026-09-10：修复网易云系统浮窗、冷启动与音乐设置语义

### 实现

- 删除 `WM_APPCOMMAND` 定向投递及相关 Win32 后端、投递枚举和死代码；上一首、播放/暂停、下一首统一使用网易云全局快捷键，保留受保护前台、实体键占用、350 毫秒限流和成对抬键保护；
- 冷启动等待不再只相信首次 `Process.Start`：每 250 毫秒检查可控制窗口，3 秒仍未出现就重新解析并启动，包含首次最多三次；窗口与曲目准备好后仍只发送一次播放快捷键；
- 网易云启动失败、命令发送失败与切歌超时只显示明确气泡并恢复持续状态，不再请求呜呜动画，也不会出现缺素材圆框；
- 音乐设置页改为播放动画总开关、智能分配说明卡和三张固定唯一动画预览卡；智能分配为普通歌固定享受音乐、洛天依本人或合唱随机音乐摇摆/一键唱歌，固定项改为无论歌手均只使用所选动画。旧唱歌彩蛋开关从界面移除并在配置归一化时固定开启。

### 验证

- 本机日志复现首次启动请求返回 `Started` 后没有留下 `cloudmusic.exe` 的情况；同一路径手工再次启动后约 5 秒出现可控制主窗口，确认问题是网易云启动进程偶发立即退出与桌宠缺少重试共同导致，不是安装路径解析失败；
- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 519/519 通过（375、37、107）；新增/更新断言覆盖旧彩蛋开关迁移、自动双彩蛋、三个固定项跨歌手唯一使用和媒体命令只走快捷键；`git diff --check` 通过；
- 当前 Windows 界面控制通道没有暴露原生应用窗口，因此未能自动截取设置页；XAML 已通过 Release 编译，隔离设置预览留给本地运行时直接验收。

### Git 提交

- 本轮代码、测试、配置与文档将以“修复网易云控制与音乐动画设置”提交并推送。

### 下一步

- 用户在本地最新版验证：连续点击三个媒体按钮不再出现 Windows 媒体浮窗；完全退出网易云后点击播放可自动重试并开始播放；音乐设置的智能分配、固定唯一动画与总开关符合预期。

## 2026-09-10：收口尺寸、身体动作、发丝缺口与吃包子投喂彩蛋

### 实现

- 用 `AccessorySizingResolver` 统一约束歌名岛与控制岛：50% 小窗口按比例缩小控制岛以完整容纳，常规大小保持原尺寸，200% 大窗口把歌名岛封顶为 240 DIP；已在 50%/200% 真实 WPF 窗口检查；
- 晶蓝礼服与经典猫耳身体动作完成后直接恢复同锚点待机帧，不再追加第二次青色脉冲；经典猫耳摸摸头降为 0.60 倍速，手部按最后要求调整为左手原向、右手镜像；
- `prepare_full_body_idle.py` 在归一化图中只修复用户指出的右侧发丝针孔：紧约束多边形内以邻近不透明发色补齐 97 个像素。自动检查确认小洞 Alpha=255，左右大缺口采样仍为 Alpha=0；同步重建紧凑/晶蓝待机与 7 组晶蓝动作首尾帧；
- 追包改为 180→800 DIP/s、5 秒线性加速，返程固定 850 DIP/s；闭嘴尾段以 1.35 倍速约 0.54 秒结束。唯一包子满速后被连续拖动 3 秒会每轮一次播放“心律共鸣·可爱”并定格；新包子恢复追逐但不重复彩蛋；
- 包子松手时用整个圆形足迹扫描当前人物 Alpha，足迹与任一可见像素重叠即可投喂，避免中心恰落发缝时失败；150 ms 切张嘴、400 ms 弧线飞入口中，有剩余目标时 350 ms 后继续；
- 两套奔跑循环在双向光流前增加 premultiplied RGBA 上半身重心稳定。最终 V1/V2 横向重心范围为 1.276/2.320 像素，纵向为 3.184/5.040 像素，循环首尾差均低于 0.2 像素；进食图集哈希保持不变。对照接触表保存为 `docs/validation/bun-run-stabilized-contact-sheet-2026-09-10.png`；
- 桌面设计确认文档已更新为最终 5 秒/800/850/3 秒参数和“已确认并实现”状态。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 533/533 通过（389、37、107）；新增覆盖显式速度上限、4K/200% 归一化、3 秒唯一拖动彩蛋和包子足迹跨透明中心命中；
- AssetCompiler 验证 60/60 项运行时动画；新增 `resonance-cute-bun-request` 为 10 帧、205×205 DIP；场景和动画来源 JSON 解析通过，`git diff --check` 通过；
- `--qa-bun-chase --qa-window` 两次真实 WPF 演出均完成追逐、进食与返程，日志记录 `file_treat.completed`，进程保持响应并正确载入 60 项动画；桌面截图确认晶蓝礼服补洞后人物和两侧透明发缝显示正常；
- 连续拖住唯一包子 3 秒并手动松到人物上的最终鼠标手感仍需用户在已启动本地最新版亲自确认；Core 判定和 Alpha 足迹已有自动化覆盖。

### Git 提交

- 本轮以“完善投喂彩蛋并修复动画与布局细节”提交并推送。

### 下一步

- 用户在本地最新版观察晶蓝礼服补洞、两套奔跑稳定度，并实际完成一次 3 秒拖包彩蛋与身体投喂。
## 2026-09-10：接入模式二新动作并拆分三套待机规则

### 实现

- 接入用户在 `候选素材_官方/模式二新添加动作/` 提供的透明序列：`摸胸` 映射 Chest，`摸裙边` 映射 LowerBodySensitiveArea，`新比心` 沿用 `crystal-hand-heart` 运行时 ID 覆盖旧比心；三项继续使用模式二专属原地播放和身体动作期间锁定双击；
- 新增 `crystal-yawn`：模式二每轮连续无操作在 60～285 秒随机安排一次打哈欠，最多一次；5 分钟整直接进入“嘿嘿”，不经过“摸鱼一分钟”倒计时。模式一使用 `NoMediumIdle`，永不进入“嘿嘿”；模式三继续使用 2 分钟倒计时、3 分钟“嘿嘿”的原规则；
- `IdleSceneResolver` 改为按 `NoMediumIdle / CrystalDress / ClassicCatEars` 三种 profile 解析，并新增独立 `CrystalYawnScheduler`，用户产生输入导致空闲时长回落后才重置本轮随机门槛；
- 离线动作脚本支持不同来源目录和不同原始帧数。新比心 121 帧、摸胸/摸裙边各 145 帧、打哈欠 169 帧均生成 240×260 图集并显示为 220×238 DIP；首尾六帧继续与真实待机预乘 Alpha 混合。鸭子坐 217 帧和睡觉 361 帧只生成轻量预览与来源哈希，不进入运行时目录或状态机；约 649 MiB 原始母版留在候选目录并由 Git 忽略；
- 动画挑选器增至 109 张卡片、51 项正式用途；新比心替换旧卡，摸胸、摸裙边、打哈欠标为已使用，鸭子坐和睡觉标为待互动设计。配置、需求、动画映射、技术架构和两处归档说明同步更新。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 544/544 通过（400、37、107），新增覆盖模式一无“嘿嘿”、模式二跳过倒计时并在 5 分钟进入“嘿嘿”、模式三边界不变、打哈欠单次随机门槛与输入后重置，以及模式二胸部/下体映射；
- AssetCompiler 验证 63/63 项运行时动画；新比心为 121 帧/5082 ms，摸胸和摸裙边各 145 帧/6090 ms，打哈欠为 169 帧/7098 ms。十项模式二原地动作的首末帧 RGBA 哈希完全一致；23 项派生输出连续重生成后的 SHA-256 无变化；
- 真实 WPF 分别预演新比心、摸胸、摸裙边和打哈欠，四项均保持响应并记录 `animation.in_place_reaction_started`，完整等待后均记录原地恢复完成；运行图集中段和首末帧视觉抽查保存在 `docs/validation/crystal-new-actions-contact-sheet-2026-09-10.png`；
- 动画挑选器校验通过；场景 JSON 和动画清单可解析，`git diff --check` 通过。Computer Use 当前没有暴露原生应用窗口，因此没有额外的系统级窗口截图；运行图集接触表与真实 WPF 播放日志共同完成本轮机器侧视觉检查。

### Git 提交

- 本轮代码、运行资产、轻量预览、测试、配置、验证接触表与文档将以“接入模式二新动作与分层待机”提交并推送；原始逐帧母版不进入 Git 或安装包。

### 下一步

- 用户在本地最新版进入模式二（心率共鸣简约Q版），依次点击胸部、裙边和左右手，并完成一次 5 分钟无操作观察，确认动作内容、随机哈欠时机以及无倒计时直接进入“嘿嘿”的整体手感。

## 2026-09-10：延长模式三摸摸头、柔化收尾并提升模式二清晰度

### 实现

- “天依游学记·国乐季·摸摸”维持 0.60 倍速，将原 10 帧、1.2 秒周期有限循环两次；有效展示从约 2 秒增加到 4 秒，末帧到首帧的平均像素差与普通相邻帧同量级，不插入静止帧；
- 为模式三（心率共鸣表情包版）全部身体部位反应增加无闪层收尾：人物 110 ms 淡出，在完全透明的中点恢复持续待机和原 Alpha 边缘锚点，再以 170 ms 淡入；过程中不缩放、不染色，新的反应或状态会通过既有 transition version 立即取消旧收尾；
- 模式二十项运行图集从 240×260 提升为 360×390，显示槽仍为 220×238 DIP，WPF 继续使用高质量缩放；动画挑选器 12 项预览独立回落到 240×260，避免非运行资源跟随放大；
- 重新以当前 `候选素材_官方/Q版小人全身_新默认.png` 为源生成模式二待机透明图。该源文件仍为整张 Alpha=255 的 RGB 烘焙棋盘，新版灰格更深，因此使用最低通道 160 的低色差背景候选、边缘洪泛和两处内部矩形，人物白衣、发丝、薄纱线条及左右大开口保持，右侧已确认的小针孔继续局部发色补齐；
- 图像生成式抠图候选经通道检查仍为 RGB、没有真实 Alpha，已拒绝进入仓库；正式资产全部来自可复现的像素级脚本和来源哈希。新增蓝底/深底抠图与 200% 新旧运行采样对比图 `docs/validation/crystal-visual-quality-20260910.png`。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 544/544 通过（400、37、107）；
- AssetCompiler 验证 63/63 项运行时动画；`guoyue-headpat` 清单为 10 帧、2 次循环，按 0.60 倍速计算总时长精确 4000 ms；模式二十项清单均为 360×390 帧，全部首末帧 RGBA 哈希一致，12 个挑选器预览均保持 240×260；
- 新待机输出为 480×520 RGBA，含 122,930 个全透明像素与 10,727 个半透明边缘像素，可见边界为 `(32,11)-(448,509)`；蓝底与深底视觉检查无棋盘残留，人物白衣和透明薄纱完整；
- 当前用户设置为 200%：真实 WPF 模式三摸摸头从 `animation.reaction_started` 到 `animation.body_reaction_fade_restore_completed` 间隔约 4.01 秒；模式二摸摸头完整播放并直接回待机，窗口持续响应，单动作加载时私有内存约 270 MiB；
- Python 派生脚本语法检查、动画挑选器 109 项/51 项正式用途校验、场景 JSON 解析及 `git diff --check` 均通过。

### Git 提交

- 本轮代码、配置、派生素材、验证图和文档将以“优化身体动作过渡与放大清晰度”提交并推送；未触碰用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 用户在本地最新版以 200% 观察模式二各部位动作细节与待机透明边缘，并在模式三连续触发摸摸头和其它部位，确认约 4 秒长度及淡出/淡入收尾手感。

## 2026-09-10：修复请求文字镜像并同步高速追包渲染

### 实现

- 请求小笼包动画进入前强制把 `PetDirectionTransform.ScaleX` 恢复为 1，带字画面不再继承向左追包时的水平镜像；重新追逐或返程后仍会按目标方向正常翻转人物；
- 追逐加速时间改为 3.5 秒，180→800 DIP/s 上限和返程 850 DIP/s 保持；1080p、2K、4K 在相同逻辑对角线比例下的完整对角线理论时间约 4.11 秒；
- 逐帧检查两套进食素材后拆分闭嘴起点：V1 从第 120 帧、V2 从第 112 帧，以 1.1 倍速播放到第 172 帧；延时由动画清单逐帧求和，不再维护会与素材时长漂移的单一硬编码值，实际约 0.79/0.91 秒；
- `AnimationFramePlayer` 和追包窗口位移均改由 `CompositionTarget.Rendering` 驱动，人物换帧与窗口移动跟随真实屏幕刷新；位移改用 `Stopwatch` 单调时钟、物理像素吸附和 34 ms 单帧补偿上限，降低高速时两个定时器错拍、亚像素闪烁及卡顿后瞬移造成的视觉模糊；
- 模式二十项动作从 720×720 原始透明帧直接生成 440×476 图集；模式三身体反应、拖拽反应和请求小笼包补足 2 px/DIP；两套吃包子图集由 384×384 提升到 410×410。所有放大继续使用可复现的预乘 Alpha Lanczos，低分辨率表情只加轻度边缘锐化，不重绘或改写原图文字；
- 新增 200% 密度抽查图 `docs/validation/animation-200pct-density-2026-09-10.png`；运行时动画目录约 392.8 MiB，较上一版增加约 109.2 MiB，明确记录清晰度与包体积权衡。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 544/544 通过（400、37、107）；更新加速中点、3.5 秒上限、跨分辨率对角线时长及 4K 200% 归一化断言；
- AssetCompiler 验证 63/63 项运行时动画；模式二十项均为 440×476，模式三目标反应和请求小笼包均达到 2 px/DIP，两套追包和进食均为 410×410；派生脚本语法检查及场景/动画来源 JSON 解析通过；
- `--qa-bun-chase --qa-window` 分别以晶蓝礼服和经典猫耳运行真实 WPF 追逐、进食、闭嘴与返程，两套均记录 `file_treat.completed` 且进程保持响应；约 12 秒采样私有内存分别为 378.7/378.1 MiB；
- `--preview-body-reaction=crystal-headpat` 在 440×476 图集下完整播放并记录 `animation.body_reaction_in_place_completed`，进程保持响应，结束前私有内存约 337 MiB；模式三 360×360 摸摸头预演同样保持响应；
- 静态接触表在蓝底原尺寸检查了请求文字、模式二/三身体动作及两套奔跑代表帧，人物和透明边缘完整。向左追逐并持续拖住唯一包子 3 秒的最终鼠标操作仍需用户在正常桌面中亲自确认。

### Git 提交

- 本轮代码、测试、配置、高分辨率派生素材、验证图与文档将以“同步追包渲染并提升动画清晰度”提交并推送；未触碰用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 用户在本地最新版用 200% 档位观察模式二/模式三动作和高速追包，再向左拖住唯一包子触发请求彩蛋，确认文字正向、移动舒适度以及两套闭嘴节奏。

## 2026-09-11：压缩高分辨率运行动画并限制解码缓存

### 实现

- 对运行素材做体积画像：模式二十项 440×476 PNG 图集约 303 MiB，是放大后包体增长的主因；代码和普通单帧素材不是主要占用；
- 将模型二十项、模式三 14 项 2 px/DIP 反应/拖拽/请求素材以及两套追包与进食，共 28 项运行动画改为质量 95、精确 Alpha 的动画 WebP。分辨率、逻辑帧数、逐帧时长、循环次数和显示槽不变，官方源 PNG/GIF/WebP 不覆盖；
- 新增 SkiaSharp 3.119.4 解码路径并随 win-x64 自包含包发布，目标电脑无需单独安装 Microsoft WebP Image Extensions。WebP 物理帧少于逻辑帧时按编码时长扩展完全重复帧，两套 172 物理帧进食文件仍恢复为 173 个 16 ms 逻辑帧；
- `AnimationFramePlayer` 增加 192 MiB 最近使用缓存上限；当前动画始终保留，切走后可回收旧大动作，避免逐项点击模型二动作导致解码缓存持续增长；
- 发布只保留 `zh-Hans` 卫星资源并剔除 80.14 MiB 的 `libSkiaSharp.pdb`；MSIX 图标生成器可直接从动画 WebP 指定帧取图。

### 体积与画质

- 被替换的 PNG 图集共 377.07 MiB，新 WebP 共 112.85 MiB，减少 264.22 MiB（70.1%）；完整运行时动画目录由 392.80 MiB 降至 128.59 MiB；
- win-x64 自包含发布目录为 321.08 MiB，Optimal ZIP 为 208.09 MiB；正式签名测试包 `LuoTianyiPet_0.1.0.34_x64.msix` 为 217,080,402 字节（207.02 MiB），SHA-256 `5afb34793ff897223c0231a460ceb78dd3a9625e7f8274df18112a8a73c6f0d0`；
- 另行只做体积评估的 framework-dependent 发布目录为 166.12 MiB、Optimal ZIP 为 141.15 MiB，但要求目标电脑预装匹配的 .NET 10 Desktop Runtime，因此没有替代默认的全电脑可直接运行包；
- 以模型二摸摸头第 0/72/144 帧做原 PNG、质量 95 WebP 和 6 倍差异图并人工检查，Alpha 最大误差为 0；对可见像素的中间动作帧平均绝对 RGB 误差约 1.79/255、PSNR 约 39.67 dB。对比图保存在 `docs/validation/animation-webp-q95-comparison-2026-09-11.jpg`。

### 验证

- Release 全量构建成功，0 警告、0 错误；Core、Animation、Windows 三组自动化测试 544/544 通过（400、37、107）；AssetCompiler 验证 63/63 项运行时动画；
- Python 派生脚本语法检查通过；28 项 WebP 的尺寸、物理帧、Alpha 时间轴总时长和清单逻辑帧逐项一致；NuGet 漏洞检查未发现直接或传递漏洞；
- 真实 WPF 预演模型二摸摸头，完整记录 `animation.body_reaction_in_place_completed`；两套 `--qa-bun-chase` 均完整记录 `file_treat.completed`，进程持续响应且新版日志没有 `animation.play_failed`；
- `0.1.0.34` MSIX 构建、签名和清单校验通过；开发证书和应用包没有在本轮自动安装。

### Git 提交

- 本轮代码、配置、压缩运行素材、验证图与文档将以“压缩高分辨率运行动画”提交并推送；未触碰用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 用户在本地最新版以 200% 依次观察模式二摸摸头、模式三身体反应和两套高速追包，确认压缩前后观感一致；若仍要进一步缩包，下一阶段优先评估“需预装 .NET Desktop Runtime 的约 140 MiB 精简版”，而不是降低现有分辨率或帧率。

## 2026-09-11：默认安装版迁移到 Windows 自带 .NET Framework 4.8

### 实现

- 没有更换语言、UI 框架或重写桌宠；四个源码项目和三组测试增加可选 `net48` 目标，默认开发构建仍保持 .NET 10，只有安装发布显式开启双目标并选择 .NET Framework 4.8；
- 增加小范围兼容实现，替换 .NET Framework 缺少的参数检查、数值、随机数、文本分割、路径、文件异步、哈希、时间和原生指针便捷 API；旧目标使用兼容版 NAudio 与 Windows SDK Contracts，行为由同一组测试约束；
- `build_msix.ps1` 与 `build_sideload_bundle.ps1` 默认生成完整的 .NET Framework 4.8 x64 MSIX，最低系统版本为 Windows 10 22H2（19045）。所有动画仍随包安装，用户无需额外下载 .NET；可以用 `-Framework Net10SelfContained` 生成原来的自包含回退包；
- net48 发布布局删除未使用的 macOS、x86、Arm64 和重复 x64 SkiaSharp 原生库以及调试符号，只保留根目录实际加载的 x64 `libSkiaSharp.dll`；
- 真实安装测试发现旧 WinRT 投影在通知事件订阅失败时可能抛出基础 `Exception` 而不是 `COMException`；通知适配器现按 `0x800706BA`、`0x800706BE`、`0x80010108` 三个 RPC HRESULT 统一视为可恢复平台瞬断，授权状态查询和事件订阅都安全降级，并增加三项精确回归测试。

### 体积

- 优化后的 net48 发布目录为 143.45 MiB；默认命令重建的签名 `LuoTianyiPet_0.1.0.36_x64.msix` 为 141,967,058 字节（135.39 MiB），SHA-256 为 `8c88b5159e49be5164dc3e2ca80dee9dcda76a00682268b53854b2d2e1e4e05c`；
- 完整测试安装包 `LuoTianyiPet-Installer-0.1.0.36-win-x64.zip` 为 141,867,125 字节（135.30 MiB），SHA-256 为 `f9ffd99a8d32eb78b7f17d9d898ca64e537ffb33e27a18ecbfe72371522f8414`；
- 与相同动画内容的 .NET 10 自包含 `0.1.0.34` 207.02 MiB 相比，MSIX 减少 71.63 MiB，即 34.6%；128.59 MiB 动画目录、原分辨率、60 FPS 补帧和显示逻辑均未削减。

### 验证

- .NET 10 Release 全量构建 0 警告、0 错误，Core、Animation、Platform 测试 547/547 通过（400、37、110）；同三组测试在 net48 通过 547/547；
- 未安装发布目录启动测试加载 63 项动画和两套身体多边形区域，模型二 `crystal-headpat` WebP 可完整播放，窗口持续响应；
- `0.1.0.36` 已真实覆盖安装，包状态为 `Ok`；从 AppsFolder 启动的可执行文件位于 WindowsApps 版本目录，日志确认安装存储模式、63 项动画、两套身体区域、通知 `Allowed`、桌面监听、Core Audio、曲名检测和托盘全部启动，无新的调度器或未处理异常；测试结束后只关闭了进程，没有卸载当前安装包或更改用户设置。

### Git 提交

- 本轮代码、测试、打包脚本与文档以“缩减完整安装包体积”提交并推送；未触碰用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 在另一台 Windows 11 电脑用完整安装 ZIP 做一次冷机安装，确认无需额外下载运行时；再由用户亲自授权并实际接收 QQ/微信通知。特殊精简系统或 Windows 10 22H2 以前版本改用显式 .NET 10 自包含回退包。

## 2026-09-11：修复安装无反馈并交付可直接运行的便携版

### 问题定位

- 本机包查询显示 `LuoTianyiPet.Dev 0.1.0.36` 已安装且状态 `Ok`，桌宠进程从 20:33 起持续响应；再次运行安装脚本时返回成功，但相同版本不会重复注册，AppsFolder 再启动的第二进程又被单实例锁立即结束，旧成功窗口没有暂停或提示，因此用户看到“没反应”；
- 日志没有新启动记录，进一步确认不是安装失败或 .NET Framework 缺失，而是“已有主实例 + 静默第二实例”的反馈缺陷。

### 实现

- 安装脚本在完成注册后刷新当前用户桌面 `洛天依桌宠.lnk`，快捷方式通过 AppsFolder AUMID 启动包身份应用；检测已有 `LuoTianyiPet` 进程时保留原实例并明确提示查看桌面或托盘，没有实例时启动并等待最多 15 秒确认；双击安装脚本会显示安装结果弹窗，自动化可用 `-Quiet` 关闭弹窗；
- 安装包版本升级到 `0.1.0.37`，避免开发阶段相同版本内容变化被 Windows 正确地视为无需升级；
- 便携打包默认切到 net48，解压目录和 ZIP 去掉“Test”命名。程序发现同目录 `LUOTIANYI_PET_PORTABLE.marker` 时自动使用相邻 `UserData`，因此用户可以直接双击 `LuoTianyiPet.exe`；命令脚本仍作为兼容入口；
- 便携构建校验不再按中文文件名字面量判断，规避 Windows PowerShell 5.1 在无 BOM/不同区域设置下把中文脚本名解码成乱码；补充显式加载 `System.IO.Compression.FileSystem`，保证 `ZipFile` 在干净 PowerShell 5.1 会话可用；
- 项目明确采用双交付：真便携版不注册证书、包、服务或注册表，因此没有 `UserNotificationListener`；完整 MSIX 继续提供 QQ/微信通知。未来需要任意安装路径时采用微软“外部位置身份包”路线，但仍需传统安装、可信签名和卸载处理。

### 验证与交付

- 便携 `LuoTianyiPet.exe` 不带参数直接启动，进程持续响应；本地日志为 `app.storage_mode Portable`，载入 63 项动画和两套身体区域，设置/日志只位于相邻 `UserData`。包内清理脚本精确停止 1 个便携进程并删除 `UserData`；
- 本机从 `0.1.0.36` 真实升级到 `0.1.0.37`，包状态 `Ok`，进程位于对应 WindowsApps 目录并响应；桌面快捷方式目标为 `explorer.exe`、参数为包的 AppsFolder AUMID、图标取当前安装 EXE；重复执行安装脚本保持同一进程 ID 并打印“已安装且正在运行”；
- 安装版启动日志确认通知为 `Allowed`，63 项动画、身体区域、桌面观察、Core Audio、曲名检测和托盘全部启动；
- .NET 10 与 net48 Release 构建均为 0 警告、0 错误；两套 Core/Animation/Platform 测试各 547/547 通过；五份 PowerShell 发布/安装/清理脚本语法解析通过；
- `LuoTianyiPet-Portable-0.1.0.37-win-x64.zip` 为 141,573,824 字节（135.02 MiB），SHA-256 `7e742444b46e16fb90840ca11a724e29c4ddb0ee4a09b2ee2842d2e5c7805e84`；
- `LuoTianyiPet_0.1.0.37_x64.msix` 为 141,967,129 字节（135.39 MiB），SHA-256 `ff60c03da58ac1a8cf1f46f5392a8681337c9eee7769c18beee563cb3b5aff1e`；完整安装 ZIP 为 141,867,941 字节（135.30 MiB），SHA-256 `3d9a58ab0dcb9da139da3582b3100a63dd3afe2f1f154984c9aa395600fa77b9`。

### Git 提交

- 本轮代码、便携/安装打包脚本和四份权威文档以“修复安装反馈并交付便携版”提交并推送；不提交用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 在另一台 Windows 11 电脑分别测试便携直接启动与完整安装；安装版由用户亲自批准通知权限并实收 QQ、微信各一条。若确认必须让安装版选择任意目录，再建立传统安装器和外部位置身份包，不改变真便携版的无注册承诺。

## 2026-09-11：清除“一键唱歌”文字并放慢嘴型

### 实现

- 保留官方 `洛天依元旦祝福_一键唱歌.gif` 原件不变；在现有派生脚本中对稳定歌唱段逐帧清除左侧 `(0,15)-(33,126)` 竖排“一键唱歌”文字，角色主体与左右动态音符全部保留；
- 派生仍使用源第 `7,8,9,10,11,12,13,14,15,14,13,12,11,10,9,8` 帧，首尾继续只跨相邻源动作；每帧由 100 毫秒延长到 160 毫秒，整圈由 1.60 秒延长到 2.56 秒，嘴型节奏降为原来的 62.5%；
- 脚本新增保护性像素检查：文字坐标带必须完全透明，坐标带外 RGBA 必须与对应官方源帧一致。派生元数据记录文字范围、16 项时长和新哈希；运行图集、动画清单以及设置页“一键唱歌/智能分配”预览同步更新；
- 安装与便携构建默认版本提升到 `0.1.0.38`，避免 Windows 将新内容视为已安装的相同版本。

### 验证与交付

- 输出 GIF 解码为 16 帧，全部为 160 毫秒，总周期 2560 毫秒；文字带 Alpha 最大值为 0。蓝底 16 帧接触表确认人物轮廓、服装和两侧音符完整，保存在 `docs/validation/one-click-singing-textless-slower-2026-09-11.png`；
- Release 构建 0 警告、0 错误；.NET 10 与 net48 的 Core、Animation、Platform 三组测试各 547/547 通过（400、37、110）；AssetCompiler 验证 63/63 项运行时动画，动画挑选器 109 项/51 项正式用途校验通过；
- `LuoTianyiPet_0.1.0.38_x64.msix` 为 141,915,558 字节（135.34 MiB），SHA-256 `fbce769bba7b9bf3359878d3a79ded6bccf4e1c0521cd20db41f15f33766ca5e`；完整安装 ZIP 为 141,829,332 字节（135.26 MiB），SHA-256 `e83bc4a6ad94811b1b4b58dd6c804db0294a9f7f28de36f958b1ecccb2a54ae9`；
- 本机已升级到 `LuoTianyiPet.Dev 0.1.0.38`，包状态 `Ok`；停止仍占用单实例锁的旧 `0.1.0.37` 便携进程后，从 AppsFolder 启动 WindowsApps 安装版，当前进程响应正常。

### Git 提交

- 本轮派生脚本、正式 GIF/图集、预览、清单、打包版本和权威文档将以“清除一键唱歌文字并放慢嘴型”提交并推送；不提交用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 用户在本地播放洛天依歌曲并触发“一键唱歌”，连续观察数轮，确认 2.56 秒循环下嘴型速度和整体停顿感合适；若还偏快，只需继续调整单一帧时长常量，不改人物画面。

## 2026-09-11：加快播放状态反馈并补全洛天依合唱识别

### 实现

- 把控制岛播放/暂停图标从人物音乐状态机中拆出：外部停止连续静音 750 毫秒即可更新按钮，人物动画仍保留 5 秒切歌抗闪宽限；桌宠成功发出播放/暂停快捷键后按钮立即切换，以 2 秒预期窗等真实音频确认，未生效时自动纠正；
- 上一首/下一首已有 15 秒加载保持期间不消费按钮快速静音样本，避免网易云换歌加载时按钮与人物动画来回跳；探测异常继续保留原按钮状态；
- 洛天依歌手判断支持她出现在合唱字段任意位置，识别 `/`、顿号、逗号、`&`、`+`、竖线、间点、乘号以及 `feat.`、`featuring`、`with`、`vs` 等写法；`洛天依Official`、`Luo Tianyi Official` 也进入洛天依彩蛋，仍拒绝 `洛天依然` 这类仅前缀相同的名字；
- 音量弹层继续支持点击其它区域自动关闭；再次点击音量图标时直接收起，并抑制 WPF 自动关闭与同次按钮点击造成的立刻重开；
- 安装、旁加载和便携构建默认版本提升到 `0.1.0.39`。

### 验证与交付

- 新增 15 项 Core 回归测试，覆盖 750 毫秒外部静音、短静音、有声恢复、播放/暂停即时预期、两种命令未生效纠正、探测失败保持，以及合唱后位、空格连接、全角竖线、乘号、中英文 `Official` 等歌手写法；
- Release 构建 0 警告、0 错误；.NET 10 与 net48 的 Core、Animation、Platform 三组测试各 562/562 通过（415、37、110）；AssetCompiler 验证 63/63 项运行时动画；
- `LuoTianyiPet_0.1.0.39_x64.msix` 为 141,916,506 字节（135.34 MiB），SHA-256 `dae922731ed43787d1a3e84ac8428dacf40312f378a77229d1787c7ed08d6337`；完整安装 ZIP 为 141,830,332 字节（135.26 MiB），SHA-256 `173cd32546b6fffd9e67efee270eb97fdb438b2a50c2c8c22daf23e40ea8a19c`；
- `0.1.0.39` 已在本机真实升级，包状态、WindowsApps 进程与启动日志均正常。

### Git 提交

- 本轮代码、测试、打包版本和权威文档以“加快音乐反馈并补全歌手识别”提交并推送；不提交用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 用户用真实网易云分别验证桌宠按钮暂停/恢复、网易云内直接暂停、合唱与 `洛天依Official` 歌曲；由于这些操作会改变当前播放状态，自动验证不代替用户主动体验。

## 2026-09-11：重整时段问候、模式三彩蛋与动画挑选器

### 实现

- 五段时间调整为早晨 06:00～12:00、中午 12:00～13:30、下午 13:30～18:00、傍晚 18:00～20:00、夜间 20:00～06:00；新增“十二周年·饿”傍晚运行素材。开机和跨时段问候统一展示 10 秒，可被人物或媒体输入打断；音乐中挂起，停歌稳定 10 秒后仍处于对应时段才补播，期间恢复音乐立即让位；
- 经典猫耳摸头把 10 张原帧编为 28 帧 `0→9→0→9` 连续路线，约完成三次抚摸、总时长 2.24 秒；“重罪/黑化”有效播放速度提高到约 0.8/0.6。嘿嘿状态收到按下或拖拽即回待机，`眠了呼吸` 只在模式三启用；
- 新增快速左右甩动判定：900 毫秒内三段横移均达到 32 DIP、360 DIP/秒且换向两次后，模式三循环“十周年·旋转舞”，直到下一次点击或音乐开始。完整删除快速下甩判定、降落弹跳动作、呜呜晃动反馈和相应运行资产；
- 动画挑选器删除用户点名的 12 项预览与三项旧文件吞食派生预览，新增三个待机模式的简明动画/触发规则卡；当前校验为 97 项预览、51 项正式用途。图集编译器新增显式 `frameSequence`，运行清单现有 62 项；
- 通知实机验证发现 `NotificationChanged` 在通知平台不可用时会把 `0x803E0105` 异步送入 WPF 调度器。安装版改用 `GetNotificationsAsync(Toast)` 每 750 毫秒读取官方快照：首次只建立通知 ID 基线，后续仅派发新增项，单通道门控避免重叠，平台瞬断下一轮自动恢复；不再经过不稳定的事件订阅桥接。

### 验证与交付

- Release 构建 0 警告、0 错误；.NET 10 与 net48 的 Core、Animation、Platform 三组测试各 570/570 通过（419、37、114）；其中新增三项覆盖通知快照首次基线、ID 重新出现和停止后重新基线；动画挑选器校验为 97/51，运行动画清单为 62 项；
- 真实 WPF 隔离预览确认摸头完整展示约 2.25 秒并柔和恢复；傍晚“饿”和旋转舞分别连续运行 6 秒且窗口持续响应。新摸头源帧接触表保存在 `docs/validation/headpat-source-contact-sheet-2026-09-11.png`；
- `LuoTianyiPet_0.1.0.43_x64.msix` 为 143,342,544 字节（136.70 MiB），SHA-256 `c12e510ca9618034cad5d8a7a9f30d5a9dc755abe56e710a127b5703871b2a19`；完整安装 ZIP 为 143,257,009 字节（136.62 MiB），SHA-256 `1ded65f66d5d4fe665f00eb0391829bb6866c54c507a13e8b5329fef338db3b9`；便携 ZIP 为 143,170,367 字节（136.54 MiB），SHA-256 `8d8bc6cdb14f7a472e18d3af2776a1d63d19b26a5b3c0f4be30968e261fe4c36`；
- 本机已真实升级到 `LuoTianyiPet.Dev 0.1.0.43`，包状态 `Ok`，WindowsApps 进程响应正常；既有通知授权显示 `Allowed`，快照监听、桌面文件观察、Core Audio、曲名检测和托盘均启动，最终启动日志无新增 `ERROR`。

### Git 提交

- 本轮代码、测试、运行素材、挑选器、打包版本和权威文档以“重整待机时段与模式三拖拽彩蛋”提交并推送；不提交用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 用户在本机体验三次摸头节奏、模式三左右快速甩动与点击退出；再分别在音乐跨时段、停歌超过 10 秒和 QQ/微信新通知三种真实场景验收优先级与提醒延迟。

## 2026-09-12：增加乐正绫打 call 彩蛋并精简桌宠提示

### 实现

- 音乐歌手匹配扩展到“乐正绫”/`Yuezheng Ling`、`Official` 后缀及常见合唱分隔；智能分配下乐正绫本人或参与合唱时持续循环“十二周年·打 call”。选择器先判断洛天依，因此“乐正绫 feat. 洛天依”等二人合唱仍优先随机音乐摇摆/一键唱歌；固定唯一动画和关闭总开关保持原语义；
- “十二周年·打 call”官方 196 帧 GIF 经逐帧哈希确认是同一 14 帧周期重复 14 次。运行时只取首周期，生成质量 95、380×380 的透明动画 WebP，在 190×190 DIP 槽内无限循环；设置自动预览改为四画面网格，动画挑选器标注乐正绫用途与洛天依优先级，当前为 97 项预览、52 项正式用途；派生脚本同时按当前晶蓝礼服待机源重新同步应用图标与来源哈希；
- 删除“已发送网易云控制命令”“网易云已开始播放”“切歌成功”“已开启/关闭开机自启动”、回收进度/成功和重复投放说明等过程气泡；保留错误、超时、网易云冷启动过程、音量失败、文件达到可松手区域和回收失败。提示外观改为带“依”字徽记的浅天依蓝对话条，复用歌名安全槽，显示时先隐藏歌名，结束后按原显示条件恢复，不再遮挡人物或叠字；
- 修正中等待机手势：鼠标按下不再提前清除“嘿嘿”，只有确认是单击才恢复待机；确认进入拖拽后状态机保存 `MediumIdle`，松手重新显示“嘿嘿”。新增 `--qa-feedback=` 隔离预览入口，仅用于稳定复现提示布局；
- 桌面生成 `洛天依桌宠提示清单_待核对.md`，列出全部保留提示、已移除过程提示、显示时长与需要用户确认的项目；安装、旁加载和便携默认版本提升到 `0.1.0.44`。

### 验证与交付

- .NET 10 与 net48 的 Core、Animation、Platform 三组测试各 585/585 通过（434、37、114）；新增 15 项 Core 用例覆盖乐正绫中英文/Official/合唱、洛天依优先级及从“嘿嘿”拖拽后的状态恢复；Release 构建 0 警告、0 错误；
- AssetCompiler 验证 63/63 项运行时动画；`twelfth-anniversary-call` 为 14 帧、每帧 100 ms、无限循环，运行文件 751,026 字节。首周期接触表保存在 `docs/validation/yuezheng-call-loop-2026-09-11.png`；场景与来源 JSON 解析、动画挑选器 97/52 校验、`git diff --check` 均通过；
- 真实 WPF 以歌名与持续提示同时开启的隔离场景截图，确认提示位于人物上方、歌名先隐藏且角色未被遮挡，证据为 `docs/validation/tianyi-feedback-bubble-2026-09-11.png`。Computer Use 的受信 RPC 本轮未配置，故使用进程窗口矩形的只读截图完成视觉检查，未对其它应用注入输入；
- `LuoTianyiPet_0.1.0.44_x64.msix` 为 144,132,104 字节（137.46 MiB），SHA-256 `ac355cb07f1309a46a301dbd277af8937c2bf69e6fdacdc6f963d3702b69e76d`；完整安装 ZIP 为 144,034,220 字节（137.36 MiB），SHA-256 `520b472dc3a8898f363c743f4cda4d07126f753434dc761331688853e74d05bc`；便携 ZIP 为 143,736,883 字节（137.08 MiB），SHA-256 `3d414ba8d24b1a62dc6bfdec33a9fe055eeb20e69c29d3b6426a5ab55ccf8ebd`；
- 本机已升级到 `LuoTianyiPet.Dev 0.1.0.44`，包状态 `Ok`，WindowsApps 进程持续响应；启动日志载入 63 项动画，通知权限为 `Allowed`，桌面观察、Core Audio、曲名检测与托盘均启动且无新增 `ERROR`。

### Git 提交

- 本轮代码、运行素材、测试、设置预览、提示清单依据、配置与权威文档将以“接入乐正绫彩蛋并精简提示”提交并推送；继续不提交用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 用户在网易云实际播放乐正绫独唱、乐正绫与其他歌手合唱、乐正绫与洛天依合唱各一首，确认“打 call”循环和洛天依优先级；随后核对桌面提示清单，决定是否继续合并网易云冷启动的持续提示。

## 2026-09-12：补齐新版微信任务栏提醒并收紧旋转舞手势

### 实现

- 复核本机实际状态：设置中消息提醒已开启、Toast 授权为 `Allowed`，但日志从未收到新增通知；当前新版微信运行 `Weixin.exe` 与 `WeChatAppEx.exe`，Windows 通知注册中也没有微信来源。确认原实现只轮询 Toast，无法从一个“已有未读”反推出新消息；
- 在不接触聊天内容的前提下增加 Windows Shell 任务栏闪烁回退。桌宠注册 `SHELLHOOK` 消息并处理 `HSHELL_FLASH`，仅将 HWND 解析为进程名；补充 QQNT 与 WeChatAppEx 内置别名，使旧设置无需迁移。连续闪烁按 QQ / 微信来源在 8 秒滑动窗口内合并一次，来源回到前台后重置；设置页明确区分 Toast 标题/图标权限和仅显示来源的闪烁回退；
- 模式三旋转舞由 900 毫秒内三段、两次换向调整为 1400 毫秒内五段、四次换向，即必须至少完成两次完整左右往返；每段 32 DIP、360 DIP/秒的距离与速度门槛保持不变。示例配置、动画挑选器和权威文档同步；
- 打包默认版本提升到 `0.1.0.47`。

### 验证与交付

- Release 双目标构建 0 警告、0 错误；.NET 10 与 net48 的 Core、Animation、Platform 三组测试各 599/599 通过（445、37、117）。新增进程别名、Shell 消息分类、不同来源会话去重、连续闪烁和前台重置测试；
- AssetCompiler 验证 63/63 项运行时动画；动画挑选器校验为 97 张卡片、52 项正式用途，区域编辑器脚本和两份 JSON 均通过解析，`git diff --check` 通过；
- 独立 `Weixin.exe` QA 窗口真实调用系统 `FlashWindowEx` 三次，安装版每轮只记录一次 `notification.shell_attention_detected WeChat`。一轮正逢启动问候，先记为 `Deferred`，问候结束后展示；最终 `0.1.0.47` 安装版在安全桌面直接记录 `Show` 与 `notification.reaction_started`，证明即时和延后链均有效。视觉证据为 `docs/validation/wechat-shell-attention-2026-09-12.png`；
- `LuoTianyiPet_0.1.0.47_x64.msix` 为 144,133,936 字节（137.46 MiB），SHA-256 `919f1462ce26c5efd072d7f50e22b2748bd4ebd0eb736068feec7a009b3d5137`；完整安装 ZIP 为 144,036,135 字节（137.36 MiB），SHA-256 `21f45b94f43431bbdafbb62b795179a9bb821d158b4fd39b16a1da793ad54f65`；便携 ZIP 为 143,738,704 字节（137.08 MiB），SHA-256 `8f327e4089b9211d222aa77fa20391bdad1303bf3766b060cc76e7c162d9c852`；
- 本机已升级到 `LuoTianyiPet.Dev 0.1.0.47`，包状态 `Ok`、WindowsApps 进程响应正常；启动日志确认 Shell 回退 `Available`、Toast 仍为 `Allowed`，且桌面观察、Core Audio、曲名检测和托盘均正常启动。

### 下一步

- 让后台真实微信或 QQ 收到一条新的测试消息，确认该客户端的“系统通知/任务栏闪烁”设置会产生至少一种公开事件。保留的旧未读不会触发补报；若客户端同时关闭两种提示，桌宠不会越过隐私边界读取聊天数据库或窗口内容。
## 2026-09-12：晶蓝礼服两段式睡觉 / 鸭子坐与梦境装饰

- 取消第二模型 5 分钟进入“嘿嘿”，将 30 分钟无操作改为睡觉和鸭子坐各 50% 随机；睡觉在第 250 帧睁眼卧姿定格，鸭子坐在第 120 帧闭眼坐姿定格，点击人物后才播放剩余起身段并恢复待机。
- 新增第二个轻量动画播放器作为人物头部右上角装饰层；保持期立即且每 5 分钟按 zzz 60%、梦见包子 35%、梦见乐正绫 5% 重抽，允许连续相同；云朵类点击唤醒时快速播放一次云朵消散。
- 扩展 `prepare_crystal_interactions.py`：睡觉 Z 字只从派生帧限定区域移除；小装饰的 PR 近白/近黑色块按边缘连通洪泛转 Alpha，保留封闭前景内部同色细节；按联合 Alpha 边界裁切、预乘 Alpha 缩放并编码小尺寸 WebP。原始用户 PNG 均不覆盖。
- 更新场景规则、动画挑选器、需求、状态映射、架构和总控文档；新增 50:50 与 60:35:5 边界测试。
- .NET 10 与 net48 Release 全量自动化测试各 606/606 通过（Core 452、Animation 37、Windows 117），Release 构建 0 警告、0 错误；AssetCompiler 验证 69/69 项运行时动画，动画挑选器验证 97 张卡片/54 项正式用途。
- 真实 WPF `--qa-long-idle --qa-window --qa-full-body-style=full-body-crystal-dress` 演出依次记录睡觉进入、第 250 帧保持、DreamBun/zzz 装饰选择、251～360 帧唤醒与恢复待机；窗口始终为 236×364 且保持响应。接触表和保持态实窗证据保存于 `docs/validation/crystal-long-idle-runtime-2026-09-12.png` 与 `docs/validation/crystal-long-idle-hold-live-2026-09-12.png`。
- 构建 `0.1.0.48`：便携 ZIP 169.57 MiB（解压 177.84 MiB）、开发签名 MSIX 169.76 MiB、安装包 ZIP 169.69 MiB；MSIX 安装后状态为 `Ok`，正式进程持续响应，桌面快捷方式已刷新。

## 2026-09-12：延长 QQ / 微信提醒并按模式重做动画挑选器

- 修复通知展示的自取消：1 秒安全状态轮询现在允许当前消息提醒令牌继续占用一次性反应槽，不会再把自己的提醒误判成其它动画；“代号洛天依·好奇摇摆”改为 30 秒最短展示，期间程序摇摆持续循环。打开对应来源应用、全屏/锁屏、拖拽或更高优先级反应仍会提前取消并恢复此前持续状态；
- 动画挑选器新增“模式一（心率共鸣完整Q版） / 模式二（心率共鸣简约Q版） / 模式三（心率共鸣表情包版） / 三个模式通用”四个可切换视图。每个模式显示实际会出现的专属和公共动画，提供缩略预览、完整触发规则与适用范围；三个模式共有项另有独立汇总，原有全部素材、候选筛选、分配和待删除功能保留在下方；
- 修正挑选器与权威文档中模式二仍会 5 分钟进入“嘿嘿”的过期说明；当前模式计数为 22 / 34 / 39，公共动画 20 项，总素材 101 项、正式用途 54 项。离线校验同时覆盖默认模式、四个模式的隔离显示和旧 v3 选择数据迁移；
- .NET 10 与 net48 Release 双目标构建 0 警告、0 错误；Core、Animation、Platform 三组测试两套目标各 608/608 通过（454、37、117），其中新增两项覆盖 30 秒默认值及“当前消息令牌可继续占用反应槽”；AssetCompiler 验证 69/69，两个 JSON 解析、挑选器脚本和 `git diff --check` 通过；
- 真实 WPF `--qa-message-notification=wechat --qa-window` 记录提醒在 `01:13:27.961` 开始、`01:13:57.506` 完成，约 30 秒且进程持续响应；旧日志对照为不到两秒即 `presentation_cancelled`。Computer Use 的浏览器安全策略拒绝 `file://` 与 localhost 页面自动截图，因此挑选器以脚本执行和 DOM 四模式断言完成机器校验，最终视觉由现有本地标签页刷新确认；
- 构建并安装 `0.1.0.49`：开发签名 MSIX 169.76 MiB，SHA-256 `173d3e9cc600456b87aa7a14c366ce4842e1958d4f57abd7de2bfdebbf087928`；完整安装 ZIP 169.69 MiB，SHA-256 `caeb20f834e1a4d53d23ab88a553e57a452201ed4eb086d3d7e68a02e1ea968a`；便携 ZIP 169.57 MiB，SHA-256 `afde2b99d963b00bda1bb509ee6cefac52fe8f59a8786c5d7a3877c78c70e77d`。本机包状态 `Ok`，旧 `0.1.0.37` 便携测试进程已由已安装 `0.1.0.49` 取代，WindowsApps 进程持续响应，通知授权 `Allowed`、Shell 回退可用且启动日志无新增错误。

### Git 提交

- 本轮以 `延长消息提醒并按模式重做动画挑选器` 提交到 `main` 并推送；继续不纳入用户尚未归档的 Blender 脚本和两项拖拽候选 WebP。

### 下一步

- 用户让后台 QQ 或微信收到一条新消息，感受 30 秒是否过长；再在动画挑选器逐项切换三套模式，核对专属/公共归类后继续调整。

## 2026-09-12：增加模式二两种长待机的临时人工测试入口

- 将单一 `--qa-long-idle` 扩展为可指定 `--qa-long-idle=sleep` 与 `--qa-long-idle=duck-sit`：启动后约 700 毫秒直接进入真实的睡觉或鸭子坐前半段，分别在第 250/120 帧保持，不再自动唤醒；用户点击人物后仍走正式后半段、装饰消散和待机恢复链路。无值旧参数继续随机并在 20 秒后自动唤醒，保留无人值守回归用途；
- 桌面新增临时启动器 `测试模式二睡觉定格与恢复.cmd`，可用数字键选择两种动作；测试实例使用独立 QA 单实例和晶蓝礼服外观覆盖，不改变安装版设置、正式 30 分钟门槛、50:50 随机概率或点击重置规则；
- Core 新增 QA 参数解析边界测试；.NET 10 与 net48 Release 双目标构建均为 0 警告、0 错误，两套 Core、Animation、Platform 测试各 613/613 通过（459、37、117）；睡觉实窗记录第 250 帧保持，鸭子坐实窗记录第 120 帧保持且额外停留 3 秒后未自动起身，两次进程均持续响应；
- 构建并安装 `0.1.0.50`：MSIX 169.76 MiB，SHA-256 `443e599aca99c09d42b1dcf70aafc89b9b0fca307bbd0358e8e349ae04783c1f`；完整安装 ZIP 169.69 MiB，SHA-256 `8fa68cb9d0de363c02e65b41ae2635d83a652aaeced81921fc8b355f24dc0448`；便携 ZIP 169.57 MiB，SHA-256 `bddfd1f73fb3e61239ed7d1d2bb37a705435ef8f9f6b78e49ad9c18eeb6be056`。本机包状态 `Ok`，正式进程持续响应。

## 2026-09-12：修正晶蓝礼服睡觉闭眼定格与左侧头发裁切

- 根据用户实窗反馈复核 361 张源 PNG，确认旧第 250 帧已经睁眼，不适合作为做梦保持态；保持点前移到第 220 帧的稳定闭眼卧姿，点击人物后从第 221 帧继续原始睁眼和起身段，鸭子坐第 120 帧规则不变；
- 根因不是源图缺发，而是 720×720 源方画面按 244 DIP 比例缩放后宽 488 像素，却被放进标准 440×476 画布，横向两边各裁掉 24 像素。睡觉动作现单独使用 488×476 运行画布和 244×238 DIP 显示槽，人物缩放比例不变；源方画面在宽画布内右移 4 像素，使第 220 帧左侧 Alpha 从接触边缘变为保留 4 像素透明安全边；
- 原始 PNG 未覆盖，没有用生成式重画补头发；确定性派生脚本、WebP、预览、元数据、运行清单、动画挑选器与权威文档同步。真实 WPF `--qa-long-idle=sleep` 在 260×364 窗口保持第 220 帧，进程持续响应，闭眼和两侧头发均完整；证据为 `docs/validation/crystal-long-idle-sleep-hold-fixed-2026-09-12.png`；
- Release 双目标构建均为 0 警告、0 错误；.NET 10 与 net48 的 Core、Animation、Platform 测试各 613/613 通过（459、37、117）；AssetCompiler 校验 69/69，动画挑选器校验 101 张卡片/54 项正式用途。打包默认版本提升到 `0.1.0.51`。
- 构建并安装 `0.1.0.51`：MSIX 170.00 MiB，SHA-256 `6d8d63e56d72f2e5f661b9bdbb3751e3c97959730a6999fce6da05769c8fcce7`；完整安装 ZIP 169.93 MiB，SHA-256 `ed4738ad83b2c1b073b8f7a806c53ba210dfdaa3511e2dc1fb2f031a6f60d6ac`；便携 ZIP 169.81 MiB，SHA-256 `6c8d5e2b8aa064e2456fc71924fe5a7f6c21e950728d3d747bde05fbacca64e3`。本机包状态 `Ok`，WindowsApps 进程持续响应。

## 2026-09-12：消除模式二身体点击的同步 WebP 解码等待

- 根因确认：晶蓝礼服身体反应大多为 440×476、145 帧的动态 WebP，完整展开约 115.8 MiB；192 MiB LRU 无法同时保留两套大动作。每次换部位后旧实现会在 WPF UI 线程同步展开全部帧，真实日志中从 `interaction.body_hit` 到反应开始耗时 2026～2239 毫秒，再叠加单/双击识别等待，形成明显迟滞；
- `AnimationFramePlayer` 改为只同步解码当前播放所需首帧，随即开始时间轴；剩余物理帧在一个全局串行、`BelowNormal` 优先级后台任务中渐进转成冻结的 `Pbgra32 BitmapSource`。后台解码复用上一物理帧，遇到 `RestorePrevious` 处安全回退完整合成；时间轴短暂领先时显示最近可用帧，末帧未就绪前不误报完成；
- 继续使用原 192 MiB LRU 预算，没有提高缓存上限。动作离场被淘汰时立即取消其后台任务，应用退出时统一释放；不创建并行解码洪峰，不触碰其它进程、前台窗口、输入或游戏状态；后台失败会记录并走现有安全回退；
- Skia 解码基准以 `crystal-cover-eyes` 全 145 帧逐像素对照旧路径，结果 0 帧不一致；复用前帧的完整解码由约 2040 毫秒降至约 798 毫秒。真实 net48 WPF 隔离预演中，固定 500 毫秒 QA 延时之后仅约 74 毫秒即记录 `animation.in_place_reaction_started`，窗口全程响应；10 秒采样峰值私有内存约 264.6 MiB，低于旧同类预演约 337 MiB；
- 实窗中间帧确认遮眼动作、比例与透明边缘正常。Release 的 .NET 10 / net48 双目标构建均为 0 警告、0 错误；Core、Animation、Platform 六个测试目标共 1226/1226 通过（每套目标 459、37、117），`git diff --check` 通过。

### Git 提交

- 本轮以“优化动态 WebP 首帧响应”形成独立提交并推送；不纳入工作区中既有的长待机素材、装饰定位和 Blender 候选动作修改。

### 下一步

- 用户在包含本次变更的版本中连续点击模式二不同身体区域，确认主观跟手程度及首段是否仍有可见卡帧；若仍有个别动作异常，再按具体动画 ID 定点测量，不扩大缓存或后台并发。

## 2026-09-12：补全网易云合唱歌手并安装 0.1.0.52

- 目标：解决公开标题包含洛天依，但智能音乐动画仍为享受音乐的问题。
- 根因：本机系统媒体会话给出的歌手只有首位（3 字），同曲网易云公开主窗口标题为完整合作名单（15 字）；旧实现只要系统会话有曲名就返回，不再考虑窗口里的合唱歌手。诊断仅输出长度、匹配布尔值，不记录实际曲名、歌手或窗口标题。
- 修改模块：Windows `SystemMediaTrackInfoSource` 增加同曲、相容歌手信息补全；未知/失败窗口、切歌不同曲名、歌手冲突保持系统元数据；新增 8 项回归用例覆盖两个随机唱歌分支和安全降级。三个打包脚本默认版本统一为 0.1.0.52；产品选择规则和用户设置不变。
- 实际验证：从干净基线 `7dcf468` 加本项修复的隔离工作树构建，避免打入其它任务正在修改的菜单、待机素材或设置。Release 双目标编译成功；.NET 10 与 net48 测试各 621/621 通过（Core 459、Animation 37、Windows 125）；当前网易云元数据直接探测确认完整歌手名单与窗口一致并识别洛天依。
- 视觉：隔离 WPF 只读取真实曲目信息，在 QA 内触发音乐持续状态，日志为 `Animation=newyear-one-click-singing; ArtistClass=LuoTianyi`，实际窗口显示一键唱歌且持续响应。测试开关只在隔离构建期间临时加入并恢复源码，不进入安装版。computer-use 截图接口两次返回 `SetIsBorderRequired / 0x80004002`，改用原生 PrintWindow；截图仅保留人物区，见 `docs/validation/music-collaborator-singing-2026-09-12.png`。
- 安装：生成签名 MSIX 与完整安装 ZIP，位于 `artifacts/music-fix-worktree/artifacts/msix/release/` 与 `artifacts/music-fix-worktree/artifacts/sideload/release/`；已覆盖安装 0.1.0.52，包状态 Ok，正式 WindowsApps 进程响应正常，启动载入 69 项动画、通知权限 Allowed，启动无 ERROR。仅关闭本轮音乐 QA 实例，不干扰其它任务的 QA 窗口。
- 未验证/风险：实机当前没有检测到有声播放；本轮未替用户发送播放/切歌快捷键。真实恢复播放后的完整自动触发仍请用户观察；窗口标题在切歌时不同步的场景有回归覆盖。
- Git 提交：本节与修复同一提交，提交主题“修复网易云合唱歌手信息缺失”；验证后推送 origin/main。
- 下一步单一入口：`docs/项目总控.md` 第 8 节；请用户继续播放当前洛天依合作曲，确认正式桌宠进入音乐摇摆或一键唱歌。

## 2026-09-12：重整托盘、人物右键与音乐悬浮岛，安装 0.1.0.53

- 目标：落实用户确认的托盘左右键习惯、人物常用菜单及完全可关闭的音乐附件。
- 模块：新增 `MainWindow.QuickActions`、人物快捷面板和共享样式；简化托盘；完整设置新增显示大小；schema v15 增加位置锁与音乐悬浮岛偏好，v14 保留旧设置迁移。关闭附件同步停止计时器和音量弹层，拦截异步重新出现，不影响音乐检测／动画。
- 验证：基于已提交 `bc99906` 的独立工作树，仅加入本项交互改动；Release .NET 10 / net48 双目标 0 警告、0 错误，Core 459、Animation 37、Windows 126 各双份，共 1244/1244 通过。新回归验证 v14 无损迁移及新设置往返保存。
- 窗口验证：隔离 `--qa-quick-actions` 在真实 WPF 窗口内执行 10 项断言，包括关闭后悬停／切歌／延迟曲名不显示、完整控制岛开启、空曲目文案、锁定禁止拖动而允许换装、解锁恢复拖动、托盘隐藏／召回及重复召回不隐藏；退出码 0。渲染并人工查看人物菜单、托盘菜单、完整设置和附件开关截图，证据位于 `docs/validation/quick-actions-*-2026-09-12.png`。QA 使用独立单实例标识、不保存用户设置、不发送播放或切歌命令。
- 限制：本机 computer-use 截图接口返回 `SetIsBorderRequired / 0x80004002`，本轮视觉证据来自程序内部 WPF RenderTargetBitmap，不把它描述成真实鼠标输入端到端录制；多显示器异 DPI、真实头部抚摸和长期日常使用仍待用户体验。
- 安装：签名 MSIX `0.1.0.53` 为 178260824 字节，SHA-256 `4af87df49659542e9afcfe0c49e28435d514630c4de296590735a8e10fb2883b`；经 Windows 覆盖更新并从 AppsFolder 重启，包状态 `Ok`，WindowsApps 正式进程响应正常，载入 69 项动画、通知权限 `Allowed`，本次启动无 ERROR。保留 `0.1.0.52` 已提交的合唱歌手识别修复，其它未提交的反馈与长待机修改未打入此包。
- Git 提交：本节与交互修改独立提交“重整托盘和桌宠右键交互”，验证后推送 origin/main。源程序与文档按变更块暂存，保留其它任务的重叠修改。
- 下一步单一入口：`docs/项目总控.md` 第 8 节，用户体验新版菜单与音乐开关。

## 2026-09-12：修正长待机装饰颜色、锚点、换侧与点击起身

- 根因：zzz 源序列把上飘的大 Z 以灰褐 RGB 中间色导出；旧布局按装饰外框粗略贴右上角，最小 Z 与最小云朵没有统一起点，也没有工作区溢出判断。另一个确定性错误在鼠标按下：通用空闲恢复先把 `Sleeping` 改成 `Idle`，延迟到达的单击唤醒因此丢失长待机变体并直接回默认画面。
- 素材：原始 PNG 不覆盖。`prepare_crystal_interactions.py` 以 zzz 首帧淡蓝色阶校正循环中的灰褐中间色，保留 Alpha、白色高光与深蓝描边；睡觉/鸭子坐改为 600×476、300×238 DIP 的透明宽画布，只增加装饰留白，不缩小人物。运行清单仍为 69 项。
- 布局：新增 `LongIdleDecorationPlacementResolver`，分别用最小 Z `(49,121)/180` 和最小云朵 `(19,152.5)/(240,180)` 的真实像素中心对齐头侧锚点；比较左右候选在当前工作区的实际溢出，右边放不下时镜像到左边。云朵消散沿用当前梦境侧。
- 唤醒与测试：活动长待机跳过鼠标按下的通用 Idle 恢复；单击在进入段可排队，保持后完整播放睡觉 221～360 或鸭子坐 121～216 帧。桌面测试脚本现可固定睡觉+包子、鸭子坐+zzz、鸭子坐+包子及右边缘换侧，不保存设置。
- 验证：Release .NET 10 / net48 双目标 0 警告、0 错误；每套目标 Core 466、Animation 37、Platform 126，共 1258/1258 通过；新增 3 项左右溢出/像素锚点测试与 5 项 QA 装饰参数解析测试。AssetCompiler 69/69，挑选器 101 张/54 项正式用途，区域编辑器和两份 JSON 通过。真实 WPF 鼠标点击日志依次记录 `wake_started Frames=121-216` 与约 4 秒后的 `wake_completed`，中途截图为睁眼起身；右边缘实窗中云朵已镜像到左侧。证据见 `docs/validation/crystal-zzz-pale-blue-2026-09-12.png`、`crystal-duck-zzz-anchor-before-wake-2026-09-12.png`、`crystal-duck-wake-in-progress-2026-09-12.png` 与 `crystal-duck-bun-right-edge-flipped-2026-09-12.png`。
- 本节完成时默认打包版本提升到 `0.1.0.56`；安装与包哈希在隔离提交构建后补录。

## 2026-09-12：修复托盘菜单偏移并取消退出二次确认，安装 0.1.0.55

- 目标：菜单不再因内部按钮操作逐次向左上角漂移；退出只需单击一次。
- 根因：退出确认与取消按钮会重新读取鼠标坐标并调用定位，此时鼠标已经在菜单内部，导致新位置递进偏移。删除确认面板与相关重定位；打开前捕获本次托盘点击屏幕坐标，显示后仅按该锚点定位。原有告别动画及重复退出保护保持有效。
- 模块：TrayQuickPanel XAML / code-behind、MainWindow.QuickActionsQa；同步产品行为与技术约束说明。配置没有新增参数。
- 验证：基于 292cfc4 的隔离工作树，仅加入本轮修改；Release .NET 10 / net48 双目标编译 0 警告、0 错误，Core 459、Animation 37、Windows 126 各两套，共 1244/1244 测试通过。真实 net48 WPF 执行 21 项窗口断言（连续重开 8 次坐标和大小一致、一次点击派发退出并隐藏、不重新定位、Esc 收起不退出，以及原有召回和附件检查），退出码 0。
- 视觉证据：docs/validation/tray-stable-first-2026-09-12.png、tray-stable-reopened-2026-09-12.png，来自真实 WPF 窗口内部 RenderTargetBitmap；已人工查看。computer-use 截图重试两次均报 SetIsBorderRequired / 0x80004002，因此不宣称系统截图或真实鼠标端到端验证。跨显示器异 DPI 仍待用户体验。
- 安装：签名验证 Valid，0.1.0.55 已覆盖安装，包状态 Ok，WindowsApps 正式进程响应正常。MSIX SHA-256：ef3fd76ce25c2d0ecfee2ad5ec2b66aa18eee78617ace70e1cd6e31f65ce31f8；包位于 artifacts/tray-fix-worktree/artifacts/msix/release/。其它任务未提交修改未混入本包。
- Git 提交：本节与修复形成独立提交“修复托盘菜单偏移并取消退出确认”，验证后推送 origin/main。
- 下一步单一入口：docs/项目总控.md 第 8 节，用户体验托盘重复打开与单击退出。

## 2026-09-12：修复拖到顶部松手下移，安装 0.1.0.57

- 目标：第三模式从膨胀拖拽恢复待机后保持实际可见顶部贴边，不再自动下移。
- 根因：旧版拖动按膨胀的 Alpha 顶边限位，松手先用更高的待机／展示槽边界向下纠偏，再用尚未替换的膨胀画面重算附件，错误恢复上方歌名留白。旧基线在 200% 可稳定复现从 0 到约 70.07 DIP 的顶部偏移。
- 修改模块：MainWindow 在纠偏前捕获顶部接触；需要换图时使用 110/170 ms 纯透明过渡，在不可见中点按当前已解析持续动画的 Alpha 顶部定位并保持下方附件，不使用临时缩放矩阵。画面未变的模式一／二及音乐拖动仅保持顶部，不附加过渡。二次拖动会取消旧恢复；左右收纳与旋转舞保留优先级。
- 合并：本轮期间其它任务安装了 0.1.0.56 稳定布局，因此最终分支合入 2fe3704 的稳定展示槽和 main 的 ab73746 长待机修复，再验证并发布 0.1.0.57；没有用最初基于 0.1.0.55 的旧测试包覆盖新布局。其它未提交的提示、模式一投放限制和旋转舞倍率修改未打入本包。
- 实际验证：合并后 Release .NET 10 / net48 编译均为 0 警告、0 错误，双目标自动化 1278/1278（每套 Core 476、Animation 37、Windows 126）；顶部真实 WPF 检查双目标各 85/85，覆盖 50/100/150/200%、附件开关、重复顶部拖动、普通移动、打断过渡、音乐开始与三个模式；net48 原稳定布局 890/890 通过。共享项目的 .NET 10 Release 编译也通过。
- 视觉：检查真实 WPF 窗口 RenderTargetBitmap，证据为 docs/validation/top-drag-released-2026-09-12.png；坐标断言在 200% 恢复前后顶部均为 0。QA 调用程序内真实拖拽／过渡方法，不向其它应用注入输入；不把该截图描述为系统鼠标端到端录屏。125%/150% 系统 DPI 与跨屏仍待人工体验。
- 安装：0.1.0.57 签名 Valid、Windows 包状态 Ok；WindowsApps 正式 EXE 哈希与发布目录一致，进程持续响应，启动日志无新增 ERROR、通知 Allowed。MSIX SHA-256：9848822d60a0f82271fc0103cba155ca35198f4f24ed2a165dee8ccf31ffbaad。MSIX 与完整安装 ZIP 位于 artifacts/top-drag-fix-worktree/artifacts/msix/release/ 和 artifacts/top-drag-fix-worktree/artifacts/sideload/release/。
- Git：初始修复 88970a0，稳定布局与长待机合并后以“完成顶部拖拽修复并验证发布 0.1.0.57”收尾，推送 codex/fix-classic-top-drag；同样变更已按块同步原项目，保留已有未提交修改及暂存区。下次统一发布应合入该分支以同时保留最新布局和顶部修复。
- 下一步单一入口：docs/项目总控.md 第 8 节，请用户直接在已安装 0.1.0.57 以第三模式拖到顶部松手体验。

## 2026-09-12：统一拖拽四边定位，修复不能靠右并安装 0.1.0.58

- 目标：修复第三模式能拖近右侧、松手又被推回的问题，同时覆盖左边、底部及角落，保留 0.1.0.57 顶部修复。
- 根因：0.1.0.57 只对顶部接触跳过展示槽纠偏；其它松手仍由 SnapDragIntentPetInsideWorkArea 把最大透明展示槽夹回工作区。实际待机比展示槽窄，因此 200% 右侧空白约 134.08 DIP、左侧约 133.17 DIP；即使右上角走顶部特例，恢复后较窄的待机仍留下约 41.08 DIP。
- 实现：Core DragReleasePlacement 独立计算两轴，按松手时可见边界保留四边／角落接触，并仅夹取恢复后人物的真实 Alpha 范围。WPF 普通松手只保留这一条定位路径，移除按整个展示槽纠偏与顶部专用分支；换图统一在可取消纯透明过渡的不可见中点完成，同画面直接定位。旋转舞持续状态也按可见画面落位；真实越界收纳仍优先处理。附件尺寸与普通动画的稳定展示槽保持原方案。
- 验证：隔离分支 Release .NET 10 / net48 双目标编译 0 警告、0 错误，1302/1302 自动化（Core 488、Animation 37、Windows 126 各双份）；新增 12 项 Core 用例含四边、角落、轻微越界、目标变大、负坐标显示器与小工作区。两个目标各执行 553 项真实 WPF 检查，覆盖三种模式／音乐、50/100/150/200%、四边、角落、附件刷新，以及实际指针位移触发左／右／底部预览、收纳与召回。net48 另通过原顶部 85 项及稳定布局 890 项检查；原项目同步源码的 .NET 10 Release 编译通过。
- 结果与视觉：200% 右侧恢复后间隙由 134.08 降到约 0.08 DIP（窗口坐标舍入），其它边和角落也在 1 DIP 内。记录见 docs/validation/drag-edges-before-2026-09-12.txt、drag-edges-net48-2026-09-12.txt、drag-edges-net10-2026-09-12.txt；已查看真实 WPF 窗口渲染图 drag-right-restored-2026-09-12.png。图片包含透明窗口的离屏留白，屏幕实际接触由坐标断言验证；这不是系统鼠标录屏。跨显示器的真实异 DPI 拖动仍未实测。
- 安装：0.1.0.58 MSIX 签名 Valid，覆盖安装后包状态 Ok，正式 WindowsApps 进程响应，安装 EXE 与已验证发布目录哈希相同，通知 Allowed，启动无新增 ERROR。MSIX 位于 artifacts/top-drag-fix-worktree/artifacts/msix/release/，SHA-256 为 bc1abd4f93780677ac95432c4b6e25d8a213ef9cc98b71b0b60ace86f231c092。保留 0.1.0.57 所含稳定展示槽、托盘与已提交长待机修复；其它任务未提交改动未混入安装包。
- Git：本节与修复提交“统一拖拽松手的可见边界定位并发布 0.1.0.58”，推送 codex/fix-drag-release-edges；同一代码按方法块同步原项目，保留已有未提交内容和暂存区。后续汇总发布应合入该分支。
- 下一步单一入口：docs/项目总控.md 第 8 节；用户直接在 0.1.0.58 把第三模式拖至右边松手，并试左边、底部与角落。


## 2026-09-12：摸鱼一分钟恢复原白底并修复移动鼠标误退出

- 目标：按用户要求先试原白底，不再抠白色；倒计时只能由点击天依或拖拽提前退出。
- 模块：素材准备脚本／GIF／图集／manifest，IdleSceneResolver，WPF 倒计时单调时钟和拖拽入口，规则与需求文档。
- 原因：此前用 Windows 无输入时长同时决定进入和保持状态，鼠标移动清零后导致立即回待机。现在无输入时长只触发进入，画面开始后用 Stopwatch 独立计时；嘿嘿按既有规则保持到点击或更高优先级状态。
- 实际验证：双目标编译成功；自动化 1326/1326（每目标 Core 500、Animation 37、Platform 126）；net48／net10 WPF 各 14/14，发布 EXE 14/14。覆盖移动、零无输入时长、60 秒时钟边界、点击退出、拖拽退出、新一轮计时、音乐优先；69 帧解码逐像素与源帧一致，时长 60000 ms，全部 69 项运行图集校验通过。WPF 白底截图见 docs/validation/fishing-white-background-2026-09-12.png。
- 发布：签名有效，0.1.0.59 已覆盖安装，状态 Ok，安装 EXE 与验证发布 EXE 哈希一致。
- 未验证／风险：本轮 WPF 时钟边界通过前移时间戳验证，未人工等待完整两分钟加一分钟；白底为用户要求的试用效果，待其体验。
- Git 提交：本条所在提交，分支 codex/fix-fishing-countdown；与 0.1.0.58 四边拖拽修复一起保留。
- 下一步：docs/项目总控.md 第 8 节，体验白底和点击／拖拽退出行为。


## 2026-09-12：侧边通知、QQ 详情与设置单窗口，发布 0.1.0.60

- 目标：按用户标注把消息卡放在人物右侧提示符旁，右侧溢出时镜像人物并换到左侧；QQ 提供可单独关闭的详情；重复打开设置恢复已有窗口并保留未保存编辑。
- 模块：Core 新增 MessageSidePlacement 和 QqTrayDetailsParser；平台新增限定 QQ 托盘卡的 QqTrayDetailsReader；WPF 新增不激活、鼠标穿透的独立消息窗口与详情调度；完整设置增加默认开启的 QQ 详情开关，旧配置保持兼容。卡片关闭时清空文字、图像与无障碍元数据，晚到的响应不能重新显示。QQ／微信按来源排队，同源更完整标题只补全现有卡，不重启动画或计时。保留既有“好奇摇摆”动画、30 秒展示期和托盘单击退出。
- 实机字段核对：用户手动展开 QQ 卡片后，正式解析器返回 HasDetails=True、NameLength=27、Count=6。卡片行结构为标题 Text／预览 Custom／未读 Text；读取首尾字段，未读取中间预览的 Name、值或后代，未输出或保存原始昵称、正文。微信此前 UIA／MSAA 未暴露内部字段，仍使用来源回退。未展开 QQ 卡片时返回无详情，不移动鼠标或访问私有数据。
- 实际验证：最终合入 4d6f87e 的已发布摸鱼修复及 78cdb58 的拖拽四边修复后，Release .NET 10 / net48 编译均为 0 警告、0 错误；双目标各 Core 516、Animation 37、Windows 126，共 1358/1358 通过。两个目标的通知真实 WPF QA 各 34 项通过，覆盖 50/100/200% 左右换侧、屏内约束、文字不镜像、显示不抢焦点、全屏/未知前台抑制、同源补全不重播、QQ/微信排队、关闭详情后晚到事件拦截，以及设置最小化恢复和未保存编辑保留。合并后摸鱼 WPF 双目标各 14 项通过；此前托盘原有 21 项 WPF 回归通过。同步后的共享项目 Release .NET 10 编译通过。
- 视觉证据：docs/validation/message-side-right-2026-09-12.png、message-side-left-2026-09-12.png、qq-detail-settings-2026-09-12.png 和两个 message-details 断言文件。图片来自真实 WPF 各窗口 RenderTargetBitmap 按实测坐标合成，已人工查看；computer-use 截图接口失败，因此不宣称系统截图或真实鼠标端到端录制。跨显示器混合 DPI 未实测，1/1.25/1.5/2 倍坐标算法已单测。
- 发布合并：最初 0.1.0.59 通知包安装被 Windows 拒绝，原因是另一任务已安装同号摸鱼版本；未卸载用户程序。合入该任务已提交代码和素材后改发 0.1.0.60，保留已验证的摸鱼、拖拽、稳定布局、托盘及长待机修复；其它未发布修改仍保留在原工作区。
- 安装：0.1.0.60 MSIX 签名 Valid，覆盖安装后包状态 Ok，WindowsApps 正式进程持续响应；已安装 EXE 与发布目录哈希一致。MSIX 位于 artifacts/notification-worktree/artifacts/msix/release/，SHA-256：e6ec3757a6c9d37c81058da253986b9eca48463316f2178fe38749bf81d2c6ea。
- Git：功能提交 4ab4881，合入已发布修复 7b23f43；本节与最终发布记录以独立提交保存并推送 codex/notification-side-details。源码按三方差异同步原项目，保留其它修改和既有暂存区；后续汇总发布应合入该分支。
- 下一步单一入口：docs/项目总控.md 第 8 节，在已安装新版体验右边缘换侧、QQ 详情开关和设置复用。


## 2026-09-12：下午问候改用十周年生日“来不及了！”

- 目标：按用户截图寻找并下载原始 GIF，替换 13:30～18:00 的摸鱼漂浮。
- 已完成：公开分享库 L12.GIF 与截图一致，原始文件单独归档，记录来源和 SHA-256；原生 9 帧×100 ms 无损编为 PNG 图集，循环持续 10 秒，不叠加程序漂浮。需求、动画映射、默认规则和挑选器同步更新。
- 验证：基于已发布 0.1.0.60 的双目标 Release 编译成功；1358 项自动化测试通过；net10/net48 各 11 项真实 WPF 验证通过，覆盖时间映射、持续换帧、循环、10 秒结束恢复、取消及音乐优先。全部 9 帧与源图逐像素一致（含 Alpha）；AssetCompiler 69 项与挑选器 101 卡片/54 正式项通过；已查看 WPF 渲染截图。
- 发布状态：打包期间发现另一任务已安装 0.1.0.61，当前动画包尚未安装；准备合入其 QQ 通知修复后改发 0.1.0.62。
- Git：本节与功能同一提交，分支 codex/afternoon-hurry-greeting。
- 下一步单一入口：docs/项目总控.md；合入最新已发布修复、复验并安装，保留其他任务改动。

## 2026-09-12：实测并修复 QQ 无需悬停的自动昵称提醒，安装 0.1.0.61

- 目标：验证 QQ 后台收到私聊时能否自动取得并显示昵称，不要求用户展开托盘。
- 实测与根因：net10 探针捕获三条真实新 QQ Toast，标题长度 4、正文独立，期间无 QQ 托盘卡。正式 net48 安装版却只显示来源：UI 创建的 WinRT 监听器跨 apartment 调用抛出 0x8001010E，且 QQ 空 Logo 引发 NullReferenceException，两个错误均会丢掉详情。
- 修改：WindowsMessageNotificationSource 每轮在后台线程取得 Current，权限检查不缓存跨线程对象；空图标直接回退，保留昵称；单槽文本不读取。完整设置文案明确系统通知昵称无需悬停，未读总数仅是公开托盘卡补充；加入有时限、仅匿名字段的后台探针。没有自动移动鼠标或读消息正文。
- 验证：修复后 net48 正式解析方法成功派发 TitleLength=4，LogoNull=True 不再失败；双目标 Release 0 警告、0 错误，1360/1360 自动化和双目标各 34 项 WPF 通知检查通过。WPF 设置截图已查看。真实背景私聊完整显示仍在与用户核对，不能用探针成功替代界面验收。
- 安装：0.1.0.61 签名 Valid、包状态 Ok、WindowsApps 进程响应，EXE 与发布目录哈希匹配；MSIX SHA-256 为 9a3822ff952374f398ce1a8636ea056bf97c4ef8728822a63941208d9820af68，位于 artifacts/notification-worktree/artifacts/msix/release/。
- Git：修复、匿名证据及交接文档将形成独立提交，推送 codex/notification-side-details；相同代码已按三方差异同步原项目，保留其它修改和暂存区。
- 下一步单一入口：docs/项目总控.md 第 8 节，核对用户真实私聊在新版中自动显示正确昵称。

## 2026-09-12：QQ 自动昵称实机验收通过，讨论正文与计数范围

- 用户确认：真实后台私聊已自动显示昵称，并提供人物左侧提醒卡的实际截图；完成此前最后一项界面验收。不将截图中的实际昵称另行保存到仓库。
- 范围讨论：Windows Toast 支持后续正文文本元素，本机已发现独立文本槽，但尚未读取正文或改变“不读取正文”的现有约定。可实现的是系统提供的预览，不保证完整消息。新通知次数可在桌宠内统计，但不是 QQ 实际未读总数；精确未读仍依赖已展开公开托盘卡。
- 本轮修改：仅更新验证报告、项目总控及日志；没有改动应用代码、配置或重新安装，原有编译与回归证据沿用，不重复运行无关测试。主工作区已由其它任务更新到 .63，不用 .61 覆盖。
- Git：文档独立提交“确认QQ自动昵称实机验收并记录正文计数边界”，推送 codex/notification-side-details；保留共享工作区和其它任务的修改。
- 下一步：docs/项目总控.md；正文预览与计数为待选方案，不作为已实现能力。

## 2026-09-12：确认统一详情开关并研究微信／QQ 未读开源实现

- 用户决定：昵称、正文预览、新增通知计数统一启停，不设分项开关。仅允许系统通知预览在内存显示，不扩展到历史或私有数据。
- 调研：读取项目官方仓库与关键源码，比较 wxauto／wxauto-4.0、NapCatQQ、Lagrange.Core、WeChatFerry、wechatauto-replica 和 NotiForward。核实未读数字来自 UIA 会话字段、客户端内部服务或数据库；手机转发仍是通知摘要。没有把 README 功能名当成无干扰全局未读能力。
- 修改：研究报告、产品需求、安全范围、决策、示例 JSON 和交接状态；尚未实现新的运行时预览／计数，未安装、执行第三方项目，未读取本机聊天正文。
- 验证：官方源码链接核查，双份示例 JSON 解析和关键规则断言通过；git diff --check 通过。本轮没有应用代码或窗口改动，不重复运行编译与视觉测试，不改变当前 .63 安装。
- Git：本节及研究形成独立提交“研究微信QQ未读方案并确认统一详情开关”，推送 codex/notification-side-details；同步原工作区并保留其它任务修改。
- 下一步单一入口：docs/项目总控.md 第 8 节，按确认规则实现系统通知详情组合，微信列表路线保持候选状态。


## 2026-09-12：重做摸鱼透明背景，保留人物白色与文字原色

- 目标：撤销白底试用，透明背景下不误删人物部位、文字，不全局替换颜色；保留一分钟及点击／拖拽退出。
- 模块：fishing_transparency.py、素材准备和校验脚本、WebP 图集编译器、派生 GIF／无损 WebP／PNG 图集、默认规则和动画映射。
- 方法：用原始 GIF 的逐帧纯白背景连通区域和已审查空隙种子生成遮罩，不把所有画布边缘当背景；底部接边裙装、鱼眼鱼腹、衣服与高光保留。GIF 预览独立保留透明索引，所有前景 RGB 精确映射，不量化。正式运行时使用无损 RGBA WebP 编译的 PNG 图集，仅还原贴着背景的浅色抗锯齿边缘；重新合成白底的通道误差不超过 4/255。imagegen 生成图仅用于前景语义参考，未替换正式原图像素。
- 发现并修复：Pillow 二次迭代 WebP 会复用末帧时长，使 60 秒误编为 69 秒；编译器现在从 ANMF 读取真实逐帧时长，并增加不等帧时长回归。
- 实际测试：1358 项初始双目标回归；合入最新 QQ 修复 aea02fc 后 1360/1360（每目标 Core 516、Animation 37、Platform 127）。WebP 时长回归 1/1；源文件哈希不变，69 帧、60000 ms，1,731,243 个实色像素原色保留，其中 133,042 个白色像素保留，84,267 个边缘像素半透明，2,158,890 个背景像素移除；实际 PNG 图集逐像素与无损来源一致，69 项运行资源校验通过。双目标 WPF 各 14/14，合并后发布 EXE 同项 14/14；已查看三种背景接触表与实际窗口截图。
- 发布：原计划的 .61 被最新 QQ 修复占用，已先快进合入该提交再构建 .62。0.1.0.62 签名 Valid、包状态 Ok，安装 EXE、manifest 和摸鱼图集哈希均匹配验证版本。
- 风险／未验证：源素材仅 240×240，放大后仍保留其原始低分辨率线条；本轮时钟边界采用自动化前移时间戳，未人工等待完整两分钟加一分钟。用户最终桌面背景观感待体验。
- Git 提交：本条所在提交，codex/fix-fishing-transparency；原项目按本轮差异同步，保留其他任务修改与暂存。
- 下一步：docs/项目总控.md 第 8 节。


## 2026-09-12：下午“来不及了！”验证完成并发布 0.1.0.63

- 目标及模块：以用户截图对应的十周年生日 L12.GIF 替换下午摸鱼漂浮；Core 时间映射、WPF 程序动效分支、素材配置与清单、挑选器和权威文档同步。原图完整保留于候选素材_官方/13_新添加_下午问候/，来源文章 https://luotianyi.vc/6727.html 与下载地址、SHA-256 均随素材记录。
- 代码与原图：9 帧×100 ms 无损 PNG 图集，逐帧 RGBA 与下载原图完全一致；透明背景、文字及人物不处理，保持原生 900 ms 循环和 180×180 DIP 显示槽。10 秒展示、音乐优先及输入取消沿用真实问候调度；旧原始素材保留。
- 集成：先以 0585d6a 保存功能，再合入 aea02fc 的 .61 QQ 通知修复及 4d2d369 的已安装 .62 摸鱼透明修复；先后生成的 .61/.62 动画包均未安装，最终只安装 .63，未卸载或回退用户已有版本。
- 实际验证：双目标 Release 0 警告/0 错误；合入 QQ 修复后 1360/1360 自动化（每目标 Core 516、Animation 37、Windows 127）；net10/net48 各 11/11 下午 WPF 检查，发布 EXE 下午 11/11、摸鱼 14/14。覆盖实际帧推进、跨多个原生循环、10 秒结束恢复、取消释放和音乐优先。摸鱼集成仅素材/工具改变，69 项运行资源和 WebP 时长回归 1/1 通过；root manifest 的两个相关条目与最终工作区一致，原项目同步后 .NET 10 编译成功。一次误用摸鱼 QA 参数导致等待超时，改用正式 --qa-fishing 后 14 项通过，已关闭本轮所有临时 QA 实例。
- 视觉证据：docs/validation/afternoon-greeting-hurry-2026-09-12.png（实际 WPF 渲染，已人工查看），两个 afternoon-greeting-*-2026-09-12.txt；发布版追加 afternoon-greeting-release 与 afternoon-fishing-release 结果。图片不是源图拼贴；未修改系统时钟，真实下午时间边界由已验证调度参数进入。
- 安装：.63 签名 Valid、包状态 Ok、正式进程响应，安装 EXE 与 publish 哈希相同；通知授权维持 Allowed。MSIX SHA-256 为 8a829db5bcf6bda441042de86a1880744c5ad7831833ea71a151e8af2aa04c81。MSIX 与完整安装 ZIP 位于 artifacts/afternoon-greeting-worktree/artifacts/msix/release/ 和 artifacts/afternoon-greeting-worktree/artifacts/sideload/release/。
- 风险/未验证：原图只有 240×240，未人工重画放大；多 DPI 桌面观感和用户在真实下午启动的最终体感待体验。安装时已过下午段，因此不把当前正常晚间问候误报为下午自然触发；不为验证更改系统时间。
- Git：本节随发布记录提交至 codex/afternoon-hurry-greeting 并推送；源码按三方差异同步回原 main 工作目录，保留其其它修改和暂存区。
- 下一步单一入口：docs/项目总控.md 第 8 节。

## 2026-09-12：验证微信会话列表并发布 QQ 统一详情 0.1.0.64

- 目标：执行用户授权的微信 wxauto 同类字段验证；落实 QQ 昵称／正文预览／新增通知计数共用一个开关。
- 修改模块：Core 通知摘要／计数与去重、Windows Toast 读取及快照标识、WPF 卡片／偏好应用与 QA；增加仅输出长度／数值的独立诊断工具，同步需求、安全、架构、默认配置。合并 07cb7be 保留 .63 下午问候和 .62 透明摸鱼。
- 已执行：双目标 Release 编译成功、0 警告／错误；Core 520＋Windows 131＋Animation 37，各两个目标共 1376 项通过。双目标各 35 项 WPF 检查、正式发布 EXE 35 项通过，已查看测试预览卡；权限不可用、全屏／未知前台、关停与无效槽降级均有覆盖。
- 实机：微信 4.1.13.12 后台定向只读 UIA 返回 11 个会话条目，读取数字标记及免打扰字段，数据中无独立通用计数槽。QQ 正式解析本机现有 Toast，开启取得标题长度 4／预览长度 2，关闭均为 0，不保存正文；详细证据见 docs/validation/wechat-session-qq-preview-2026-09-12.md。
- 发布：0.1.0.64 签名 Valid、安装状态 Ok、正式进程响应；EXE、Core DLL、Windows DLL 与发布文件哈希一致。MSIX SHA-256 5e9e47cf56c7decb104c0424f34cf427f88a07044a1f9b838f306277ac197ffe。
- 未验证：computer-use 最小化操作报 coordinate input geometry is unavailable，已请求手动最小化；微信最小化／关闭到托盘／动态增加及 99+ 未验证。QQ 待新发两条消息的自动界面确认；不能用现有 Toast 解析加合成 WPF 代替该最终验收。未启用微信持续监听。
- Git：实现、证据、文档形成独立提交“更新QQ统一详情并验证微信后台会话字段”，推送 codex/notification-side-details；按三方差异同步原工作区，保留其它任务修改及暂存。
- 下一步单一入口：docs/项目总控.md 第 8 节。


## 2026-09-12：消除摸鱼透明轮廓的灰白颗粒，发布 .66

- 目标：按用户实际截图修复白色颗粒／毛刺，继续保护人物白色部位、原色和文字。
- 根因：此前仅处理 min(RGB)>=190 且局部拟合误差<=3 的边缘，首帧人物轮廓仍有 460 个较亮的不透明边缘像素，其中 369 个因亮度门槛漏过、91 个因拟合失败漏过；GPU RenderTargetBitmap 捕获另会有透明碎片伪影，因此本轮 QA 固定软件渲染并在 Root 背景实际合成，正式渲染路径未改变。
- 模块：fishing_transparency.py 完整灰阶边缘去白底，3 像素邻域与容差 8 拟合覆盖率，再按源 RGB 逐通道还原色彩；更新派生 WebP、元数据及图集；新增灰边回归，扩展 FishingQa 六种缩放／背景组合。
- 验证：2/2 新回归通过，其中灰阶亮点测试在旧实现失败；69 帧、60000 ms，实际 PNG 图集与无损来源一致；内部不透明颜色保留，133042 个白色像素与上一版数量相同，边缘半透明像素从 84267 增至 157164（补齐 72897），白底重合成最大误差 <=1/255。69 项运行资源校验通过。两目标编译 0 警告／错误；1382 项自动化通过（每目标 Core 520、Animation 37、Platform 134）；WPF 两目标各 20/20，检查 100/150/200% 深蓝／深色背景、点击／拖拽及计时行为。验证图见 docs/validation/fishing-fringe-before-after-2026-09-12.png 与 fishing-fringe-200-0756B8-2026-09-12.png。
- 发布：0.1.0.66 签名 Valid，安装状态 Ok，EXE、manifest、运行图集哈希匹配。继承 .65 全部运行时代码。
- 未验证／风险：原素材 240×240，放大后的原始低清细节仍存在；本轮没有改生产 GPU 渲染设置，最终桌面观感由用户体验确认。原始 GIF 未修改。
- Git 提交：本条所在提交，分支 codex/fix-fishing-edge-fringe；原目录仅同步本项差异，保留其他任务修改与暂存。
- 下一步：docs/项目总控.md 第 8 节。


## 2026-09-12：全面筛查动画边缘，完成 30 套定向修复

- 目标：按用户要求检查各种动画的白边、颗粒和毛刺，保护白色部位、文字、原色与时序。
- 修改：新增可复现的边界覆盖率／灰白底还原处理及显式配置；30 套运行素材更新，选中 WebP 无损保存，已有柔和 Alpha 的 39 套不变；新增全帧审计、像素保护回归与 WPF 全目录 QA。
- 实测：69 套、3375 逻辑帧（3373 实际解码帧）；选中 373 源帧，520892 个边界像素调整透明度、6521 个像素还原混色。非零透明区域、内部 RGBA、时长、尺寸、锚点及 alphaBounds 均通过校验。摸鱼既有 69 帧及 133042 个白色像素保持。6 项 Python 边缘回归、1382 项双目标现有测试通过，修正 QA Brush 名称冲突后独立双目标编译 0 警告／错误；两目标各 414 项 WPF、发布 EXE 553 项四边拖拽通过，已查看全部 69 套实际窗口代表帧。
- 发布状态：本任务基于 .66 打出的 .67 包签名 Valid，但安装前发现微信任务已安装 .67，因此保护检查阻止替换，未安装本任务旧包。下一步合入微信 .67 已验证提交并发布 .68。
- 风险：原生白色贴纸描边与内部低分辨率纹理保留；全帧像素检查不等于人工查看每一张。生产 GPU 渲染设置未改。原素材未写入。
- Git：本条所在提交，codex/audit-animation-edges；待集成后按差异同步主目录，保留其他修改／暂存。
- 下一步单一入口：docs/项目总控.md 的动画边缘集成发布条目。

## 2026-09-12：微信最小化字段验证通过并发布详情试用 0.1.0.67

- 用户：QQ 已确认正常，授权微信试用；新增微信统一详情开关，无昵称／摘要／计数分项开关。
- 模块：Core 微信字段解析／变化基线、Windows 定向 UIA 单通道来源、WPF 来源组合／开关／提醒标签，增加匿名诊断工具与回归。合入 9c70ae0，保留已安装 .65/.66 的音乐设置和摸鱼修复。
- 实测：微信 4.1.13.12 普通后台与最小化后台均返回 11 个可解析会话、5 个带未读标记、2 个免打扰；两次 120 秒来源探针不补报旧未读。不保存昵称或正文，无点击／激活／切换聊天。
- 测试：修复可空前台快照访问编译错误后，最终双目标 Core 533、Windows 134、Animation 37，共 1408 项通过；双目标及发布 EXE 各 37 项 WPF 通过，已查看微信试用卡和设置渲染。验证初始／重复快照、未知字段、恢复基线、减少、免打扰、前台和统一关闭；真实游戏／锁屏与版本兼容未验证。
- 安装：0.1.0.67 签名 Valid、包状态 Ok、主进程响应，三项二进制哈希与发布匹配，摸鱼图集与 .66 一致。MSIX SHA-256 ae9e1cc9c2cd96d1737741b0ba3c13e7e3754b9b4e58c029b71e6e6d3edbce47。
- 未验证：新微信消息到实际卡片的最后端到端验收等待用户；首次新出现会话、关闭到托盘和跨版本覆盖不作保证。详见 docs/validation/wechat-trial-2026-09-12.md。
- Git：形成“接入微信公开会话详情试用并发布0.1.0.67”，推送 codex/notification-side-details；差异同步原目录并保留并行任务与暂存。并行动画任务等本提交合入后发布 .68，本任务不再次发布覆盖。
- 下一步单一入口：docs/项目总控.md 第 8 节。


## 2026-09-12：集成微信更新并完成动画毛边 .68 安装

- 目标：完成 69 套动画审计与 30 套边缘修复的正式交付，不覆盖并行发布的微信功能。
- 集成：03e752a 保存动画处理与验证；f651691 合入 272b5fc 微信 .67，保留两项功能；原计划的动画 .67 未安装，最终只安装 .68。
- 最终验证：集成后双目标 1408 项自动化通过、编译成功；发布 EXE 414 项全动画显示及 37 项提醒卡检查通过，合并后 net10 提醒卡 37 项通过。此前双目标各 414 项 100/150/200% 深浅背景 WPF 及发布 EXE 553 项四边定位通过；6 项 Python 边缘回归、30 套／373 帧像素不变量与 69 项图集哈希校验通过。检查全部 69 套实际窗口代表帧，内部低清纹理与有意白描边保留。主目录按差异同步并保留已有暂存。
- 安装：.68 签名 Valid、Status Ok；正式 WindowsApps 进程 Responding=True；EXE、3 个项目 DLL、manifest 及 69 个运行素材共 74 项哈希与发布目录一致。安装后的首次校验脚本因 Windows PowerShell 默认编码无法解析 UTF-8 清单而中断；改为显式 UTF8 后只补做校验和启动，没有重复安装。MSIX SHA-256：779824a518216b15f460593bb00ac9def978c868a64b953b5f4e9b978cf50ce6。
- 交付：artifacts/top-drag-fix-worktree/artifacts/msix/release/LuoTianyiPet_0.1.0.68_x64.msix；同帧对照见 docs/validation/animation-edges-hurry-before-after-2026-09-12.png，实际全目录窗口对照见 animation-edges-wpf-01 至 08-2026-09-12.png。
- 未验证／风险：原始低分辨率细节不重绘，非每张帧人工逐张审美验收；正式 GPU 模式不变。QQ／微信真实新消息的最终交互仍按微信试用报告由用户验收，本轮不把合成卡检查当作真实收信。
- Git：03e752a（处理）、f651691（集成）及本条所在最终交付提交，分支 codex/audit-animation-edges，推送已配置远端。
- 下一步单一入口：docs/项目总控.md 第 8 节。

## 2026-09-12：嘿嘿拖动全程保持

- 用户确认：拖拽期间及松手后保持嘿嘿，不切换膨胀；单击退出。
- 修改：Core 拖动状态保留嘿嘿；WPF 不启用快速摇动彩蛋，越界预览保持原图且不镜像文字；同步需求、映射、默认规则，新增状态机和实窗回归。
- 验证：新状态机两例在旧实现失败；修改后基于 .68 的双目标 1412 项测试通过。WPF 及最终集成安装待完成。
- 发布：发现已安装微信修复 .69，下一步先合入 35faeee，再发布 .70，保留已验证微信功能。
- Git：本条所在提交，codex/keep-hehe-during-drag。下一步单一入口 docs/项目总控.md。

### 2026-09-12：微信首条详情和已读后迟到修复，发布 .69

- 目标：解决用户已确认可读后遇到的首条来源卡先到、看完微信才补弹的问题。
- 模块：Core 会话事件有效性与微信队列时效；Windows 控件定位复用、750 ms 观察和瞬时刷新；App 详情等待、前台时间与显示前复核；WPF QA、平台时钟／读取注入测试及只读耗时探针。
- 实际验证：双目标编译和 1432 项测试通过；三组真实 WPF 各 42 项通过并查看测试文字渲染。实机首次定位 800 ms、复用读取 16 ms；25 秒 34 次快照均有效，捕获一次会话变化，只输出长度与状态。根目录差异同步后 .NET 10 编译 0 警告／错误，保留原暂存修改。
- 交付：0.1.0.69 已安装运行，签名 Valid、包 Ok、正式进程响应，EXE／Core／Windows DLL 哈希匹配发布目录；保留动画 .68。源码与证据提交 cee51bc，分支 codex/notification-side-details。
- 用户实际反馈：首条昵称／摘要及时出现，读完后不补弹；两项端到端体验验收通过。
- 限制：首次无基线会话仍不猜测补报；UIA 暂不可用／升级兼容与极短前台停留无法保证覆盖，也不承诺账号零风控。
- 下一步唯一入口：docs/validation/wechat-timing-2026-09-12.md。用户要求先讨论多联系人列表，不实施该新界面。


## 2026-09-12：完成嘿嘿拖动保持并发布 .70

- 用户确认：嘿嘿状态拖拽全程保持嘿嘿，不变膨胀；正常松手仍保持，单击退出。
- 修改模块：Core IsDraggingHehe 统一识别并返回嘿嘿播放计划；WPF 保持现有播放器、不启用快速摇动旋转舞、越界预览不换图且文字正向；普通猫耳待机／摸鱼取消后的膨胀、音乐优先、真正越界松手收纳逻辑保留。需求、动画映射与示例规则同步。
- 实际测试：新两例在旧实现失败，修复后合入 35faeee（含微信 .69 时效修复）的双目标 1436 项通过（Core 544、Platform 137、Animation 37，各两个目标）；net10/net48 与最终发布 EXE 各 76 项 WPF，覆盖 100/150/200% 四边、快速来回、左右及底部越界预览取消、文字方向、正常松手、单击退出、摸鱼计时及音乐优先。发布 EXE 提醒卡 42 项通过；主目录差异同步后编译 0 警告／错误，原暂存保留。已查看实际 WPF 150% 拖动截图。
- 发布：0.1.0.70 签名 Valid、Status Ok，正式 WindowsApps 进程响应正常；EXE、3 个项目 DLL、manifest 与 69 份运行素材共 74 项哈希匹配。MSIX SHA-256 aba8c2325fd36a217e0c8396465acd73d490bd6ca9f0e2f65b0cdb242b1e5c90。保留 .68 毛边修复和 .69 微信时效修复。
- 风险／未验证：WPF 自动化调用正式鼠标处理链的拖动方法，未代替用户在任意多屏 DPI 下的真实鼠标体验；未改变生产 GPU 或公开外部应用能力。
- Git：dae75a8 实现，a9be4e3 集成，本条所在提交记录发布；codex/keep-hehe-during-drag 推送远端。
- 下一步单一入口：docs/项目总控.md 第 8 节；本项等待日常拖动体验反馈。

## 2026-09-12：托盘右键轻量四行菜单

- 目标：落实用户选择的方案①；修改 TrayQuickPanel XAML、本窗口打开焦点及 QuickActionsQa。
- 结果：移除头像标题，200 DIP 实体宽、35 DIP 行高、11 DIP 圆角；召回／隐藏／设置／退出配系统图标，仅退出前分隔，浅青悬停、退出悬停红字，保留键盘实线焦点。
- 验证：.NET 10／net48 编译成功；托盘位置自动化各 3 项通过；真实 WPF QA 各 26 项通过，含鼠标打开无预选、Tab 导航、跳过禁用项、重开清除焦点、八次重开位置、退出与 Escape；已查看真实菜单渲染。初次焦点回归暴露重开保留焦点，显式聚焦可聚焦窗口后通过；一次构建遇 QA EXE 占用重试后成功。
- 未验证：跨 DPI 多屏最终观感；当前未安装，等待 .72 集成，避免回退其他任务更新。
- Git：本条随独立菜单实现提交，分支 codex/compact-tray-menu。
- 下一步：集成 .72，发布并安装菜单更新。
## 2026-09-12：回收站动画固定正向（集成前验证）

- 目标：修复“给我”偶尔继承身体互动、追逐或贴边预览镜像，连文字一起反转的问题。
- 修改：PlayAnimation 在“给我”和“大成功”的首帧之前清除两层水平镜像；其他动画仍按自身规则转向。需求、动画映射、示例参数及独立 WPF QA 同步。
- 验证：旧代码 64 项实窗检查中 30 项失败；修复后 net10、net48 和发布 EXE 各 64 项通过，覆盖三档大小、四种遗留方向、播放/定格、重复 DragOver、取消、锁屏及高优先级拒绝。双目标 1436 项自动化通过，主目录编译 0 警告/错误，已查看实际窗口截图。
- 发布：本轮 .71 未交付；安装核验发现并行消息列表 .71 已安装，EXE 哈希不符后停止，没有覆盖。下一步合入该更新再发布 .72。
- 风险：本轮验证未实际删除用户文件；文件回收站服务安全拒绝测试通过，生产文件操作不变。
- Git：本条所在实现提交，codex/fix-recycle-animation-direction；下一步单一入口 docs/项目总控.md。
## 2026-09-12：耳旁图标与多联系人待处理列表，发布 .71

- 目标：落实用户的三形态、左右各 8 状态参考图与交互要求；保留当前待机及音乐，不再播放消息提醒立绘。
- 模块：新增 Core MessageInbox、MessageInboxInteraction、MessageRailPlacement；WPF MessageInboxWindow 与主窗口组合／QA；微信来源补充新鲜快照已读查询；通知设置、默认规则、需求／动画／安全／架构文档同步。打包脚本版本 .71，保留 Windows PowerShell 5 所需 UTF-8 BOM。
- 实际测试：双目标编译 0 警告／错误，1462 项单元测试通过；net48、net10 和发布 EXE 各 262 项 WPF 检查，通过实际窗口渲染查看 48 状态、长文本、99+、7 会话滚动与屏幕边缘。路由点击、菜单、原生命中查询、300/500 ms、固定／外部关闭、阅读时顺序／滚动保持、拖动换边、未知前台／全屏降级、音乐保持均通过。
- 安装：.71 签名 Valid、Status Ok，正式进程响应；EXE／Core／Windows DLL 与发布目录三项哈希一致。安装后校验脚本路径误加 app/，已修正并补做校验启动，没有重复安装。包 SHA-256 A37D674AC7F6C291A2EAE0A8CD0326A07EF90B14EB6356127BC322C07FED1F5A。
- 未验证／风险：原生自动化被 Windows 10 截图接口 0x80004002 和点击坐标不可用阻塞；真实物理鼠标及跨 DPI 多屏体验待验证。计数为捕获的待处理提醒次数，QQ 不猜测自动已读；同名公开会话可能合并，来源闪烁无唯一身份。正文只驻留内存，忽略不影响客户端。
- Git：本条所在实现提交，codex/notification-side-details；主目录按差异同步并保留已有暂存。证据与六组预览 docs/validation/message-inbox-2026-09-12.md。
- 下一步单一入口：docs/项目总控.md 第 8 节，收集 .71 新界面的真实收信与连续操作反馈。

### .71 交付补记

实现提交 3625c1c 已推送 origin/codex/notification-side-details。主目录按差异同步完成，保留已有暂存和回收站相关改动；主目录 .NET 10 编译 0 警告／错误，证据 message-inbox-root-build-2026-09-12.txt。用户回复“还没测试”，真实新消息与物理鼠标验收保持待测试，不视作已通过。

## 2026-09-12：采用用户提供的 QQ／微信图标，安装 .74

- 目标／修改：使用候选素材_官方/微信QQ.png 左侧推荐版，imagegen 抠成两个透明 PNG，替换 MessageInboxWindow 的代码绘制图标，24 DIP 槽和现有角标交互保留；来源及输出哈希在 assets/ui/notification-icons.meta.json，原素材未改。合入回收站方向与轻量托盘菜单，避免覆盖较新功能。
- 验证：双目标与主目录编译 0 警告／错误；net48 和发布 EXE 各 262 项 WPF 通过，查看新图标小尺寸及左右状态。PNG Alpha 均有完整透明和不透明像素；.74 签名 Valid、包 Ok、正式进程响应，三项安装二进制与 publish 哈希一致。
- 未验证：本轮只替换图标，未重新触发实际收信；真实鼠标／跨 DPI 多屏体验保持待测。生成式抠图不宣称逐像素无损裁切。
- Git：c3bae2f 实现，codex/notification-side-details 已推送；根目录按差异同步并保留已有暂存。证据 docs/validation/notification-icons-2026-09-12.md。
- 下一步单一入口：docs/项目总控.md 第 8 节，收集消息列表实际体验。


## 2026-09-12：托盘菜单再缩窄 15%，安装 .75

- 用户确认：菜单仍显宽，按建议缩小约 15%，左右内边距收紧，行高保持。
- 修改模块：TrayQuickPanel.xaml 实体宽 200→170 DIP（含阴影窗口 224→194）；外层水平 Padding 6→4，按钮水平 Padding 10→7，合计内容边距 12 DIP；四行仍为 35 DIP。基于 c2e7980 保留 .74 用户素材图标及之前消息列表、回收站方向修复。
- 实际验证：双目标及主目录编译 0 警告／错误；net48 与最终发布 EXE 各 26 项既有 WPF 检查通过，覆盖开关、焦点、禁用跳过、重开位置、单击退出和 Escape。查看最终菜单截图，四行图标文字完整，尺寸 194×189 PNG、实体宽 170。本次仅布局属性改变，不新增重复实现的测试。
- 安装：0.1.0.75 签名 Valid、包状态 Ok，74 项安装文件哈希匹配发布目录，正式 WindowsApps 进程 Responding=True。包 SHA-256 98BF7500C5358A5E55A2FB367D0B12349CBA11E8DDEB92E7902D38094F5C4A8C。首次校验额外查找消息图标外部文件时中断；确认图标为 EXE 内嵌 Resource 后补做校验启动，没有重复安装。
- 风险／未验证：未新增跨 DPI 多屏物理鼠标测试；本次未改定位及外部应用能力。源码和证据按本任务差异同步主目录，保留原暂存。
- Git：本条所在提交，codex/narrow-tray-menu，推送已配置远端。
- 下一步单一入口：docs/项目总控.md 第 8 节，收集缩窄菜单实际观感和既有消息列表体验。


## 2026-09-12：托盘菜单按文字自适应，安装 .76

- 用户要求：菜单仍大，宽度只比文字稍大，去掉“设置”后省略号。
- 修改模块：TrayQuickPanel.xaml 取消固定 Width，SizeToContent 改为 WidthAndHeight；文案显示“设置”，按图标＋最长文字＋既有少量边距决定宽度，35 DIP 行高保持。
- 验证：双目标及主目录编译 0 警告／错误，发布 EXE 26 项既有 WPF 检查通过；查看最终截图，四行文字／图标完整，重开、焦点、定位、Escape 和单击退出行为通过。只修改两个布局／文案属性，不新增重复测试。
- 安装：0.1.0.76 签名 Valid、包 Ok、74 项安装文件哈希匹配，正式 WindowsApps 进程 Responding=True。包 SHA-256 4C2B2A26F1140D4C75FDFB7C50DF1E6F18911F3678ED98ED523077587854B511；保留 .74 消息图标及既有修复。
- 风险／未验证：跨 DPI 多屏物理鼠标体验仍待用户验证，未修改外部应用或文件操作能力。
- Git：本条所在提交，codex/fit-tray-menu-content，推送远端；主目录仅同步本项，原暂存保留。
- 下一步单一入口：docs/项目总控.md 第 8 节，收集自适应菜单观感与原消息列表体验。

## 2026-09-13：按确认效果图重排通知设置

- 目标：简洁设置行、推荐扁平 QQ／微信图标、权限状态行与默认折叠的规则说明。
- 模块：SettingsWindow.xaml/.cs；新 SettingsWindow.NotificationQa 与独立 App QA 入口。复用 assets/ui/notification-qq.png 与 notification-wechat.png，来源与哈希沿用 notification-icons.meta.json，不修改原素材。
- 验证：双目标编译 0 警告／错误；每目标 Core 557、Animation 37、Windows 137，共 1462 自动化通过。net10/net48 各 81 项真实 WPF 检查通过，覆盖五种权限状态、默认一屏、独立开关、键盘、折叠／滚动、授权与保存；已查看正常、未授权、展开及150%渲染。暂不可用状态说明过长已缩短并重新通过。
- 规则：使用真实系统权限状态，已授权隐藏重复按钮；既有系统许可可随保存恢复本地标记，打开设置不主动请求权限。昵称与摘要只在本机内存用于当前提醒，不写日志／历史、不上传。
- 未验证：125/150%为 WPF 放大渲染，不等于真实系统 DPI 切换；真实授权弹窗和收信未触发，使用隔离来源验证。
- Git：本条随实现提交，分支 codex/notification-settings-redesign，基线 ed5cda9（已安装 .76）。
- 下一步：验证签名 .77 发布程序并安装，避免混入日历／提醒等其它未提交工作。

## 2026-09-13：通知设置新版 .77 发布验收

- 完成：fdde762 实现通知页；正式发布 EXE 再过 81 项 WPF（五状态、一屏、键盘、折叠、授权及保存），主目录同步后的 .NET 10 编译 0 警告／错误。QQ／微信 PNG 哈希与原派生记录一致。
- 安装：0.1.0.77 签名 Valid、包状态 Ok、75 项文件哈希匹配；经 AppsFolder 启动正式进程 Responding=True。验证见 docs/validation/notification-settings-install-2026-09-13.txt。
- 同步：仅应用本轮 App QA 入口、SettingsWindow 两文件及新 QA 的补丁，保留主目录 ReminderPaths、日历／闹钟等并行修改及原暂存。版本已协调，后续日历任务使用 .78 并继承本项。
- 限制：WPF 125/150%渲染不代替真实跨 DPI 多屏实测；未实际触发系统授权弹窗或 QQ／微信消息。
- Git：本条所在提交记录交付，codex/notification-settings-redesign 推送远端。
- 下一步单一入口：docs/项目总控.md 第 8 节，用户体验 .77 通知设置。

### 2026-09-13：离线日历与闹钟功能首版

- 目标：用户确认功能先实现，周／月日历、作息覆盖、来源日历闹钟、24小时相对提醒、离线节气和本地数据。
- 修改：Core/Reminders、CalendarLabels；Platform.Windows/ReminderStore；App/ReminderService、PlannerWindow、MainWindow.Planner 与右键入口；同步需求／安全／动画／架构／默认配置。
- 验证：基于已安装 .77（4649791）隔离集成。两目标编译0警告错误；1500自动化通过（每目标 Core574、Platform139、Animation37）；两目标各17项真实WPF检查，通过编辑创建、24小时拒绝、磁盘往返、计时到期、焦点不变和安全抑制。节气74年1776条，33744字节；逐日2026～2099标签测试通过。
- 风险：前端基础控件待打磨；手动改系统时钟影响相对剩余时间；物理休眠／跨DPI多屏／长时性能待验。无运行时网络／数据库进程；用户内容不进日志。
- Git：本条随 feat: add offline calendar and alarms 提交；最终安装验证另行追加。
- 下一步单一入口：docs/validation/planner-2026-09-13.md 的 .78 发布和实际体验验收。


### 2026-09-13：.78 日历／闹钟安装交付

- 功能：人物右键日历／闹钟，周月视图、详细行程、来源关联、班休覆盖、多种重复、24小时相对提醒及本地备份导入导出；离线节气表33,744字节。
- 验证：.77基线隔离构建，1500双目标自动化；两目标及发布版各17项WPF，通知设置发布版81项；新增存储上限测试两目标通过。签名Valid、安装状态Ok，102文件SHA256一致，包内无测试数据，正式进程响应。
- 大小：三个程序文件相对 .77 增加101376字节，未增加第三方运行依赖。整只桌宠短时运行采样见planner-installed.json（含动画开销，不能用于推断功能增量内存／CPU）。
- 风险：基础UI、物理休眠、跨DPI多屏和长期性能待体验。完全退出不实时响铃；手动改系统时钟影响相对剩余时间。
- Git：5c02587实现，02c6c17数据上限保护；本次交付记录随独立后续提交，codex/offline-calendar-alarms。主目录按功能差异同步，保留已有修改及原暂存。
- 下一步唯一入口：docs/validation/planner-2026-09-13.md，用户从人物右键体验并继续修改UI。

### 2026-09-13：补入常见国际节日并安装 .79

- 目标与模块：CalendarLabels增加6项固定日期、3项星期规则；配置及产品文档同步，新增InternationalHolidayTests和节日WPF截图入口。无用户数据迁移、无新增运行依赖。
- 实际验证：Core net10/net48各586项全部通过；应用双目标编译0警告／错误；net48与发布EXE各20项WPF通过，人工查看6月父亲节与夏至、11月感恩节、12月平安夜与圣诞节截图。签名Valid、包Ok、102项安装哈希一致、正式进程响应。
- 未验证：本轮不重复物理休眠、跨DPI多屏及长期性能测试；基础UI仍待用户反馈。
- Git提交：本条随 feat: add common international calendar observances 提交，分支codex/offline-calendar-alarms；验证后推送。
- 下一步单一入口：docs/validation/international-holidays-2026-09-13.md。

### 2026-09-13：日历精简、窗口适配及提前倒计时 .80

- 模块：PlannerWindow精简入口和月网格布局；Reminders.Upcoming集中判断30分钟边界；ShowCountdown逐行程保存、全局ShowUpcoming沿用旧值；MainWindow.Planner使用统一判断。移除导入导出界面及方法，保留自动本地保存与备份。
- 实际验证：Core net10/net48各593项通过（新增7项）；应用双目标及主目录编译0警告／错误；两运行目标和发布版各23项WPF通过，包括实际倒计时卡、全局及单项保存、六周月份完整呈现、多日期编辑展开。截图已查看；正式.80签名Valid、状态Ok、102项安装文件一致、进程响应。
- 风险／未验证：物理休眠、跨DPI多屏与长期性能本轮不重复；小屏编辑使用滚动兜底。UI继续打磨。
- Git提交：本条随 fix: simplify planner UI and add optional thirty-minute countdown 提交，分支codex/offline-calendar-alarms；验证后推送。
- 下一步唯一入口：docs/validation/planner-refinement-2026-09-13.md。


### 2026-09-13：落实日历UI与提前处理提醒 .81

- 模块：PlannerTheme、PlannerWindow、DateSelectionWindow、CalendarLabels、Reminders、MainWindow.Planner；界面／历法／调度分别实现，音乐岛原有资源和控制文件未改。
- 行为：日期单行压缩、自由星期作息、单内容兼容旧数据、多日期确认后提交；SkippedAt及HiddenCountdownAt按本次发生时间持久保存，不删除行程或关闭后续重复，操作后8秒撤销；提醒按原岛实际位置居中放下方。
- 验证：Core两目标各599项（共1198项）通过；应用双目标及主目录编译0警告／错误；两目标及发布EXE各26项实际WPF通过。覆盖旧回归、多日期无Ctrl点选、隐藏仍保留到点、撤销、跳过与内容重启读取、原岛／桌宠下方定位及安全抑制；截图已检查。签名Valid、包Ok、102项安装文件一致，正式.81进程响应。
- 未验证：物理休眠、实际跨DPI多屏及长期性能本轮不重测；悬停真实鼠标时序未自动模拟，点击展开已验证。小屏编辑保留滚动兜底。
- Git提交：本条随 feat: implement compact planner design and dismiss upcoming occurrences 提交，分支codex/offline-calendar-alarms，验证后推送。
- 下一步唯一入口：docs/validation/planner-ui-2026-09-13.md。


### 2026-09-13：.82 全页面重做与批量删除

- 目标：按效果图重做全部日历相关界面，解决多日期误设后清理成本。
- 模块：PlannerWindow／Groups、PlannerTheme、DateSelectionWindow、MainWindow.Planner及QA、ReminderSchedule和组删除测试。
- 验证：双目标编译0警告0错误，Core各604项通过；net10、net48、发布EXE实际WPF各34项通过；已检查真实页面截图。签名Valid、安装Ok，102项哈希一致。
- 范围：单日期／整组／多选组删除和重启持久化均通过，原音乐岛未修改，无新增运行依赖。
- 未验证：实际跨DPI多屏、物理休眠及长期性能本轮未重测。
- Git提交：本条随 codex/offline-calendar-alarms 的 feat: redesign planner pages and add scoped group deletion 提交。
- 下一步唯一入口：docs/validation/planner-redesign-2026-09-13.md。

### 2026-09-13：保留拖动状态、旋转舞续拖和平滑追逐

- 目标：按用户确认仅改变位置；模式三普通膨胀与快速甩动彩蛋、越界收纳保留，自动移动禁拖；改善追包子突增观感。
- 模块：PetStateMachine拖动期间原状态和反应保留，MainWindow手势／睡眠／旋转舞／动画生命周期与追逐计时，BunChasePlanner平滑曲线，配置与行为文档。
- 验证：1576项双目标测试全部通过，双目标及主目录编译0警告错误，net10/net48/发布版各93项实际WPF通过。发布版摸鱼76、回收站安全64、日历34项通过；已看睡姿与装饰截图。签名Valid、安装.83状态Ok、102项哈希一致、正式进程响应。
- 限制：实际跨DPI多屏、物理休眠与长期性能未重测；最终加速观感待用户体验。安全降级为合成场景，未处理真实文件。
- Git提交：ab06daf；分支codex/preserve-animation-drag，基于92acecc。主目录三方同步保留已有修改及暂存，不在脏main直接提交。
- 下一步单一入口：docs/validation/preserve-drag-2026-09-13.md。


### 2026-09-13：.83 日历收尾复核

- 用户要求继续完成；核对已安装 .83 状态Ok，保留 .82 日历与 .83 拖动改动。
- 再次运行 .83 发布EXE的隔离日历QA，34项全部通过，包括多日期删除范围和持久化；不触碰真实行程数据。
- 整理10个真实界面为可直接浏览的离线预览 docs/validation/planner-pages-2026-09-13.html。未改程序或提升版本。
- Git：随 docs: verify integrated planner and add real page gallery 提交。
- 未验证项仍为物理休眠、跨DPI多屏和长期性能。下一步单一入口为该预览，收集具体页面反馈。

### 2026-09-13：按最新九图重做日历、闹钟与倒计时

- 目标：用户最新文字优先，采用图二月历+侧栏、补图一班休、图三周列、图四五事项、图六七闹钟及图九倒计时布局；AI规格作为次级约束。
- 修改模块：PlannerWindow/Editor/Groups/Theme/Number、ReminderEngine/Placement/Service、MainWindow.Planner、全局ReminderSettings、托盘入口、默认规则及相关测试；原音乐岛、动画素材和回收站实现未改。
- 实际验证：Core两目标各636、Windows两目标各139、Animation两目标各37，共1624通过。net48/net10 WPF各50通过；最后仅月格边线、周卡换行调整后，最终发布EXE再跑50通过。发布保留拖动93、托盘26通过。双目标与主目录编译0警告错误。已查看月历、班休、周历、事项与闹钟表单、计时器、提醒/胶囊与设置真实截图。安装签名Valid、版本.84状态Ok、99项哈希一致、正式进程响应，安装包无QA数据。
- 未验证：跨DPI多屏物理拖动、物理休眠/系统时钟变更、长期CPU内存；全局音色主观听感和各外观可选动画未逐一人工验证。原生日期选择框并非图片的完全定制控件，头像复用原应用图标。详见验收表A01～A30。
- Git提交：分支 codex/offline-calendar-alarms，本次提交标题 `feat: align planner with approved calendar alarm and countdown designs`；包含.83基线。主目录只同步本次差异，已有用户修改与暂存保留。
- 下一步单一入口：docs/validation/planner-spec-2026-09-13/index.html，继续按用户实际页面反馈调整。


### 2026-09-13：原版闹钟动画、整曲提醒及启动修复

- 目标：落实用户原表情、3分42秒/10分钟、通知页设置、启动未响应及九图美化要求。
- 模块：Core提醒实例轮次与Alarm优先级；WPF声音、动画token/置顶token、通知设置、日历控件；Windows桌面图标搜索与异步启动、异步音频检测；原素材与运行时资源、LFS音乐、打包.86。
- 实际验证：1636项双目标自动化；net10/net48日历各62；最终发布日历62/回收站64；同轮.85通知82/拖动93/托盘26。限定搜索后平台双目标各140复测。主目录双目标编译0警告错误；.86签名Valid、安装Ok、101文件哈希一致、冷启动5次响应采样。详情docs/validation/planner-alarm-2026-09-13/report.md。
- 风险：无用户当次挂起堆栈；跨DPI、物理休眠、长时和真实扬声器未逐项验证。未连续等待多个10分钟周期，以可控时钟覆盖。
- Git提交：本节所在提交，分支codex/offline-calendar-alarms；主目录保留用户修改与原暂存。
- 下一步唯一入口：docs/validation/planner-alarm-2026-09-13/index.html。


### 2026-09-13：快速提醒卡单组件整改

- 目标：实现用户确认的提前→知道了→快速卡→正式提醒流程及附着样式。
- 模块：PetReminderCard单一owned非激活宿主；MainWindow.Planner呈现、动画高度及定位；ReminderPlacement居中上下策略；PlannerWindow.OpenItem发生日；配置与行为文档、参考图及实际截图。
- 测试：Core net10/net48各644；应用/主目录双目标0警告错误；net10与最终发布WPF各76，包括同一组件/锚点/拖动/不误取消/单次关闭/次周触发/详情日期/不抢焦点/安全降级。安装.88签名Valid、状态Ok、101项hash一致与进程响应验证。
- 未验证：真实跨DPI多屏物理拖动、系统DPI切换、物理休眠及长期运行；几何模型测试不冒充硬件验证。
- Git提交：本节所在提交，codex/offline-calendar-alarms；主目录保持原暂存。
- 下一步唯一入口：docs/validation/quick-reminder-2026-09-13/index.html。


### 2026-09-13：右键菜单新增设置和退出

- 目标与模块：PetQuickPanel新增两个入口，MainWindow.QuickActions接入现有设置及正常退出回调；原托盘功能保留。
- 实际验证：双目标和主目录编译0警告错误；net48调试/最终发布各28项菜单WPF检查通过，验证先隐藏、回调一次，已查看真实截图。签名Valid、安装.89状态Ok，进程响应。
- 未验证：真实跨DPI硬件；退出回调在QA中计数验证，未通过真实菜单退出用户进程。
- Git提交：本节所在提交，codex/offline-calendar-alarms；保留主目录修改及暂存。
- 下一步唯一入口：docs/validation/pet-menu-2026-09-13/03-pet-menu.png。
### 2026-09-14：模块 A 运行时素材安全迁移与引用审计

- 目标：将仍被运行时构建直接依赖的候选身体命中图复制到正式 `assets/`，保留候选原文件并验证功能不变。
- 修改模块：`assets/body-hit-maps/`、App/Core 测试项目构建输入；新增 `docs/validation/asset-migration-2026-09-14.md` 审计报告。
- 实际验证：候选源与运行时副本 SHA-256 一致；net48 Release 编译 0 警告/0 错误；Core 644、Animation 37、Platform.Windows 140，共 821/821 通过；App 发布输出和 Core 测试输出均包含两份命中图。
- 未验证/风险：仍有 41 条动画编译配置来源、1 条场景示例来源、9 个素材工具引用候选目录；`assets/manifests/animations.json` 的 61 条历史 `sourcePath` 按硬约束未改。候选素材库和 artifacts 均未删除。
- Git 提交：`c1464bf`（结构重构基线）；本次素材副本与文档尚未提交，等待来源迁移边界确认。
- 下一步：确认 `assets/source/` 构建输入迁移范围及受保护 manifests 历史来源记录的处理规则。
### 2026-09-14：清理已合并的历史 worktree

- 目标：按用户确认清理 5 个干净且已合并的历史 worktree，保留其余指定 worktree 不动。
- 实际操作：删除 `artifacts/afternoon-greeting-worktree`、`artifacts/notification-settings-worktree`、`artifacts/notification-worktree`、`artifacts/preserve-drag-worktree`、`artifacts/top-drag-fix-worktree`，并使用 `git branch -d` 删除对应 5 个本地分支。
- 验证：删除前已核对分支、工作区状态和合并关系；删除后 `git worktree list` 确认 5 个目标不再存在，保留 worktree 仍在；主工作区原有用户修改未改变。
- 未执行：未删除旧 `assets/source/`、候选素材、`.local-tools/` 或 `docs/validation/` 文件；发布包体积分析按要求留到迁移清理完成后。
- Git提交：本条随 `maintenance: remove merged worktrees` 提交。
- 下一步单一入口：等待用户确认后，逐项处理旧副本、中间产物和精选验收证据归档。
### 2026-09-14：清理迁移前的 `assets/source/` 副本

- 目标：在构建输入切换到 `原素材/` 并验证通过后，删除旧的 `assets/source/` 副本。
- 实际操作：删除 Git 已跟踪的旧副本，共 3,015 个文件、约 1,463.7 MiB；`原素材/` 未修改。旧路径加入 `.gitignore`，防止误重新创建后进入 Git。
- 实际验证：Release 编译 0 警告/0 错误；Core 644、Animation 37、Platform.Windows 140，共 821/821 通过；活动源码、测试、配置和工具不再读取旧目录。
- 未执行：未处理 `.local-tools/`、候选素材或 `docs/validation/` 归档对象；发布包体积分析继续留到迁移清理完成后。
- Git提交：本条随 `refactor: remove migrated source copy` 提交。
- 下一步单一入口：审计 `.local-tools/` 子目录并提出逐项清理清单。
### 2026-09-14：清理已迁移的本地源帧副本

- 目标：删除已复制到 `原素材/` 的 `.local-tools` 重复源帧，不误删 Blender 工具环境和仍有复用价值的工程文件。
- 审计结果：`.local-tools/` 约 2,006.7 MiB；保留 Blender 4.5.13、下载包、Blender 工程、渲染帧、拖动实验帧、试听候选和 QA 文件。
- 实际操作：删除 `.local-tools/work/ai-bun-source-frames/`，共 121 个文件、约 34.7 MiB。
- 实际验证：`原素材/animations/bun-source-frames/` 仍有 121 个源帧；未触碰其它 `.local-tools` 内容，主项目不读取该本地重复目录。
- Git提交：本条随 `maintenance: remove duplicated source frames` 提交。
- 下一步单一入口：等待确认后，按精选清单归档 `docs/validation/` 历史证据。
### 2026-09-14：精选验收证据归档

- 目标：缩小 `docs/validation/` 的正式 Git 内容，只保留精选验收证据，同时保留历史证据本机副本。
- 实际操作：保留 28 个精选文件；将其余 319 个已跟踪文件（150 PNG、1 JPG、129 TXT、18 MD、16 JSON、4 HTML、1 CSV，约 23.3 MiB）归档至 `artifacts/archive/validation/2026-09-14/`。用户当前新增的 15 个未跟踪验证文件未移动。
- 实际验证：28 个精选文件仍存在；归档目录文件数 319，且被 `.gitignore` 排除；未发现归档文件在项目活动目录中的引用。
- 未执行：未删除候选素材或 `.local-tools/` 其余内容；发布包体积分析尚未开始。
- Git提交：本条随 `maintenance: archive historical validation evidence` 提交。
- 下一步单一入口：发布包体积分析，分别核对便携包和 MSIX 的内容组成及瘦身空间。
### 2026-09-14：发布包体积分析

- 目标：解释便携包约 170 MiB 与 MSIX 约 200 MiB 的组成，评估不改变用户可见功能的瘦身空间。
- 实际分析：便携包 0.1.0.56 为 169.83 MiB；MSIX 0.1.0.93 为 199.64 MiB；旁加载 ZIP 为 199.46 MiB。MSIX 内运行时动画 178.88 MiB、提醒 MP3 8.68 MiB、程序和依赖 11.97 MiB；`crystal-*` 动画占运行时资源约 120.53 MiB。
- 判断：两个包不是同一版本，不能直接比较格式差额；原素材、候选素材、`.local-tools/`、docs 和 tools 均未进入发布包。0.1.0.90 到 0.1.0.93 的新增体积主要来自 EXE 增长，不是资源重复。
- 方案：只建议后续实验音频重编码和 WebP 近无损重编码，当前不改正式资源、不删除动画、不修改打包脚本。
- Git提交：本条随 `docs: analyze release package size` 提交。
- 下一步单一入口：等待用户确认是否开始音频/WebP 编码实验。
### 2026-09-14：代表性 WebP 编码候选实验

- 目标：在不覆盖正式资源的前提下，验证运行时 WebP 的体积收益和基本透明度保持情况。
- 实际操作：使用本地 Blender Python/Pillow，在临时目录生成 `crystal-touch-chest`、`crystal-headpat`、`ai-bun-v2-eat` 的 Q90/Q80 候选。
- 实际验证：3 组候选均保持帧数、总时长、尺寸和首帧 Alpha 边界；Q90 样本节省 4.66 MiB，Q80 样本节省 9.82 MiB；已查看代表性 Q90/Q80 首帧，未见明显透明边缘或主体变形。
- 未验证：全帧视觉差异、正式运行时加载、不同 DPI 和音频重编码；本机没有 ffmpeg/lame，未安装新外部工具。
- 结果：候选保留在 `.local-tools/qa/package-size-experiment-2026-09-14/`，`assets/` 正式资源未改。
- Git提交：本条随 `docs: record webp size experiment` 提交。
- 下一步单一入口：等待确认后选择全量 WebP 回归或音频编码器方案。
# 2026-09-14：模块 C artifacts 专项审计完成

- 目标：按桌面原始执行需求完成 `artifacts/` 只读专项审计，不删除、不移动任何产物。
- 完成内容：统计 16,902 个文件、约 18.06 GiB；列出全部顶层条目及分类；逐项检查 4 个 `artifacts/` 历史 worktree 的分支、工作区状态和与 `main` 的提交关系；列出未合并本地分支。
- 修改文件：`docs/artifacts专项审计-2026-09-14.md`、`docs/项目总控.md`、`docs/开发日志.md`。
- 验证：只读执行 `git worktree list --porcelain`、各 worktree `git status`、`git log`、`git rev-list`、`git merge-base`；未执行删除、移动或构建。
- 风险：4 个 artifacts worktree 均不可直接删除；其中 2 个有未提交改动，2 个所在分支有未合并提交。其他发布物和 QA 产物的清理仍需最终验收后由用户确认。
- Git 提交：未提交，待本轮阶段性文档变更统一提交。
- 下一步：进入阶段 3「拆分启动组合根」，先审计并拆分 `App.OnStartup`，保留全部启动参数、单实例行为和用户可见行为。
# 2026-09-14：阶段 3——启动组合根拆分完成

- 目标：拆分 `App.OnStartup`，保留全部现有启动参数、单实例行为和窗口启动行为。
- 修改模块：`Bootstrap/StartupOptions.cs`、`Bootstrap/ApplicationCompositionRoot.cs`、`Bootstrap/App.xaml.cs`。
- 完成内容：集中解析 QA/Preview 参数和单实例 ID；集中创建平台服务与 `MainWindow` 依赖；App 仅负责生命周期、路径/日志初始化、异常处理和窗口启动。
- 兼容性：未改变命令行参数、单实例命名规则、软件渲染条件、便携模式、用户数据路径、资源路径、配置格式、存档格式或用户可见行为；未新增依赖。
- 验证：Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- 未验证内容：尚未进行多显示器/DPI/真实托盘和长时间运行视觉回归，本次只验证启动链和自动化测试。
- Git 提交：未提交，待阶段性变更统一提交。
- 下一步：阶段 4，先拆分 MainWindow 的托盘、设置、退出职责。
# 2026-09-14：阶段 4·步骤 1——MainWindow 托盘、设置、退出职责归档完成

- 目标：按阶段 4 顺序先整理托盘、设置和退出职责，不改变行为。
- 修改模块：新增 `src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.TraySettingsExit.cs`；从 `MainWindow.xaml.cs` 移出对应方法。
- 完成内容：托盘创建与回调、置顶/自启动设置、设置窗口打开、退出按钮和退出动画集中到新 partial；窗口关闭清理仍保留在主生命周期文件，避免改变资源释放顺序。
- 验证：Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- 未验证内容：托盘真实右键菜单、设置窗口完整交互和跨 DPI 视觉回归未在本批次重测。
- Git 提交：未提交，待阶段 4 分批完成后统一提交。
- 下一步：阶段 4·步骤 2，归档音乐与媒体控制职责。
# 2026-09-14：阶段 4·步骤 2——MainWindow 音乐与媒体控制第一批归档完成

- 目标：归档音乐/媒体控制的低耦合窗口事件处理，不改变现有行为。
- 修改模块：新增 `src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.MediaControls.cs`；从 `MainWindow.xaml.cs` 移出播放按钮和独立音量控制方法。
- 完成内容：上一首、播放/暂停、下一首、音量弹层、音量拖动、音量状态刷新和相关时间问候中断逻辑集中到媒体 partial；Core Audio 检测、轨道元数据和窗口悬停仍按原顺序运行。
- 验证：Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- 未验证内容：真实网易云按键发送、Core Audio 设备变化、音量拖动视觉和跨 DPI 未在本批次单独重测。
- Git 提交：未提交，待阶段 4 分批完成后统一提交。
- 下一步：阶段 4·步骤 3，归档通知状态、订阅、提醒卡和通知详情职责。
# 2026-09-14：阶段 4·步骤 3——MainWindow 通知监控职责归档完成

- 目标：整理通知监控入口和状态协调，不改变 QQ/微信提醒行为。
- 修改模块：新增 `src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.Notifications.cs`；从 `MainWindow.xaml.cs` 移出通知监控、Shell 注意事件和 QA 预览方法。
- 完成内容：通知源订阅、前台安全判断、通知状态计时器、Shell 事件去重入口、收件箱入列和通知预览集中到通知 partial；消息详情、微信会话、收件箱和卡片渲染仍按原文件边界保留。
- 验证：Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- 未验证内容：真实 QQ/微信通知权限、前台安全状态和通知卡片视觉未在本批次单独重测。
- Git 提交：未提交，待阶段 4 分批完成后统一提交。
- 下一步：阶段 4·步骤 4，归档日历/提醒调度与窗口入口职责。
# 2026-09-14：阶段 4·步骤 4——日历提醒职责复核完成

- 目标：确认日历与提醒逻辑已按职责归档，避免重复重构造成存档或提醒行为变化。
- 复核模块：`src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.Planner.cs`、`src/LuoTianyiPet.App/Application/Planner/ReminderService.cs`。
- 结论：提醒初始化、提醒存储、日历窗口、提醒卡定位/刷新、提前/正式提醒、稍后/确认/取消、音效和关闭清理已集中在现有 planner partial 与服务中，本步骤无需再次移动代码。
- 验证：沿用前一批 Release net48 编译 0 警告/0 错误、821/821 测试和 `--preview-exit --qa-window` 启动退出验证。
- 风险：真实日期跨日、系统休眠恢复、提醒音效和跨 DPI 视觉未在本步骤单独重测。
- Git 提交：未提交，待阶段 4 分批完成后统一提交。
- 下一步：阶段 4·步骤 5，归档文件拖入回收站与包子追逐逻辑。
# 2026-09-14：阶段 4·步骤 5——文件追逐入口职责归档完成

- 目标：整理文件追逐的入口职责，保持回收站安全策略和包子动画不变。
- 修改模块：新增 `src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.FileTreats.cs`；从 `MainWindow.xaml.cs` 移出文件偏好、桌面消失事件和包子目标入队。
- 完成内容：文件追逐偏好开关、桌面图标消失事件安全判断、运行时包子资源定位和追逐启动入口集中到新 partial；运动、进食、返回和取消状态机暂留主文件。
- 验证：Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- 未验证内容：真实桌面文件消失、回收站移动、包子追逐和拖动视觉未在本批次单独重测。
- Git 提交：未提交，待阶段 4 分批完成后统一提交。
- 下一步：阶段 4·步骤 6，归档边缘躲藏与拖动职责。
# 2026-09-14：阶段 4·步骤 6——边缘躲藏与拖动第一批归档完成

- 目标：归档边缘躲藏的静态映射规则，不改变拖动状态机和窗口定位行为。
- 修改模块：新增 `src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.EdgeDocking.cs`；从 `MainWindow.xaml.cs` 移出边缘镜像、动画 ID、关键帧和播放速率映射。
- 完成内容：边缘躲藏的资源/帧映射与视觉镜像规则独立归档；拖动开始/结束、边缘判定、位置计算和事件处理暂留主文件。
- 验证：Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- 未验证内容：真实多屏跨 DPI 拖动、边缘吸附/收纳和透明窗口视觉未在本批次单独重测。
- Git 提交：未提交，待阶段 4 完整结束后统一提交。
- 下一步：阶段 4·步骤 7，整理状态与动画协调职责，然后进入阶段 5。
# 2026-09-14：阶段 4·步骤 7——状态与动画播放职责归档完成

- 目标：归档动画播放底层方法，保持状态机决策、反应优先级和连续动画过渡不变。
- 修改模块：新增 `src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.AnimationPlayback.cs`；从 `MainWindow.xaml.cs` 移出普通播放、区间播放、指定帧、清单应用、Fallback 和附属控件尺寸同步。
- 完成内容：动画播放器调用与窗口视觉尺寸应用独立归档；状态机和过渡协调仍由主窗口负责，避免改变功能状态。
- 验证：Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- 未验证内容：动画全量视觉、透明边界、多 DPI 拖动和长时间播放未在本批次逐项重测。
- Git 提交：未提交，待阶段 5 完成前统一整理提交。
- 下一步：阶段 5，建立统一运行时资源定位器并盘点硬编码资源路径。
# 2026-09-14：阶段 5——运行时资源定位与 UI 结构审计完成

- 目标：建立统一运行时资源定位入口，为后续 UI 美化提供稳定边界；本阶段不改外观。
- 修改模块：新增 `src/LuoTianyiPet.App/Application/RuntimeAssetLocator.cs`；更新动画清单、提醒音效、命中图、指针、包子对象、设置动态插图、日历头像和通知头像的 C# 资源读取。
- 完成内容：统一文件型运行时资源路径和动态 `pack://application` URI；XAML 静态资源声明保持原样，避免改变 WPF 编译资源行为。
- 验证：Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- 未验证内容：完整设置页视觉、多 DPI 资源加载、所有动画逐项播放未在本阶段逐项重测。
- Git 提交：未提交，待阶段 6 审计和最终验证后统一整理。
- 下一步：阶段 6，审计 QA 入口、旧 helper、未引用文件和临时路径；删除动作仍需全局引用确认和用户确认。
# 2026-09-14：阶段 6 QA 隔离与最终清理审计完成

- 目标：确认 QA 隔离、兼容代码、临时目录和可删除边界，不擅自删除用户或历史资源。
- 审计结果：15 个 QA partial 已在 `Diagnostics/Qa/`；源码未发现 TODO/FIXME/占位实现；7 个 Compatibility 文件仍被 net48 兼容逻辑使用；Git 未跟踪 bin/obj/artifacts/.local-tools/日志/临时/崩溃文件；`候选素材_官方/` 仍有 1,311 个历史跟踪文件，按约定保留。
- 修改文件：`docs/阶段6审计-2026-09-14.md`、`docs/项目总控.md`、`docs/开发日志.md`。
- 删除/移动：无。未处理 artifacts、候选素材、worktree、用户已有删除项和未跟踪文件。
- 验证：`git diff --check` 通过（仅换行转换提示）；Release net48 编译 0 警告/0 错误；Animation 37、Platform.Windows 140、Core 644，共 821/821 测试通过；`--preview-exit --qa-window` 实际启动退出码 0。
- Git 提交：未提交，待最终验收和用户确认后统一整理。
- 下一步：最终验收，然后 Module D 临时文件/Git 历史审计；Module E 重建仓库前必须再次确认。

# 2026-09-14：细化身体反应、问候、通知、音乐与闹钟的输入打断策略

- 目标：落实用户确认的单击/拖拽行为，不再把所有一次性动画统一视为不可打断或始终保留。
- 修改模块：`src/LuoTianyiPet.Core/State/PetStateMachine.cs`、`src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.xaml.cs`、`src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.Planner.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PreserveDragQa.cs`、`tests/LuoTianyiPet.Core.Tests/PetStateMachineTests.cs`。
- 完成内容：普通身体反应增加确认单击取消和拖拽取消策略；时间问候增加拖拽取消策略；普通身体反应取消时清理令牌、附加运动、镜像和视觉过渡；闹钟设置为不可拖动；通知、音乐、追包子和文件拖入保持各自保护规则。
- 文档：同步更新需求、动画映射、技术架构、决策记录和项目总控，明确 `InterruptibleByDrag` 只表示是否允许移动窗口。
- 测试：Core net48 `646/646` 通过；`git diff --check` 通过。完整解决方案 Release 编译未通过，阻塞点是用户并行修改的 `PlannerTheme.cs` 中 `Geometry` 方法与类型同名造成的两处 `CS0119`，本轮未修改该文件。
- 未验证内容：完整 App 编译、全量测试、实际窗口视觉和真实鼠标交互 QA，需先解决上述并行 UI 编译冲突。
- Git 提交：未提交；保留用户同时修改的 `PlannerTheme.cs`、`CalendarLabels.cs`，不混入本轮变更。
- 下一步：处理并行 UI 修改的编译冲突后，重新执行 Release net48 编译、全量测试和 PreserveDrag/Planner 交互 QA。

# 2026-09-14：并行修改验证完成并创建最新源码启动入口

- 目标：在并行 UI 修改结束后完成合并前回归，并提供始终构建当前源码的桌面启动方式。
- 修改模块：Planner 五表面 UI 变更、动画输入策略与 QA 变更、`tools/Launch-LuoTianyiPet.ps1`，以及项目总控/架构/需求/动画映射/决策文档的同步记录。
- 完成内容：确认 Planner 编译冲突已消除；保留普通身体反应和时间问候的拖拽取消、身体反应确认单击取消、通知/音乐/闹钟/包子/文件拖入保护规则；新增启动脚本，每次点击先构建当前 `net48 Debug` 源码，构建失败不启动旧输出；已在桌面创建 `洛天依桌宠-最新开发版.lnk`。
- 验证：Release net48 全解决方案编译 0 警告/0 错误；Core 646、Animation 37、Platform.Windows 140，共 823/823 测试通过；`--qa-preserve-drag` 93 项 PASS；最新 `--qa-planner` 76 项 PASS，含 100%/125%/150% DPI 快照；启动脚本 `-BuildOnly` 通过；`git diff --check` 无内容错误。
- 未验证内容：真实用户鼠标长时间使用、真实 QQ/微信通知、安装器生产证书和发布包最终验收不属于本轮；桌面快捷方式依赖当前项目目录不被移动。
- Git 提交：`c35a10f`（`feat: finalize planner UI and interaction policies`），已推送到 `origin/main`。
- 下一步：进入用户主导的 UI 美化与 bug 修复阶段。
### 2026-09-14：音乐时间轴接入与网易云现场探测

- 日期和目标：在统一音乐悬浮岛中接入 Windows 系统媒体会话的可选播放位置/总时长，并确认当前网易云实际能力。
- 修改模块：`src/LuoTianyiPet.Core/Music/MediaTrackInfo.cs` 增加内存时间轴模型；`src/LuoTianyiPet.Platform.Windows/Audio/SystemMediaTrackInfoSource.cs` 读取并校正 SMTC 时间轴；`src/LuoTianyiPet.App/Presentation/MainWindow/MainWindow.xaml` 和 `MainWindow.xaml.cs` 增加播放按钮进度环与已播放/总时长工具提示；快速面板 QA 增加时间轴显示断言；平台测试增加相对位置、越界和非法值用例。
- 实际执行的测试及结果：Release net48 全解决方案编译 0 警告/0 错误；Core 646/646、Platform.Windows 146/146；`--qa-quick-actions --portable` 30/30 PASS。当前网易云现场 `--qa-track-info-live` 实际启动并安全退出，探测结果为 `Session=True;Track=True;Timeline=False;Artwork=False`，未发送任何媒体控制命令。
- 未验证内容或风险：当前网易云版本/播放状态未向 Windows SMTC 提供可用时间轴，因此现场未能显示真实进度环；在提供时间轴的网易云版本或其他 SMTC 播放器上需再做一次视觉复核。进度环对缺失、非法、直播流或总时长为零自动隐藏。
- Git 提交：本轮尚未提交；保留用户已有的文档工作区修改不覆盖、不擅自合并。
- 下一步的单一入口：用户确认是否接受当前“可用时显示、不可用自动隐藏”的降级策略后，再决定是否继续研究网易云非公开界面读取（不建议绕过 Windows 公开媒体会话）。
### 2026-09-14：网易云 SMTC 重新打开后的现场复测

- 日期和目标：用户重新打开网易云 SMTC 后，复测已实现的歌曲封面和可选时间轴读取。
- 实际执行的测试及结果：当前 Release net48 版本只读启动 `--qa-window --qa-media-controls --qa-track-info-live --portable` 并安全退出；探测结果为 `Session=True;Track=True;Timeline=False;Artwork=True`。封面读取已确认恢复，时间轴仍由网易云当前 Windows 媒体会话缺省提供。
- 未验证内容或风险：本机当前网易云版本没有返回播放位置/总时长，因此无法在真实网易云上验证进度环的动态变化；其他提供 SMTC 时间轴的播放器或网易云后续版本可直接使用已实现的进度环。未发送任何媒体命令。
- Git 提交：代码功能已包含在 `091ce89` 并已推送；本次仅追加现场复测记录，尚未单独提交。
- 下一步的单一入口：继续使用公开 Windows SMTC；若要支持当前网易云无 SMTC 时间轴的情况，需要用户另行确认是否接受研究网易云私有 UI/进程接口。
### 2026-09-15：修复切歌时封面缓存滞留

- 日期和目标：修复网易云切歌后歌名/歌手已更新，但音乐悬浮岛仍显示上一首封面的时序问题。
- 修改模块：`src/LuoTianyiPet.Platform.Windows/Audio/SystemMediaTrackInfoSource.cs` 增加新曲目封面重新校验；新曲目切换时不复用旧封面，6 秒内拒绝与上一首相同的疑似过渡缩略图并持续重读；同曲目的瞬时读取失败保留已确认封面。`tests/LuoTianyiPet.Platform.Windows.Tests/SystemMediaTrackInfoSourceTests.cs` 增加上一首封面候选回归用例。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；Animation 37/37、Core 646/646、Platform.Windows 147/147，共 830/830；美化后的 `--qa-quick-actions --portable` 32/32；Debug 启动构建通过；网易云只读实时探测仍正常读取封面，未发送媒体命令。
- 未验证内容或风险：用户已实际切歌验收并确认修复有效；若连续歌曲确实使用完全相同封面，最多在 6 秒校验窗口内暂显示占位，随后接受该封面。
- Git 提交：本轮待提交；保留用户已有美化提交和文档修改。
- 下一步的单一入口：提交并推送本轮已验收的封面刷新修复。

### 2026-09-15：班休调整移除重复日期窗并增加拖动选区

- 日期和目标：让月视图班休设置直接复用左侧月历，降低连续设置日期时的操作成本。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；同步更新需求、技术架构、决策记录和项目总控。
- 完成内容：移除班休面板的“添加日期”入口，保留提醒编辑器的独立日期选择窗；月历调整模式支持单击切换选中和按住左键拖动沿路径连续选中；右侧显示已选数量并提供清空；无选区时批量操作置灰；批量保存成功后清空选区但继续留在调整模式，失败保留选区。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；Animation 37/37、Core 646/646、Platform.Windows 147/147，共 830/830 测试通过；`--qa-planner --portable` 全部通过，包含新增的重复入口、拖动路径、按钮禁用、应用后模式保持和清空选择断言；100/125/150% DPI 快照已生成并目视检查。
- 未验证内容或风险：尚未进行长时间真实鼠标拖动和跨显示器 DPI 的人工体验；当前拖动按鼠标经过的日期格加入选区，不增加立即写库的刷子模式。
- Git 提交：本轮已提交并推送到已配置的 `origin/main`。
- 下一步的单一入口：用户实际体验月视图长按拖动；如需连续整列选择，再单独评估 Shift 范围选择。

### 2026-09-15：班休面板固定操作区并支持反向拖动取消

- 日期和目标：解决选中日期增多后按钮下移、日期列表滚动影响布局，以及已选日期无法拖动取消的问题。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；同步更新需求、技术架构、决策记录和项目总控。
- 完成内容：班休右侧改为固定 Grid 分区，日期列表独立滚动，四个操作按钮固定在底部；增加“指定日期覆盖每周设置，恢复默认后跟随每周设置”的说明；拖动按起始日期状态统一执行选中或取消。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；`--qa-planner --portable` 全部通过，新增每周规则优先级、恢复默认、反向拖动取消和 22 天选区固定操作区快照；100/125/150% DPI 快照已生成并目视检查。
- 未验证内容或风险：尚未进行长时间真实鼠标拖动和跨显示器 DPI 的人工体验；当前仍不引入 Shift 范围选择或立即写库的刷子模式。
- Git 提交：本轮变更已完成验证并推送到已配置的 `origin/main`。
- 下一步的单一入口：用户实际体验长按拖动、反向取消及大量日期列表滚动。

### 2026-09-15：班休清除指定设置、单一完成入口与边界容错

- 日期和目标：按用户反馈修正恢复动作命名，移除重复完成入口，并降低拖动划过日期边界时的漏选风险。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；同步更新需求、技术架构、决策记录和项目总控。
- 完成内容：将按钮改为“清除指定设置”，清除单日覆盖；只保留月历导航旁的“完成班休”；日期按钮命中失败时使用 8 DIP 缓冲和最近日期格回退判断。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；`--qa-planner --portable` 全部通过，新增按钮命名、完成入口唯一、边界命中断言；全量测试 830/830；100/125/150% DPI 快照已生成并目视检查。
- 未验证内容或风险：尚未进行长时间真实鼠标拖动和跨显示器 DPI 的人工体验；边界缓冲仅作用于月历日期区域，不会在月历外自动选日期。
- Git 提交：本轮变更已完成验证并纳入本次提交，随后推送到已配置的 `origin/main`。
- 下一步的单一入口：用户实际体验边界拖动和“清除指定设置”后的日期状态。

### 2026-09-15：班休全局基线与单独日期例外修正

- 日期和目标：明确每周休息日与具体日期设置的优先级，并让“清除指定设置”在月视图中恢复为未单独设置的视觉状态。
- 修改模块：Planner 月视图只为 `RestOverrides` 显示“班／休”徽标；清除后显示未单独设置提示；新增核心测试验证切换每周规则不清除已有日期例外，新增 Planner QA 验证清除后的徽标、提示与状态栏。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；Animation 37/37、Core 647/647、Platform.Windows 147/147，共 831/831；`--qa-planner --portable` 全部通过；100/125/150% DPI 快照通过。
- 未验证内容或风险：用户实机确认月视图不显示全局规则徽标后的信息密度。
- Git 提交：9f357c0，待推送到已配置的 `origin/main`。
- 下一步的单一入口：用户实际体验全局规则、单独日期例外与清除指定设置的组合。

### 2026-09-15：班休表达层收尾

- 日期和目标：保留班休活规则二值模型，只收尾按钮命名、状态栏、单日详情入口和视图图例表达。
- 修改模块：`PlannerWindow.cs` 的 `RestControls`、`DayDetails`、月/周图例和 Planner QA；不改变 `IsRest`、日期例外、每周规则或保存链路语义。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；Animation 37/37、Core 647/647、Platform.Windows 147/147，共 831/831；`--qa-planner --portable` 全部通过；单日详情状态行和 100/125/150% DPI 快照已生成并目视检查。
- 未验证内容或风险：待用户实机确认单日详情状态行和月视图弱提示的信息密度。
- Git 提交：a14b052，待推送到已配置的 `origin/main`。
- 下一步的单一入口：用户实际体验单日详情入口、批量取消反馈和月/周图例。

### 2026-09-15：修复每周休息按钮无明确生效反馈

- 日期和目标：处理用户反馈的“目前无法设置每周休息”，复核每周规则写入链路并补足可见反馈。
- 修改模块：`src/LuoTianyiPet.App/Presentation/Planner/PlannerWindow.cs`、`src/LuoTianyiPet.App/Diagnostics/Qa/MainWindow.PlannerQa.cs`；未修改班休模型、优先级或设置窗。
- 完成内容：每周按钮统一通过显式异步切换方法保存；设置或取消后状态栏报告具体星期；为每周按钮补充可定位名称，覆盖开启、持久化、再次取消的 UI 回归检查。
- 实际执行的测试及结果：Release net48 编译 0 警告/0 错误；Animation 37/37、Core 647/647、Platform.Windows 147/147，共 831/831；`--qa-planner --portable` 119 项 PASS。
- 未验证内容或风险：尚未完成用户重启桌宠后的人工点击验收；月视图仍按既定语义不显示全局每周规则徽标，只通过班休面板和状态栏表达。
- Git 提交：待本轮验证后提交并推送到已配置的 `origin/main`。
- 下一步的单一入口：用户重启桌宠，点击一个未选中的周几，再点击一次取消，确认状态栏与按钮颜色变化。
