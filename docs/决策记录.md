# 决策记录

### 2026-09-15：提醒与显示倍率小修

用户确认将“指定时间”改为更直接的“闹钟”入口；修复日历上一页图标误显示为铃铛，以及月／周按钮选中后文字消失的问题；快捷面板的日历／闹钟入口不保留蓝色焦点态。显示倍率上限从 200% 提升到 300%，完整设置调整倍率时实时作用于桌宠，取消设置恢复原值，保存行为保持原有提交边界。该调整不改提醒存档、日历数据、快捷面板操作或默认倍率。

### 2026-09-15：在回退基线上重新实现日期刷选

用户回退了先前日历改动后，重新确认事项多日期和班休需要同一套交互：普通单击切换单日；拖动每次重新进入日期格就切换当前状态；不松手反向经过同一路径即可反选，混合选区可在一次拖动中同时选中和取消。实现不改存档、班休规则求值或批量写入语义，先保留为未提交工作区供用户实机验收。

### 2026-09-15：拖动不得吞掉普通日期单击

用户验收确认往返拖动修复不能牺牲日期按钮单击。决定将拖动结束分为两条路径：无跨日期移动时不释放按钮捕获、不设置 Click 抑制，保留 WPF 普通单击；跨日期移动时才释放捕获并拦截 Click。数据和单击的选中/取消语义不变。

### 2026-09-15：拖动改为可往返刷选而非单手势去重

用户明确要求：从左向右拖动选中一组日期后，不松开鼠标再从右向左拖过同一路径，应取消刚才的全部选择。为此取消“同一手势永久跳过已访问日期”的限制，改为仅抑制同一日期格内的连续采样；每次重新进入日期格都切换当前状态，方向反转时连当前端点也重新处理。两处日期选择均使用相同语义。

### 2026-09-15：日期拖动监听提升到日历容器

实机验收发现，仅对日期按钮注册移动/松开事件时，跨出原按钮后的真实 WPF 路由不够稳定。决定保留按钮作为按下起点，但把移动和左键松开统一注册到所属日历 Grid，并接收已处理事件；不改变选区规则、单击语义或存档格式。

### 2026-09-15：班休月历与多日期选择统一为混合拖动

用户确认班休月历也必须支持一次拖动内的混合操作。最终规则为：鼠标经过未选日期就选中，经过已选日期就取消；同一手势内同一日期不重复切换，以避免边界抖动造成结果不稳定。该规则只改变选区编辑方式，不改变班休批量写入、指定日期覆盖或每周规则语义。

### 2026-09-15：修正多日期拖动为单手势混合切换

用户复核后确认，多日期选择不能要求用户从已选日期开始才能取消，也不能把一次拖动固定为“全选”或“全取消”。最终规则为：拖动路径每次进入一个日期格时，按该日期当前是否已选执行切换；未选则选中，已选则取消。同一手势内已处理过的日期不重复切换，以保证跨越边界或鼠标轻微抖动时结果稳定。普通单击仍维持单日切换，不改变存档格式。

### 2026-09-15：多日期编辑器采用单一日期入口和拖动选区

用户确认新建事项的日期区域不再同时出现普通日期和“已选日期”两个入口。实现保留单日期模式的原生日期选择器，多日期模式将同一上方区域改为摘要按钮并打开现有日期选择窗；不增加第二个内嵌日历，不改变 `ReminderRepeat.Dates`、`ReminderItem.Dates` 或用户可见的保存语义。选择窗的日期按钮支持按住鼠标左键拖动，起始日期未选时沿经过路径全选，起始日期已选时沿经过路径全取消；普通单击仍只切换一天。

### 2026-09-14：音乐悬浮岛合并并补充封面

用户确认将原曲目气泡与音乐控制条合并为一个简约统一表面，以减少上下两个悬浮岛的臃肿感；统一表面保留上一首、播放/暂停、下一首和网易云独立音量四个控制，音量键必须位于切歌控制区。实现采用现有 Windows 系统媒体会话读取可选封面，失败回退占位符；只在内存中缓存和解码，不新增依赖、不改变用户可见控制逻辑和存档格式。该决定替代 2026-09-14 较早的“双表面”规格，其他快捷面板视觉约束继续有效。

### 2026-09-14：透明边缘审计必须同时覆盖亮边和暗边

用户要求排查所有动画中可能出现的白色或黑色边缘颗粒。决定将现有审计扩展为逐帧的亮边／暗边对称检查，并分别在深色和浅色背景上生成最坏帧接触页。筛查指标只负责定位候选，不直接等同于缺陷：连续的原画描边、文字、贴纸外圈、衣物边缘和半透明软边必须通过人工复核保留；只有能够确认是误扣背景的孤立颗粒才允许进入定点清理 profile。这样可以避免为了消除黑边而破坏角色线稿，也避免为了消除白边而误删白色内容。

### 2026-09-14：根据用户复核放开深色轮廓 profile 的纯白底色清理

上一轮边缘修复未清除“来啦”动画头发、裙摆和鞋边外沿的纯白颗粒。复盘确认原因是宽范围 profile 仍跳过完全不透明的纯白像素。本轮仅对已经确认的深色轮廓白底素材允许纯白边缘参与白底重合成；普通白色衣服、文字、贴纸描边、高光和其他动画继续保留，不做全局删白。

### 2026-09-14：透明动画边缘采用隐藏 RGB 清理与定点宽边缘 profile

用户发现“来啦”动画在深色背景上仍有白色点状毛刺。决定继续使用现有离线、确定性的边缘处理管线：只对已审计的二值 Alpha 来源生效；完全透明像素的 RGB 统一清零，避免 WPF 插值采样隐藏白底；仅对“来啦”和“来不及了”两组已确认存在白底残留的深色轮廓扩大检查范围。白色文字、服装、贴纸描边、原始光效以及已有柔和 Alpha 素材不做全局删白。原始素材、动画 ID、帧时序和存档格式保持不变。

### 2026-09-13：附着快速提醒卡

以用户最新十五条规则为准，将原角落胶囊改为人物下方同一表面展开。保留现有AcknowledgedEarly/Cancelled等实例状态机，避免以删除条目或关闭整条规则代替单次取消。WPF使用单个owned无激活宿主承载扩展范围，保持同一外框与头栏，而不扩张人物自身窗口的命中区域或创建两个提醒浮窗。多条快速实例共用一个可滚动内容区；不增加通知中心。


### 2026-09-13：原始素材与提醒轮次

采用找到的十周年L15.GIF原「来啦」，原件单独保存在候选素材目录，220×220 atlas处理参数与下载来源均保留。整个MP3随包离线分发，使用现有WPF MediaPlayer避免额外音频依赖。用每个发生实例的RoundStartedAt约束播放，而不采用全局清空提醒，确保多个事项互不误取消。启动未响应有Cross-process历史事件，故先移除已确认可阻塞启动的Explorer同步调用，并增加阻塞探测回归；未捕获用户当次进程堆栈，不将该推断称为唯一原因。


本文件追加重要决策。新决定可以取代旧决定，但要注明替代关系，不能删除历史原因。

## 2026-09-12：长待机装饰按像素锚点定位并保留完整起身段

- 取代 ADR-064 中“默认位于头部右上”的粗略布局：最小 Z 与最小云朵按各自运行帧中的真实像素中心对齐人物头侧；默认使用右侧，工作区内无法完整显示时镜像到左侧对称位置。
- 睡觉和鸭子坐统一使用 600×476 / 300×238 DIP 透明宽画布，只增加透明留白，不缩小人物；zzz 灰褐阶段用首帧淡蓝色阶确定性校正，原始序列不覆盖。
- 长待机会话优先于通用鼠标按下恢复；一次点击必须续播原动作的完整起身段后才能回到 Idle。固定装饰和右边缘位置仅作为隔离 QA 参数，不进入用户设置。

## ADR-001：使用 C#、.NET 10 LTS 和 WPF

- 日期：2026-08-30
- 状态：已采用
- 决定：主程序采用 C#、.NET 10 LTS 和 WPF。
- 原因：项目只面向 Windows，并大量依赖透明窗口、Win32、Core Audio、OLE、COM 和 Shell 能力；WPF 的窗口互操作成熟，适合先完成个人桌宠。
- 影响：不追求跨平台；现代 Windows API 通过平台适配层接入。

## ADR-002：把仓库作为跨对话记忆

- 日期：2026-08-30
- 状态：已采用
- 决定：`AGENTS.md` 规定每轮恢复流程，`docs/项目总控.md` 保存当前状态，开发日志保存验证历史。
- 原因：聊天可能变长、压缩或切换任务，不能依赖模型对旧对话的隐式记忆。
- 影响：每个可交付变更必须同步更新项目总控和开发日志，否则不算完成。

## ADR-003：原始素材和运行时素材分离

- 日期：2026-08-30
- 状态：已采用
- 决定：`候选素材_官方/` 只保存原始来源；`assets/` 保存可追溯的运行时派生资产。
- 原因：现有格式和画布差异较大，直接在 UI 中混用解码器会增加故障和性能风险。
- 影响：建立资产编译工具、动画清单、来源哈希和转换参数。

## ADR-004：显示模式、持续状态和一次性反应分离

- 日期：2026-08-30
- 状态：已采用
- 决定：不使用一个枚举表示桌宠全部状态；调度器从三个正交状态生成播放计划。
- 原因：QQ提醒、音乐、睡眠和用户选择模式会交叉；单一状态容易在动画结束时恢复错误。
- 影响：所有平台联动只能发领域事件，不能直接命令窗口播放并写死返回动画。

## ADR-005：安全降级优先于功能命中率

- 日期：2026-08-30
- 状态：已采用
- 决定：受保护应用前台、通知权限拒绝、回收站不可用或媒体结果不确定时，功能可以不触发，但不能绕过权限或给出错误成功反馈。
- 原因：账号安全、文件安全和隐私高于动画完整性。
- 影响：每个外部联动必须提供禁用、超时、取消和恢复路径。

## ADR-006：音乐状态临时覆盖显示，不修改用户模式选择

- 日期：2026-08-30
- 状态：已采用
- 决定：音乐开始后播放“享受音乐”并进入“音乐摇摆”；音乐停止后恢复用户此前选择的迷你或完整全身模式。
- 原因：保留已经确认的音乐专属待机，同时不让音乐事件永久改变用户的交互选择。
- 影响：设置中需要分别保存 selected display mode 和 resolved playback state。

## ADR-007：运行时统一使用透明 PNG 图集和帧时序清单

- 日期：2026-08-30
- 状态：已采用
- 决定：选定的 GIF、动态 WebP 和 PNG 在构建前编译为透明 PNG 图集；WPF 根据带来源哈希的 JSON 清单逐帧裁切播放。
- 原因：WPF 对现有动画格式的原生支持不一致；统一图集可以准确保留透明度、毫秒级帧时长、循环次数和结束回调，并减少运行时解码器依赖。
- 影响：新增可复现的图集生成脚本和清单校验器；原始文件继续保留，图集通过 Git LFS 管理；需要在 M1 完成内存稳定性验证。

## ADR-008：每次播放会话随机选择一个音乐持续动画

- 日期：2026-08-30
- 状态：已采用；覆盖 ADR-006 中“享受音乐后固定进入音乐摇摆”的两阶段描述，ADR-006 的显示模式恢复约束继续有效。
- 决定：每次从暂停或停止进入播放状态时，在 0.5 倍速“享受音乐”和“音乐摇摆”中随机选择一项，并让它在整个播放会话中循环；暂停后继续播放重新选择。
- 原因：两种素材人物大小、服装和画风不同，播放中途强制切换仍会让体验割裂；按播放会话选择可保留随机性并避免会话内换装。
- 影响：两项素材都必须支持无限循环；持续状态保存当前会话选中的动画，音乐停止后仍恢复用户原本选择的“嘿嘿”或完整全身模式。

## ADR-009：使用只读 Core Audio 会话峰值判断网易云是否有声

- 日期：2026-08-30
- 状态：已采用
- 决定：通过 Windows Core Audio 枚举默认多媒体输出设备的会话，以公开的会话进程号匹配 `cloudmusic.exe`，读取归一化峰值；使用 `NAudio.Wasapi` 3.0.1 作为薄封装。默认每 250 毫秒读取一次，峰值达到 0.001 立即视为播放，连续 1.5 秒无声才视为停止。
- 原因：用户要求不接网易云私有接口；单纯检查进程无法区分播放和暂停，而公开音频会话峰值无需注入、Hook、内存读取或网络监听。
- 影响：功能可以在运行时配置中单独关闭；会话消失立即停止，临时探测失败保留上次状态并重试；不记录进程号、峰值或歌曲信息。真实播放器在不同输出设备、静音歌曲和独占输出下仍需端到端验证。

## ADR-010：将音乐静音宽限缩短为 0.5 秒

- 日期：2026-08-30
- 状态：已采用；覆盖 ADR-009 的 1.5 秒默认宽限参数，其余检测方案不变。
- 决定：连续检测到 0.5 秒无声后退出音乐状态并恢复用户选择的“嘿嘿”或完整全身模式。
- 原因：1.5 秒会让暂停操作显得迟钝；0.5 秒仍能过滤极短的采样空隙，同时明显改善暂停反馈速度。
- 影响：用户 `settings.json` 配置版本升级为 2；旧版本恰好使用原默认值 1500 毫秒时自动迁移为 500 毫秒，用户已设置的其他数值保持不变。

## ADR-011：将音乐静音宽限调整为 1.0 秒折中值

- 日期：2026-08-30
- 状态：已采用；覆盖 ADR-010 的 0.5 秒默认宽限参数。
- 决定：连续检测到 1.0 秒无声后退出音乐状态；未来由桌宠发起的上一首/下一首操作使用独立加载状态，不再只依赖静音宽限判断。
- 原因：0.5 秒会把网易云切换下一首时的常见加载时间误判为暂停，1.5 秒又让真实暂停反馈偏慢；1.0 秒作为当前只读检测阶段的折中值。
- 影响：用户 `settings.json` 配置版本升级为 3；版本 1 的旧默认 1500 毫秒和版本 2 的旧默认 500 毫秒均迁移为 1000 毫秒，其他自定义值保持不变。

## ADR-012：全局快捷键发送采用失败关闭的用户级输入适配器

- 日期：2026-08-30
- 状态：已采用。
- 决定：网易云控制只使用普通用户权限的 `SendInput` 发送用户配置组合；发送前确认前台进程不在受保护名单，并检查相关实体键未被用户按住。前台查询不可靠、输入被占用、过于频繁或系统未完整接受时一律不发送或报告失败。
- 原因：桌宠需要复用网易云自身全局快捷键，同时不得把组合键送入原神等受保护应用，也不能打乱用户正在进行的键盘输入。
- 影响：默认提供 `Ctrl+Alt+Left`、`Ctrl+Alt+P`、`Ctrl+Alt+Right` 作为首轮可见配置；右键菜单显示当前组合。快捷键“已发送”与播放器“已响应”是两个事件，只有后者可触发 OK，下一阶段需用音频状态变化完成确认、加载和超时闭环。

## ADR-013：媒体控制改为角色内悬浮控制条

- 日期：2026-08-30
- 状态：已采用；覆盖 ADR-012 中“右键菜单显示媒体组合”的临时界面决定，ADR-012 的输入安全约束继续有效。
- 决定：鼠标移入角色后显示上一首、播放/暂停、下一首、喜欢歌曲四键控制条，移出后隐藏；控制条覆盖在现有透明窗口底部，不扩大桌宠常驻窗口。喜欢歌曲使用网易云默认 `Ctrl+Alt+L`。
- 原因：常用媒体操作需要比右键子菜单更直接，并与用户给出的简洁播放器按键参考保持一致；悬停显示可避免长期占据屏幕和遮挡角色。
- 影响：右键菜单只保留显示模式、调试、置顶和退出等低频功能；按钮工具提示显示当前快捷键，播放/暂停字形跟随检测状态，控制条隐藏时不得参与身体命中。

## ADR-014：悬浮媒体组件使用项目内轻量矢量样式

- 日期：2026-08-30
- 状态：已采用；细化 ADR-013 的视觉与显隐行为。
- 决定：媒体控制岛使用洛天依代表色 `#66CCFF`、蓝色玻璃表面、本地 WPF `Path` 矢量图标和轻量淡入淡出控制器；只在指针停留于桌宠窗口时可交互，移出后经过 220 毫秒宽限隐藏。
- 原因：MahApps、MaterialDesignInXaml 和 WPF-UI 都提供成熟按钮与图标组件，但当前只需要四个按钮；引入整套框架会增加包体、主题资源和升级风险，本地组件可以保留同样的圆形按钮、状态反馈和 DPI 清晰度。
- 影响：当前不新增第三方 UI 依赖；后续设置页若需要大量标准控件再单独评估 UI 框架，媒体组件仍可复用当前品牌色和矢量图标资源。

## ADR-015：媒体控制岛移到角色动画正下方

- 日期：2026-08-30
- 状态：已采用；覆盖 ADR-013 中“覆盖在现有透明窗口底部、不扩大窗口”的布局决定，四键内容和显隐规则不变。
- 决定：为媒体控制区在窗口底部预留 58 DIP，高度随当前动画画布一起计算；角色视觉层与音乐切换闪白层均避开该区域。控制岛只在悬停期间显示，但即使显示也不覆盖角色、表情文字或身体命中区。
- 原因：用户希望控制按钮出现在动画正下方，不能挡住桌宠本身；单纯继续缩小覆盖层会损害按钮可点性和视觉层级。
- 影响：窗口高度比素材显示高度多 74 DIP（基础边距 16 DIP 加控制区 58 DIP），桌宠主体尺寸不变；旧窗口位置仍可恢复，贴近屏幕下缘时由现有工作区约束校正。

## ADR-016：新增官方动态素材先进入独立候选区

- 日期：2026-08-30
- 状态：已采用。
- 决定：将洛天依 12 周年 QQ 动态表情的 16 个 300×300 原始 GIF 放入独立“新添加”目录，在动画挑选器中统一显示蓝色标记并提供专用筛选；不自动写入运行时状态映射。
- 原因：这组素材覆盖场景广，但画风、动作时长和语义仍需用户实际预览后决定，直接接入会推翻已确认的动画规则。
- 影响：素材来源、归档提交、原始文件名和 SHA-256 都随目录保存；用户选择前，程序包大小和运行时行为不受这些候选影响。

## ADR-017：曲目信息使用系统媒体会话并以网易云窗口标题兜底

- 日期：2026-08-30
- 状态：已采用。
- 决定：曲名和歌手优先读取 Windows `GlobalSystemMediaTransportControlsSessionManager`；目标网易云没有注册系统媒体会话时，只读其顶层主窗口标题并按“歌曲 - 歌手”解析。角色上方预留 52 DIP 显示曲名，悬停时显示，曲目变化或切歌后保留 4 秒。
- 原因：系统媒体会话是语义最可靠的公开来源，但用户当前运行的网易云实测没有任何系统媒体会话；窗口标题仍由 Windows 桌面管理器公开，能够在不接入网易云接口、不读取进程内存的前提下满足功能。
- 影响：只匹配 `cloudmusic.exe`，不枚举其它应用内容；标题和歌手只在内存中保存，日志只记录“曲目已变化”事件而不记录文本。网易云标题为空、通用或格式改变时显示安全降级文案。

## ADR-018：启动按本地时间问候，系统恢复统一播放苏醒

- 日期：2026-08-30
- 状态：已采用。
- 决定：桌宠启动时按 06:00～12:00、12:00～13:30、13:30～20:00、20:00～次日 06:00 四个半开区间播放一次既定问候；Windows 解锁或电源恢复统一播放“心律共鸣·苏醒弹出”，5 秒内的解锁/电源重复事件只响应一次。
- 原因：启动问候表达当前时间，而同一轮系统唤醒常会同时触发两个 Windows 事件；统一苏醒语义并去重可避免恢复时连续换装和重复打扰。
- 影响：时间边界由 Core 解析器和自动化测试维护；平台层只订阅公开 `SystemEvents`，不安装服务或 Hook。反应不激活、不置顶，结束后恢复用户选择的显示模式和当前音乐等持续状态。

## ADR-019：长待机睡眠复用输入空闲查询并与系统苏醒去重

- 日期：2026-08-30
- 状态：已采用。
- 决定：每秒使用既有公开 `GetLastInputInfo` 读取系统空闲时长；5 分钟进入“躺平”，15 分钟且无音乐时进入“晚安浮动”。睡眠后的首次输入播放一次“心律共鸣·苏醒弹出”，并与 Windows 解锁/电源恢复共用 5 秒去重门。
- 原因：轮询公开的累计空闲时长足以判断状态，不需要键盘/鼠标全局 Hook；真实睡眠恢复通常同时产生输入、会话和电源事件，共用去重可以避免同一动作被播放两次。
- 影响：桌宠不记录具体按键、鼠标位置或输入内容；音乐、拖拽和安全隐藏状态不会被空闲规则覆盖。睡眠素材为官方静态 PNG 的可追溯单帧图集，低幅度漂浮由 WPF 在运行时生成。

## ADR-020：原神存在性使用低频过滤进程枚举

- 日期：2026-08-30
- 状态：已采用。
- 决定：正式应用每 2 秒只按配置的 `YuanShen` 与 `GenshinImpact` 名称执行一次普通权限进程枚举，只读取进程标识并立即释放对象；启动时存在的进程仅作为基线，不补播启动反应。前台和全屏状态另用公开 Win32 窗口 API 失败关闭。
- 原因：原计划采用的 WMI `Win32_ProcessStartTrace` / `StopTrace` 在本机普通用户会返回“拒绝访问”；要求提权会扩大风险，轮询公开进程表即可满足低频桌宠场景，并更容易审计其读取边界。
- 影响：检测延迟上限约为 2 秒，不新增 `System.Management` 依赖；不读取游戏内存、模块、线程、文件、画面或网络，不向游戏发送输入，也不建立长期游戏句柄。若一次枚举失败，保留上次状态并在下一轮重试，不猜测启动或退出。

## ADR-021：消息提醒使用有包身份的 Windows 通知监听器

- 日期：2026-08-30
- 状态：已采用；真实授权等待 MSIX 测试包。
- 决定：QQ/微信提醒只使用 `UserNotificationListener` 的应用来源元数据。应用必须先具备 MSIX 包身份并声明 `uap3:userNotificationListener`；设置页仅在用户明确点击后从 UI 线程申请权限。当前免安装 EXE 检测到没有包身份时显示原因并安全停用，不尝试绕过。
- 原因：通知监听器能在不接触聊天进程内部数据的前提下提供可靠的来源分类，但 Windows 将该能力绑定到包身份和用户同意。任务栏闪烁只能作为以后另行验证的兼容性补充，不能冒充已收到消息。
- 影响：平台层只读取 AUMID、应用显示名和创建时间，从不访问消息负载；Core 只在内存中保留来源和时间做 3 秒去重。实际 QQ/微信通知测试需要先构建、签名并由用户安装 MSIX，再由用户亲自批准通知访问。

## ADR-022：测试 MSIX 使用自包含发布和未自动信任的开发证书

- 日期：2026-08-30
- 状态：已采用；等待用户确认安装。
- 决定：用微软 `WinAppCLI` 将 .NET 10 `win-x64` 自包含输出打为 MSIX；包名、发布者和版本由仓库清单固定。首次构建生成随机密码自签名开发证书，PFX 与密码只留在 Git 忽略的本地 `artifacts/msix/private/`，发布目录只放 MSIX、公钥 CER 和 SHA-256。
- 原因：自包含包不依赖目标机预装 .NET；确定性脚本比手工打包更容易审计通知能力和包内容。开发证书可以用于本机旁加载，但自动信任证书会改变系统安全状态，不应由普通构建步骤代替用户决定。
- 影响：当前包约 103 MB，签名完整但在信任公钥前会显示证书链不受信任。构建脚本不会安装证书、应用或申请通知权限；这三项依次等待用户明确确认。实机证明 `CurrentUser\TrustedPeople` 不足以部署该自签名 MSIX，因此测试安装需要用户批准管理员操作，将公钥导入 `LocalMachine\TrustedPeople`；不改用当前用户根证书存储，也不自动开启开发者模式。正式分发仍需要受信任的生产证书或 Microsoft Store 签名。

## ADR-023：通知监听只在用户成功授权后启动

- 日期：2026-08-30
- 状态：已采用；细化 ADR-021 的授权时序。
- 决定：配置新增“Windows 通知访问已成功授权”标志，默认和所有版本 6 旧配置均为否。应用启动与设置页打开不得在该标志为否时调用 `UserNotificationListener.Current` 或 `GetAccessStatus`；只有用户在设置页亲自点击按钮后才调用 `RequestAccessAsync`，且仅在返回 `Allowed` 后缓存标志并启动监听。
- 原因：微软要求在使用 `UserNotificationListener` 前从 UI 线程取得明确许可；本机 `0.1.0.0` 在未授权启动阶段探测监听器时同时触发 Windows 推送通知服务故障和未处理 `COMException`。消息提醒是可选联动，不应阻止桌宠主体启动。
- 影响：设置架构升级到版本 7；更新后的包即使 Windows 通知服务不可用也能启动音乐、动画和其它功能。权限被拒绝或请求失败时保持未授权，不在下次启动自动重试；用户需要在设置页再次主动操作。

## ADR-024：外部普通测试使用自包含便携包

- 日期：2026-08-30
- 状态：已采用。
- 决定：普通外部测试提供 x64 自包含 ZIP，以 `--portable` 启动并把设置、日志全部写入解压目录的 `UserData`；不包含 MSIX、开发证书或安装步骤。附带的清理脚本必须验证包标记、可执行文件和目标路径，只停止可执行路径完全匹配的便携进程并删除包内数据。
- 原因：测试者需要能直接解压运行、方便移除且不在个人电脑留下应用配置；为普通功能测试导入本机级自签名证书会扩大安全和清理成本。
- 影响：便携版可测试动画、鼠标、音乐、音量、时间和原神低权限联动，但因没有包身份而不能测试 QQ/微信通知监听；后者继续使用单独的 MSIX 测试流程。“无残留”只承诺程序自身创建的数据，不试图删除 Windows 下载历史、SmartScreen、安全扫描或系统事件记录。

## ADR-025：移除爱心切换并按歌手选择音乐陪伴动画

- 日期：2026-08-30
- 状态：已采用；覆盖 ADR-013/014/015 中的四键内容和 ADR-007 的无条件双动画随机规则，原有悬停、布局与显示模式恢复约束继续有效。
- 决定：媒体控制岛只保留上一首、播放/暂停和下一首。“喜欢歌曲”命令、快捷键配置和按钮全部移除。普通或未知歌手播放时固定使用戴耳机的“享受音乐”；歌手字段包含独立的“洛天依”或 `Luo Tianyi`（包括合唱）时，才在“享受音乐”和“音乐摇摆”之间随机。
- 原因：网易云的喜欢快捷键是切换操作，而 Windows 系统媒体属性、当前网易云窗口标题和其无障碍树均不提供爱心状态；保留按钮会让用户误取消已收藏歌曲。桌宠定位转向音乐陪伴后，已获得的歌手字段可以安全驱动更有语义的“陪听/天依唱歌”差异。
- 影响：不调用网易云私有接口、不读取账号、歌单、数据库、进程内存或截图。切歌、暂停后继续以及迟到的歌手信息会重新选择动画；日志只记录 `LuoTianyi` 或 `OtherOrUnknown` 分类，不记录曲名和歌手文本。

## ADR-026：控制岛用网易云独立音量替代爱心，低频设置进入系统托盘

- 日期：2026-08-30
- 状态：已采用；继续细化 ADR-025。
- 决定：控制岛第四格使用可横向拖动的扬声器滑杆，只读写 `cloudmusic.exe` 的公开 Core Audio 会话音量；设置、永久置顶和开机自启动放入 Windows 通知区域菜单。系统音量滚轮及外部反馈继续保留为独立能力。
- 原因：爱心状态不可可靠读取，网易云独立音量更符合音乐陪伴定位，而且不会改变其它程序；始终显示在桌宠右键菜单中的低频设置不符合 Windows 常驻应用习惯，托盘更便于随时调整。
- 影响：网易云没有音频会话时滑杆安全显示不可用，绝不退回系统主音量。开机自启动默认关闭，只在用户主动启用时写入当前用户 Run 项中的一个产品值；不安装服务、计划任务或驱动。受保护游戏前台或前台查询失败时，两类音量写入与快捷键发送都失败关闭。

## ADR-027：取消悬浮音量控件和网易云独立音量写入

- 日期：2026-08-30
- 状态：已采用；覆盖 ADR-026 中关于控制岛第四格和应用会话音量的部分，ADR-026 的系统托盘设置决定继续有效。
- 决定：控制岛恢复为上一首、播放/暂停、下一首三键，取消音量图标、滑杆、`IApplicationVolumeService` 及 Windows 应用会话音量写入实现。
- 原因：用户实际体验后明确要求取消该图标及功能；不保留隐藏入口或不可见后台能力，避免代码与产品界面不一致。
- 影响：既有系统音量滚轮控制和外部系统音量动画反馈不变；网易云音频会话仍只读用于判断音乐是否出声，不再用于调节音量。

## ADR-028：边缘躲藏采用明确越界意图和素材原生触发面

- 日期：2026-08-31
- 状态：已采用；覆盖原 18 DIP 近边即隐藏和 12 DIP 天依蓝三点条方案。
- 决定：以人物实际画面越过工作区 28 DIP 作为躲藏意图，单纯贴边保持普通摆放；左右隐藏后保留“偷看”素材砖墙，底部隐藏后保留“闪亮登场”猫耳/头发。动画只遍历连续第 3～7 帧，并在鼠标方向变化时从当前帧反转。距底边 80 DIP 内将媒体控制岛移到歌名/作者上方。
- 原因：透明窗口为歌名和控制区保留的边距不等于人物边界，旧判定导致人物无法贴边、底部离任务栏过远；人工触发条破坏素材画面；每次正放/倒放都从端点重启会产生跳帧和首帧闪烁。
- 影响：拖拽期间只临时允许 96 DIP 受控越界，未达到阈值时人物夹回工作区；边缘模式仍不显示媒体控制、不接受身体点击，且不增加全局鼠标 Hook、系统服务或新权限。

## ADR-029：消息提醒读取结构化标题但永不读取正文

- 日期：2026-08-31
- 状态：已采用；细化并部分覆盖 ADR-021 的“完全不访问通知负载”限制。
- 决定：继续只使用用户授权的 Windows `UserNotificationListener`。除应用来源、时间和应用 Logo 外，只在 Toast 绑定至少包含两个独立文本元素时读取第一个标题元素，用作联系人昵称或群名；绝不读取第二个及后续正文元素的值。标题和图标只在内存中传递，结构不明确时回退为仅显示来源。
- 原因：用户希望在不显示消息内容的前提下辨认是哪位联系人或哪个群发来消息。Windows 的结构化文本元素可以把标题与正文分槽，但单文本通知语义不明确，不能冒险显示；公开 `UserNotification.Notification.Visual` 没有联系人头像字段。
- 影响：提醒卡显示应用图标、来源和可用标题；领域模型预留可选联系人头像槽，但当前适配器保持为空并回退应用图标。禁止为补头像读取聊天缓存、私有 XML、窗口画面、远程 URL、进程内存或网络；日志仍只记录来源类别与处理结果，不记录标题、头像或正文。

## ADR-030：边缘动画采用含字展开与无字收回的非对称路线

- 日期：2026-09-01
- 状态：已采用；覆盖 ADR-028 中左右与底部统一只遍历第 3～7 帧的部分，越界门槛、素材原生触发面和底部控制岛布局继续有效。
- 决定：左右“偷看”展开使用第 3→19 帧以保留“偷看”文字和完整人物，收回只使用无文字第 7→3 帧；底部“闪亮登场”展开保持第 3→7 帧，收回改用无文字第 5→3 帧。左右隐藏墙面只保留靠人物侧约 10.5% 图宽的砖墙窄条，并按裁剪后的变换边界贴屏和命中。
- 原因：原第 3～7 帧展开只显示头部，删掉了用户希望看到的文字和完整人物；整块 26.5% 图宽砖墙又显得过宽。反向播放含字段会让文字跟随角色缩进墙内，也不符合用户要求。
- 影响：同一无字段内的快速反向仍从当前帧继续；从含字完整展开切换到隐藏时直接进入无文字收回起点，不反向展示文字。左右镜像、仅可见素材区域响应悬停以及普通贴边不隐藏的安全边界不变。

## ADR-031：边缘动画速度按方向独立设置

- 日期：2026-09-01
- 状态：已采用；细化 ADR-030 的边缘播放节奏与墙面宽度。
- 决定：以当前帧时长为基准，左右钻出使用 1.2 倍速，左右收回保持 1.0 倍速；底部下钻使用 0.8 倍速，底部钻出保持 1.0 倍速。左右隐藏砖墙由 10.5% 图宽缩至 7.5%，约 14 DIP。
- 原因：用户实测认为下钻仍偏快、左右钻出偏慢，且现有约 20 DIP 墙面仍略宽。不同方向共用素材但节奏诉求不同，修改素材全局帧时长会误伤相反方向和其它场景。
- 影响：范围播放器新增仅作用于本次调用的速率参数，素材清单、源 GIF 和其它动画速度不变；非法、零值或非有限速率失败关闭。窄墙的透明命中区与可见裁剪同步缩小，仍按裁剪后边界贴屏。

## ADR-032：普通拖拽取消降落并为边缘候选建立稳定回差

- 日期：2026-09-01
- 状态：已采用；覆盖早期“迷你拖拽持续循环膨胀且每次松手都播放降落”的规则。
- 决定：迷你“膨胀”只完整播放一次并停在末帧；普通松手经短脉冲遮罩恢复持续动画，不播放降落。仅当总向下移动至少 72 DIP、最近 180 ms 向下至少 22 DIP 且速度达到 650 DIP/s 时播放“降落”和弹簧回弹。边缘候选使用切换素材前缓存的人物本地边界，28 DIP 进入、14 DIP 退出。
- 原因：三套不同画风素材在每次拖拽中的硬切比速度差异更显突兀；降落作为快速下甩彩蛋更符合动作语义。完整全身与边缘素材尺寸不同，若用当前素材重新判定边缘，会在临界点反复进入和退出预演。
- 影响：音乐拖拽、完整全身拖拽和边缘收纳仍优先于降落；普通迷你拖拽更安静，长时间拖动不会重复播放膨胀。边缘候选退出需要明确向屏幕内退回，减少临界抖动但仍可撤销收纳。

## ADR-033：偷看素材在编译期统一裁掉外侧大墙体

- 日期：2026-09-01
- 状态：已采用；取代只在隐藏终帧用 `PetImage.Clip` 掩住大墙体的单层处理方式。
- 决定：保留原始官方 GIF，不按用户截图红框逐帧手工裁图；资产编译时把缩放后的全部 240×240 帧统一裁为 `[0, 0, 196, 240]`，运行时按 155×190 DIP 等比例显示。隐藏终帧再仅暴露裁后画面最靠墙约 9.2% 的区域，维持约 14 DIP 的可发现触发面。
- 原因：单纯裁隐藏终帧只能让收起后的墙变窄，人物展开时仍会带出右侧大半墙体；手工逐帧容易产生抖动且不可复现。固定矩形保留“偷看”文字、头发、脸、手及必要墙边，同时所有帧边界一致，左右镜像与命中计算稳定。
- 影响：运行时图集和显示宽度变窄，但纵向比例、帧序、播放速度、含字展开/无字收回路线均不变。资产清单保留源路径、源哈希和编译参数，可随时从官方素材重新生成；其它动画没有配置该参数，不受影响。

## ADR-034：再次微调左右钻出与底部下钻速度

- 日期：2026-09-01
- 状态：已采用；仅覆盖 ADR-031 中左右钻出和底部下钻的具体速率。
- 决定：左右“偷看”钻出由 1.2 倍速提高到 1.3 倍速；底部“闪亮登场”下钻由 0.8 倍速降低到 0.7 倍速。左右收回仍为 1.0 倍速，底部钻出仍为 1.0 倍速。
- 原因：用户在裁剪版实机体验后要求左右钻出再快 0.1、底部下钻再慢 0.1；方向独立参数可以精确调整，不影响相反方向和素材的其它使用场景。
- 影响：帧路线、裁剪、边缘门槛、反向规则和隐藏触发面均不变；只缩放本次范围播放的逐帧时长。

## ADR-035：先用同模型二维程序动作验证完整全身交互方向

- 日期：2026-09-01
- 状态：候选阶段；尚未写入桌宠运行时映射。
- 决定：从现有官方模型 32 帧完整全身待机渲染确定性派生轻轻鞠躬、开心跳跃、左右探身、害羞摇摆、惊喜弹起 5 个透明 WebP，先放入动画挑选器让用户决定保留项和触发场景。所有动作首尾回到同一源帧并保持 480×520 画布，不切换角色、衣服或表情包。
- 原因：完整全身互动大量跳转到不同周年表情包会产生画风、服装、人物尺寸的突变；当前仓库虽保留官方 PMX，但没有可复现的 MMD/Blender 骨骼渲染环境。二维程序动作能快速验证统一模型方向，同时避免生成非官方自设形象。
- 影响：这 5 项只能表达整个人物的平移、缩放和脚底轴旋转，不能实现真实挥手、抬腿或独立转头。用户确认需要局部动作后，再单独恢复 PMX 骨骼渲染流水线；候选阶段不改变现有身体点击映射和安装版。

## ADR-036：建立可复现的官方 PMX 骨骼动画流水线

- 日期：2026-09-01
- 状态：已采用；补充 ADR-035，二维候选继续保留但不再承担精细局部动作。
- 决定：固定使用便携式 Blender 4.5.13 LTS 与 MMD Tools 4.5.13 导入仓库已有的官方洛天依 V4 Q 版 PMX；动作以 Python 关键帧脚本记录，透明渲染后进入现有 WebP/图集流水线。第一项正式动作是完整全身模式拖拽时的“双脚乱甩”，松手不播放跨画风“降落”，直接恢复原模式。
- 原因：用户要求真正可见的肢体动作，同时希望保持官方模型、服装和比例一致；在用户机器已拥有 MMD 的情况下，仍以便携 Blender 流水线为项目权威环境，可避免人工 GUI 操作不可复现，也不要求测试者安装制作软件。
- 影响：仓库增加 PMX 检查、动作生成和打包脚本及一项派生动画；`.local-tools/` 中的 Blender、插件、`.blend`、PNG 中间帧均被忽略。播放音乐时拖拽继续由音乐状态优先，不会切到全身模型；后续挥手、伸懒腰等动作需逐项讨论后再制作。

## ADR-037：收敛身体候选并建立可提前收起的一分钟暂停反馈

- 日期：2026-09-01
- 状态：已采用；覆盖普通身体六项随机、切歌加载动画、暂停 5 秒、苏醒弹出、夜间问候与长睡眠素材以及原神启动三轮展示的旧规则。
- 决定：普通身体只使用实际画面为“十二周年·抱抱”的素材；旋转舞、九周年点赞、八周年点赞、我推、真好和加载中摇摆退出当前触发映射但保留候选。音乐暂停进入独立 `MusicPaused` 持续状态并显示“九周年·没睡醒呼吸”最多 60 秒，双击可提前恢复原显示模式而不切换模式；桌宠发起暂停后的 2 秒内可由首个可靠静音样本快速确认。夜间启动改用“晚安浮动”，15 分钟睡眠改用“眠了呼吸”，启动问候也适用于 Windows 开机自启动；系统恢复与输入苏醒不再播放“苏醒弹出”。黑化、重罪分别使用 0.5、0.7 倍速；原神启动“你不许玩”只播一轮并保持末帧，使总展示约 5 秒。
- 原因：大量随机跨画风反应和不明显的加载动画降低一致性；5 秒暂停反馈过短，而直接延长又需要给用户一个不误切显示模式的退出手势；通用 1 秒静音宽限必须覆盖切歌间隙，但用户主动暂停可以在限定窗口内更快确认。夜间与长睡眠素材语义需要按用户最新体验互换，原神三轮重复也比一次完整展示更打扰。
- 影响：Core 新增 `MusicPaused` 持续状态，空闲解析器不会覆盖这 60 秒；恢复播放会立即取消暂停计时并重新选择音乐动画。快速确认只作用于桌宠自己发送的暂停命令，不改变外部暂停和切歌检测。十二周年源文件仍保持上游名称，运行时通过实际可见文案把 `12_在吗.gif` 映射为“抱抱”，并在文档与挑选器中标明命名倒置。

## ADR-038：播放按钮在网易云未运行时执行安全冷启动

- 日期：2026-09-01
- 状态：已采用；重新分配 ADR-037 暂停使用的“加载中”素材，但不恢复切歌加载动画。
- 决定：播放/暂停按钮发现 `cloudmusic.exe` 不存在时，先经前台安全检查，再从 Windows App Paths 或明确的常见安装目录启动网易云。等待阶段静止显示“心律共鸣·加载中”原始单帧图，不叠加旧的左右摇摆，并保持文字提示；约 2 秒后若仍未有声，只发送一次用户配置的播放快捷键。Core Audio 确认有声后直接进入当前歌曲的音乐动画，整个流程 30 秒超时。
- 原因：播放器未运行时直接发送全局快捷键没有接收者，也没有明确反馈；冷启动通常比普通切歌慢，需要可持续但安静的等待画面。单帧原图已经表达“加载中”，额外摇摆反而偏离用户最新要求。
- 影响：只有用户明确点击播放才允许启动网易云；受保护游戏前台或前台查询失败时不启动、不发送输入。实现不使用网易云私有协议、不扫描磁盘、不读取进程私有数据；网易云已运行时仍完全复用原播放/暂停快捷键流程。切歌等待继续只显示文字，不使用该静态素材。

## ADR-039：时间问候延长为 30 秒并撤销完整全身专用拖拽动作

- 日期：2026-09-01
- 状态：已采用；覆盖 ADR-036 的“双脚乱甩”运行时映射和早期启动问候只播一轮的规则。
- 决定：四段问候在正式启动以及运行中跨过 06:00、12:00、13:30、20:00 时持续展示 30 秒，并循环对应程序浮动、弹跳或呼吸；同一时段只触发一次，遇到高优先级反应时保留边界请求稍后重试。完整全身模式拖拽保持原待机画面，只移动窗口，不再加载或播放“双脚乱甩”及其它专用拖拽候选。
- 原因：单轮静态图加短程序动作不足以形成可感知的时间问候；仅启动触发也遗漏桌宠长期运行跨时段的场景。现有 PMX 拖拽动作实机观感不符合预期，保持同一模型画面比强行切动作更稳定。
- 影响：时间边界解析仍由 Core 统一管理，WPF 增加每秒轻量观察和 30 秒限时展示；启动/开机自启语义一致。专用拖拽图集从运行时清单与安装内容移除，但制作流水线和原始候选可保留用于未来新动作。动画挑选器移除用户明确弃用的 7 项动作，并新增 3 个普通歌曲听歌候选，暂不改变正式音乐状态机。
## 2026-09-01：文件吞食采用“独立包子目标 + AI 动画分段”，不直接播放整段视频

- 决定：生成视频只作为可追溯源素材，在构建阶段拆为循环奔跑与单次进食透明图集；包子是独立可拖动窗口，人物位置由程序移动。
- 原因：整段视频自带固定运动与背景，无法追踪用户拖动后的目标，也难与不同屏幕位置、多个文件和返回原位组合；分段后动作风格保持一致，同时保留实时交互能力。
- 安全边界：桌面观察只触发视觉目标，真正文件操作仍只有用户明确拖入桌宠后调用 Windows Shell 回收站；不使用永久删除、Explorer 扩展、进程注入或全局输入 Hook。

## ADR-040：用用户提供的新立绘替换完整全身待机但保持交互契约

- 日期：2026-09-02
- 状态：已采用；只替换完整全身模式的基础画面。
- 决定：保留现有 `official-v4-chibi-full-body-idle` 运行时 ID、480×520 画布、220×238 DIP 显示槽和底部锚点，将画面替换为用户放在项目根目录的新全身 PNG。源图原样归档；派生流程只将与四周连通的近白背景转换为透明 Alpha并等比例缩放，不使用生成式结果或重绘前景。身体命中矩形按新人物的眼睛、嘴、脸、手、胸部、下体、脚和头顶位置重新校准。
- 原因：用户希望改变完整全身模式的长期外观，同时明确要求其它功能全部保留。稳定动画 ID 和画布契约可以避免为双击、拖拽、音乐覆盖、边缘收纳及反应恢复分别增加特殊分支；只更新命中图即可保持各部位原来的触发语义。
- 影响：完整全身基础画面从 32 帧模型动画变成单帧透明立绘，原模型素材继续保留在候选目录但不再作为正式待机。白底移除与适配参数、源文件和输出哈希记录在伴随元数据中，可重复生成和审计。
## ADR-041：默认紧凑待机改为用户 Q 版小人，重排长短待机

- 背景：用户在 `候选素材_官方/Q版小人全身.png` 放入新的 Q 版立绘，希望它代替“嘿嘿”成为完整全身模式双击切换的另一侧；“嘿嘿”转为 3 分钟无操作反馈，取消“躺平”，并将“眠了呼吸”延后到 30 分钟。
- 决定：新增稳定运行时 ID `user-chibi-compact-idle` 作为 `PetDisplayMode.Compact` 的默认持续动画；完整全身互动 ID 和身体命中图保持不变。`mediumIdle` 从 3 分钟开始并解析为 `resonance-hehe`，`sleeping` 从 30 分钟开始并继续解析为来源实际为“代号洛天依·眠了”的 `tenth-anniversary-goodnight-float`。移除“躺平”的运行时配置、清单和派生图集，官方源 GIF 留在候选目录。
- 素材处理：新源图是没有 Alpha 的 RGB PNG，透明棋盘格已烘焙为约 245/254 两档近白像素；复用 `prepare_full_body_idle.py` 的边缘连通背景算法，并为该源图显式使用最低通道值 240。只有与画布四周连通的近白区域会透明，封闭在线稿内的头发、衣服和鞋不会被当成背景；输出统一到 480×520 画布、底部锚定并以 180×195 DIP 显示。
- 理由：将长期显示的两种模式都收敛到用户选择的同类全身立绘，减少默认表情包与完整全身间的画风跳变；把“嘿嘿”保留为低频待机仍能利用既有动画，而 30 分钟睡眠更符合真正长时间离开的语义。

## ADR-042：再次替换紧凑立绘并按身体区域切换本地光标

- 日期：2026-09-05
- 状态：已采用；覆盖 ADR-041 中紧凑模式的具体源图，不改变状态机与待机门槛。
- 决定：保持 `user-chibi-compact-idle`、480×520 画布和 180×195 DIP 显示契约，把画面换为用户新提供的全身 Q 版图；上一版源图继续保留。鼠标只在人物不透明像素上显示用户提供的普通指向手型；完整全身互动模式命中 `HeadAndHair` 时显示另一张摸头专属手型，其它身体区域仍显示普通指向手型。
- 实现：两张透明 PNG 由确定性脚本缩为 64×64 Windows CUR，普通指向热点落在伸出食指尖，摸头热点落在左侧手指接触处；应用只设置 `PetImage.Cursor`，不替换系统全局光标，也不影响媒体按钮。资源丢失或 Windows 拒绝加载时回退系统 Hand。
- 理由：稳定 ID 能让音乐覆盖、拖拽、边缘隐藏和双击切换继续复用现有逻辑；区域专属光标能在点击前提示“这里支持摸头”，比触发后才反馈更容易理解，同时避免桌宠透明区域和其它软件也被替换指针。

## ADR-043：撤销系统主音量并只考虑网易云应用会话音量

- 日期：2026-09-05
- 状态：已采用（系统主音量撤销）；应用会话入口由 ADR-045 落实。
- 决定：设置页删除系统主音量滑块、滚轮控制、外部主音量反馈与步长设置；正式运行不再创建 `ISystemVolumeService`，桌宠滚轮不写入系统默认输出设备。若后续恢复音量功能，只允许按 `cloudmusic.exe` 进程号枚举 Windows Core Audio 会话，并通过 `ISimpleAudioVolume` 调节网易云自身在音量合成器中的音量。
- 原因：系统主音量会同时影响网易云、游戏、语音和其它应用，不符合音乐陪伴助手只控制播放器的边界；Windows 已公开提供应用音频会话枚举、进程号识别和会话独立音量接口，不需要网易云私有 API。
- 影响：旧版 `volume` 配置和系统音量平台类型暂时只作为兼容资料保留，没有生产调用者。网易云未播放时可能还没有会话；多进程、多会话、会话重建或输出设备变化都必须重新枚举，失败时显示不可用且绝不回退系统主音量。最终入口、滚轮语义与反馈方式见 ADR-045。
## 2026-09-05：角色与文件吞食动画改为可持久化外观选项

- 决定：完整全身互动模式不再只有一张固定立绘。设置页提供“模式一（心率共鸣完整Q版）”“模式二（心率共鸣简约Q版）”“模式三（心率共鸣表情包版）”三项真实帧预览，保存稳定样式 ID；状态机仍只维护紧凑/完整全身两种显示模式，完整全身动画 ID 作为 `PetVisualState` 的可配置字段，避免把每套皮肤扩成新的状态枚举。
- 决定：吃包子 V1 与 V2 同时保留，设置只选择一组奔跑/进食动画 ID。文件观察、包子目标、Shell 回收站和安全取消逻辑不复制，防止画风选项改变安全边界。
- 决定：显示大小使用托盘内嵌滑杆，范围 50%～200%。缩放乘到清单显示尺寸，不重采样或覆盖图集；身体命中继续使用归一化坐标，控制岛保持固定尺寸以保证可点击性。
- 理由：用户可以在不丢失素材的前提下即时比较并记住选择；把视觉选择与业务状态、文件操作分离，后续增加更多皮肤或吃包子版本时只需扩展选项和资产映射。

## ADR-044：桌宠使用系统工具窗口语义并不响应“显示桌面”最小化

- 日期：2026-09-05
- 状态：已采用。
- 决定：正式主窗口和包子目标窗口显式增加 `WS_EX_TOOLWINDOW`、移除 `WS_EX_APPWINDOW`，并继续保持 `ShowInTaskbar=false`；主窗口拦截 `WM_SYSCOMMAND/SC_MINIMIZE`，另用 WPF `StateChanged` 处理非消息路径的意外最小化。QA 窗口显式要求任务栏可见时不套用该策略。
- 原因：单独设置 WPF `ShowInTaskbar=false` 只能隐藏任务栏按钮，不能稳定表达桌面伴侣的窗口身份，也不会保证 Win+D 后留在桌面。工具窗口扩展样式是 Windows 对“不进入任务切换器”的原生语义；最小化拦截保证桌宠与普通应用窗口在“显示桌面”场景下行为不同。
- 影响：桌宠不再出现在 Alt+Tab、Win+Tab 和任务栏窗口区，唯一程序级入口是右下角通知区域图标；桌宠自身没有最小化语义，退出仍由托盘菜单和既有关闭流程完成。

## ADR-045：在设置页启用 Win11 风格网易云应用会话音量

- 日期：2026-09-05
- 状态：已采用；落实 ADR-043 的应用会话边界，继续保留 ADR-027 关于控制岛不显示音量控件的决定。
- 决定：设置页增加 Win11 风格横向音量滑杆，只枚举所有活动 Render 输出设备上属于 `cloudmusic.exe` PID 的公开 Core Audio 会话，并通过 `ISimpleAudioVolume` 同步读写全部匹配会话。设置值不另行持久化，Windows 音频会话本身是当前值来源。
- 原因：Windows 11 允许单个应用选择不同输出设备，只看默认端点可能漏掉已被路由的网易云会话；每次重新枚举也能处理切歌、重启播放器和设备变化造成的会话重建。入口位于低频设置页，不占用桌宠下方的三键音乐控制岛。
- 影响：网易云没有创建会话时滑杆禁用，并提示先播放歌曲后重新检测；多个会话显示数量并一起调整。受保护游戏前台、前台查询失败、音频服务失效或系统拒绝时失败关闭，绝不读取或修改系统主音量、其它应用音量及音频内容。

## ADR-046：按完整全身外观隔离触摸策略，并开放音乐循环选择

- 日期：2026-09-05
- 状态：已采用；覆盖 ADR-025 的按歌手自动分流规则，以及“所有完整全身外观共用表情包互动”的旧假设。
- 决定：模式一（心率共鸣完整Q版）作为纯展示外观，不执行任何身体触摸动画；模式二（心率共鸣简约Q版）预留同模型连贯动作，素材到位前不播放旧表情包；模式三（心率共鸣表情包版）继续使用现有身体区域到表情包的映射。设置页另提供“每次随机”“固定享受音乐”“固定音乐摇摆”三种音乐循环选择。
- 原因：跨画风切换到独立表情包会破坏模式二（心率共鸣简约Q版）的动作连续性，而模式一（心率共鸣完整Q版）本身不准备制作触摸素材；模式三（心率共鸣表情包版）与现有表情包造型接近，保留原映射更自然。音乐动画属于用户偏好，不应强制由歌手字段决定。
- 影响：设置架构升级到版本 11，旧设置迁移后默认使用随机。循环候选集中登记，未来增加音乐动作时同步登记即可自动进入设置下拉框；模式二（心率共鸣简约Q版）后续每加入一个 AI 动作，需要从待机起止帧、画布、人物比例与透明边缘保持一致后再启用对应身体区域。

## ADR-047：将“嘿嘿”限定为模式三（心率共鸣表情包版）的 3 分钟中等待机

- 日期：2026-09-05
- 状态：已采用；收窄此前所有外观共用 3 分钟中等待机的规则。
- 决定：`resonance-hehe` 只在完整全身模式选中 `full-body-classic-cat-ears` 且系统连续 3 分钟无输入时触发；模式一（心率共鸣完整Q版）、模式二（心率共鸣简约Q版）及紧凑模式均保持自身待机。30 分钟“眠了呼吸”继续适用于全部外观。
- 状态收敛：双击离开模式三（心率共鸣表情包版）或在设置中改选其他外观时，如果当前正在 `mediumIdle`，立即回到 `Idle`，不等待下一次一秒轮询。
- 理由：“嘿嘿”只属于由同套表情包衍生的经典猫耳人物，限制作用域能避免其他人物外观发生突兀画风跳转。

## ADR-048：重新标定经典猫耳命中图，并把网易云音量放回悬浮控制岛

- 日期：2026-09-05
- 状态：已采用；控制岛音量入口按用户最新决定重新启用，覆盖 ADR-027 与 ADR-045 中“只在设置页提供”的限制，但仍遵守不控制系统主音量的边界。
- 决定：模式三（心率共鸣表情包版）的 12 个归一化区域按当前 480×520 透明立绘重新标定。悬浮控制岛扩展为四键，最右侧使用 `#66CCFF` 扬声器按钮，点击后向上展开竖向滑杆。
- 音量边界：滑杆复用 `IApplicationVolumeService`，只读写所有匹配 `cloudmusic.exe` 的公开 Core Audio 会话；无会话时禁用并提示，绝不回退到系统主音量或其它进程。
- 理由：旧区域来自更早的人物比例，导致五官和四肢整体偏上；常用网易云独立音量放在悬浮控制岛比进入设置页更符合音乐陪伴定位。

## ADR-049：双击循环三套外观并让吃包子画风自动联动

- 日期：2026-09-05
- 状态：已采用；覆盖此前在设置页独立选择完整全身外观和吃包子版本的入口；其中“开关同时禁用快捷回收站”已被 ADR-085 覆盖。
- 决定：正式桌宠始终使用完整全身显示槽；双击按“模式一（心率共鸣完整Q版） → 模式二（心率共鸣简约Q版） → 模式三（心率共鸣表情包版）”循环，切换后立即保存当前外观。设置只保留循环开关，关闭时双击不改变外观，重启继续使用当前项。
- 决定：模式一（心率共鸣完整Q版）、模式二（心率共鸣简约Q版）固定使用“心率简约追包版”，模式三（心率共鸣表情包版）固定使用“心率表情追包版”。吃包子总开关同时禁用桌面删除后的追逐与文件拖到桌宠回收，不再提供独立画风选择。
- 影响：设置架构升级到版本 12；旧设置保留当时选中的人物外观，并按新映射重算吃包子画风。两套动画资产均继续保留，不删除候选或运行时文件。

## ADR-050：边缘收纳以实际人物三分之一越界为意图门槛

- 日期：2026-09-05
- 状态：已采用；覆盖 ADR-028 和 ADR-032 的固定 28/14 DIP 进入/退出门槛。
- 决定：从当前动画清单的 `alphaBounds` 投影出缩放后的不透明人物边界；左右按人物实际宽度、底部按实际高度计算越界比例，达到三分之一才预演收纳，候选建立后退回到四分之一以内才取消。未达门槛松手时把 Alpha 边界而非透明画布贴回工作区。
- 原因：三套人物原图透明边距不同，固定 DIP 在改变显示大小后会表现为不同的触发比例，也会让画布已贴边但人物肉眼仍留空。比例判定能让 50%～200% 缩放与三套外观保持一致手感。
- 影响：拖拽允许的越界余量随窗口缩放增加，确保大尺寸人物也能真正移出三分之一；不改变钻入素材、展开/收回帧路线、隐藏触发面或任务栏上方控制岛规则。

## ADR-051：仅模式三（心率共鸣表情包版）拖拽使用“膨胀”

- 日期：2026-09-05
- 状态：已采用；覆盖按旧紧凑/完整全身二分显示模式分配拖拽动画的规则。
- 决定：双击三套全身外观中，仅第三种“模式三（心率共鸣表情包版）”开始拖拽时完整播放一次 `resonance-expand` 并停在末帧；模式一（心率共鸣完整Q版）与模式二（心率共鸣简约Q版）保持当前立绘。进入贴边预览后若又退回屏幕内，只恢复膨胀末帧，不重新播放。
- 优先级：音乐播放期间拖拽继续保持本次音乐动画，左右/底部达到收纳门槛后仍由对应边缘动画覆盖；松手统一恢复当前持续状态且不播放降落。
- 原因：膨胀素材与模式三（心率共鸣表情包版）同属表情包式 Q 版画风，放到另外两套完整立绘上会形成明显跳变；把能力绑定到外观 ID 也能避免未来新增外观时误继承。

## ADR-052：收纳门槛降至四分之一并用目标外观定位

- 日期：2026-09-05
- 状态：已采用；调整 ADR-050 的三分之一/四分之一门槛。
- 决定：人物越界四分之一时进入左右或底部收纳预演，退回到六分之一以内时取消。模式三（心率共鸣表情包版）拖拽显示“膨胀”期间，位置判定与普通松手回正始终使用经典猫耳本体的 Alpha 边界，不使用膨胀素材尺寸。
- 定位：动画尺寸切换时，窗口允许素材透明左、右、下边缘越过工作区，只限制真实 Alpha 可见内容；因此 50%～200% 显示倍率下都可让脚底或人物侧边贴屏。
- 原因：三分之一需要把较大人物拖出过多才出现反馈；模式三（心率共鸣表情包版）的膨胀画布又与待机图透明边距不同，若用当前动画定位，恢复待机后会出现间歇性留缝。

## ADR-053：模式三（心率共鸣表情包版）与膨胀使用相同显示画布

- 日期：2026-09-05
- 状态：已采用。
- 决定：`user-chibi-classic-full-body-idle` 与 `resonance-expand` 的运行显示尺寸统一为 220×238 DIP。膨胀的 162×162 原始帧不直接拉伸，而是按底部锚点补成 162×175 的透明画布，再进入现有图集。
- 位置稳定：播放膨胀时，窗口尺寸与透明越界约束继续使用模式三（心率共鸣表情包版）布局；松手恢复不发生窗口缩放或贴边位置跳动。
- 原因：拖拽是同一人物状态的即时反馈，窗口随素材从 220×238 缩到 180×180 会产生明显跳变；透明补边可以兼顾尺寸稳定与原素材比例。

## ADR-054：模式三边缘预演锁定外窗与当前方向

- 日期：2026-09-05
- 状态：已采用。
- 背景：模式三（心率共鸣表情包版）拖拽到左右临界位置时，膨胀与偷看素材尺寸不同；预演切图触发外窗缩放后，下一次鼠标事件会基于变化后的窗口几何重新越过阈值。靠近屏幕角落时，左右和下方还可能同时满足门槛并交替抢占，表现为动画抽搐。
- 决定：模式三拖拽期间切换膨胀、偷看或底部预演只改变图像显示尺寸，不改变已建立的外窗边界；边缘候选一旦建立，只要该方向仍有至少六分之一人物在屏外，就保持原方向，不切换为另一个同时达标的方向。
- 影响：松手确认收纳后仍按目标边缘动画的正式尺寸调整窗口；普通拖拽、其它外观、收纳展开/缩回路线和四分之一进入门槛均不改变。

## ADR-055：身体区域由离线描边工具输出归一化多边形

- 日期：2026-09-05
- 状态：工具已完成，运行时接入等待用户输出。
- 背景：当前 BodyHitMap 使用矩形，手、头发和衣摆等不规则部位难以贴合；由开发者反复猜测坐标无法达到用户希望的精度。
- 决定：提供单文件离线 HTML 编辑器，预载三套人物和现有区域，允许每个部位绘制多块多边形、拖动顶点、即时查看命中结果，并导出带模型 ID、优先级和归一化坐标的版本化 JSON 及标注 PNG。
- 安全边界：工具只在浏览器内存和本机 localStorage 保存草稿，不自动改写代码或设置；用户确认并提供导出文件后才接入正式运行时。

## ADR-056：吃包子运行素材提升为 384 像素与光流 2 倍补帧

- 日期：2026-09-05
- 状态：已采用。
- 背景：两套源序列都是 720×720、标称 24 FPS，但运行图集曾先缩到 256×256；用户把桌宠放大后会再次上采样而发糊，24 FPS 的奔跑姿态变化也有明显跳帧感。直接把相邻帧透明叠加会形成双重轮廓，不能作为正式补帧。
- 决定：运行帧提高到 384×384；在中性底上计算相邻帧的双向光流，把前后帧分别扭到时间中点，再以预乘 Alpha 合成。每个 42 ms 源间隔拆成两个 21 ms 间隔，循环末尾也补首尾中间帧，单次动画末帧补等时定格，所以总时长完全不变。
- 权衡：384 像素在默认 205 DIP 与最大 200% 显示之间兼顾清晰度和解码内存；不采用 512 像素加 48 FPS，以免单个 132 帧进食图集的解码内存过高。OpenCV 只属于离线资产生成依赖，不进入桌宠安装包。

## ADR-057：模式二（心率共鸣简约Q版）内部假棋盘按限定区域转为 Alpha

- 日期：2026-09-05
- 状态：已采用。
- 背景：`Q版小人全身_新默认.png` 是整张 Alpha=255 的 RGB 合成图，灰白棋盘已经烘焙。原有边缘洪泛能清除外部背景，却无法进入被两侧发丝和薄纱线条围住的棋盘区域。
- 决定：保留候选原图不变，在确定性派生工具中登记两块源像素内部背景区域；区域内只有近白、低色差的棋盘像素转为透明，有色发丝、装饰线和深色轮廓保留。运行时继续引用派生透明图，不增加运行时图像处理。
- 理由：直接擦除矩形会损伤人物线稿，生成式重绘又可能改变角色细节；“限定区域 + 原背景色判定”可复现、可审计，也能精确回应当前两处残留。

## ADR-058：取消音乐暂停专用呼吸状态

- 日期：2026-09-06
- 状态：已采用；覆盖 ADR-037 中“音乐暂停显示没睡醒呼吸一分钟”的决定。
- 决定：音乐暂停确认后直接恢复用户当前选择的待机外观，不再播放“九周年·没睡醒呼吸”，删除独立 `MusicPaused` 持续状态、一分钟计时器和暂停画面双击拦截。原始官方图片只留在候选素材库，不进入运行时图集和安装清单。“代号洛天依·眠了呼吸”继续用于 30 分钟无操作长待机。
- 原因：用户明确不再需要该功能；直接恢复当前外观也让暂停行为与正常双击外观切换保持一致。
- 影响：桌宠发起暂停后的 2 秒快速确认、普通外部变化的 1 秒静音宽限以及恢复播放时的音乐动画选择规则均保持不变。

## ADR-059：第三模型命中改用用户导出的多边形

- 日期：2026-09-06
- 状态：已采用。
- 决定：第三模型“模式三（心率共鸣表情包版）”不再使用粗略矩形作为正常命中图，正式加载用户从离线描边编辑器导出的 12 区域归一化多边形 JSON，并按文件中的唯一 priority 执行首个命中。当前帧 Alpha 检查、身体反应映射与恢复规则保持不变；`OtherBody` 仍位于最后优先级并触发“抱抱”。
- 实现边界：Core 负责多边形几何与严格 JSON 校验，WPF 负责从安装资产加载并在缺失或无效时回退内置矩形；调试覆盖层和摸头移动也使用同一几何，不允许视觉描边与真实判定分叉。
- 原因：矩形会把头发、脸和手周围的空角一并算入，无法体现用户逐点圈出的边界；保留外部 JSON 作为可追溯来源，也便于用户后续用同一工具继续微调。

## ADR-060：第二模型使用独立多边形与同模型连续动作

- 日期：2026-09-06
- 状态：已采用。
- 决定：第二模型“模式二（心率共鸣简约Q版）”载入用户导出的 `full-body-crystal.json`，并把 7 组 145 帧透明动作按说明映射为眼睛遮眼、嘴巴捂嘴、脸部捏脸、双手比心、双脚摸腿脚、头部摸摸头和 OtherBody 捂肚子。胸部、下体及未命中区域没有对应素材时不播放任何反应。
- 隔离：第一模型“模式一（心率共鸣完整Q版）”继续关闭身体互动；第三模型“模式三（心率共鸣表情包版）”继续使用自己的多边形和表情包解析器。第二模型不得回退第三模型的啾啾、重罪、黑化等素材。
- 资产：720×720 源序列作为本地候选母版保留；可复现脚本用预乘 Alpha 缩放生成 256×256 运行图集、WebP 预览和逐序列哈希，运行时只读取预生成资产。

## ADR-061：吃包子动画升级为 60 帧循环与缓启动位移

- 日期：2026-09-06
- 状态：已采用；覆盖 ADR-056 的 2 倍、约 48 FPS 运行参数。
- 决定：两套 384×384 透明素材使用通用双向光流时间重采样；奔跑循环固定至少 60 帧，进食段为 173 帧，均以 16 ms/帧播放（约 62.5 FPS）。奔跑循环同时拉长到约 960 ms，降低腿部动作频繁重复造成的急促感。
- 位移：窗口追包从 72 DIP/s 起步，按时间平滑加速到 270 DIP/s；返程也从低速起步，上限 290 DIP/s。素材帧率与桌面位移继续解耦。
- 嘴部校准：心率简约追包版依据张嘴保持帧标定为横向 62.5%、纵向 45.2%；向左奔跑时横向镜像为 37.5%，确保包子终点随人物朝向一致。

## ADR-062：身体部位动作播放期间锁定双击换装

- 日期：2026-09-06
- 状态：已采用。
- 决定：只有由身体区域点击或摸头手势启动的反应携带 `BlocksDisplayModeToggle` 标记；该反应仍在播放时，双击形态切换直接忽略，不中止动作，也不排队等待一次延迟切换。动作完整结束后立即解除换装锁。
- 生命周期：锁属于状态机当前活动反应，不使用独立布尔量；反应完成、过期、被更高优先级反应替换或因拖拽取消时自动消失，避免异常路径残留永久锁。动作后的 800 毫秒恢复期只继续抑制新的部位单击和摸头，不延长双击锁。
- 范围：音乐、时间问候、消息、原神、文件失败等非身体部位动画不采用此锁，保持原有双击规则。

## ADR-063：左右隐藏后的砖墙触发面再减半

- 日期：2026-09-06
- 状态：已采用。
- 决定：左右完全钻入屏幕后，可见宽度由裁后画面的 9.2% 减为 4.6%，并把起点由 90% 移到 92.3%，相当于在旧墙条两侧各收掉四分之一；默认显示约从 14 DIP 降为 7 DIP，同时避开墙边人物发梢。完整钻入、钻出帧路线、“偷看”文字和底部猫耳触发面均不改变。
- 定位：窗口与透明悬停按钮继续依据 `PetImage.Clip` 的真实变换边界计算，缩窄后重新贴齐对应工作区边缘；左侧仍通过同一裁剪的水平镜像获得，避免左右参数分叉。

## ADR-064：上下边缘使用统一的附件纵向顺序

- 日期：2026-09-06
- 状态：已采用；覆盖 ADR-028 中“底边控制岛在歌名上方”的旧顺序。
- 决定：歌名/作者与播放控制统称为人物附件，并采用三态布局。人物进入工作区顶部 80 DIP 时，两座附件一起移到人物下方；进入底部 80 DIP 时，两座附件一起移到人物上方；其余位置保持歌名在人物上、控制岛在人物下。两个堆叠状态都固定为歌名/作者在上、播放控制在下。
- 定位：布局切换补偿窗口坐标以保持人物位置连续；顶部拖拽下限依据当前动画的 Alpha 可见上边界计算，允许附件窗口越出工作区，使人物可见像素真正贴顶。底部仍以人物可见底边贴齐任务栏上沿。
- 理由：人物在顶部时，原本预留在头顶的曲目信息阻止角色贴边；底部旧顺序又与用户认知相反。统一顺序减少上下切换时的视觉歧义，并避免附件遮挡人物或被任务栏覆盖。

## ADR-065：第二模型身体动作取消通用换图过渡

- 日期：2026-09-06
- 状态：已采用；细化 ADR-060 的“同模型连续动作”表现。
- 决定：模式二（心率共鸣简约Q版）七项部位动作直接在当前人物位置播放，不经过通用 `VisualSwapTransition` 的缩小、闪光和弹出，也不在动作结束后再次播放遮罩过渡。动作清单显示尺寸由 238×238 调整为 244×244 DIP，运行时按显示倍率向下补偿 7 DIP，使人物中心、头顶和脚底与 220×238 DIP 待机本体尽量重合。
- 边界：只有 `CrystalBodyInteractionResolver` 明确登记的七项动作使用原地路径；经典猫耳表情包、音乐、系统反馈和未来未登记素材仍保留原有过渡，避免一次行为修改外溢到其它状态。
- 理由：动作素材首尾已经是晶蓝礼服的中立站姿，再套一层缩小闪光会让用户感知为切换到另一个表情包；直接衔接并校正锚点更接近“本体被触摸后做动作”。

## ADR-066：第二模型运行图集加入精确待机首尾帧

- 日期：2026-09-06
- 状态：已采用；补充 ADR-065，修正其中“动作素材首尾已经是中立站姿”的判断。
- 事实：逐帧检查用户提供的七套 720×720 源序列后确认，它们从第 0 帧就已经处于动作姿势；例如“摸摸头”第一帧已有完整外部手掌。仅取消 WPF 闪光并对齐脚底，仍会在待机切入第 0 帧时产生一瞬间轮廓抽搐。
- 决定：离线准备阶段把真实晶蓝礼服待机图映射到 256×256 动作显示空间，以它替换每套运行图集的第 0 与第 144 帧；第 1～6 帧从待机逐步混入对应原动作，第 139～143 帧逐步混回待机。混合在预乘 Alpha 色彩空间执行，保留原 145 帧、42 ms 帧时长和完整中段动作。
- 影响：运行时第一次换图和最终恢复时看到的像素都与待机一致，人物不会瞬间换姿势；约 0.25 秒的进入/退出段承担姿势衔接。源 PNG 永不覆盖，运行图集、WebP 预览、哈希和派生元数据同步重建。

## ADR-067：第二模型动作与待机共用固定显示槽

- 日期：2026-09-06
- 状态：已采用；覆盖 ADR-065 的 244×244 DIP 动作槽与 7 DIP 运行时补偿。
- 事实：即使图集第 0 帧与待机内容一致，WPF 从 220×238 待机切到 244×244 动作时仍会把透明窗口加宽 24 DIP、增高 6 DIP并重新居中。在屏幕边缘、缩放或非整数 DPI 下，窗口取整与钳位会表现为开始瞬间左右移动。
- 决定：将 720×720 动作源等比装入 240×260 的待机纵横比画布，清单统一以 220×238 DIP 显示七项动作和待机；移除主窗口的动作专用纵向位移。首尾 6 帧预乘 Alpha 过渡继续保留。
- 验证边界：代表性“摸摸头”实窗在动作中和恢复后均为同一个 `1672,644,1908,1008` 原生窗口矩形；窗口宽高、左上角、底部中心与人物脚底不再变化。动作伸出画布的极端发丝或手部允许由固定透明窗口自然裁切，不得为了保全透明边缘再次移动本体。

## ADR-068：音量入口收敛到悬浮岛并统一附件离开宽限

- 日期：2026-09-06
- 状态：已采用；细化 ADR-048 的悬浮音量入口并覆盖 ADR-045 的设置页音量卡片。
- 决定：设置页不再重复显示网易云独立音量，只保留桌宠控制岛最右侧的竖向音量入口；用户在竖轨任意位置按下即可跳到该数值并继续拖动，直到松开鼠标，不要求先命中圆形滑块。
- 附件时序：歌名/作者岛和媒体控制岛在鼠标离开桌宠后都重新计时并保留 5 秒，再执行原有淡出；重新移入立即取消倒计时。音量弹层打开时继续阻止媒体控制岛隐藏。
- 设置视觉：设置窗口改为天依蓝头部、圆角分组卡、MDL2 本地图标和右置开关；不引入第三方主题框架，现有设置模型、保存语义与通知授权边界不变。

## ADR-069：偷看整段动画直接使用半宽墙体

- 日期：2026-09-06
- 状态：已采用；继续细化 ADR-033 与 ADR-063。
- 决定：官方 `04_偷看.gif` 仍作为只读来源；编译期把 240×240 帧统一裁到 186×240，使人物右侧墙体从约 20 像素直接减为约 10 像素。运行显示同步为 147×190 DIP，人物、手和“偷看”文字不缩放、不横向拉伸。
- 隐藏终态：显示层裁剪改为从 95.2% 起取 4.8%，继续暴露约 7 DIP 的原墙面细条，因此用户已认可的完全收起效果基本不变；左侧仍复用同一图集水平镜像。
- 理由：仅缩窄隐藏终态不能改变钻入/钻出过程中看到的墙体宽度；把统一裁剪直接应用到所有派生帧，才能使整段动画与收起终态保持一致，同时仍可从官方源文件确定性重建。

## ADR-070：竖向音量使用独立整轨输入面

- 日期：2026-09-06
- 状态：已采用；修正 ADR-068 的 WPF 实现方式，用户行为不变。
- 事实：把预览鼠标事件直接挂到 WPF `Slider` 仍会与模板内部的 `Thumb`、`RepeatButton` 类处理器竞争；圆点可以捕获拖动，但轨道其它位置只执行一次跳转，后续移动事件不会稳定到达外层处理器。
- 决定：原 `Slider` 改为不参与鼠标命中，只负责显示数值和触发音量写入；在相同 30×116 DIP 区域覆盖透明输入面，由它从按下到松开独占捕获鼠标并按坐标更新滑块。弹层关闭和检测到左键已松开时强制释放捕获。
- 结果：圆点、蓝色已填充轨道和灰色未填充轨道具有完全相同的按住拖动行为，同时保留原视觉、数值映射和网易云独立音量安全边界。

## ADR-071：媒体控制统一使用用户普通指向手型

- 日期：2026-09-07
- 状态：已采用；覆盖 ADR-042 中“控制岛不替换光标”的范围限制。
- 决定：媒体控制悬浮岛的上一首、播放/暂停、下一首、网易云独立音量按钮，以及竖向音量整轨输入面，全部复用人物普通互动所使用的 `pet-pointer.cur`。移除音量整轨的系统 `SizeNS` 上下箭头。
- 边界：歌名/作者岛仍是纯展示区域，不强制改变系统指针；人物头发/头顶摸头命中仍优先使用独立 `pet-headpat.cur`，不会被控制岛规则覆盖。普通 CUR 加载失败时统一回退系统 Hand。
- 理由：这些悬浮岛元素都是桌宠自身的可点击交互，统一成用户设计的天依手型能保持视觉语言一致；音量方向已经由竖轨外观表达，不需要额外使用上下箭头。

## ADR-072：设置页改为顶部 Tab 并统一人物应用图标

- 日期：2026-09-07
- 状态：已采用；覆盖 ADR-068 中设置页的单列卡片视觉结构，不改变其中独立音量入口决定。
- 决定：设置窗口采用常规、音乐、通知、关于四个顶部 Tab；现有设置项、读取值、保存时机和 Windows 通知授权行为不变。默认常规页底部加入“今天也，一起加油吧~”和“心律共鸣·加入我们”第 0 帧人物，不显示“Join Us!”或角色署名。
- 图标：从清理后晶蓝礼服全身帧裁出包含双耳、完整五官与少量肩部的近景，配浅天依蓝圆角底和白色分离边，生成 16、20、24、32、40、48、64、128、256 像素 ICO；EXE、设置页和托盘共同使用，替换旧的通用程序图标。
- 素材边界：不生成或重绘人物；原 GIF 和全身立绘保持只读，确定性脚本及元数据保存帧号、裁剪框、源/输出哈希，后续可以从原素材重建。
- 理由：顶部分类在当前 660 DIP 窗口内能把功能分组讲清，同时避免侧边栏挤压说明文字；近景人物比完整全身在 16～32 像素任务栏/托盘环境中更容易辨认。

## ADR-073：以歌手语义分流三套音乐动画，并在“嘿嘿”前加入精确倒计时

- 日期：2026-09-08
- 状态：已采用；覆盖 ADR-025 与 ADR-046 的旧音乐随机/固定池，并补充 ADR-047 的模式三（心率共鸣表情包版）中等待机时间轴。
- 音乐决定：普通或未知歌手固定使用“心律共鸣·享受音乐”；歌手字段包含独立的“洛天依”或 `Luo Tianyi`（包括合唱）时，自动在“九周年·音乐摇摆”和裁掉按键入场后的“元旦祝福·一键唱歌”中随机。设置可选自动、固定三项或完全关闭听歌动画；唱歌彩蛋另有开关，关闭后自动与旧固定唱歌选择都回退音乐摇摆。固定洛天依专属彩蛋时，非洛天依歌曲仍回退享受音乐。
- 待机决定：模式三（心率共鸣表情包版）从连续无输入 2 分钟起进入 `mediumIdleCountdown`，播放从 00:59 到 00:00 的精确 60000 ms“摸鱼一分钟”；3 分钟边界进入 `mediumIdle`，正好切换“嘿嘿”。其它外观不触发两者，任何输入或离开模式三（心率共鸣表情包版）都会撤销。
- 素材处理：“摸鱼”删除源第 0 帧额外 5 秒的 01:00 停留，并把 00:00 末帧改为 1 秒；“一键唱歌”删除第 0～6 帧手指入场，只循环第 7～15 帧稳定歌唱段。派生脚本、源哈希、帧表和输出哈希随资产保存，官方原件不覆盖。
- 理由：动画含义应该与实际歌曲人物一致；同时把素材自带倒计时直接对齐状态边界，比播放任意长度后再跳转更自然。关闭视觉动画不等于关闭音乐检测或控制，因此 `MusicPlaying` 业务状态继续保留。

## ADR-074：设置页改为左侧陪伴控制台，时间问候缩为 15 秒

- 日期：2026-09-08
- 状态：已采用；设置布局覆盖 ADR-072 的顶部 Tab 结构，音乐语义继续沿用 ADR-073。
- 设置决定：按《洛天依桌宠设置界面改版方案》把窗口缩为约 570×620 DIP，使用 154 DIP 常驻左栏、四项 44 DIP 整行导航、右侧独立滚动页和 58 DIP 固定保存栏。角色图始终留在左下，常规、音乐、通知、关于分别显示稳定台词，并用 160 ms 淡入替换；减少动态效果时不播放过渡。
- 控件决定：开关、按钮、焦点轮廓和无箭头窄滚动条统一为天依蓝；音乐下拉框改为两列直接选择卡，完整保留自动按歌手分流、享受音乐、音乐摇摆、一键唱歌、完全关闭动画和唱歌彩蛋开关，不增加新的媒体权限或设置字段。
- 时序决定：四段启动/跨时段问候统一从 30 秒缩短为 15 秒，并由 Core 暴露唯一持续时间常量，示例规则同步为 15 秒。
- 追包决定：每段追包或返程都从 72 DIP/s 起步按累计时间线性加速，10 秒时分别达到原 270/290 DIP/s 巡航速度的三倍并封顶；动画帧率、到达半径、嘴部坐标和回收站安全流程不变。

## ADR-075：拖拽膨胀动画移除标题文字

- 日期：2026-09-08
- 状态：已采用；仅改变 `resonance-expand` 的画面内容，不改变 ADR-051 与 ADR-053 的外观范围、播放次数和尺寸。
- 决定：官方“心律共鸣·膨胀”GIF 保持只读；离线派生阶段只在顶部区域清除橙色“膨胀”标题像素并输出无损 APNG，同时保护嘴部区域，人物轮廓、嘴巴、腮红、头饰与黄色星星全部保留。运行图集改从该派生文件构建，仍为 10 帧、每帧 100 ms、162×175 透明画布和 220×238 DIP 显示。
- 可追溯性：脚本和元数据记录源/输出 SHA-256、颜色阈值、处理区域及逐帧清理像素数；后续重新编译无需手工修图，也不会覆盖官方原件。

## ADR-076：第二模型动作按待机图校准亮度

- 日期：2026-09-09
- 状态：已采用；补充 ADR-066 的首尾待机融合，不改变动作、帧数、时长和固定显示槽。
- 背景：七套用户动作 PNG 的可见人物平均亮度比正式晶蓝礼服待机图低约 8%～10%；只在首尾混入待机图会形成明显的“亮→暗→亮”过渡。
- 决定：每套动作先以归一化第 0 帧作为源参考，在 YCbCr 空间按 Alpha 大于 128 的可见像素亮度 CDF 对齐真实待机图；全部动作帧只替换 Y 通道，保留 Cb、Cr 和 Alpha，再执行既有 6 帧预乘 Alpha 首尾融合。
- 可追溯性：派生元数据为每套动作记录 256 项亮度 LUT 的 SHA-256；候选母版不覆盖，运行仍只读取预生成图集。
- 理由：按各套素材自身分布校准能消除整体偏暗，又不会用统一增益漂白高光、改变天依蓝、绿色眼睛或肤色色相。

## ADR-077：音乐动画选择改为横排预览图库

- 日期：2026-09-10
- 状态：已采用；只改变设置页呈现，不改变 ADR-073 的歌手分流、候选池和保存值。
- 决定：自动、享受音乐、音乐摇摆和一键唱歌使用四张等宽横排缩略图卡；完全关闭动画降为下方 40 DIP 窄选项。卡片名称常显，详细用途移到工具提示和辅助功能说明，选中勾、键盘焦点及天依蓝状态保留。
- 预览素材：从三套正式运行图集抽取固定代表帧，按可见 Alpha 边界缩放到 128×128 透明 PNG；自动模式把三项缩略图组合成一张预览。脚本与应用图标共用 UI 派生元数据，记录源图集哈希、帧号与输出哈希。
- 理由：动画之间最容易通过角色画面辨认；横排预览比五张 82 DIP 文字卡减少高度和重复说明，同时不使用动画仍有明确入口。

## ADR-078：通知功能只随带身份安装版分发

- 日期：2026-09-10
- 状态：已采用；补全 ADR-021、ADR-022 的跨电脑交付路径。
- 决定：便携 EXE 继续不启用 `UserNotificationListener`；Windows 11 测试分发采用“签名 MSIX + 公开 CER + 校验安装器”ZIP。安装器先校验 MSIX/CER 的 SHA-256、证书有效期和签名者指纹，只把证书信任阶段提升到 `LocalMachine\TrustedPeople`，随后回到当前登录用户安装包，桌宠本体保持普通权限。PFX 私钥和密码永不进入分发包。
- 正式分发：公开版必须使用 Microsoft Store 签名或 Windows 默认信任的生产代码签名证书。构建脚本支持从仓库外 PFX 读取 Publisher 并签名，发布目录只输出 MSIX 与哈希；Store 包名与 Publisher 只能使用 Partner Center 分配值。
- 理由：通知监听的包身份和能力声明是 Windows 的安全边界。自动扫描聊天进程、数据库或窗口既不能替代系统授权，也违反隐私约束；自签名证书可以覆盖受控测试电脑，但不能被描述为面向公众的零提示正式分发。

## ADR-079：设置左栏使用四张页面专属官方表情

- 日期：2026-09-10
- 状态：已采用；覆盖 ADR-072、ADR-074 中四页共用无文字“加入我们”人物的决定。
- 决定：常规、音乐、通知、关于页左栏人物分别使用“心律共鸣·我推”“心律共鸣·真好”“心律共鸣·天哪”“心律共鸣·加入我们”。关于页选官方静态 PNG 的睁眼画面并保留原始 `Join Us!`；其它三页同样使用各自官方静态 PNG，不从 GIF 运行时随机取帧。
- 派生：只裁切 PNG 可见 Alpha 边界并等比装入 144×124 透明画布，不清除文字、不重绘人物。页面切换时人物与既有台词同步淡入，辅助功能名称同步说明当前表情。
- 理由：四张表情直接体现页面含义，比四页重复同一装饰更容易辨认；保留官方文字和固定代表帧也避免每次打开设置时人物状态不一致。

## ADR-080：模式三身体表情按动作语义分别延长

- 日期：2026-09-10
- 状态：已采用；替代旧文档中只为重罪/黑化指定 0.7/0.5 倍速但运行时未实际应用的节奏说明。
- 决定：模式三（心率共鸣表情包版）的九类身体反应不做统一慢放。心软软 0.72 倍、啾啾 0.82 倍、贴贴 0.80 倍、摸摸 0.75 倍、击掌与糟了 0.68 倍、重罪与抱抱 0.80 倍、黑化 0.90 倍；实际动作约为 1.3～2.2 秒。第二模型连续动作及非身体反应保持原速。
- 同步：单帧击掌和糟了依赖 WPF 程序回弹/晃动表达动作，因此附加动效与帧时间使用同一倍率；否则只延长静态图会形成动作先停、表情后退场的割裂。
- 理由：亲昵动作需要更长的辨识停留，脚部惊吓和击掌仍应有快速反射感，黑化原片已经最长，只做很轻的延长。逐项倍率比把所有素材统一慢到同一速度更自然，也不增加图集体积。

## ADR-081：追包速度按逻辑桌面对角线归一化并缩短吞咽尾段

- 日期：2026-09-10
- 状态：已采用；覆盖 ADR-074 中 72 DIP/s 起步、10 秒达到上限的追包节奏。

## ADR-082：模式三（心率共鸣表情包版）按左右部位分流并增加成对点击彩蛋

- 日期：2026-09-10
- 背景：模式三（心率共鸣表情包版）的双眼、双手、双脚此前各自共用同一方向画面，整张脸也只触发“贴贴”，左右点击缺少对应感；用户同时指定眼睛和脚部连续第二次点击的新表情。
- 决定：右眼、左手、右脚保留现有方向；左眼、右手、左脚在 WPF 播放层水平镜像同一动画，不复制图集。现有脸部多边形按归一化横坐标 0.5 分侧，左脸使用“十二周年·充电”，右脸保留“十二周年·贴贴”。眼睛和脚部各维护独立 4 秒连续点击窗口，第二次可跨左右侧并分别改播“十二周年·哭唧唧”和“十二周年·斯到普”；点击其它身体部位或超时会重置，第二次播放后下一次重新作为第一次。
- 资产：三张十二周年源 GIF 不覆盖；运行时分别只保留“充电”15 帧、“哭唧唧”14 帧、“斯到普”13 帧的首个完整周期，统一缩放为 240×240 图集并按 190×190 DIP、0.80 倍速单次播放。
- 理由：运行时镜像使左右反馈方向准确且不增加重复二进制；在现有脸部多边形内部按中心线分发可直接复用用户标注，不要求重新描边；成对计数器只承认真正连续的同类点击，避免用户中途点击其它部位后误触发第二段。
- 状态：已采用；覆盖 ADR-080 中双眼、双手、双脚和整张脸使用单一反馈的映射。
- 速度：1080p 基准初速度改为 180 DIP/s，4 秒达到既有三倍巡航上限；根据当前显示器工作区的逻辑 DIP 对角线使用 1～2 倍速度系数。原生工作区像素必须先除以 WPF DPI，确保 4K 200% 与 1080p 100% 的相对穿屏时间一致。16:9 1080p、2K、4K 完整对角线理论追逐均约 4.28 秒。
- 进食：包子飞入口中后不再等待原 173 帧自然走完；从第 128 帧续播闭嘴尾段约 800 ms，把进嘴后的停顿从约 1.65 秒缩短一半，同时保留可见闭嘴结果。
- 队列：返程中的新包子立即中断返程并恢复最近目标追逐，继续保留整轮最初起点；最后一个包子清空后再返回。包子拖动屏幕坐标统一转换到 DIP。
- 未决定：官方“我很可爱，请给我小笼包”彩蛋、手动投喂命中范围和剩余包子处理流程只写入桌面待确认文档，本 ADR 不提前采用。

## ADR-083：“摸鱼一分钟”仅清除边缘连通白底

- 日期：2026-09-10
- 背景：原 GIF 是不带 Alpha 的白底图，旧派生脚本强制把 GIF 调色板第 0 项设为透明；量化后人物内部的白色衣服、皮肤高光和身体区域也可能落入同一颜色项，导致运行图集中出现大块缺失。
- 决定：逐帧从画布四边遍历与白色距离不超过 96 的外部连通分量，仅将其中与纯白距离不超过 32 的像素设为透明；任何未与画布边缘连通的像素都保持不透明。GIF 编码后重新读取 69 帧，强制检查画布角点已透明且内部保护区没有新增透明像素。
- 理由：连通关系能区分外部白底与人物内部同色细节，不依赖不稳定的调色板编号；保留现有 60 秒时间轴、240×240 帧尺寸和原始素材，不引入生成式重绘。
- 状态：已采用，覆盖 ADR-074 中仅描述倒计时裁帧但未约束白底清除方法的部分。

## ADR-084：移除桌宠本体测试菜单并把托盘开场位移放入内容层

- 日期：2026-09-10
- 背景：桌宠本体右键仍暴露“模拟开始/停止音乐”和身体区域调试等早期开发入口；托盘快捷面板的开场动画又把 `RenderTransform` 直接设置到 WPF `Window`，触发 `Window.CoerceRenderTransform` 的 `InvalidOperationException` 和未处理异常对话框。
- 决定：主桌宠 XAML 不再挂载任何 `ContextMenu`；退出、设置、置顶、开机自启动和显示大小只从系统托盘快捷面板进入。自动化验证继续通过 `--preview-*` / `--qa-*` 参数运行，不暴露给普通用户。快捷面板窗口只动画合法的 `Opacity`，8 DIP 上移动画改施加到内部根 `Border` 的命名 `TranslateTransform`。
- 理由：移除开发菜单可以避免用户误入测试状态；WPF 顶层 Window 不支持 `RenderTransform`，将几何动画下沉到内容容器既保留视觉效果，也遵守框架约束。
- 状态：已采用，覆盖 ADR-013 中“右键菜单保留显示模式、调试、置顶和退出”等临时低频入口决定。

## ADR-085：快捷回收站与桌面吃包子彻底分流

- 日期：2026-09-10
- 背景：直接把文件拖到桌宠原本会在 Shell 回收成功后继续创建包子；同时文件拖放复用了“任意全屏都拒绝”的判断，Windows 桌面外壳覆盖整屏时被误判，表现为必须先点击桌宠才能接收。
- 决定：窗口 OLE 拖放始终作为快捷移入 Windows 回收站的独立能力，支持桌面和任意资源管理器目录；成功后结束“给我”末帧定格并恢复原持续动画，不写入包子队列。设置中的开关改为只控制桌面项目消失后的吃包子动画。前台判断对 `explorer.exe` 的桌面外壳和最大化目录窗口放行，其它全屏程序、受保护游戏和查询失败仍拒绝。
- 悬浮层：吃包子的追逐、进食和返程整轮关闭歌名、播放控制、网易云音量、操作反馈和消息悬浮岛；所有异步/悬停显示入口都检查追逐状态，不能在鼠标移入时重新出现。
- 光标：外部文件进入桌宠 OLE 目标后，在拖放生命周期内由窗口持续覆盖为本地天依指向手型，离开、拒绝、投放完成或窗口关闭时统一恢复；不安装全局鼠标 Hook。
- 状态：已采用，覆盖 ADR-032 及后续文档中“直接拖放成功后进入吃包子”的部分。

## ADR-086：“一键唱歌”使用相邻原帧往返闭环

- 日期：2026-09-10
- 背景：旧派生只循环源第 7～15 帧，循环边界会从第 15 帧直接跳回第 7 帧，人物嘴型、头部位置和音符分布同时变化，持续播放时形成明显瞬跳。
- 决定：继续删除第 0～6 帧一次性按键入场，稳定段改按 `7→8→…→15→14→…→8` 编排；每帧统一 100 毫秒，共 16 帧、1.6 秒。脚本必须检查相邻输出帧及末帧→首帧对应的源序号差均为 1。
- 理由：往返闭环只使用官方原帧，不引入交叉淡化重影或生成式补帧；旧边界第 15→7 帧改为第 8→7 帧后，持续循环的每一步都沿原动画相邻动作移动。
- 状态：已采用；覆盖 ADR-073 中“一键唱歌只循环第 7～15 帧”的派生规则，歌手分流、设置项和动画用途不变。

## ADR-087：网易云优先定向媒体命令并跨加载间隙保持音乐状态

- 日期：2026-09-10
- 背景：当前网易云即使能由桌宠启动，也会忽略过早发送或与本机全局快捷键设置不一致的控制；切歌期间 0.5 秒到数秒的静音和音频会话重建又会让人物在音乐动画与待机间闪烁。失败反应引用了未登记的 `resonance-cry-shake`，因此可能显示通用缺素材占位；文件回收成功也不应复用媒体 OK。
- 决定：上一首、播放/暂停、下一首先向配置目标进程的可交互主窗口发送 Windows 标准 `WM_APPCOMMAND`，目标窗口不可用时才回退现有全局快捷键。冷启动至少等待 2 秒并优先等曲目信息出现，10 秒仍未出现才兜底，之后只发送一次播放/暂停；Core Audio 确认有声后先播一次 OK，再恢复已选音乐持续动画。普通静音和会话缺失统一使用 5 秒宽限，桌宠发起切歌后另保持当前音乐状态最多 15 秒；用户主动暂停仍由 2 秒窗口内首个静音或会话缺失样本即时确认。
- 动画：正式登记“心律共鸣·呜呜”单帧并叠加短促晃动，供媒体控制失败和超时使用；正式登记“心律共鸣·大成功”10 帧 GIF，文件经 Windows Shell 成功移入回收站后播放一次。OK 只表示网易云控制得到确认，不再表示文件回收成功。
- 安全：两种控制路径都先执行受保护前台检查和 350 毫秒限流；定向消息只允许三种标准媒体命令且只匹配 `cloudmusic.exe` 主窗口，不发送普通字符、不读取进程内存或私有协议。快捷键回退继续执行实体键占用和成对抬起保护。
- 状态：已采用；设置架构升级为版本 14，旧版本恰好使用 1000 毫秒默认宽限时迁移为 5000 毫秒，自定义值保持不变。

## ADR-088：交换模式三（心率共鸣表情包版）左右眼与左右手的画面方向

- 日期：2026-09-10
- 背景：用户实际点击后确认，原先“右眼原向、左眼镜像；左手原向、右手镜像”的视觉对应需要整体交换。
- 决定：首次点击左眼使用“心软软”原方向，右眼使用运行时水平镜像；点击右手使用“击掌回弹”原方向，左手使用运行时水平镜像。眼睛 4 秒内第二次点击仍统一播放不镜像的“十二周年·哭唧唧”，脸部、脚部和其它身体映射均不改变。
- 实现：只交换 `BodyInteractionResolver` 返回的 `MirrorHorizontally` 标志，不复制、不反转或覆盖原始素材与运行图集；程序回弹会继续跟随手部镜像方向。
- 状态：已采用，覆盖 ADR-082 中左右眼和左右手的方向分配；脚部仍保持右脚原向、左脚镜像。

## ADR-089：网易云改回全局快捷键并重整音乐动画规则

- 日期：2026-09-10
- 背景：向网易云窗口投递 `WM_APPCOMMAND` 虽能定向控制，但目标未完整消费消息时会进入 Windows Shell 媒体命令链并显示系统媒体浮窗。真实日志还显示，部分 `Process.Start` 返回成功后网易云进程会立即退出，旧等待流程仍持续显示“正在打开”却不会再次尝试。音乐设置把自动、固定、停用和单项彩蛋开关并列，也让普通歌曲与固定动画的关系难以理解。
- 决定：删除定向 `WM_APPCOMMAND` 路径，三个按钮统一发送用户配置的网易云全局快捷键，继续以 Core Audio 与曲名变化确认实际结果。冷启动在 3 秒仍无可控制窗口时重试，包含首次在内最多发起三次启动，但播放快捷键仍最多发送一次。媒体失败只显示原因并恢复，不播放呜呜。音乐设置改为“播放动画总开关 + 智能分配 + 固定唯一动画”：智能模式普通歌使用享受音乐、洛天依本人或合唱随机使用音乐摇摆/一键唱歌；固定三项中的任一项都会应用到所有歌手。
- 兼容：旧 `none` 继续表示总开关关闭；旧的一键唱歌布尔字段保留反序列化兼容但归一为开启，不再出现在设置页。
- 状态：已采用，覆盖 ADR-087 的定向媒体命令、媒体呜呜反馈以及 ADR-086 中“歌手分流和设置项不变”的部分。

## ADR-090：控制岛按人物比例设界限，身体动作直接恢复待机

- 日期：2026-09-10
- 背景：50% 时固定控制岛会超出透明窗口，200% 时歌名岛又随窗口拉得过长；身体动作结束后再跑一次青色遮罩恢复，会短暂出现偏色和位置误差。模式三（心率共鸣表情包版）摸摸头也明显短于其它动作。
- 决定：由 Core `AccessorySizingResolver` 统一计算歌名宽度和控制岛缩放，保持可点击下限并限制大尺寸上限；身体动作完整结束后直接恢复同槽位待机帧并完成反应令牌，不再追加第二次脉冲；摸摸头播放倍率降为 0.60。按用户最后一次要求，模式三（心率共鸣表情包版）手部再次交换为左手原向、右手镜像，眼睛保持左眼原向、右眼镜像。
- 状态：已采用；手部规则覆盖 ADR-088，眼睛规则不变。

## ADR-091：追包采用显式速度上限、一次性请求彩蛋与 Alpha 足迹投喂

- 日期：2026-09-10
- 背景：旧代码以“原巡航速度×3”间接得到 810/870 DIP/s，并让返程也重新加速；用户确认追逐上限 800、返程固定 850、5 秒加速，同时希望唯一包子被拖住时出现一次请求小笼包彩蛋并可直接投喂。两套 60 FPS 奔跑素材仍含源画面全局漂移，和窗口移动叠加后像低帧率抖动。
- 决定：追逐从 180 DIP/s 线性加速并在 5 秒到 800 DIP/s，返程固定 850 DIP/s；逻辑桌面对角线与 DPI 适配继续保留。唯一包子在满速后连续拖动 3 秒时，每轮只播放一次“心律共鸣·可爱”并定格末帧；新包子打断等待但不重播。投喂使用整个圆形包子足迹与当前人物 Alpha 的重叠，不以包子中心或矩形命中替代；成功后 150 ms 切张嘴、400 ms 弧线入嘴，有剩余目标时 350 ms 后继续。闭嘴段以 1.35 倍速约 0.54 秒结束。
- 动画：两套奔跑源循环先用上部 64% Alpha 重心对齐周期中位数，再以 premultiplied RGBA 双向光流补到 60 帧；进食帧不做位置稳定，避免改变嘴部表演。最终 V1/V2 循环首尾重心差均低于 0.2 像素。
- 素材边界：晶蓝礼服右侧发丝针孔只在一个 7×22 像素左右的紧约束多边形内用邻近发色补齐；左右两块大透明间隙保持 Alpha 透明。原图不覆盖，参数写入元数据并同步重建动作首尾帧。
- 状态：已采用。

## ADR-092：经典猫耳反应柔和收尾，晶蓝动作提高运行采样

- 日期：2026-09-10
- 背景：模式三（心率共鸣表情包版）部位表情结束后直接换回待机会形成一瞬硬切，摸摸头即使降到 0.60 倍仍偏短；晶蓝礼服动作图集只有 240×260，桌宠放到 200% 时需拉伸到约 440×476 物理像素，细线和五官明显变糊。目录中的新版晶蓝待机原图仍是无 Alpha 的烘焙棋盘 RGB 图，且灰格比上一版更深。
- 决定：摸摸头维持 0.60 倍速并有限循环 2 次，总展示约 4 秒；经典猫耳全部部位反应以 110 ms 纯透明淡出，在不可见中点恢复锚定待机，再以 170 ms 淡入，不叠加闪光、颜色或缩放。晶蓝十项动作运行帧提高到 360×390、显示槽保持 220×238 DIP，挑选器预览仍为 240×260。新版待机从指定原图以灰度低色差背景阈值 160 重新生成透明 PNG，保留人物和左右发丝大开口，并继续局部补齐已确认的小针孔。
- 权衡：360×390 比 440×476 满倍率原生像素小，仍可能在 200% 下有少量插值，但相较旧图集有显著清晰度提升，同时避免运行资产直接增长到约四倍；一次只解码当前动作，实测 200% 模式二预览私有内存约 270 MiB 且窗口保持响应。
- 状态：已采用。

## ADR-093：动画与追逐共用刷新时钟并补足 200% 运行采样

- 日期：2026-09-10
- 背景：桌宠放到 200% 后，模式二 360×390 动作仍需拉伸到 440×476，模式三多数表情只有 162×162；追包人物换帧和透明顶层窗口位移又由两个独立约 16 ms `DispatcherTimer` 驱动，高速时会错拍、漏拍并在 UI 短暂停顿后一次补完大段位移，静态帧虽清楚但连续观看像低帧率模糊。请求小笼包动画还会继承向左追逐的水平镜像，导致内嵌文字反向。
- 决定：模式二十项动作从 720×720 母版直接重建为 440×476；模式三身体反应、拖拽反应及请求小笼包按显示槽补到 2 px/DIP，使用预乘 Alpha Lanczos 和轻度边缘锐化，原始候选不覆盖、不生成式重绘。两套吃包子运行帧提升到 410×410。`AnimationFramePlayer` 与追包位移统一订阅 `CompositionTarget.Rendering`，位移使用单调时钟，窗口坐标吸附当前 DPI 的物理像素，单帧时间补偿封顶 34 ms。
- 时序：追逐由 180 DIP/s 在 3.5 秒达到 800 DIP/s；返程继续固定 850 DIP/s。包子入嘴后，V1/V2 分别从第 120/112 帧以 1.1 倍速播到第 172 帧，约 0.79/0.91 秒。请求小笼包动画播放前强制把方向恢复为正向，恢复追逐后才再次按目标左右镜像。
- 权衡：运行时动画目录由约 283.6 MiB 增至约 392.8 MiB，换取 200% 档位的 1:1 采样；模式二单次真实 QA 私有内存约 379 MiB。图集仍按需解码且原始 720×720 序列不进入安装包，后续如继续压缩，应优先研究多分辨率按需包而不是再次降低当前大尺寸清晰度。
- 状态：已采用，覆盖 ADR-091 的 5 秒加速、统一第 128 帧/1.35 倍速闭嘴和 ADR-092 的 360×390 模式二采样。

## ADR-094：高分辨率动作使用动画 WebP 并限制解码缓存

- 日期：2026-09-11
- 背景：2 px/DIP 清晰度方案把运行时动画目录提高到约 392.8 MiB，其中模型二十项 440×476 动作约占 303 MiB。PNG 图集逐帧保存完整人物，不能有效利用相邻画面高度相似的时间冗余；若重新缩图或降帧，又会直接撤销用户确认的放大清晰度和动作流畅度。
- 决定：模型二十项、模式三已提高到 2 px/DIP 的身体动作/拖拽/请求动画，以及两套 410×410 吃包子动作，统一构建为质量 95、精确 Alpha 的动画 WebP；逻辑帧尺寸、帧数、逐帧时长、光流补帧和显示 DIP 全部保持。应用随自包含包携带 SkiaSharp Win32 x64 解码器，不依赖系统 WebP 扩展；小素材和单帧继续用 PNG 图集，避免无收益的格式迁移。
- 内存：WebP 首次播放时解码为冻结的 WPF `Pbgra32 BitmapSource`，播放器缓存按估算解码像素限制为 192 MiB，并以最近使用顺序回收非当前大动作。编码器合并完全重复帧时，解码器按物理帧时长扩展回清单逻辑帧，保持现有范围索引和动作时长。
- 发布：被替换的 28 个 PNG 图集共 377.07 MiB，新 WebP 共 112.85 MiB，运行时动画目录降至 128.59 MiB。自包含 `0.1.0.34` MSIX 为 207.02 MiB，发布目录为 321.08 MiB；仅保留 `zh-Hans` 卫星资源，并从发布项删除第三方原生 PDB。官方源序列完整保留且可重复重建。
- 状态：已采用；覆盖 ADR-093 中“继续用 PNG 图集并暂不压缩”的权衡，不改变任何产品触发规则或动画画面尺寸。

## ADR-095：保留 WPF 并以 .NET Framework 4.8 作为默认完整安装目标

- 日期：2026-09-11
- 背景：高分辨率动画改为 WebP 后，自包含 .NET 10 MSIX 仍为 207.02 MiB，其中安装包继续携带完整 Desktop Runtime。用户要求完整安装、不拆动画资源，也不希望目标电脑额外下载运行时；重写为其它 UI 技术会触及透明窗口、像素命中、OLE 拖放、通知、音频和大量已验证状态逻辑。
- 决定：保留 C#/WPF 和现有工程边界；源码项目在 `EnableNetFrameworkBuild=true` 时同时构建 `net48`，打包脚本默认选择 .NET Framework 4.8 x64，清单最低系统版本收敛到 Windows 10 22H2（19045）。受支持 Windows 已包含兼容的 .NET Framework，MSIX 继续一次安装本体及全部动画，不增加运行时下载或可选资源包。日常构建仍默认 .NET 10，`Net10SelfContained` 作为显式兼容回退。
- 实现：共享兼容层补足参数检查、数值 API、随机数、文本分割、`TimeOnly` 及编译器属性；平台层为旧目标选用兼容的 NAudio/Windows SDK Contracts，并对公开 WinRT 调用的 RPC HRESULT 做等价失败关闭。发布布局只保留当前 x64 SkiaSharp 原生库，剔除其它架构、macOS 库和调试符号；Core、Animation、Platform 三组测试都在 net10 与 net48 上运行。
- 结果：`0.1.0.36` 签名 MSIX 为 141,967,058 字节（135.39 MiB），较 `0.1.0.34` 自包含包减少 71.63 MiB（34.6%）；完整旁加载 ZIP 为 141,867,125 字节（135.30 MiB）。本机真实安装后从 WindowsApps 启动，加载 63 项动画和两套身体区域图，通知状态为 `Allowed`，进程正常响应且日志无未处理异常。
- 权衡：安装版不再承诺 Windows 10 1809，最低为仍受项目支持的 22H2；需要覆盖更旧、被精简掉系统组件或特殊企业镜像时，发布者仍可显式构建较大的 .NET 10 自包含包。没有选择 Electron、Qt/C++ 或整体重写，因为它们不能减少 128.59 MiB 的动画主体，且迁移风险远高于可获得的额外体积收益。
- 状态：已采用，默认安装交付从 .NET 10 自包含切换为 .NET Framework 4.8；不改变产品功能、动画画质、帧率或隐私边界。

## ADR-096：安装版与真便携版双交付，通知能力只承诺给有包身份的版本

- 日期：2026-09-11
- 背景：相同版本已经安装且桌宠主进程正在运行时，旧安装脚本会跳过重复注册、再启动一个立即被单实例保护结束的进程，成功控制台也随即关闭，用户感知为“安装没反应”。用户同时需要解压即用、可放任意目录的版本，又希望 QQ/微信 `UserNotificationListener` 在不同电脑可用。
- 决定：提供两个边界清晰的成品。便携版默认使用 net48，解压后直接运行 EXE；程序以同目录标记自动进入便携存储，不要求 `--portable`，但不声称支持系统通知监听。完整安装版继续使用 MSIX 包身份，安装脚本创建指向 AppsFolder AUMID 的桌面快捷方式，成功后总是弹出结果；若同版本主实例已运行则明确告知并保留它，不制造无反应的第二次启动。
- 平台约束：`UserNotificationListener` 仍必须由具有 `uap3:userNotificationListener` 声明的包身份进程在 UI 线程请求用户授权；无法用进程扫描、QQ/微信数据库或窗口抓取替代。微软的外部位置身份包从 Windows 10 2004 起可以让传统 EXE/MSI 安装目录保留包身份，因此“自选目录 + 通知”技术上可行，但仍需安装器注册签名包并处理卸载，不属于真便携。
- 结果：`0.1.0.37` 便携 ZIP 为 141,573,824 字节（135.02 MiB），直接双击 EXE实测写入相邻 `UserData`，清理脚本停止精确路径进程并删除该目录。完整旁加载 ZIP 为 141,867,941 字节（135.30 MiB），本机真实升级后创建桌面快捷方式、包状态 `Ok`、进程响应正常且通知状态保持 `Allowed`；重复运行安装脚本会报告既有实例，不再静默。
- 权衡：当前 MSIX 的实际安装目录由 Windows 管理，不承诺任意路径；未来若用户确认路径选择高于 MSIX 的干净更新/卸载，应单独实现并签署“传统安装器 + 外部位置身份包”，而不是让便携版暗中注册系统组件。
- 状态：已采用。

## ADR-097：“一键唱歌”移除竖排文字并降低嘴型节奏

- 日期：2026-09-11
- 背景：正式听歌动画虽已用相邻官方原帧组成往返闭环，但画面左侧仍带固定的“一键唱歌”竖排文字，角色放到桌面后显得拥挤；每帧 100 毫秒也使嘴型连续变化过快。
- 决定：官方 16 帧 GIF 保持只读；派生脚本继续只取源第 `7→15→8` 帧，并在每一帧固定坐标带 `(0,15)-(33,126)` 清除左侧文字 Alpha。该坐标带止于左侧动态音符之前，角色、音符和其它像素不重绘。每帧时长由 100 毫秒改为 160 毫秒，16 帧整圈由 1.60 秒延长到 2.56 秒，嘴型变化速度降为旧版的 62.5%。
- 验证：脚本在编码前逐像素断言坐标带外 RGBA 与官方源帧相同、坐标带内 Alpha 全透明；输出解码后再次确认 16 帧、每帧 160 毫秒、总周期 2560 毫秒，运行图集和设置页缩略图同步重建。
- 状态：已采用，覆盖 ADR-086 的 100 毫秒帧时长；相邻帧往返闭环、音乐分流和设置选择规则不变。

## ADR-098：播放按钮快速反馈与有边界的洛天依歌手匹配

- 日期：2026-09-11
- 背景：人物音乐动画为掩盖网易云切歌加载保留 5 秒静音宽限，但播放按钮复用同一状态后，用户在网易云内暂停也要等待数秒；桌宠自身发送暂停/播放后同样不能立刻看到按钮变化。智能动画只匹配首个或完全等于“洛天依”的歌手字段，又漏掉合唱中的后位歌手和 `洛天依Official`。
- 决定：人物动画的 5 秒抗闪宽限保持不变，新增独立 `MusicPlaybackIndicatorTracker`，以 250 毫秒采样和 750 毫秒连续静音更新控制岛按钮。桌宠成功发送播放/暂停快捷键时按钮立即显示预期结果，最多保持 2 秒；可靠样本确认后结束预期，命令未生效则自动回正。上一首/下一首的已知加载保护期间不把短暂无声喂给按钮快速判断，避免切歌误闪。歌手匹配按中英文常见合唱分隔符、`feat./featuring/with/vs` 和紧邻词尾解析，字段任意位置的独立 `洛天依` / `Luo Tianyi` 及其 `Official` 后缀均视为洛天依参与；`洛天依然` 等仅前缀相同的名字继续排除。
- 音量弹层：继续使用 `StaysOpen=false` 保留点击其它位置关闭；音量图标再次点击时显式关闭，并在弹层的自动关闭事件与同次按钮 `Click` 之间设置 250 毫秒抑制窗，避免关闭后立即重开。
- 权衡：网易云当前没有稳定注册系统媒体会话，外部播放状态只能由 Core Audio 有声近似判断；750 毫秒比人物动画更灵敏，但歌曲本身持续静音超过该时长时按钮可能暂时显示播放。该判断只影响按钮，不切断人物动画，下一次有声采样会立即恢复。
- 状态：已采用。

## ADR-099：时间问候让位音乐，模式三以横向甩动替代降落彩蛋

- 日期：2026-09-11
- 背景：时间问候、开机问候、音乐动画和中等待机此前各自恢复画面，切歌加载与跨时段容易互相抢占；经典猫耳摸头通过重复慢放延长后动作显得迟缓。用户同时取消快速下甩“降落”和所有“呜呜”反馈，并指定傍晚、模式三旋转拖拽及睡眠范围。
- 决定：时间划为 06:00～12:00、12:00～13:30、13:30～18:00、18:00～20:00、20:00～06:00 五段，傍晚使用“十二周年·饿”；启动与跨界都只展示 10 秒。音乐播放时不启动问候，停歌后稳定等待 10 秒再补播，期间重新播放立即让位；用户人物输入和媒体控制输入可取消展示。`眠了呼吸` 仅模式三（心率共鸣表情包版）使用，“嘿嘿”收到按下或拖拽输入时恢复待机。
- 动画：摸头以显式 28 帧 `0→9→0→9` 路径形成约三次连续抚摸，每帧约 80 ms、总计 2.24 秒；重罪/黑化有效速度调整为约 0.8/0.6。经典猫耳拖拽由 `RapidBackAndForthDragTracker` 判断 900 ms 内三段至少 32 DIP、360 DIP/秒的横移和两次换向，触发无限循环旋转舞，下一次点击或音乐开始时退出。
- 删除：移除 `DownwardFlingTracker`、`LandingBounceMotion`、降落运行资产与清单引用；操作失败改为气泡专用并移除“呜呜”运行资产。动画挑选器删除用户点名的 12 项预览和三项旧文件吞食派生 WebP，新增三个外观模式的简明触发总览；官方原始表情素材保持不变。
- 通知韧性：本机升级后实际观察到 `UserNotificationListener.NotificationChanged` 订阅可能以 `0x803E0105` 异步进入 WPF 调度器，普通事件订阅 `try/catch` 无法可靠拦截。正式监听改为每 750 毫秒读取官方 Toast 快照，首次只建基线、后续按 ID 处理新增项；单通道门控防止重叠，平台瞬断在下一轮自动重试，彻底避开不稳定事件桥接且不弹错误框。
- 状态：已采用。

## ADR-100：乐正绫音乐彩蛋与低干扰天依提示条

- 日期：2026-09-11
- 背景：智能音乐动画已能区分洛天依，但乐正绫本人或合唱仍被当作普通歌曲；官方“十二周年·打 call”196 帧实际是同一 14 帧动作重复 14 次。桌宠又会为“快捷键已发送”“切歌成功”等可从按钮和动画直接看出的过程弹出深色文字条，既遮挡人物又频繁打断观看。上一轮还把拖拽本身当成退出“嘿嘿”的输入，与用户希望移动桌宠后继续保持该状态不符。
- 决定：智能模式增加乐正绫中英文名、`Official` 和合唱匹配，持续循环“十二周年·打 call”；同一字段同时出现洛天依与乐正绫时先走洛天依随机彩蛋。运行时只编译“打 call”首个 14 帧周期为 380×380 透明 WebP，190×190 DIP 显示并无限循环。提示条改为带“依”字徽记的浅天依蓝对话条，复用歌名的安全槽位，不再覆盖人物；取消成功发送命令、启动播放、切歌成功、自启动切换成功、快捷回收进度/成功及重复拖放说明，只保留错误、超时、网易云冷启动过程和文件已达到可松手区域等必要提示。
- 手势：鼠标按下不再立即清除 `MediumIdle`；确认是单击后才退出“嘿嘿”，确认进入拖拽则由状态机记住原状态，松手重新解析为“嘿嘿”。系统恢复和其它明确状态转换仍可退出。
- 状态：已采用；音乐分流扩展 ADR-089/098，提示策略替代 ADR-089 的逐次命令发送文字，拖拽规则覆盖 ADR-099 中“嘿嘿收到拖拽输入即恢复待机”的部分。

## ADR-101：以任务栏闪烁补齐 QQ / 微信提醒，并提高旋转舞手势门槛

- 日期：2026-09-12
- 背景：本机安装版已有包身份、`UserNotificationListener` 授权为 `Allowed`，但用户在新版微信保留未读后仍没有任何提醒。实机检查确认当前微信进程为 `Weixin.exe` / `WeChatAppEx.exe`，且没有注册 Windows Toast 来源；旧实现只有 Toast 快照，因此“监听已启动”并不等于客户端真的提供了通知。模式三原有三段移动、两次换向也容易在普通拖动后撤回时误触发旋转舞。
- 决定：保留 Toast 快照作为可提供应用图标与会话标题的首选通道，同时让桌宠自己的顶层窗口通过 `RegisterShellHookWindow` 接收 `HSHELL_FLASH` 任务栏闪烁。回退通道只将系统给出的 HWND 解析为 PID 和进程名，内置识别 QQ/QQNT/WeChat/Weixin/WeChatAppEx；不读取窗口标题、控件树、聊天文件或正文。按来源合并 8 秒内的连续闪烁，来源回到前台后重置；Shell 注册失败时安全停用，不影响 Toast。旋转舞门槛改为 1400 ms 内五段有效横移、四次换向，即至少两次完整左右往返；32 DIP 与 360 DIP/秒阈值不变。
- 验证：独立 `Weixin.exe` 测试窗调用 Windows `FlashWindowEx` 后，安装版只收到一次来源事件；安全状态下直接展示，启动问候期间则延后并在问候结束后展示。测试窗不含消息内容，也不访问真实微信。真实客户端仍需后台收到一条新消息验证其自身是否产生 Toast 或任务栏闪烁；现有未读和程序启动前事件不补报。
- 状态：已采用；通知回退扩展 ADR-031/033/099，便携版也可使用仅显示来源的闪烁通道；旋转手势门槛覆盖 ADR-099 的 900 ms / 两次换向规则。
## ADR-064：晶蓝礼服采用可点击唤醒的两段式长待机

- 日期：2026-09-12
- 决定：取消模式二（心率共鸣简约Q版） 5 分钟“嘿嘿”，保留 1～5 分钟每轮一次随机哈欠；连续无输入 30 分钟后，以 50:50 选择“睡觉”或“鸭子坐”。睡觉播放到源第 220 帧闭眼卧姿、鸭子坐播放到源第 120 帧闭眼坐姿后定格；只有点击人物才续播各自后段并回到待机，桌面其它输入不隐式唤醒。睡觉动作单独使用 488×476 运行画布和 244×238 DIP 显示槽，保持原人物比例并完整容纳横向头发。
- 决定：定格后立即且每 5 分钟按 60:35:5 选择 zzz、梦见包子、梦见乐正绫，允许连续重复。梦境云朵点击时播放约 1.3 秒的“云朵消散”，zzz 直接移除。
- 素材：睡觉源帧的 Z 字只在人物上方经审计的 `250,0,520,290` 区域、源帧 147～240 内从派生帧清除，并把全透明像素的隐藏 RGB 清零；原始 PNG 保留。小装饰只清除与画布边缘连通的近白/近黑键控背景，避免误删云朵内部白色与乐正绫深色轮廓；随后按可见联合边界裁切并生成小尺寸动画 WebP。
- 理由：中等待机表情与晶蓝礼服画风不一致，而长时间姿态需要“进入—保持—醒来”的完整行为语义。独立装饰播放器能在不复制主人物整帧的情况下低成本增加梦境变化，并允许点击唤醒时同步消散。
- 影响：`IdleSceneResolver` 的晶蓝礼服 30 分钟目标改为 `Sleeping`；WPF 在该外观下接管两段式播放与保持锁，音乐仍可优先中断。模式三（心率共鸣表情包版）原有 2/3/30 分钟链路不变，模式一（心率共鸣完整Q版）仍无长睡眠。

## ADR-102：消息提醒采用 30 秒可抢占展示，挑选器按模式分层

- 日期：2026-09-12
- 背景：QQ/微信提醒启动后，1 秒安全状态轮询会看到状态机处于一次性反应，旧判断只允许持续状态，因此把提醒自己的反应立即取消；用户看到的效果只有一闪。动画挑选器又把三套外观专属动画和公共场景全部混在同一总览，难以判断每个模式实际使用哪些素材。
- 决定：消息提醒以 30 秒作为当前最短展示期，单帧“好奇”人物的程序摇摆在期间无限循环；安全判断允许当前消息令牌继续占用展示槽。来源应用进入前台、全屏/锁屏、拖拽或更高优先级反应仍可提前打断。挑选器增加模式一、模式二、模式三和三个模式通用四个入口；模式页列出该模式专属动画以及实际会覆盖到它的公共动画，公共页只列三个模式共有项，完整候选素材仍在独立区域管理。
- 状态：已采用；30 秒是便于用户继续体验调整的临时长时参数，不改变通知隐私、去重或发现通道。

## ADR-103：托盘与人物菜单分层，音乐悬浮岛采用显式开关

- 日期：2026-09-12
- 决定：托盘左键只显示／召回桌宠；右键提供显示、隐藏、完整设置和确认退出。人物右键放位置锁、置顶、音乐悬浮岛和紧凑大小调整，完整滑杆和低频设置归完整设置。
- 决定：音乐悬浮岛默认关闭，由人物右键统一开关并持久化；关闭时歌名、控制和音量全部不可见，不留下小播放按钮。取消讨论中的暂停 10 分钟自动收起设计，播放／暂停／切歌都不改变用户开关。启用后沿用鼠标悬停及离开 5 秒淡出，未运行网易云时保留完整控制岛供主动启动。
- 理由：音乐功能是用户选择的辅助入口，不应在纯桌宠互动时抢占视觉；位置锁只限制拖动，继续保留身体点击、摸头和双击换装。
- 实现：设置 schema v15 新增两个默认 false 字段，v14 无损迁移；所有音乐附件显示路径检查统一开关，包括异步结果和反馈恢复。隐藏人物时抑制追包及消息展示，召回不修改永久置顶偏好。


## 2026-09-12：托盘退出改为单击执行

用户明确取消退出二次确认。托盘点击退出后直接走原有告别动画与关闭流程；删除确认／取消界面及其按鼠标位置重复定位，面板打开前固定本次锚点。此决定取代此前要求确认退出的规则。

## ADR-104：动画展示槽与音乐附件尺寸脱钩

- 日期：2026-09-12
- 背景：按当前动画 DisplayWidth 缩放歌曲岛会使待机／听歌的岛宽不一致；每次切换重新夹紧窗口，过渡后再按不同 Alpha 边界对齐，会在靠边时发生二次位移。
- 决定：所有三种模式使用当前运行清单最大显示宽高构成稳定透明槽，保留素材原显示尺寸并底部居中；歌曲岛单独使用 220 DIP 参考尺寸随用户倍率计算，100% 宽 226 DIP，播放控制保持 178×46 DIP。切换动画不重新缩放或移动窗口。
- 边界：普通靠边摆放按稳定槽为较大动作留空；明确向外拖拽、躲藏细条和身体命中仍按真实 Alpha。附件上下换侧与过渡锚点不再读取摆动／裁剪边界。原始素材和运行时动画不改动。

## 2026-09-12：采用人物提示符旁的消息卡和可关闭的 QQ 详情

用户选择继续使用现有人物提醒动画，将消息卡移到右侧提示符旁，右边放不下时把人物水平翻转、卡片移到左侧。此前“取消人物通知动画、改占顶部信息位、5 秒后收成标记”仅是建议，不实施。使用独立不激活透明浮层避免扩宽人物窗口破坏既有边缘和拖拽修复。
用户授权 QQ 详细提醒并要求独立关闭入口；明确限定为可靠昵称／群名及独立未读数，不读取正文。QQ 公共托盘卡只有展开时才可用，系统 Toast 标题仍可补充，失败时不伪造详情、不移动用户鼠标。完整设置始终只保留一个实例。
## 2026-09-12：三项详情统一启停，先研究外部未读读取路线

用户确认昵称、系统通知正文预览、新增通知计数共用一个开关，不提供分项开关。以此最新决定替代此前“不读系统通知正文”的范围，保持禁止存档、上传和访问客户端私有数据。已比较 UIA 会话列表、QQ 内部接口、协议客户端、微信 Hook／数据库与手机通知转发；没有接入外部项目。实际运行时的预览与计数仍待实现，不能把文档确认当成交付完成。


## 2026-09-12：下午问候使用用户指定的“来不及了！”

用户指定“洛天依十周年生日”中伏案赶作业的“来不及了！”动态表情。以公开原库 L12.GIF 替换 13:30～18:00 启动及跨时段共用的下午问候素材；保留原始帧、文字、透明背景和 900 ms 原生循环，取消旧静态图的程序漂浮。问候持续 10 秒、输入可打断、音乐优先的已确认规则不变；旧原始素材保留，运行时 ID 改为 startup-afternoon-hurry，避免浮动语义残留。

## 2026-09-12：QQ 新通知计数采用系统事件标识，微信保留字段实验

QQ 昵称、预览、通知计数统一启停。计数以 Toast ID＋创建时间识别本轮捕获的新事件，不用 Shell 闪烁次数或标题内容代替；来源前台、关详情及重启清零，文案明确“新增 X 条通知”。同一来源连续新事件更新现有卡片，不重播动画，跨来源排队。微信验证确认后台有会话字段，但 Name 混合名称／数字／摘要，尚无最小化及动态变化证据，因此本轮不接入持续监听。

## 2026-09-12：微信接入可关闭的公开会话详情试用

用户确认 QQ 无问题后要求微信也试用。微信会话字段已在最小化状态实测可读，因此接入只读会话变化来源；昵称、摘要和新增提醒次数共用一个微信开关，QQ 开关及计数独立保留。为了不把滚动露出的旧未读当新消息，首次出现条目只建基线，允许首条遗漏而不伪造完整覆盖。微信提醒次数与未读总数／消息条数分开，免打扰条目不派发。字段变化端到端由用户新消息验收，当前保持试用标记。

### 2026-09-12：微信提醒只展示仍有效的新事件

用户反馈首次缺详情与已读后迟到。采用控件定位复用、Shell 信号主动刷新及 900 ms 详情等待；有验证基线才派发详情，避免拿任意旧未读补首条。微信排队超 8 秒丢弃，已读／前台或关联会话失效后撤销，不等待长动画后补播。QQ 当前行为保留。实时性改进仅改变既有公开字段的读取时序，不扩大权限或消息范围。

### 2026-09-12：用户确认耳旁图标与共享面板

按用户提供的左右 8 状态参考实现，取代先前提醒立绘：保留当前待机／音乐，只更新微信／QQ 图标及本地待处理数量；悬停或固定才展开正文。会话计数之和等于应用角标，同会话合并、阅读冻结排序，右键仅处理桌宠记录。QQ 原系统源不提供可靠逐会话已读，因此采用本地忽略／清空，不臆测进入 QQ 即全部已读。微信继续以公开字段确认已读，未扩大读取权限。


### 2026-09-13：精简日历与提前倒计时

按用户反馈移除手动JSON导入导出界面，保留本地自动保存与备份。闹钟从桌宠右键进入，日历行程以图标表明提醒；作息并入休息日调整。提前30分钟的展示与到点调度独立，全局或单项关闭倒计时不取消到点提醒。默认窗口工作区内适配、月网格均分实际周数，小屏编辑仍允许滚动。


### 2026-09-13：落实最新视觉稿与会议前提前结束提醒

用户确认原生浅青白色UI，农历日缩写与节日并列，任意星期休息，单内容框和按需多日期选择。提前操作按发生时间持久保存，不直接关闭整个重复提醒，也不删除行程；隐藏倒计时与跳过到点分开。旧标题正文不做批量破坏性转换，仅编辑时写入统一Content。现有音乐岛保持原样。


### 2026-09-13：按行程组管理，显式确认删除范围

用户反馈此前界面与效果图差距大，并且多日期误设后逐日处理麻烦。本轮统一所有日历相关界面，以底部页面入口、居中编辑、圆角轻量原生控件承接效果图。既有多日期共用记录，保留该模型，增加单日期排除和整组／多组删除。确认框列明影响范围，避免用户以为删除仅针对当前日期；不添加联网组件或额外数据库。

### 2026-09-13：拖拽保留当前状态

用户确认拖动是定位操作。保留模式三普通待机膨胀、快速甩动旋转舞及越界收纳；已有旋转舞拖动不退出，自动移动仍禁拖。将唤醒／取消移到确认单击，保持动画原有生命周期。追包子采用smoothstep和统一运动时间，解决卡顿时间提前加速与小数位移丢失，保留原速度上下限。

### 2026-09-13：最新九图与AI规格的优先级
采用用户最新文字 > 同轮规格 > 图像细节。月历采用图二的圆点和窄侧栏（覆盖此前反对圆点的旧决定），补图一班休入口；休红班蓝。倒计时遵循图九的名称→快捷值→时分秒布局。图中随机分类色、1小时默认、大备注按规格收敛为同色事项、首次30分钟和单行备注。保留新增入口，但移除行程管理主导航；批量删除由事项更多菜单进入。
提前确认保留正式提醒，取消仅作用于该实例；全局声音与动画设置统一作用于各来源。旧明确要求的提醒位置优先，实际提示仍在音乐岛/人物下方，只有新管理胶囊采用角落避让。动画具体素材未由用户指定，暂复用现有 twelfth-anniversary-call 作为可关闭的低优先级短动作，默认关闭，不引入新素材。
实施参数：提前时长编辑校验1～10080分钟（并非用户确认的上限）；备注不新增字符截断，既有有效长备注保留。未新增退出后服务或新的错过提醒策略；实际物理休眠、跨DPI多屏与长时性能仍需专门体验验证。

### 2026-09-14：最终发布目标固定为 .NET Framework 4.8

用户确认项目目标框架和最终发布统一为 `net48`。保留 .NET SDK 10.0.400 作为构建工具，不再保留 .NET 10 项目目标或自包含发布分支。三个打包脚本继续保留 `-Framework NetFramework48` 入口，但移除 `Net10SelfContained` 选项；不改变程序集名、命名空间、资源输出路径、存档路径、快捷键、托盘、自启、动画、交互或其他命令行参数。

## 2026-09-14：统一模式与追包动画的公开名称

用户确认将三种完整全身模式的公开名称统一为“模式一（心率共鸣完整Q版）”“模式二（心率共鸣简约Q版）”“模式三（心率共鸣表情包版）”，并将两套追包动画名称统一为“心率简约追包版”和“心率表情追包版”。本次只更新展示名称、说明文字、配置 displayName、命中图 name 和校验脚本文案；稳定内部 ID、资源文件名、存档格式和行为逻辑不变。

### 2026-09-14：确认安装、卸载、版本、隐私与新仓库方向

用户确认安装版必须支持自由选择任意安装目录。卸载前必须询问用户选择保留或删除用户数据；卸载清理策略不能默认静默删除。运行时 ID 可以直接改为中性名称，但必须优先保留旧 ID 兼容映射，确保既有存档、配置和资源引用继续可读。公开发布清单和公开文档需要清理 AI 痕迹及作者机器路径。

用户确认后续会重新创建新的 GitHub 仓库，当前远端不作为最终发布仓库处理；不以改写当前远端历史作为前置工作。发布版本号必须统一，现有便携版 `.56`、MSIX `.93` 及脚本默认值不一致属于并行工作线程造成的发布不同步；后续建立单一版本来源，具体基准版本另在发布阶段确认。隐私默认值保持当前用户行为，允许增加独立 QA 项目。

### 2026-09-14：确认正式版本与生产包身份

用户确认正式版本统一为 `0.1.0.94`，生产包身份名统一为 `LuoTianyiPet`，正式安装包使用受信任生产代码签名证书。新增 `config/version.props` 作为项目程序集和全部正式打包脚本的单一版本来源；历史 `.56`、`.93` 发布物不改写、不删除。测试身份和开发自签名证书只用于受控验证，不作为正式发布签名。

### 2026-09-14：任意目录安装采用外部位置身份包

为同时满足任意安装目录、既有 QQ/微信通知能力和普通用户权限，采用 Windows 10 2004+ 的外部位置身份包路线：程序文件由安装脚本复制到用户选择的目录，sparse MSIX 仅承担包身份和清单能力；EXE 嵌入与身份包一致的 `msix` 元数据。便携版不因该安装版而注册包身份，完整 MSIX 构建脚本保留用于对照回归。

安装器必须在注册身份包前完成路径、清单、签名和哈希校验；卸载必须先询问用户保留或删除 `%LOCALAPPDATA%\LuoTianyiPet`，只有注册移除成功后才清理安装目录。验证用 `0.0.0.1` 原型已完成离线构建检查，并使用隔离身份 `LuoTianyiPet.ExternalUacTest` 在 Windows PowerShell 5.1 中实际完成开发证书 UAC 信任、外部位置注册、从用户选择目录启动、`0.0.0.2` 到 `0.0.0.3` 覆盖升级、旧快捷方式参数清理、卸载保留数据和卸载自启动清理验证；删除数据路径已在隔离 LOCALAPPDATA 沙箱中通过。随后使用受信任测试证书和隔离身份 `LuoTianyiPet.NotificationTest` 完成用户授权，应用记录 `notification.monitor_status Allowed`，包清理后无注册、安装目录或快捷方式残留。真实 QQ/微信消息读取仍未验证；正式版本已确定为 `0.1.0.94`，生产身份名已确定为 `LuoTianyiPet`，正式生产构建仍待真实受信任证书路径和密码文件。

### 2026-09-14：确认一次性动画的单击与拖拽行为

用户确认：普通身体反应动画播放中，确认单击取消并恢复当前连续状态；开始普通拖拽时取消身体反应，不保留动画。时间问候允许拖拽打断；通知动画拖拽不打断；音乐动画禁止普通身体单击打断；闹钟不允许拖动窗口；追包子、进食、返程、求包子和文件拖入流程继续完全保护。实现采用反应请求上的独立输入策略，不改变存档格式、资源 ID、优先级体系或动画播放器。

### 2026-09-15：班休面板移除重复日期窗并采用拖动选区
用户确认班休调整使用方案 A，但要求降低连续修改成本：月历左侧作为唯一日期入口，取消右侧重复的“添加日期”小日历；单击仍是选中/取消，按住左键拖动则把鼠标经过的日期加入选区。批量操作无选区时禁用，成功后自动清空选区但保持调整模式，允许继续选择下一批；失败保留选区。该设计保留选择阶段的可撤销性和原有数据模型，不引入逐点击立即写库或第二套刷子模式。

### 2026-09-15：班休列表固定操作区与覆盖优先级保持不变
为避免选中日期增多时按钮被推下或面板高度跳动，班休面板固定日期列表的滚动区域和底部操作区。数据语义不改：每周休息是默认规则，指定日期覆盖它，恢复默认只删除指定日期覆盖；不把恢复默认改成强制工作日或强制休息日。拖动从未选日期开始执行选中，从已选日期开始执行取消，以较低学习成本支持连续批量反选。

### 2026-09-15：清除指定设置并容错日期格边界
用户确认将“恢复默认”改名为“清除指定设置”，强调清除单日例外而不是生成新的工作日/休息日状态；用户确认只保留一个“完成班休”入口，避免顶部和面板底部重复。拖动选区增加日期格边界缓冲，防止手抖划过分隔线时漏选，同时不在月历外自动猜测目标日期。

### 2026-09-15：每周班休作为低优先级基线
每周星期设置只维护全局基线，具体日期的工作日／休息日设置永远优先，切换每周星期不得清除已有日期例外。清除指定设置删除日期例外并回到未单独设置；月视图徽标只表达日期例外，避免把全局规则误认为单独设置。

### 2026-09-15：班休模型与表达层收尾
1. 保留活规则二值模型（流派 A）：每周规则实时求值，日期例外优先；不引入“未安排”第三状态或豁免集，理由是提醒引擎需要确定二值，个人提醒日历只需轻量定位，并对齐钉钉固定班制与 Outlook 重复事件语义。
2. 与每周规则同值的日期例外不剪枝、不去重；它是用户有意留下的锚点，用于抵抗未来每周规则变更。
3. “清除指定设置”更名为“取消单独设置”，语义对应 Outlook 的“删除单个实例”：只删除日期例外，恢复跟随每周规则；批量和单日详情都要明确显示恢复落点。

### 2026-09-15：用户确认日历恢复 250b990 基线
用户要求将日历／班休界面恢复到 `250b990 fix: separate weekly rules from date overrides`。本次恢复只针对 Planner 日历源码和 QA，不改变每周规则与日期例外的数据模型、求值优先级或其他模块；后续新增的单日取消入口、混合拖动反选、表达层改名与每周按钮反馈不作为当前实现。

### 2026-09-15：月视图保留生效状态并降低来源歧义
用户确认清除日期例外后仍应显示当天实际生效的“班／休”徽标，避免闹钟选择“工作日／休息日”时因无徽标产生误判。浅色徽标表示跟随每周规则，深色徽标表示高优先级单独设置；“清除指定设置”改为更通俗的“恢复每周安排”，并在班休面板和详情标题中说明来源。
